@SPRING_1ST

CALL SPRING_PERSON_A

;욕망을 체크한다. 욕망이 상대의 욕망을 웃돌지 어떨지. 성공이라면 욕망 레벨의 2배, 실패라면 1배 들어갑니다.
;소프트 매춘에서는, 상대의 욕망의 최대치가 낮은으로, 만족시키기 쉽습니다.
LOCAL@SPRING_DUMMY:0 = RAND:4
IF LOCAL@SPRING_DUMMY:0 <= ABL:COUNT:11
	LOCAL@SPRING_DUMMY:0 += ABL:COUNT:11
	LOCAL@SPRING_DUMMY:0 += ABL:COUNT:11
ELSE
	LOCAL@SPRING_DUMMY:0 += ABL:COUNT:11
ENDIF

;상대로부터의 행동은 없기 때문에, 노출버릇, 봉사 정신이 그대로 득점에 들어갑니다.
;다만 5로 스톱 합니다.
LOCAL@SPRING_DUMMY:9 = ABL:COUNT:13
SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
	LOCAL@SPRING_DUMMY:9 = 5
LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9
LOCAL@SPRING_DUMMY:9 = ABL:COUNT:14
SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
	LOCAL@SPRING_DUMMY:9 = 5
LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9

LOCAL@SPRING_DUMMY:4 = 0


;순종이 2 LV 없으면 현저하고 미묘하게 됩니다
IF ABL:COUNT:10 < 2
	LOCAL@SPRING_DUMMY:3 /= 2
ENDIF




;여기로부터는, 플레이 내용의 영역에 들어 오기 때문에, 분기 후에 맡깁니다.
;여기까지의 만족도는 합계 최대 60점.
;플레이 내용의 (분)편으로 20점.
;결과, 최대 80포인트, 평균 40 정도의 만족도가 됩니다. 랜덤이 40을 차지하므로, 생각보다는 안정되지 않습니다.
;다만, 소질적인 요인이 있는 경우는 80포인트를 돌파를 해도 상관하지 않습니다. 생각보다는 어바웃.


IF 1 == 0
;더미 분기입니다
ELSEIF RAND:12 == 0 && ABL:COUNT:20 >= 3
	;역애널. 손가락이라든지 완구라든지
	TFLAG:200 = 1112
	;기교, 봉사 2, 새디즘
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:13
	SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9 * 2
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:12
	SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:20
	SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9
	PRINTFORML %조사처리(CALLNAME:COUNT,"는")% 고객이 옷을 아직 다 못벗고 꾸물거리자 기다리지 못하고 
	IF FLAG:3000 & 2
		PRINTL 로션으로 촉촉하게 적셔둔 고객의 애널에 진동기를 집어넣기 시작했다.
	ELSE
		PRINTFORML 로션으로 촉촉하게 적셔둔 고객의 애널에 손가락를 집어넣고 애무하기 시작했다.
	ENDIF
	PRINTFORML 전립선과 장내를 꿀럭꿀럭 애무하니 고객이 비명을 지르면서 정액을 배출했다.
	CALL SPRING_HAPPY
	IF LOCAL@SPRING_DUMMY:0 >= 3
		PRINTW 고객은 굉장한 쾌감에 소리도 나오지 않는 것 같다.
	ELSEIF LOCAL@SPRING_DUMMY:0 < 2
		PRINTW 유감스럽지만, 고객들의 욕구를 채우지 못했다.
	ENDIF
	LOCAL@SPRING_EXP:4 = LOCAL@SPRING_DUMMY:6
	LOCAL@SPRING_EXP:77 = 10
	LOCAL@SPRING_DUMMY:5 = 4
ELSEIF RAND:11 == 0 && FLAG:3000 & 2
	;향기 플레이? 성감 맛사지라고도 말하는 것 같다
	TFLAG:200 = 1111
	;기교 2, 봉사 2
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:13
	SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9 * 2
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:12
	SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9 * 2
	PRINTFORML %조사처리(CALLNAME:COUNT,"는")% 고객의 옷에 손을 대어, 자기와 똑같이 완전히 옷을 벗겼다.
	PRINTFORML 그리고, 손에 향유를 바른뒤 고객의 몸 구석구석을 마사지 했다.
	CALL SPRING_HAPPY
	IF LOCAL@SPRING_DUMMY:0 >= 3
		PRINTW 고객들은 체력이회복되는 마음을 느꼇다.
	ELSEIF LOCAL@SPRING_DUMMY:0 < 2
		PRINTW 하지만 유감스럽게도 고객들의 욕구를 채울 수는 없었던 모양이다.
	ENDIF
	LOCAL@SPRING_EXP:77 = 10
	LOCAL@SPRING_DUMMY:5 = 4
ELSEIF RAND:10 == 0 && ABL:COUNT:20 >= 3 && FLAG:3000 & 2
	;마 산호개르나틱. 조건은 새디즘기분 3LV
	TFLAG:200 = 1110
	;욕망 1, 노출 1, 새디즘 2
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:11
	SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:14
	SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:20
	SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9 * 2
	PRINTFORML 옷을 벋어던진 %조사처리(CALLNAME:COUNT,"는")% 고객의 옷에 손을 대어, 자기와 똑같이 완전히 옷을 벗겼다.
	PRINTFORML 그리고 그 위에 기승위로 올라타, 마치 말을 다루듯이 몇번이고 채찍을 내리치며 매도했다.
	IF ABL:COUNT:20 >= 5
		PRINTFORML 온몸이 새빨갛게 물들면서도 기뻐하는 고객의 엉덩이에 말의 꼬리를 모방한 바이브를 냅다 꽃으면서, 
		PRINTFORML %조사처리(CALLNAME:COUNT,"는")% 가벼운 절정을 맞이했다.
	ELSEIF TALENT:COUNT:120
		PRINTFORML 여성 고객은 %CALLNAME:COUNT%가 찔러 넣어둔 바이브래이터를 격렬하게 움직이면서 넣었다.
		PRINTFORML %조사처리(CALLNAME:COUNT,"는")% 가학의 쾌감에 미소를 띄웠다.
	ELSE
		PRINTFORML 채찍을 내려치고 매도하는데도 거기에 맞춰 쾌락의 소리를 내는 고객을 보며, 
		PRINTFORML %조사처리(CALLNAME:COUNT,"는")% 열락의 표정을 띠웠다.
	ENDIF
	CALL SPRING_HAPPY
	IF LOCAL@SPRING_DUMMY:0 >= 3
		PRINTW 고객들은 그런 모습에 몸을 떨며, 만족한 듯하다.
	ELSEIF LOCAL@SPRING_DUMMY:0 < 2
		PRINTW 하지만 유감스럽게도 고객들의 욕구를 채울 수는 없었던 모양이다.
	ENDIF
	LOCAL@SPRING_EXP:77 = 10
	LOCAL@SPRING_DUMMY:5 = 4
ELSEIF RAND:9 == 0 && (TALENT:COUNT:130 || TALENT:COUNT:131) && TALENT:COUNT:120 == 0
	;마 산호와. 조건은 유치한가 유아
	
	TFLAG:200 = 1109
	;욕망 2, 노출 1, 순종 1
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:11
	SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9 * 2
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:14
	SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:10
	SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9
	PRINTFORML 알몸이 된 %조사처리(CALLNAME:COUNT,"는")% 고객의 위에 올라타서, 
	PRINTL 말타기를 하거나 껴안거나 하며 순진하게 말타기 놀이를 즐겼다
	IF ABL:COUNT:11 >= 5
		PRINTFORML 진동에 느껴버렸는지, 고객의 등은 %CALLNAME:COUNT%의 애액으로 흥건하게 되어버렸다.
	ENDIF
	CALL SPRING_HAPPY
	IF LOCAL@SPRING_DUMMY:0 >= 3
		PRINTW 고객들은 그런 순수한 소녀의 신체와의 접촉을 충분히 즐겼다.
	ELSEIF LOCAL@SPRING_DUMMY:0 < 2
		PRINTW 하지만 유감스럽게도 고객들의 욕구를 채울 수는 없었던 모양이다.
	ENDIF
	LOCAL@SPRING_EXP:77 = 10
	LOCAL@SPRING_DUMMY:5 = 4
ELSEIF RAND:8 == 0 && ABL:COUNT:35 && FLAG:3000 & 8 && TALENT:COUNT:120 == 0
	;촉수입니다
	PRINTFORML %조사처리(CALLNAME:COUNT,"는")% 고객의 눈앞에서 촉수에게 공격받고 있는 모습을 보여주고 있다.
	TFLAG:200 = 1108
	;욕망 1, 노출 1, 촉수 중독 2
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:11
	SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:14
		SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:35
		SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9 * 2
	;V경험 50이상이라면 V에 촉수가, 똑같이 A도 처리가.
	PRINT 온몸을 촉수에 칭칭 묶여서, 
	IF EXP:COUNT:1 >= 50
		PRINT 뒷구멍을 가는 촉수에 의해 희롱당하고 
		LOCAL@SPRING_DUMMY:3 += 1
		LOCAL@SPRING_DUMMY:4 |= 1
	ENDIF
	IF EXP:COUNT:0 >= 50 && TALENT:COUNT:0 == 0 && TALENT:COUNT:120 == 0
		PRINT 앞구멍은 거대한 촉수에게 꿰뚫리고 
		LOCAL@SPRING_DUMMY:3 += 1
		LOCAL@SPRING_DUMMY:4 |= 2
	ENDIF
	PRINTL 입은 목구멍 안쪽까지 꿈틀대는 촉수에 침입당한 모습을 관객들은 마음껏 감상했다.
	LOCAL@SPRING_DUMMY:4 |= 64
	CALL SPRING_HAPPY
	
	IF LOCAL@SPRING_DUMMY:0 >= 3
		PRINTW 고객들은 그 치태를 씹어먹을 듯이 훑어보고 있는 듯하다.
	ELSEIF LOCAL@SPRING_DUMMY:0 < 2
		PRINTW 하지만 유감스럽게도 고객들의 욕구를 채울 수는 없었던 모양이다.
	ENDIF
	SIF LOCAL@SPRING_DUMMY:4 & 1
		LOCAL@SPRING_EXP:1 = LOCAL@SPRING_DUMMY:6
	SIF LOCAL@SPRING_DUMMY:4 & 2
		LOCAL@SPRING_EXP:0 = LOCAL@SPRING_DUMMY:6
	SIF LOCAL@SPRING_DUMMY:4 & 16
		LOCAL@SPRING_EXP:72 = LOCAL@SPRING_DUMMY:6
	LOCAL@SPRING_EXP:55 = LOCAL@SPRING_DUMMY:6
	LOCAL@SPRING_EXP:31 = LOCAL@SPRING_DUMMY:6
	LOCAL@SPRING_EXP:77 = 13
	LOCAL@SPRING_DUMMY:5 = 7
ELSEIF RAND:7 == 0 && ABL:COUNT:14 > 2 && FLAG:3000 & 2
	;야외에서 데이트. 물론, 성적인 의미로. 노출 3 LV필요
	PRINTFORML %조사처리(CALLNAME:COUNT,"는")% 개목걸이만을 차고 고객과의 심야 데이트를 나섰다.
	PRINTFORML 창관에서 미리 준비해둔 전용 데이트 코스를, 고객이 개줄을 끄는대로 끌려다니며 걷는 것 뿐인데도
	IF TALENT:COUNT:120
		PRINTFORML %조사처리(CALLNAME:COUNT,"는")% 발기한 상태로 귀두쪽이 젖어있다.
	ELSE
		PRINTFORML %CALLNAME:COUNT%의 가랑이 사이에선 꿀이 맻혀 떨어지고 있다.
	ENDIF
	TFLAG:200 = 1107
	;노출 4
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:14
		SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9 * 4
	;유치, 흘리는 버릇, 동물귀라면 보너스 2
	IF TALENT:COUNT:45 && ABL:COUNT:14 >= 5
		PRINTL 중간에 개처럼 방뇨를 해 보이자, 고객은 몹시 기뻐했다.
		LOCAL@SPRING_DUMMY:3 += 2
		LOCAL@SPRING_EXP:6 = LOCAL@SPRING_DUMMY:6
	ELSEIF TALENT:COUNT:116 && ABL:COUNT:10 >= 3
		PRINTL 도중에 동물같은 몸짓을 보이자, 고객은 몹시 기뻐한 것 같다.
		LOCAL@SPRING_DUMMY:3 += 2
	ELSEIF TALENT:COUNT:131 && ABL:COUNT:10 >= 3
		PRINTL 아무것도 모르는듯 순수하게 밖을 즐기는 모습을 보며, 고객은 몹시 기뻐한 듯하다.
		LOCAL@SPRING_DUMMY:3 += 2
	ELSEIF RAND:2500 <= EXP:COUNT:77 && TALENT:COUNT:127 == 0 && TALENT:COUNT:120
		PRINTFORML 그 모습에 흥분해버린 여성 고객은 나무 아래로 끌고간뒤 %CALLNAME:COUNT%를 덮쳤다.
		PRINTFORMW %CALLNAME:COUNT%가 내는 짐승같은 소리와 고객의 신음소리가 주위로 퍼져갔다.
		LOCAL@SPRING_DUMMY:3 += 10
		LOCAL@SPRING_EXP:3 += 2
		LOCAL@SPRING_EXP:2 += 2
		LOCAL@SPRING_DUMMY:4 |= 10
	ELSEIF RAND:2500 <= EXP:COUNT:77 && TALENT:COUNT:0 && TALENT:COUNT:127 == 0
		PRINTFORML 그 모습에 흥분해버린 고객은 나무그늘로 끌고가 %조사처리(CALLNAME:COUNT,"를")% 갑자기 덮쳤다.
		PRINTFORMW %CALLNAME:COUNT%의 비명은 누구도 들을 수 없었다.
		LOCAL@SPRING_NIN = 1
		LOCAL@SPRING_DUMMY:3 += 50
		LOCAL@SPRING_DUMMY:4 |= 14
	ELSEIF RAND:1000 <= EXP:COUNT:77 && TALENT:COUNT:0 == 0 && TALENT:COUNT:127 == 0
		PRINTFORML 그 모습에 흥분해버린 고객은 나무그늘로 끌고가 %조사처리(CALLNAME:COUNT,"를")% 갑자기 덮쳤다.
		PRINTFORMW %CALLNAME:COUNT%의 응석부리는 강아지 같은 신음소리가 주변에 울려퍼졌다.
		LOCAL@SPRING_NIN = 1
		LOCAL@SPRING_DUMMY:3 += 10
		LOCAL@SPRING_DUMMY:4 |= 10
	ENDIF
	
	CALL SPRING_HAPPY
	
	IF LOCAL@SPRING_DUMMY:0 >= 3
	IF ABL:COUNT:14 > 3
		PRINTW 야외에 있는 것만으로도 느끼고 있는 그 모습이 고객에게 먹힌 것 같다.
	ELSE
		PRINTW 부끄러워하는 듯한 모습이 고객들의 하트를 쥔 모양이다.
	ENDIF
	ELSEIF LOCAL@SPRING_DUMMY:0 < 2
		PRINTW 하지만 유감스럽게도 고객들의 욕구를 채울 수는 없었던 모양이다.
	ENDIF
	SIF LOCAL@SPRING_DUMMY:4 & 16
		LOCAL@SPRING_EXP:72 = LOCAL@SPRING_DUMMY:6
	SIF LOCAL@SPRING_DUMMY:4 & 8
		LOCAL@SPRING_EXP:0 = 1
	LOCAL@SPRING_EXP:77 = 8
	LOCAL@SPRING_DUMMY:5 = 4
ELSEIF RAND:6 == 0 && ABL:COUNT:12 > 3 && FLAG:3000 & 2
	;M형. 상대가 탓할 수 있는……것은 아니다. 왜냐하면, 상대로부터 하행동불가이니까.
	;즉, 묶은 상태를 바라본다고 하는 신사 플레이.
	PRINTFORML %조사처리(CALLNAME:COUNT,"는")% 전라의 몸에 눈가리개를 한 상태에, 
	PRINTFORML Ｍ자로 다리를 벌린 포즈로 움직일 수 없도록 탁자 위에 꽁꽁 묶였다.
	TFLAG:200 = 1106
	;욕망 1, 노출 1, 매저키스트 2
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:11
		SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:21
		SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9 * 2
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:14
		SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9
	;흘리는 버릇이라면 보너스 2
	IF TALENT:COUNT:45
		PRINTFORML 공포 때문인지 아님 쾌감 때문인지, 고객 앞에서 %조사처리(CALLNAME:COUNT,"는")% 몇번이고 묶여있는 채로 소변을 내뿜었다.
		LOCAL@SPRING_DUMMY:3 += 2
		LOCAL@SPRING_EXP:6 = LOCAL@SPRING_DUMMY:6
	ENDIF
	PRINTFORML 고객들은 그것을 충분히 감상하며 즐긴 모양이다.
	
	CALL SPRING_HAPPY
	
	IF LOCAL@SPRING_DUMMY:0 >= 3
	IF LOCAL@SPRING_EXP:6
		PRINTW 고객들은 아름다운 것을 봤다며 기뻐하는 듯하다.
	ELSEIF TALENT:COUNT:120 && ABL:COUNT:21 > 2
		PRINTW 속묶여있는 것만으로도 발기한 모습이 여성 고객들은 몹시 마음에 들어한 모양이다.
	ELSEIF ABL:COUNT:21 > 2
		PRINTW 묶여있는 것만으로도 애액을 흘리는 모습을 고객들은 몹시 마음에 들어한 모양이다.
	ELSE
		PRINTW 고객들은 그 절경에 보고 어머라고 있던 것 같다.
	ENDIF
	ELSEIF LOCAL@SPRING_DUMMY:0 < 2
		PRINTW 하지만 유감스럽게도 고객들의 욕구를 채울 수는 없었던 모양이다.
	ENDIF
	SIF LOCAL@SPRING_DUMMY:4 & 16
		LOCAL@SPRING_EXP:72 = LOCAL@SPRING_DUMMY:6
	LOCAL@SPRING_EXP:31 = LOCAL@SPRING_DUMMY:6
	LOCAL@SPRING_EXP:77 = 8
	LOCAL@SPRING_DUMMY:5 = 4
ELSEIF RAND:5 == 0 && ABL:COUNT:12 >= 2 && ABL:COUNT:20 && FLAG:3000 & 2
	;S형. 상대를 탓한다. 뜯는다. . 공격한다. 기교 2로 새디즘1이 필수
	PRINTFORML 본디지 슈츠를 몸에 두른 %조사처리(CALLNAME:COUNT,"는")%, 고객의 옷을 벗기고 채찍과 촛불을 사용해 공략하기 시작했다.
	TFLAG:200 = 1105
	;패턴 1. 상대를 오로지에 공격한다. 진성 새디즘
	;기교 2, 새디즘2
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:12
		SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:1 += LOCAL@SPRING_DUMMY:9 * 2
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:20
		SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:1 += LOCAL@SPRING_DUMMY:9 * 2
	;프라이드가 높은가 새디즘이라면 보너스 2
	SIF TALENT:COUNT:16 || TALENT:COUNT:81
		LOCAL@SPRING_DUMMY:1 += 2
		
	;패턴 2. 부끄러운 것 같은 표정으로 상대를 흥분시킨다. 더듬거린 손놀림이 또, 사랑스러운 것……같다.
	;예외적으로, 능력이 낮은 것이 효과가 오른다.
	;5-기교,5-노출버릇, 봉사 정신, 욕망
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:11
		SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
			LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:5 += LOCAL@SPRING_DUMMY:9
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:13
		SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:5 += LOCAL@SPRING_DUMMY:9
	LOCAL@SPRING_DUMMY:5 += 10
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:12
		SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:5 -= LOCAL@SPRING_DUMMY:9
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:14
		SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:5 -= LOCAL@SPRING_DUMMY:9
	;겁장이인가 수줍음으로 보너스 2
	SIF TALENT:COUNT:10 || TALENT:COUNT:35
		LOCAL@SPRING_DUMMY:5 += 2
		
	IF LOCAL@SPRING_DUMMY:1 > LOCAL@SPRING_DUMMY:5
		LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:1
		LOCAL@SPRING_DUMMY:5 = 0
	ELSE
		LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:5
		LOCAL@SPRING_DUMMY:1 = 0
	ENDIF
	
	CALL SPRING_HAPPY
	
	IF LOCAL@SPRING_DUMMY:0 >= 3
	IF LOCAL@SPRING_DUMMY:1 > 0
		PRINTW 짓밟히고 차이고 희롱당하고 범해져서, 고객은 몹시 만족한 듯하다.
	ELSE
		PRINTW 새빨개 져가지고도 어설픈 솜씨로 계속 공략해오는 모습에 고객들의 하트는 장악당했다.
	ENDIF
	ELSEIF LOCAL@SPRING_DUMMY:0 < 2
		PRINTW 하지만 유감스럽게도 고객들의 욕구를 채울 수는 없었던 모양이다.
	ENDIF
	SIF LOCAL@SPRING_DUMMY:4 & 16
		LOCAL@SPRING_EXP:72 = LOCAL@SPRING_DUMMY:6
	LOCAL@SPRING_EXP:77 = 8
	LOCAL@SPRING_DUMMY:5 = 4
ELSEIF RAND:4 == 0 && ABL:COUNT:12 > 0 && ABL:COUNT:13 > 0 && TALENT:COUNT:54 == 0
	;봉사형. 상대의 성기를 꾸짖는다. 뜯는다. . 봉사 정신 1 LV와 기교 1이 필수가 된다. 뜨거운 것을 먹지 못하는 사람 불가
	PRINTFORML %조사처리(CALLNAME:COUNT,"는")% 고객을 개인실로 맞이한 뒤
	PRINTL 그의 옷을 벗기고 성기에 입을 맞춘 뒤 봉사를 시작했다.
	TFLAG:200 = 1104
	;기교 2, 봉사 정신 1, 정액 중독 1
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:12
		SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9 * 2
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:13
		SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:31
		SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9
	
	;대상이 여성으로 게다가 레즉기분이나 레즈비언 중독이 있으면 보너스 2
	IF RAND:2 == 0 && TALENT:COUNT:120 == 0
		SIF ABL:COUNT:22 > 3
			LOCAL@SPRING_DUMMY:3 += 2
		SIF ABL:COUNT:32 > 3
			LOCAL@SPRING_DUMMY:3 += 2
		LOCAL@SPRING_EXP:12 = LOCAL@SPRING_DUMMY:6
	ELSEIF RAND:1000 <= EXP:COUNT:77 && TALENT:COUNT:127 == 0 && TALENT:COUNT:120
		PRINTFORML %CALLNAME:COUNT%의 파렴치한 복장에 흥분한 여성 고객은 느닷없이 몸을 덮쳐눌렀다.
		PRINTFORMW %조사처리(CALLNAME:COUNT,"는")% 미소를 띄우며 자신이 봉사하던 고객의 비부에 자신의 세번째 다리를 찔러 넣었다.
		LOCAL@SPRING_DUMMY:3 += 10
		LOCAL@SPRING_DUMMY:4 |= 10
		LOCAL@SPRING_EXP:3 = 2
	;처녀의 경우1%의 확률로 습격당합니다. 처녀가 아니면2%의 확률로 습격당합니다.
	ELSEIF RAND:2500 <= EXP:COUNT:77 && TALENT:COUNT:0 && TALENT:COUNT:127 == 0 && TALENT:COUNT:120 == 0
		PRINTFORML %CALLNAME:COUNT%의 봉사하는 모습에 흥분한 고객은 느닷없이 몸을 덮쳐눌렀다.
		PRINTFORMW %조사처리(CALLNAME:COUNT,"는")% 저항하지도 못한 채 처녀를 유린당하고 말았다.
		LOCAL@SPRING_NIN = 1
		LOCAL@SPRING_DUMMY:3 += 50
		LOCAL@SPRING_DUMMY:4 |= 14
		LOCAL@SPRING_EXP:4 = 1
	ELSEIF RAND:1000 <= EXP:COUNT:77 && TALENT:COUNT:0 == 0 && TALENT:COUNT:127 == 0 && TALENT:COUNT:120 == 0
		PRINTFORML %CALLNAME:COUNT%의 파렴치한 복장에 흥분한 고객은 느닷없이 몸을 덮쳐눌렀다.
		PRINTFORMW %조사처리(CALLNAME:COUNT,"는")% 요염한 미소를 띄우고 고객을 받아들였다.
		LOCAL@SPRING_NIN = 1
		LOCAL@SPRING_DUMMY:3 += 10
		LOCAL@SPRING_DUMMY:4 |= 10
		LOCAL@SPRING_EXP:4 = 1
	ELSE
		LOCAL@SPRING_EXP:4 = LOCAL@SPRING_DUMMY:6
	ENDIF
	;헌신적이면 보너스 2
	SIF TALENT:COUNT:63
		LOCAL@SPRING_DUMMY:3 += 2
	CALL SPRING_HAPPY
	
	IF LOCAL@SPRING_DUMMY:0 >= 3
	IF ABL:COUNT:12 >= 3
		PRINTW 그 기교로 고객을 포로로 한 모양이다.
	ELSEIF ABL:COUNT:13 >= 4
		PRINTW 그 봉사정신으로 고객을 실로 상쾌하게 만들어준 모양이다.
	ELSE
		PRINTW 고객들은 몹시 만족한 모양이다.
	ENDIF
	ELSEIF LOCAL@SPRING_DUMMY:0 < 2
		PRINTW 하지만 유감스럽게도 고객들의 욕구를 채울 수는 없었던 모양이다.
	ENDIF
	SIF LOCAL@SPRING_DUMMY:4 & 1
		LOCAL@SPRING_EXP:1 = LOCAL@SPRING_DUMMY:6
	SIF LOCAL@SPRING_DUMMY:4 & 2
		LOCAL@SPRING_EXP:0 = 1
	SIF LOCAL@SPRING_DUMMY:4 & 16
		LOCAL@SPRING_EXP:72 = LOCAL@SPRING_DUMMY:6
	LOCAL@SPRING_EXP:22 = LOCAL@SPRING_DUMMY:6
	LOCAL@SPRING_EXP:77 = 7
	LOCAL@SPRING_DUMMY:5 = 3
	
	
ELSEIF RAND:3 == 0 && MARK:COUNT:2 >= 2 && EXP:10 > 0
	;기본 그 2. 자위보여주기. 굴복 각인 2 LV와 자위 경험 필수. 설비 LV2로 도구를 사용해 자위가 된다.
	PRINTFORM %조사처리(CALLNAME:COUNT,"는")% 고객을 개인실로 맞이하더니, 
	IF RAND:3 == 0
		PRINT 인사도 그냥저냥 하고 옷을 입은 채로 팬티만을 비껴 
	ELSEIF RAND:2 == 0
		PRINT 침대 위에서 전라인 채로 다리를 Ｍ자로 벌린 뒤 
	ELSE
		PRINT 눈앞에서 옷을 벗어던지고 크게 몸을 열어 
	ENDIF
	IF FLAG:3000 & 2 && ABL:COUNT:10 >= 3 && TALENT:COUNT:121
		PRINT 오나홀을 사용하면서
		LOCAL@SPRING_DUMMY:3 += 1
		LOCAL@SPRING_DUMMY:4 |= 2
	ELSEIF FLAG:3000 & 2 && ABL:COUNT:1 >= 3 && EXP:COUNT:0 > 10 && TALENT:COUNT:120 == 0
		PRINT 거대한 바이브를 스스로 삽입하고 
		LOCAL@SPRING_DUMMY:3 += 1
		LOCAL@SPRING_DUMMY:4 |= 2
	ELSEIF FLAG:3000 & 2 && ABL:COUNT:2 >= 3
		PRINT 애널바이브를 스스로 삽입하고 
		LOCAL@SPRING_DUMMY:3 += 1
		LOCAL@SPRING_DUMMY:4 |= 1
	ELSEIF FLAG:3000 & 2 && ABL:COUNT:10 >= 3 && TALENT:COUNT:120 == 0
		PRINT 진동하는 로터를 가랑이 사이에 대고 
		LOCAL@SPRING_DUMMY:3 += 1
	ELSEIF FLAG:3000 & 2 && ABL:COUNT:10 >= 3
		PRINT 오나홀을 사용하면서
		LOCAL@SPRING_DUMMY:3 += 1
		LOCAL@SPRING_DUMMY:4 |= 2
	ELSE
		PRINT 수치로 떨고 있는 손가락으로 가랑이 사이를 희롱하며 
	ENDIF
	PRINTL 자위를 보여줬다.
	TFLAG:200 = 1103
	;욕망 1, 노출버릇 2, 자위 중독 1
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:11
		SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:14
		SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9 * 2
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:30
		SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9
	;눈에 띄고 싶어해, 자위하기 쉽고 보너스 2
	SIF TALENT:COUNT:28 || TALENT:COUNT:60
		LOCAL@SPRING_DUMMY:3 += 2
	;처녀의 경우1%의 확률로 습격당합니다. 처녀가 아니면3%의 확률로 습격당합니다.
	IF RAND:2500 <= EXP:COUNT:77 && TALENT:COUNT:0 && TALENT:COUNT:127 == 0 && TALENT:COUNT:120 == 0
		PRINTFORML %조사처리(CALLNAME:COUNT,"가")% 절정을 맞이하여 방심하고 있자, 거기에 흥분한 고객이 갑자기 덮쳐눌렀다.
		PRINTFORML 두 사람만의 밀실공간, 절정 직후의 권태감.
		PRINTFORMW %조사처리(CALLNAME:COUNT,"는")% 저항하지도 못한 채 처녀를 유린당하고 말았다.
		LOCAL@SPRING_NIN = 1
		LOCAL@SPRING_DUMMY:3 += 50
		LOCAL@SPRING_DUMMY:4 |= 14
	ELSEIF RAND:1000 <= EXP:COUNT:77 && TALENT:COUNT:0 == 0 && LOCAL@SPRING_DUMMY:4 == 2 && TALENT:COUNT:127 == 0 && TALENT:COUNT:120 == 0
		PRINTFORML %조사처리(CALLNAME:COUNT,"가")% 절정을 맞이하여 방심하고 있자, 거기에 흥분한 고객이 갑자기 덮쳐눌렀다.
		PRINTFORMW %조사처리(CALLNAME:COUNT,"는")% 바이브를 뽑아내고 요염한 미소를 띄운 채 고객을 받아들였다.
		LOCAL@SPRING_NIN = 1
		LOCAL@SPRING_DUMMY:3 += 10
		LOCAL@SPRING_DUMMY:4 |= 8
	ELSEIF RAND:1000 <= EXP:COUNT:77 && TALENT:COUNT:0 == 0 && LOCAL@SPRING_DUMMY:4 == 2 && TALENT:COUNT:127 == 0
		PRINTFORML %조사처리(CALLNAME:COUNT,"가")% 절정을 맞이하여 방심하고 있자, 거기에 흥분한 고객이 갑자기 덮쳐눌렀다.
		PRINTFORMW %조사처리(CALLNAME:COUNT,"는")% 오나홀을 뽑아내고 요염한 미소를 띄운 채 고객을 받아들였다.
		LOCAL@SPRING_EXP:3 += 2
		LOCAL@SPRING_DUMMY:3 += 10
		LOCAL@SPRING_DUMMY:4 |= 8
	ELSEIF RAND:1000 <= EXP:COUNT:77 && TALENT:COUNT:0 == 0 && TALENT:COUNT:127 == 0 && TALENT:COUNT:120 == 0
		PRINTFORML %조사처리(CALLNAME:COUNT,"가")% 절정을 맞이하여 방심하고 있자, 거기에 흥분한 고객이 갑자기 덮쳐눌렀다.
		PRINTFORMW %조사처리(CALLNAME:COUNT,"는")% 요염한 미소를 띄운 채 고객을 받아들였다.
		LOCAL@SPRING_NIN = 1
		LOCAL@SPRING_DUMMY:3 += 10
		LOCAL@SPRING_DUMMY:4 |= 10
		SIF TALENT:COUNT:120
			LOCAL@SPRING_EXP:3 += 2
	ENDIF
	
	CALL SPRING_HAPPY
	
	IF LOCAL@SPRING_DUMMY:0 >= 3
	IF ABL:COUNT:30 >= 3
		PRINTW 자위에 몰두하여 신음하는 모습이 고객을 만족시킨 모양이다.
	ELSEIF ABL:COUNT:14 >= 3
		PRINTW 도발적인 포즈로 보여주는 자위가 고객의 마음에 깊이 남은 모양이다.
	ELSE
		PRINTW 그 모습과 봉사에 많은 고객들이 만족했다.
	ENDIF
	ELSEIF LOCAL@SPRING_DUMMY:0 < 2
		PRINTW 하지만 유감스럽게도 고객들의 욕구를 채울 수는 없었던 모양이다.
	ENDIF
	SIF LOCAL@SPRING_DUMMY:4 & 1
		LOCAL@SPRING_EXP:1 = LOCAL@SPRING_DUMMY:6
	SIF LOCAL@SPRING_DUMMY:4 & 2
		LOCAL@SPRING_EXP:0 = LOCAL@SPRING_DUMMY:6
	SIF LOCAL@SPRING_DUMMY:4 & 4
		LOCAL@SPRING_EXP:0 = 1
	SIF LOCAL@SPRING_DUMMY:4 & 16
		LOCAL@SPRING_EXP:72 = LOCAL@SPRING_DUMMY:6
	LOCAL@SPRING_EXP:2 = LOCAL@SPRING_DUMMY:6
	LOCAL@SPRING_EXP:10 = LOCAL@SPRING_DUMMY:6
	LOCAL@SPRING_EXP:77 = 7
	LOCAL@SPRING_DUMMY:5 = 3
	
	
ELSEIF RAND:2 == 0
	;기본이 되는 접대. 같은 접대. 카바레식 클럽? 조건 없음
	;설비 2 LV 한편, V경험 3 LV이상이나 A경험 3 LV이상이면, 바이브래이터를 사용하면서의 접대가 된다.
	;만족도가 다른 한쪽이라면+1, 양쪽 모두라면+3된다.
	PRINTFORM %조사처리(CALLNAME:COUNT,"는")%
	IF FLAG:3000 & 2
		IF ABL:COUNT:1 >= 3 && ABL:COUNT:2 >= 3 && TALENT:COUNT:0 == 0 && TALENT:COUNT:120 == 0
			PRINT 바이브를 앞에도 뒤에도 꽃은 채로 
			LOCAL@SPRING_DUMMY:3 += 3
			LOCAL@SPRING_DUMMY:4 |= 3
			LOCAL@SPRING_EXP:20 = LOCAL@SPRING_DUMMY:6
		ELSEIF ABL:COUNT:1 >= 3 && TALENT:COUNT:0 == 0 && TALENT:COUNT:120 == 0
			PRINT 바이브를 앞에 꽃은 채로 
			LOCAL@SPRING_DUMMY:3 += 1
			LOCAL@SPRING_DUMMY:4 |= 2
			LOCAL@SPRING_EXP:20 = LOCAL@SPRING_DUMMY:6
		ELSEIF ABL:COUNT:2 >= 3
			PRINT 바이브를 뒤에 꽃은 채로 
			LOCAL@SPRING_DUMMY:3 += 1
			LOCAL@SPRING_DUMMY:4 |= 1
			LOCAL@SPRING_EXP:20 = LOCAL@SPRING_DUMMY:6
		ENDIF
	ENDIF
	PRINTL 알몸에 에이프런이라는 한점 불결 없는 모습으로 고객을 개인실로 맞이해서, 
	PRINT 눈앞에서 요리를 만들더니, 고객의 명령에 따라 그것을 
	IF ABL:COUNT:10 >= 3
		PRINT 입으로
		LOCAL@SPRING_EXP:23 = LOCAL@SPRING_DUMMY:6
	ENDIF
	PRINTL 먹여 주었다.
	TFLAG:200 = 1102
	;조리 기능 1, 노출버릇 2, 봉사 정신 1
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:70
		SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:13
		SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:14
		SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:9 * 2
	
	;대상이 여성으로 게다가 레즉기분이나 레즈비언 중독이 있으면 보너스 2
	IF RAND:2 == 0
		SIF ABL:COUNT:22 > 3
			LOCAL@SPRING_DUMMY:3 += 2
		SIF ABL:COUNT:32 > 3
			LOCAL@SPRING_DUMMY:3 += 2
	;처녀의 경우1%의 확률로 습격당합니다. 처녀가 아니면2%의 확률로 습격당합니다.
	ELSEIF RAND:2500 <= EXP:COUNT:77 && TALENT:COUNT:0 && TALENT:COUNT:127 == 0 && TALENT:COUNT:120 == 0
		PRINTFORML %CALLNAME:COUNT%의 파렴치한 복장에 흥분한 고객은 느닷없이 덮쳐눌렀다.
		PRINTFORMW %조사처리(CALLNAME:COUNT,"는")% 저항하지도 못한 채 처녀를 유린당하고 말았다.
		LOCAL@SPRING_NIN = 1
		LOCAL@SPRING_DUMMY:3 += 50
		LOCAL@SPRING_DUMMY:4 |= 14
	ELSEIF RAND:1000 <= EXP:COUNT:77 && TALENT:COUNT:0 == 0 && LOCAL@SPRING_DUMMY:4 == 2 && TALENT:COUNT:127 == 0 && TALENT:COUNT:120 == 0
		PRINTFORML %CALLNAME:COUNT%의 파렴치한 복장에 흥분한 고객은 느닷없이 덮쳐눌렀다.
		PRINTFORMW %조사처리(CALLNAME:COUNT,"는")% 바이브를 뽑아내고 요염한 미소를 띄운 채 고객을 받아들였다.
		LOCAL@SPRING_NIN = 1
		LOCAL@SPRING_DUMMY:3 += 10
		LOCAL@SPRING_DUMMY:4 |= 8
	ELSEIF RAND:1000 <= EXP:COUNT:77 && TALENT:COUNT:0 == 0 && TALENT:COUNT:127 == 0
		PRINTFORML %CALLNAME:COUNT%의 파렴치한 복장에 흥분한 고객은 느닷없이 덮쳐눌렀다.
		PRINTFORMW %조사처리(CALLNAME:COUNT,"는")% 요염한 미소를 띄운 채 고객을 받아들였다.
		LOCAL@SPRING_DUMMY:3 += 10
		LOCAL@SPRING_DUMMY:4 |= 10
		IF TALENT:COUNT:120
			LOCAL@SPRING_EXP:3 += 1
		ELSE
			LOCAL@SPRING_NIN = 1
		ENDIF
	ENDIF
	;헌신적이면 보너스 2
	SIF TALENT:COUNT:63
		LOCAL@SPRING_DUMMY:3 += 2
	CALL SPRING_HAPPY
	
	IF LOCAL@SPRING_DUMMY:0 >= 3
	IF ABL:COUNT:13 >= 4
		PRINTW 열렬한 접대에 고객은 헤롱헤롱대고 있다.
	ELSEIF ABL:COUNT:14 >= 3
		PRINTW 격한 노출과 도발적인 포즈로 고객의 하트를 꽉 붙잡은 듯하다.
	ELSE
		PRINTW 그 모습과 봉사에 많은 고객들이 만족했다.
	ENDIF
	ELSEIF LOCAL@SPRING_DUMMY:0 < 2
		PRINTW 하지만 유감스럽게도 고객들의 욕구를 채울 수는 없었던 모양이다.
	ENDIF
	SIF LOCAL@SPRING_DUMMY:4 & 1
		LOCAL@SPRING_EXP:1 = LOCAL@SPRING_DUMMY:6
	SIF LOCAL@SPRING_DUMMY:4 & 2
		LOCAL@SPRING_EXP:0 = LOCAL@SPRING_DUMMY:6
	SIF LOCAL@SPRING_DUMMY:4 & 4
		LOCAL@SPRING_EXP:0 = 1
	SIF LOCAL@SPRING_DUMMY:4 & 16
		LOCAL@SPRING_EXP:72 = LOCAL@SPRING_DUMMY:6
	LOCAL@SPRING_EXP:70 = LOCAL@SPRING_DUMMY:6
	LOCAL@SPRING_EXP:77 = 6
	LOCAL@SPRING_DUMMY:0 = 2
	
ELSE
	;신사인 내용
	PRINTFORML %조사처리(CALLNAME:COUNT,"는")% 고객과 단 둘이 되자, 야한 말들을 섞어서 수다를 떨기 시작했다.
	TFLAG:200 = 1101
	;수패턴 가운데, 제일 높은 것을 선택합니다.
	;패턴 1. 상대를 오로지 성적으로 도발한다.
	;순종 1, 욕망 1, 노출버릇 2,
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:10
		SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:1 += LOCAL@SPRING_DUMMY:9
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:11
		SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:1 += LOCAL@SPRING_DUMMY:9
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:14
		SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:1 += LOCAL@SPRING_DUMMY:9 * 2
	;정조무구애, 해방으로 보너스 2
	SIF TALENT:COUNT:31 || TALENT:COUNT:33
		LOCAL@SPRING_DUMMY:1 += 2
	;패턴 2. 부끄러운 것 같은 표정으로 상대를 흥분시킨다.
	;예외적으로, 능력이 낮은 것이 효과가 오른다.
	;순종 1,5-노출버릇 2, 매저키스트 1
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:10
		SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:5 += LOCAL@SPRING_DUMMY:9
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:21
		SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:5 += LOCAL@SPRING_DUMMY:9
	LOCAL@SPRING_DUMMY:5 += 10
	LOCAL@SPRING_DUMMY:9 = ABL:COUNT:14
		SIF LOCAL@SPRING_DUMMY:9 >= 5 && (FLAG:3000 & 8388608) == 0
		LOCAL@SPRING_DUMMY:9 = 5
	LOCAL@SPRING_DUMMY:5 -= LOCAL@SPRING_DUMMY:9 * 2
	;겁장이인가 수줍음으로 보너스 2
	SIF TALENT:COUNT:10 || TALENT:COUNT:35
		LOCAL@SPRING_DUMMY:5 += 2
	
	IF LOCAL@SPRING_DUMMY:1 > LOCAL@SPRING_DUMMY:5
		LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:1
		LOCAL@SPRING_DUMMY:5 = 0
		B = 1
	ELSE
		LOCAL@SPRING_DUMMY:3 += LOCAL@SPRING_DUMMY:5
		LOCAL@SPRING_DUMMY:1 = 0
		F = 1
		PRINTFORML 고객이 명령하자, %조사처리(CALLNAME:COUNT,"는")% 눈에 눈물이 가득한 채 음탕한 이야기를 몇번이고 몇번이고 반복했다.
	ENDIF
	
	CALL SPRING_HAPPY
	
	IF LOCAL@SPRING_DUMMY:0 >= 3
	IF LOCAL@SPRING_DUMMY:1 > 0
		PRINTW 도발적인 언동과 매력적인 몸짓에, 고객은 충분히 매료된 것 같다.
	ELSE
		PRINTW 새빨개져가지고도 음탕한 소리를 이어가는 그 모습이, 고객들에게 대단히 먹힌 모양이다.
	ENDIF
	ELSEIF LOCAL@SPRING_DUMMY:0 < 2
		PRINTW 하지만 유감스럽게도 고객들의 욕구를 채울 수는 없었던 모양이다.
	ENDIF
	SIF LOCAL@SPRING_DUMMY:4 & 16
		LOCAL@SPRING_EXP:72 = LOCAL@SPRING_DUMMY:6
	LOCAL@SPRING_EXP:77 = 5
	LOCAL@SPRING_DUMMY:5 = 1


ENDIF


