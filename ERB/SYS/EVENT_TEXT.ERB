;≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡
;커맨드의 지문 추가분 · 커맨드의 특수 처리
;≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡
;=============================================================================
;커맨드의 지문(추가분)
;=============================================================================
;--------------------------------------------------------
;애무
;--------------------------------------------------------
@TEXT_0
PRINTL 
LOCAL = UP:0 + UP:3

IF LOCAL < 100
	IF TALENT:11
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 애무당하면서도 반항적인 태도를 취하고 있다.
	ELSE
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 욱신대는 듯 몸을 꼬지만 쾌락을 느끼는 데까진 이르지 못한 것 같다.
	ENDIF
ELSEIF LOCAL < 300
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 이쪽의 애무에 반응해 몸을 떨고 있다.
ELSEIF LOCAL < 1000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 가벼운 전기라도 몸에 통한 듯이 작게 경련했다.
ELSEIF LOCAL < 3000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 명백히 쾌락을 느끼고 있는 모습으로 요염한 신음소리를 내고 있다.
ELSEIF LOCAL < 6000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 애무에 격하게 쾌감을 느끼며, 스스로 매달리듯이 손을 당신의 머리 뒤로 돌리고 있다.
ELSE
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 이쪽의 애무만으로도 격렬하게 느끼며, 더욱 큰 자극을 찾아 매달려 왔다.
ENDIF

;--------------------------------------------------------
;커닐링구스
;--------------------------------------------------------
@TEXT_1
PRINTL 
LOCAL = UP:0

IF LOCAL < 300
	PRINTFORMW %CALLNAME:TARGET%의 그곳은 아직 딱 다물린 채다. 쾌락을 느끼려면 좀 더 걸릴지도 모르겠다.
ELSEIF LOCAL < 1000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 커닐링구스의 자극을 꾹 참고 있었지만, 확실히 느끼기 시작한 것 같다.
ELSEIF LOCAL < 3000
	PRINTFORM 민감한 콩알을 핥은 것에 느낀 건지, %조사처리(CALLNAME:TARGET,"는")% 
	SIF TALENT:35
		PRINTFORMW 수치에 뺨을 붉게 물들이 \@(TEQUIP:42) ? # 손으로 자신의 입을 누른 채 목소리가 나오는 걸 억누르 \@면서 
	IF TALENT:33 || TALENT:70
		PRINTW 뜨거운 숨결을 토하면서 쾌락에 몸을 맡기고 있다.
	ELSEIF TALENT:32 || TALENT:71
		PRINTW 쾌락을 부정하듯이 고개를 세차게 흔들며 참고 있다.
	ELSE
		PRINTFORMW 보지에서 애액을 흘렸\@(TEQUIP:42 == 0) ? 고, 작은 한숨을 토해냈\@다.
	ENDIF
ELSEIF LOCAL < 6000
	PRINTFORMW %CALLNAME:TARGET%의 작은 콩알을 혀로 마찰시키자 %조사처리(CALLNAME:TARGET,"는")% \@(TEQUIP:42) ? # 귀여운 목소리를 흘리면서 \@움찔움찔 몸을 경련했다.
ELSEIF LOCAL < 10000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% \@(TEQUIP:42) ? # 명백히 쾌락의 목소리를 지르며 \@, 무의식중에 이족의 머리를 손으로 누르고 그 부분에 더욱 깊이 밀착시켰다.
ELSE
	IF TALENT:100
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 민감한 \@(TALENT:121) ? 페니스 # 클리토리스\@로의 자극에 격렬히 몸을 꼬\@(TEQUIP:42) ? # 며, 계속해서 실로 엄청난 신음을 질러대\@고 있다.
	ELSE
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% \@(TALENT:121)? 페니스 #클리토리스\@로의 자극에\@(TEQUIP:42) ? 격렬히 교성을 지르며\@ 몸부림치고 있다.
	ENDIF
ENDIF

;--------------------------------------------------------
;애널 애무
;--------------------------------------------------------
@TEXT_2
PRINTL 
LOCAL = UP:2

IF LOCAL < 300
	PRINTFORM %조사처리(CALLNAME:TARGET,"는")% \@(TEQUIP:42) ? 작게 # 비명을 지르며\@ 몸을 비틀었다. 
	IF ABL:2 <= 2
		PRINTW 차근차근 개발해가면 될 것이다.
	ELSE
		PRINTW 몸의 준비가 부족한 것 같다.
	ENDIF
ELSEIF LOCAL < 1000
	PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 작게 몸을 요동쳤다. 
	IF ABL:2 <= 2
		PRINTW 조금 더 개발해줄 필요가 있을 것 같다.
	ELSE
		PRINTW 쾌락을 얻기에는 조금 더 몸을 개발하고 나서 하는 쪽이 좋을 것 같다.
	ENDIF
ELSEIF LOCAL < 3000
	PRINTFORM %조사처리(CALLNAME:TARGET,"는")% \@(TEQUIP:42) ? 몸을 비틀어댔다. # 작게 교성을 내며 몸을 비틀어댔다.\@ 
	SIF TALENT:104
		PRINT 애초부터 애널을 향한 자극에 약했던 모양인지, 
	PRINTW 명백히 쾌락을 느끼기 시작했다.
ELSEIF LOCAL < 6000
	PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 애널을 겨냥한 자극에 확실히 쾌락을 느끼고 있는 것 같다. 
ELSEIF LOCAL < 10000
	PRINTFORM \@(TALENT:104) ? 민감한 애널을 자극당하자 # 애널로 받는 자극에 \@ %조사처리(CALLNAME:TARGET,"는")% \@(TEQUIP:42) ? 몸을 요동치며 # 환희의 신음을 질러대며 \@ 
	IF TALENT:33 || TALENT:70
		PRINTW 쾌락을 호소했다.
	ELSEIF TALENT:32 || TALENT:71
		PRINTW 필사적으로 쾌락을 부정했다.
	ELSE
		PRINTW 미친 듯 몸부림쳤다.
	ENDIF
ELSE
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 완전히 개발된 애널을 만져주자 \@(TEQUIP:42) ? 미친 듯 좋아하고 있었다. # 참을 수 없다는 듯이 비명 섞인 신음을 지르며 미친 듯 좋아하고 있었다.\@
ENDIF

;--------------------------------------------------------
;가슴 애무
;--------------------------------------------------------
@TEXT_3
PRINTL 
LOCAL = UP:3

IF LOCAL < 300
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 아직 가슴으로 쾌락을 느끼는 수준까진 이르지 못한 것 같다.
ELSEIF LOCAL < 1000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 가슴을 주무르니 얼굴이 희미하게 붉어진 것 같다.
ELSEIF LOCAL < 3000
	PRINTFORMW %CALLNAME:TARGET%의 가슴을 계속 주무르고 있으니 유두가 딱딱하게 섰고 \@(TEQUIP:42) ? 뜨거운 # 숨결을 흘리기 시작했다.\@
ELSEIF LOCAL < 6000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 가슴으로의 애무에 몽롱한 표정을 지으며 그저 하는 대로 흘러가고 있다.
ELSEIF LOCAL < 10000
	PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
	IF TALENT:110
		PRINT 그 부풀음이 거의 없다시피한 가슴으로도 
	ELSEIF TALENT:111
		PRINT 그 다소 작은 크기의 가슴으로도 
	ENDIF
	PRINTW 엄청나게 느끼고 있어서, 가슴에 주는 자극만으로 열락의 극치에 달하려고 하고 있다.
ELSE
	PRINTFORM %CALLNAME:TARGET%의 가슴을 계속 주무르고 있자 그것만으로도 느낀 나머지 몸부림을 치며, 
	IF TALENT:113
		PRINTW 눈앞에서 그 폭유를 크게 튕기며 이쪽의 눈을 즐겁게 해주고 있다.
	ELSEIF TALENT:112
		PRINTW 눈앞에서 거유를 크게 흔들며 이쪽의 눈을 즐겁게 해주고 있다.
	ELSE
		PRINTW 완전히 풀린 표정으로 쾌락에 몸을 맡기고 있다.
	ENDIF
ENDIF

;--------------------------------------------------------
;펠라한다
;--------------------------------------------------------
@TEXT_5
PRINTL 
LOCAL = UP:0

IF LOCAL< 300
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 아직 펠라를 받는 것으로 쾌감을 느끼는 데에는 이르지 못한 듯하다.
ELSEIF LOCAL < 1000
	PRINTFORMW %CALLNAME:TARGET%의 \@(TALENT:1) ? 페니스와 같은 클리토리스 # 페니스\@로 오는 자극에 근질거림을 느끼는 듯하다.
ELSEIF LOCAL < 3000
	PRINTFORMW %CALLNAME:TARGET%의 \@(TALENT:1) ? 페니스와 같은 클리토리스 # 페니스\@는 약한 전기라도 통한 듯이 작게 떨렸다.
ELSEIF LOCAL < 6000
	PRINTFORMW %CALLNAME:TARGET%의 \@(TALENT:1) ? 페니스와 같은 클리토리스 # 페니스\@에 %CALLNAME:PLAYER%의 혀가 닿을 때마다 %조사처리(CALLNAME:TARGET,"는")% \@(TEQUIP:42) ? 몸을 떨고 있다. # 신음을 흘리고 있다.\@
ELSEIF LOCAL < 10000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 펠라에 쾌락을 느끼\@(TEQUIP:42) ? 고 있다 # 며 더욱 큰 자극을 구하며 마치 보채듯 신음을 냈다\@.
ELSE
	PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 펠라에 격렬하게 쾌락을 느껴, 
	PRINTFORMW 더욱 깊은 쾌감을 찾아 스스로 재촉하듯 손으로 %CALLNAME:PLAYER%의 머리를 움켜쥐었다.
ENDIF

;--------------------------------------------------------
;자위
;--------------------------------------------------------
@TEXT_7
PRINTL 
LOCAL = UP:0 + UP:1 + UP:2 + UP:3

PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
SIF TALENT:35 && ABL:14 <= 3
	PRINT 수치로 얼굴을 새빨갛게 물들인 채, 눈물을 삼키고 
IF TALENT:128
	PRINT 작은 몸으로 
ELSEIF TALENT:128 == 0 && (TEQUIP:20 == 1 || TEQUIP:25 == 1)
	PRINT 자신의 안에 
ENDIF
IF TEQUIP:20 == 1 && TEQUIP:25 == 1
	PRINT 두 개의 바이브를 깊이 삽입하고 있는 야한 모습을 보이며, 
ELSEIF TEQUIP:20 == 1
	PRINT 삽입되어 있는 바이브를 손에 쥐고, 
ELSEIF TEQUIP:25 == 1
	PRINT 깊숙히 꽃혀 있는 애널 바이브를 쥐고, 
ELSEIF TEQUIP:20 == 2 && TEQUIP:25 == 2
	PRINT 두 개의 요석을 깊숙히 삽입한 채, 
ELSEIF TEQUIP:20 == 2
	PRINT 요석을 바기나에 깊이 문 채, 
ELSEIF TEQUIP:25 == 2
	PRINT 요석을 애널로 깊이 문 채, 
ENDIF
SIF ABL:14 == 4
	PRINT 노출의 쾌락에 취한 표정을 보이면서 
IF ABL:14 >= 5
	PRINT 완전히 남에게 보이는 쾌감을 깨닫고 만 건지, 음탕한 표정을 지으며 스스로 애액이 샘솟듯 흐르는 보지를 훤히 벌려 보이며 
	IF TALENT:120
		IF TEQUIP:30 == 2
			PRINT 오나홀에 사로잡힌 페니스를 보여주며 
		ELSE
			PRINT 당장 사정할듯한 페니스를 보여주며 
		ENDIF
	ELSEIF TALENT:121
		IF TEQUIP:30 == 2
			PRINT 오나홀에 사로잡힌 페니스와 애액이 흘러넘치는 보지를 보여주며 
		ELSE
			PRINT 애액과 정액이 넘쳐나는 성기들을 보여주며 
		ENDIF
	ELSE
		PRINT 애액이 넘쳐나는 바기나를 보여주며 
	ENDIF
ENDIF
SIF TEQUIP:50
	PRINT 비디오카메라를 향해 
PRINTL 자위를 계속하고 있다.
IF LOCAL < 300
	PRINTW 그 손 움직임은 아직 서투르고, 쾌락보다도 수치심 쪽이 더 강한 것 같다.
ELSEIF LOCAL < 1000
	PRINTW 시시때때로 작게 몸을 떠는 모습은 확실히 쾌감을 느끼기 시작한 듯했지만 아직 몰두할 수는 없는 모양이다.
ELSEIF LOCAL < 3000
	PRINTFORMW 그 표정에는 명백히 쾌락의 전조가 보였고, 뜨거운 숨결이 새어나왔다.
ELSEIF LOCAL < 6000
	PRINTFORMW 견딜 수 없는 정감에 쾌락의 목소리를 지르며 자위행위에 몰두하고 있다.
ELSEIF LOCAL < 10000
	PRINTW 그리고 자위행위를 계속하면서 쾌락에 전신을 비틀고, 등으로 브릿지를 그려가며 자신의 성기를 계속 희롱했다.
ELSE
	PRINTW 너무 큰 쾌락에 모든 것을 잊고 녹아내릴 듯한 표정으로 찌걱찌걱 소리를 내며 
	IF TALENT:120
		PRINT 격렬하게 자지를 훑고 있다. 
	ELSEIF TALENT:121
		PRINT 자지와 보지를 열심히 만지며 두 곳의 쾌감을 느끼고 있다. 
	ELSE
		PRINT 성기를 희롱하고 있다. 
	ENDIF
	PRINTW 내버려두면 언제까지고 하고 있을 것 같다.
ENDIF

;--------------------------------------------------------
;로터
;--------------------------------------------------------
@TEXT_10
PRINTL 
LOCAL = UP:0

IF LOCAL < 300
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 아직 큰 쾌락을 얻는데는 이르지 못한 것 같다.
ELSEIF LOCAL < 1000
	PRINTFORMW %CALLNAME:TARGET%의 \@(TALENT:120 || TALENT:121) ? 페니스 # 클리토리스\@에 로터를 누르자 \@(TEQUIP:42) ? # 작게 비명을 지르며\@ 꿈틀 하고 몸을 튕겼다.
ELSEIF LOCAL < 3000
	IF TALENT:70
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 로터의 자극에 기분 좋은 듯이 쾌락을 받아들이고 있다.
	ELSE
		PRINTFORMW %CALLNAME:TARGET%의 \@(TALENT:120 || TALENT:121) ? 페니스 # 클리토리스\@에 로터를 누르자\@(TEQUIP:42) ? # 쾌락의 목소리를 내며\@ 자극에서 도망치려고 몸을 비틀었다.
	ENDIF
ELSEIF LOCAL < 6000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% \@(TALENT:120 || TALENT:121) ? 페니스 # 클리토리스\@를 직접 로터로 자극당해 격렬한 쾌락에 전신을 마구 요동쳤다.
ELSEIF LOCAL < 10000
	PRINTFORMW %CALLNAME:TARGET%의 \@(TALENT:120 || TALENT:121) ? 페니스 # 클리토리스\@를 로터로 자극해주자 격렬한 쾌락에 내던져져, 허리를 음란하게 그라인드시켰다.
ELSE
	IF TALENT:42
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 로터를 꾹 누르자, 너무 강렬한 쾌락에 격렬히 등을 휘며 연속으로 애액을 내뿜었다.
	ELSE
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 로터를 누르자 엄청난 쾌락에 침마저 줄줄 흘리며 전신을 미친듯 몸부림쳤다.
	ENDIF
ENDIF

;--------------------------------------------------------
;전기안마기
;--------------------------------------------------------
@TEXT_11
PRINTL 
LOCAL = UP:0

IF LOCAL < 300
	PRINTFORMW %CALLNAME:TARGET%의 \@(TALENT:120 || TALENT:121) ? 페니스 # 클리토리스\@에 전기안마기를 들이대어봤지만, 진동을 못 견뎌할 뿐이지 크게 쾌락을 얻는데까지는 이르지 못하고 있는 듯하다.
ELSEIF LOCAL < 1000
	PRINTFORMW %CALLNAME:TARGET%의 거기로 전기안마기를 바짝 대보자 온 몸이 욱신거리는 듯 몸부림치면서도 표정엔 쾌락의 전조가 보인다.
ELSEIF LOCAL < 3000
	PRINTFORMW \@ (TALENT:120 || TALENT:121) ? 페니스 # 클리토리스 \@에 전기안마기를 대어보자 %조사처리(CALLNAME:TARGET,"는")% \@(TEQUIP:42) ? # 작고 귀여운 목소리를 내면서\@ 몸을 부들부들 떨었다
ELSEIF LOCAL < 6000
	PRINTFORMW %CALLNAME:TARGET%의 거기로 전기안마기를 대어보자 견딜 수 없는 쾌락에 무심코 양다리를 닫아 전기안마기를 딱 잡아버리곤, %조사처리(CALLNAME:TARGET,"는")% 몸부림쳤다.
ELSEIF LOCAL < 10000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% \@(TALENT:120 || TALENT:121) ? 페니스 # 클리토리스\@에 느껴지는 전기안마기의 진동이 주는 지나칠 정도의 쾌락에 필사적으로 도망치려고 하고 \@(TEQUIP:42) ? # 동시에 에로틱한 신음을 계속 내고\@ 있다.
ELSE
	PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 전기안마기를 댄 순간 격하게 몸을 꼬며, 
	IF TALENT:42
		PRINTW 거기에서 몇 번이고 애액을 분출했다.
	ELSE
		PRINTW 강렬한 쾌락에 몸을 마구 튕겨댔다.
	ENDIF
ENDIF

;--------------------------------------------------------
;바이브
;--------------------------------------------------------
@TEXT_12
PRINTL 
LOCAL = UP:1

IF TEQUIP:20 == 1
	IF LOCAL < 300
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 바이브의 굵기에 괴로운 듯 신음했다.
	ELSEIF LOCAL < 1000
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 익숙치 않은 바이브의 감각에 곤혹스러워 하면서도 때때로 자신을 찔러오는 쾌락의 충격에 몸을 떨고 있다.
	ELSEIF LOCAL < 3000
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 이미 바기나로도 쾌락을 느낄 수 있는 몸이 된 것인지, 진동하는 바이브를 넣어주자 \@(TEQUIP:42) ? 명백히 느끼고 있는 신음을 냈다.\@.
	ELSEIF LOCAL < 6000
		PRINTFORM %CALLNAME:TARGET%의 
		SIF TALENT:127 || TALENT:128
			PRINT 작은 
		PRINTW 바기나에 바이브를 꽃아넣자 바로 쾌락에 쩔은 표정이 되었다.
	ELSEIF LOCAL < 10000
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
		SIF TALENT:127 || TALENT:128
			PRINT 그 작은 몸으로 
		PRINTW 극대의 바이브를 물고는, 격렬한 진동에 의한 자극과 쾌락에 나신을 건져놓은 물고기처럼 파닥거렸다.
	ELSE
		PRINTFORMW %CALLNAME:TARGET%에게 진동하고 있는 바이브로 격렬히 왕복을 하자 애액을 뿌려대며 격렬한 쾌락에 몸부림치고 있다.
	ENDIF
ELSE
	IF LOCAL < 300
		PRINTFORMW %CALLNAME:TARGET%의 몸에서 바이브를 뽑아주자 안심한 듯한 표정을 했다.
	ELSEIF LOCAL < 1000
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 자신의 몸에서 바이브를 뽑을 때 \@(TEQUIP:42) ? 몸을 비틀었다 # 작게 귀여운 신음을 흘렸다.\@
	ELSEIF LOCAL < 3000
		PRINTFORMW %CALLNAME:TARGET%의 몸에서 바이브를 뽑을 때 가볍게 왕복해 주자 \@(TEQUIP:42) ? # 작게 신음을 흘리며\@ 몸부림을 쳤다.
	ELSEIF LOCAL < 6000
		PRINTFORMW %조사처리(CALLNAME:TARGET,"를")% 신나게 바이브로 자극시킨 뒤 그제서야 뽑아주자 질에서 대량의 애액이 터져나왔다.
	ELSEIF LOCAL < 10000
		PRINTFORMW %조사처리(CALLNAME:TARGET,"가")% 바이브를 뽑아낸 것 만으로도 귀여운 신음을 지르기에 마지막으로 한 번 더 세게 찔러넣고 격렬히 왕복을 하자 애액을 분출하며 전신을 활처럼 튕겼다.
	ELSE
		PRINTFORMW %CALLNAME:TARGET%의 몸에서 바이브를 뽑기 전에 잡고서 힘껏 왕복을 해 보자 전신을 몸부림치며 미친듯 느꼈다.
	ENDIF
ENDIF

;--------------------------------------------------------
;애널 바이브
;--------------------------------------------------------
@TEXT_13
PRINTL 
LOCAL = UP:2

IF TEQUIP:25 == 1
	IF LOCAL < 300
		PRINTFORMW 애널바이브를 넣어 주자 %조사처리(CALLNAME:TARGET,"는")% \@(TEQUIP:42) ? 고통스러워했다. # 비명을 질렀다.\@ 아직 고통 쪽이 더 우위인 듯하다.
	ELSEIF LOCAL < 1000
		PRINTFORMW %CALLNAME:TARGET%의 애널은 제법 바이브를 받아들이게 된 것 같지만 아직 좀 괴로운 것 같다.
	ELSEIF LOCAL < 3000
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 애널에 바이브를 박히고는 다소 괴로워하면서도 쾌락이 더 느껴지게 된 것 같다.
	ELSEIF LOCAL < 6000
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 애널에 바이브가 꽃힌 채로 \@(TEQUIP:42) ?# 확실히 교성을 지르며\@ 쾌락을 느끼고 있다.
	ELSEIF LOCAL < 10000
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 애널바이브가 가져다주는 강렬한 쾌락에 표정이 풀어져 있다.
	ELSE
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 애널을 겨냥한 자극에 미친듯이 느끼며 
		IF TALENT:127 || TALENT:128
			PRINT 그 자그마한 몸을 
		ELSE
			PRINT 그 땀투성이의 몸을 
		ENDIF
		PRINTW 마구 몸부림쳤다.
	ENDIF
ELSE
	IF LOCAL < 300
		PRINTFORMW %CALLNAME:TARGET%의 몸에서 애널바이브를 뽑아내자 \@(TEQUIP:42) ? 다행인 표정을 지었다 # 안도의 한숨을 토해냈다.\@
	ELSEIF LOCAL < 1000
		PRINTFORMW %CALLNAME:TARGET%의 애널이 죄어드는 것을 즐기면서 천천히 바이브를 뽑아냈다.
	ELSEIF LOCAL < 3000
		PRINTFORMW 애널에서 바이브를 뽑아낸 순간, %조사처리(CALLNAME:TARGET,"는")% \@(TEQUIP:42) ? 황홀한 표정을 지었다. # 견딜 수 없는 신음을 질렀다.\@
	ELSEIF LOCAL < 6000
		PRINTFORMW %CALLNAME:TARGET% 안에서 계속 진동하던 바이브를 억지로 뽑아내주자 \@(TEQUIP:42) ? 고통과 쾌감이 섞인 표정을 보였다. # 달콤한 쾌락이 섞인 비명을 질렀다.\@
	ELSEIF LOCAL < 10000
		PRINTFORMW 애널바이브를 뽑기 전에 가볍게 왕복을 해 보자 %조사처리(CALLNAME:TARGET,"는")% 달콤한 소리를 지르며 엉덩이를 흔들었다.
	ELSE
		PRINTFORMW 애널바이브를 잡아 격렬히 왕복을 하자 %조사처리(CALLNAME:TARGET,"는")% 강렬한 쾌락에 격한 신음과 함께 음란하게 허리를 흔들며 쾌락을 탐했다.
	ENDIF
ENDIF

;--------------------------------------------------------
;전동 클리캡
;--------------------------------------------------------
@TEXT_14
PRINTL 
LOCAL = UP:0

IF LOCAL < 300
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 클리토리스로의 자극에 눈을 꽉 감고 버티고 있다.
ELSEIF LOCAL < 1000
	PRINTFORMW 전동 클리캡으로 음핵을 계속 자극해 주자 %조사처리(CALLNAME:TARGET,"는")% \@(TEQUIP:42) ? 몸을 비틀었다. # 작게 신음을 흘렸다.\@
ELSEIF LOCAL < 3000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 클리토리스로의 자극에 견디지 못하고 몸을 작게 떨며 쾌락에 내몰리고 있다.
ELSEIF LOCAL < 6000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 전동 클리캡의 자극이 가져다주는 격렬한 쾌감에 허리를 들어올리며 음란한 댄스를 추고 있다.
ELSEIF LOCAL < 10000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 클리토리스로 보내지는 쾌락에 의식이 몽롱한 채로 완전히 풀린 표정을 보이고 있다.
ELSE
	PRINTFORMW 전동 클리캡을 집어 움직여주자 %조사처리(CALLNAME:TARGET,"는")% 클리토리스로의 엄청난 자극에 못 견디고 애액을 대량으로 흘리며 몸을 활처럼 튕겼다.
ENDIF

;--------------------------------------------------------
;전통 유두캡
;--------------------------------------------------------
@TEXT_15
PRINTL 
LOCAL = UP:3

IF LOCAL < 300
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 가슴이 빨리는 감각에 곤혹스러운 표정으로 버티고 있다.
ELSEIF LOCAL < 1000
	PRINTFORMW 전동 유두캡을 가슴에 덮어씌우자 %조사처리(CALLNAME:TARGET,"는")% \@(TEQUIP:42) ? 몸을 떨었다 # 작게 신음을 흘렸다.\@
ELSEIF LOCAL < 3000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 가슴으로의 자극에 견디지 못하고 몸을 작게 떨고 있다.
ELSEIF LOCAL < 6000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 전동 유두캡의 자극이 가져다주는 격렬한 쾌감에 전신을 떨고 있다.
ELSEIF LOCAL < 10000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 가슴에 보내지는 쾌락에 의식이 몽롱한 채로 완전히 풀린 표정을 보이고 있다.
ELSE
	PRINTFORMW 전동 유두캡을 잡고 움직여 주자 %조사처리(CALLNAME:TARGET,"는")% 가슴으로의 엄청난 자극에 견디지 못하고 \@(TEQUIP:42) ? 몸을 마구 비틀었다. # 입에서 침을 줄줄 흘리며 몸을 활처럼 튕겼다.\@
ENDIF

;--------------------------------------------------------
;오나홀
;--------------------------------------------------------
@TEXT_16
PRINTL 
LOCAL = UP:0

IF LOCAL < 300
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 페니스에 가해지는 익숙치 않은 자극에 곤혹스러운 표정을 하며 참고 있다.
ELSEIF LOCAL < 1000
	PRINTFORMW 오나홀을 페니스에 뒤집어 씌우자 %조사처리(CALLNAME:TARGET,"는")% \@(TEQUIP:42) ? 몸을 튕겼다. # 작게 신음소릴 냈다.\@
ELSEIF LOCAL < 3000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 페니스로 가해지는 자극을 견디지 못하고 몸을 작게 떨고 있다.
ELSEIF LOCAL < 6000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 오나홀의 자극이 가져다주는 격렬한 쾌감에 전신을 와들와들 떨고 있다.
ELSEIF LOCAL < 10000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 오나홀에서 페니스로 보내오는 쾌락을 맛보는데 무아지경이다.
ELSE
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 오나홀에서 페니스로 보내오는 엄청난 자극을 견디지 못하고 \@(TEQUIP:42) ? 온몸을 튕겼다. # 입에서 침을 흘리며 몸을 펄떡펄떡 튀었다.\@
ENDIF

;--------------------------------------------------------
;전극 오나홀
;--------------------------------------------------------
@TEXT_17
PRINTL 
LOCAL = UP:0

IF LOCAL < 300
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 페니스에 전기가 통할 때마다 아픔에 울 것 같은 표정을 하며 참고 있다.
ELSEIF LOCAL < 1000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 페니스로 오는 쾌감보다 전격에 의한 아픔 쪽이 아무래도 신경쓰이는지 필사적으로 견디고 있다.
ELSEIF LOCAL < 3000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 페니스로 오는 자극과 진동을 견디지 못하고 몸을 작게 경련하고 있다.
ELSEIF LOCAL < 6000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 전극오나홀에서 보내지는 가지가지 자극에 의해 격렬한 쾌감을 몸 전체로 맛보고 있다.
ELSEIF LOCAL < 10000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 전격에 의한 충격에 강한 쾌감을 느껴, 더욱 강한 전격이 오기를 기다리는 듯 몸을 꿈틀거렸다.
ELSE
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 전극오나홀에서 페니스로 보내지는 엄청난 전격과 진동에 \@(TEQUIP:42) ? 몸을 뒤틀었다 # 입에서 침을 줄줄 흘리며 몸을 펄떡거렸다.\@
ENDIF

;--------------------------------------------------------
;기승위
;--------------------------------------------------------
@TEXT_24
PRINTL 
LOCAL = UP:1

IF LOCAL < 300
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 괴로운 듯한 얼굴이면서도 명령에 따라 허리를 흔든다.하지만 아직 쾌락을 얻기에는 먼 것 같다.
ELSEIF LOCAL < 1000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 괴로운 듯 하면서도 표정엔 쾌락의 전조가 보인다.
ELSEIF LOCAL < 3000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 허리를 흔들면서 억누를 길 없는 쾌락에 \@(TEQUIP:42) ? 빠져있다 # 귀여운 신음을 흘리고 있다.\@
ELSEIF LOCAL < 6000
	IF TALENT:112 || TALENT:113
		PRINTFORMW 눈앞에서 %조사처리(CALLNAME:TARGET,"는")% 커다란 유방을 허리의 움직임에 맞춰 상하로 흔들면서 쾌락에 푹 빠져 있다.
	ELSE
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 상당히 익숙해 진 것인지, 쾌락에 찔려 올려지면서도 스스로 자발적으로 허리를 흔들고 있다.
	ENDIF
ELSEIF LOCAL < 10000
	PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
	SIF TALENT:127 || TALENT:128
		PRINTFORM 그 자그마한 몸으로 
	IF TALENT:71 || TALENT:32
		PRINTFORMW 쾌락을 부정하는 \@(TEQUIP:42) ? 몸짓을 하려 하지만 몸은 정직했다. # 말을 뱉으면서도, 계속해서 억누를 길 없는 신음을 지르고 있다.\@
	ELSE
		PRINTFORMW 심히 허리를 흔들고 등을 활처럼 휘면서 \@(TEQUIP:42) ? # 쾌락의 신음을 질렀다.\@
	ENDIF
ELSE
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 허리를 흔들며 완전히 풀어진, 음란한 얼굴로 쾌락을 탐하고 있다.
ENDIF

;--------------------------------------------------------
;애널 기승위
;--------------------------------------------------------
@TEXT_34
PRINTL 
LOCAL = UP:2

IF LOCAL < 300
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 괴로운 듯한 얼굴이면서도 명령에 따라 허리를 흔든다. 하지만 아직 쾌락을 얻기에는 먼 것 같다.
ELSEIF LOCAL < 1000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 괴로운 듯 하면서도 표정엔 쾌락의 전조가 보인다.
ELSEIF LOCAL < 3000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 허리를 흔들면서 억누를 길 없는 쾌락에 \@(TEQUIP:42) ? 침 # 귀여운 신음\@을 흘리고 있다.
ELSEIF LOCAL < 6000
	IF TALENT:112 || TALENT:113
		PRINTFORMW 눈앞에서 %조사처리(CALLNAME:TARGET,"는")% 가슴을 허리 움직임에 맞춰 위아래로 튀기면서 쾌락에 빠져있다.
	ELSE
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 상당히 익숙해 진 것인지, 쾌락에 찔려 올려지면서도 스스로 자발적으로 허리를 흔들고 있다.
	ENDIF
ELSEIF LOCAL < 10000
	PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
	SIF TALENT:127 || TALENT:128
		PRINTFORM 그 작은 몸으로 
	IF TALENT:71 || TALENT:32
		PRINTFORMW 쾌락을 부정\@(TEQUIP:42) ? 하는 것처럼 몸을 흔들면서도, 확실하게 느끼고 있다. # 하는 말을 뱉으면서도, 계속해서 억누를 길 없는 신음을 지르고 있다.\@
	ELSE
		PRINTFORMW 열심히 허리를 흔들어 등을 젖\@(TEQUIP:42) ? 혔다. # 히며 쾌락의 신음을 질렀다.\@
	ENDIF
ELSE
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 허리를 흔들며 완전히 풀어진, 음란한 얼굴로 쾌락을 탐하고 있다.
ENDIF

;--------------------------------------------------------
;로터 삽입
;--------------------------------------------------------
@TEXT_151
PRINTL 
LOCAL = UP:1

IF TEQUIP:20 == 3
	IF LOCAL < 300
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 로터의 자극을 \@(TEQUIP:42) ? # 입술을 깨물고\@ 견디고 있다.
	ELSEIF LOCAL < 1000
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 아직 익숙치 않은 로터의 감각에 곤혹스러워 하면서도, 열락의 빛을 감추지 못하고 있다.
	ELSEIF LOCAL < 3000
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 진동하는 로터를 넣어주자 명백히 느끼고 있는 목소리를 냈다.
	ELSEIF LOCAL < 6000
		PRINTFORM %CALLNAME:TARGET%의 
		SIF TALENT:127 || TALENT:128
			PRINT 작은 
		PRINTW 바기나에 로터를 꽃아 넣자 바로 쾌락에 쩔은 표정이 되었다.
	ELSEIF LOCAL < 10000
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
		SIF TALENT:128
			PRINT 그 작은 몸으로 
		PRINTW 로터를 삼키고는, 격렬한 진동에 의한 자극과 쾌락에 전신을 부들부들 떨고 있다.
	ELSE
		PRINTFORMW 로터를 넣고 진동을 최대로 하자, %조사처리(CALLNAME:TARGET,"는")% 전신을 경련시키며 눈을 뒤집은 채 미친듯 몸부림치고 있다.
	ENDIF
ELSE
	IF LOCAL < 300
		PRINTFORMW %CALLNAME:TARGET%의 몸에서 로터를 뽑아주자 부끄러워 하면서도 조금 아쉬운 듯한 표정을 했다.
	ELSEIF LOCAL < 1000
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 몸에서 로터를 뽑아낼 때 \@(TEQUIP:42) ? 몸을 뒤틀었다 # 조금 귀여운 신음 소리를 냈다.\@
	ELSEIF LOCAL < 3000
		PRINTFORMW %CALLNAME:TARGET%의 몸에서 일부러 애태우다 로터를 빼내니, 도중에 작게 꿈틀! 하고 몸을 떨\@(TEQUIP:42) ? 었다. # 며 귀여운 교성을 질렀다.\@
	ELSEIF LOCAL < 6000
		PRINTFORMW %조사처리(CALLNAME:TARGET,"를")% 로터로 신나게 괴롭히다 일거에 빼내니, 이미 다리 아래는 애액과 오줌으로 작은 물웅덩이가 생겨 있었다.
	ELSEIF LOCAL < 10000
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 로터를 뽑는 것만으로 절정에 달했는지, 두 번 세 번 왕복을 반복해 보자 그 때마다 전신을 떨며 절정에 오르고 있다.
	ELSE
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 로터를 뽑으려고 하자 고개를 흔들며 싫어하길래, 벌로 깊숙히 한 번에 찔러 넣어 
		PRINTL 한 번 절정에 오르게 해 입을 다물게 만들고 나서 천천히 시간을 들여 뽑아냈다.
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 쉴 틈도 없이 계속 절정하다 숨이 끊어질 듯 되었다.
	ENDIF
ENDIF

;--------------------------------------------------------
;로터 A 삽입
;--------------------------------------------------------
@TEXT_170
PRINTL 
LOCAL = UP:2

IF TEQUIP:25 == 3
	IF LOCAL < 300
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 로터의 자극과 애널에 물건이 박혔다는 부끄러움에 입술을 깨물며 견디고 있다.
	ELSEIF LOCAL < 1000
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 아직 익숙치 않은 로터의 감각에 곤혹스러워 하면서도, 열락의 빛을 감추지 못하고 있다.
	ELSEIF LOCAL < 3000
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 진동하는 로터를 애널에 넣어주자 \@(TEQUIP:42) ? 몸을 비비꼬았다 # 명백히 느끼고 있는 목소리를 냈다.\@
	ELSEIF LOCAL < 6000
		PRINTFORM %CALLNAME:TARGET%의 
		SIF TALENT:127 || TALENT:128
			PRINT 작은 
		PRINTW 애널에 로터를 꽃아 넣자 바로 쾌락에 젖은 표정이 되었다.
	ELSEIF LOCAL < 10000
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
		SIF TALENT:128
			PRINT 그 작은 몸으로 
		PRINTW 로터를 삼키고는, 격렬한 진동에 의한 자극과 쾌락에 전신을 부들부들 떨고 있다.
	ELSE
		PRINTFORMW 로터를 애널에 넣고 진동을 최대로 하자, %조사처리(CALLNAME:TARGET,"는")% 전신을 경련시키며 눈을 뒤집은 채 미친듯 몸부림치고 있다
	ENDIF
ELSE
	IF LOCAL < 300
		PRINTFORMW %CALLNAME:TARGET%의 애널에서 로터를 뽑아주자 부끄러워 하면서도 조금 아쉬운 듯한 표정을 했다.
	ELSEIF LOCAL < 1000
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 애널에서 로터를 뽑아낼 때 조금 귀여운 신음 소리를 냈다.
	ELSEIF LOCAL < 3000
		PRINTFORMW %CALLNAME:TARGET%의 애널에서 일부러 애태우다 로터를 빼내니, 도중에 작게 꿈틀！하고 몸을 떨며 귀여운 교성을 질렀다.
	ELSEIF LOCAL < 6000
		PRINTFORMW %CALLNAME:TARGET%의 애널을 로터로 신나게 괴롭히다 일거에 빼내니, 이미 다리 아래는 애액과 오줌으로 작은 물웅덩이가 생겨 있었다.
	ELSEIF LOCAL < 10000
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 애널에서 로터를 뽑는 것만으로 절정에 달했는지, 두 번 세 번 왕복을 반복해 보자 그 때마다 전신을 떨며 절정에 오르고 있다.
	ELSE
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 애널에서 로터를 뽑으려고 하자 고개를 흔들며 싫어하길래, 벌로 깊숙히 한 번에 찔러 넣어 
		PRINTL 한 번 절정에 오르게 해 입을 다물게 만들고 나서 천천히 시간을 들여 뽑아냈다.
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 쉴 틈도 없이 계속 절정하다 숨이 끊어질 듯 되었다.
	ENDIF
ENDIF

;--------------------------------------------------------
;유두 빨기
;--------------------------------------------------------
@TEXT_203
PRINTL 
LOCAL = UP:3

IF TALENT:134
	IF LOCAL < 300
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 유두를 빨리자, 순수하게 간지러운 듯한 모습만 보인다.
	ELSEIF LOCAL < 1000
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 유두를 혀로 희롱할 때마다, 작게 떨며 반응하고 있다.
	ELSEIF LOCAL < 3000
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 유두를 빠는 만큼 딱딱해지고, 그 때마다 %조사처리(CALLNAME:TARGET,"는")% 모유가 나올 것 같은 감각을 애써 참고 있는 듯하다.
	ELSEIF LOCAL < 6000
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 유두에서 모유가 빨려나갈 듯한 그 자극만으로도 몽롱한 표정이 되어 하는 대로 휘둘리고 있다.
	ELSEIF LOCAL < 10000
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
		IF TALENT:110
			PRINT 평평한 
		ELSEIF TALENT:111
			PRINT 작은 
		ELSEIF TALENT:112
			PRINT 커다란 
		ELSEIF TALENT:113
			PRINT 실로 거대한 
		ENDIF
		PRINTW 가슴 끝을 빨리며, 유선 속을 모유가 도는 감각만으로도 절정의 극치에 달하려고 하고 있다. 
	ELSE
		PRINTFORM %CALLNAME:TARGET%의 유두를 강하게 빨자 그것만으로도 엄청나게 느끼는지, 
		IF TALENT:112
			PRINTFORM 그 거유에 무심코 %CALLNAME:PLAYER%의 머리를 끌어안고 가슴에 파묻으며 
		ELSEIF TALENT:113
			PRINTFORM 그 폭유에 무심코 %CALLNAME:PLAYER%의 머리를 끌어안고 가슴에 파묻으며 
		ELSE
			PRINTFORM 그 가슴에 무심코 %CALLNAME:PLAYER%의 머리를 끌어안아 누르며 
		ENDIF
		PRINTW 모유가 빨려나가는 듯한 감각만으로도 계속되는 쾌락에 몸부림치고 있다.
	ENDIF
ELSE
	IF LOCAL < 300
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 유두를 빨리자, 순수하게 간지러운 듯한 모습만 보인다.
	ELSEIF LOCAL < 1000
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 유두를 혀로 희롱할 때마다, 작게 떨며 반응하고 있다.
	ELSEIF LOCAL < 3000
		PRINTFORMW %CALLNAME:TARGET%의 유두를 입 안에서 굴릴 때마다 딱딱해지고, 그 때마다 %조사처리(CALLNAME:TARGET,"는")% \@(TEQUIP:42) ? 몸을 뒤틀기 시작했다.# 뜨거운 숨결을 토하기 시작했다.\@
	ELSEIF LOCAL < 6000
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 유두로의 혀와 입을 쓴 애무만으로도 몽롱한 표정이 되어 그저 하는 대로 흘러가고 있다.
	ELSEIF LOCAL < 10000
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
		IF TALENT:110
			PRINT 평평한 
		ELSEIF TALENT:111
			PRINT 작은 
		ELSEIF TALENT:112
			PRINT 커다란 
		ELSEIF TALENT:113
			PRINT 실로 거대한 
		ENDIF
		PRINTW 가슴 끝에 오는 자극에 극한까지 느끼며, 유두를 빨리는 것 만으로도 열락의 극치를 달리려고 하고 있다. 
	ELSE
		PRINTFORM %CALLNAME:TARGET%의 유두를 입 안에서 희롱하는 것만으로도 엄청나게 느끼는지, 
		IF TALENT:112
			PRINTFORM 그 거유에 무심코 %CALLNAME:PLAYER%의 머리를 끌어안고 가슴에 파묻으며 
		ELSEIF TALENT:113
			PRINTFORM 그 폭유에 무심코 %CALLNAME:PLAYER%의 머리를 끌어안고 가슴에 파묻으며 
		ELSE
			PRINTFORM 그 가슴에 무심코 %CALLNAME:PLAYER%의 머리를 끌어안아 누르며 
		ENDIF
		PRINTW 거기에서 오는 새로운 유두로의 자극에 몸부림을 계속했다.
	ENDIF
ENDIF

;--------------------------------------------------------
;가슴 자위
;--------------------------------------------------------
@TEXT_205
PRINTL 
LOCAL = UP:3 / 4

PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
SIF TALENT:35 && ABL:14 <= 3
	PRINT 수치로 얼굴을 새빨갛게 물들이고, 눈물 가득한 눈으로 
SIF TALENT:127 || TALENT:128
	PRINT 작은 몸으로 
IF ABL:14 == 4
	PRINT 노출의 쾌락에 취한 표정을 보이며 
ELSEIF ABL:14 >= 5
	PRINT 완전히 보여지는 쾌감을 깨우쳐 버린 것인지, 음탕한 표정을 띠고 스스로 
ENDIF
IF TALENT:112
	PRINT 풍만한 유방을 손으로 잡고 
ELSEIF TALENT:113
	PRINT 무거워보일 정도로 큰 유방을 손으로 잡고 
ELSEIF TALENT:110
	PRINT 평평한 가슴을 
ELSEIF TALENT:111
	PRINT 거의 없는 가슴을 
ENDIF
PRINT 앞으로 내밀듯이 하곤 
SIF TEQUIP:50
	PRINT 비디오카메라를 향한 채 
IF TEQUIP:35 == 2
	PRINTL 모유를 짜내려고 하고 있다.
ELSE
	PRINTL 유방을 계속 희롱하고 있다.
ENDIF

IF LOCAL < 300
	PRINTW 그 손의 움직임은 아직 서투르고, 쾌락보다도 수치심 쪽이 더 강한 것 같다.
ELSEIF LOCAL >=300 && LOCAL <1000
	PRINTW 시시때때로 작게 몸을 떠는 모습은 확실히 쾌감을 느끼기 시작한 듯했지만 아직 몰두할 수는 없는 모양이다.
ELSEIF LOCAL >= 1000 && LOCAL < 3000
	PRINTFORMW 그 표정에는 명백히 쾌락의 전조가 보였\@(TEQUIP:42) ? 다 # 고 뜨거운 숨결이 새어나왔다\@.
ELSEIF LOCAL >= 3000 && LOCAL < 6000
	PRINTFORMW 그리고 견딜 수 없는 정감에\@(TEQUIP:42) ? 몸을 비비꼬며 #  쾌락의 목소리를 내며\@ 열심히 유방을 희롱하는데 몰두하고 있었다.
ELSEIF LOCAL >= 6000 && LOCAL < 10000
	PRINTW 그리고 유방 전체를 계속 주물러 대면서 쾌락에 전신을 요동쳤고, 때때로 유두를 손톱으로 집어 그 때마다 몸을 활처럼 휘었다.
ELSEIF LOCAL >= 10000 && LOCAL < 40000
	PRINT 엄청난 쾌락에 모든 것을 잊고 녹아내릴 듯한 표정으로 
	SIF TEQUIP:16
		PRINT 한결같이 하얀 열매의 즙을 짜내려고 하고 
	PRINTW 있다.
ELSE
	IF TALENT:110 || TALENT:111
		PRINT 끝까지 발기한 유두를 잡고 계속 돌리고 
	ELSEIF TALENT:112
		PRINT 풍만한 유방을 가지가지 형태로 일그러뜨리고 
	ELSEIF TALENT:113
		PRINT 손에 다 들어가지도 않을 크기의 유방을 무거운 듯 계속 주무르고 
	ELSE
		PRINT 유방을 계속 주무르고 
	ENDIF
	PRINTW 있다. 내버려 두면 언제까지고 이러고 있을 것 같다.
ENDIF

;--------------------------------------------------------
;스팽킹
;--------------------------------------------------------
@TEXT_220
PRINTL 
LOCAL = UP:16

IF LOCAL < 300
	LOCAL:1 = 0
ELSEIF LOCAL >= 300 && LOCAL < 1000
	LOCAL:1 = 1
ELSEIF LOCAL >= 1000 && LOCAL < 2000
	LOCAL:1 = 2
ELSEIF LOCAL >= 2000 && LOCAL < 3000
	LOCAL:1 = 3
ELSE
	LOCAL:1 = 4
ENDIF
IF TALENT:40
	LOCAL:1 += 1
ELSEIF TALENT:41 && LOCAL:1 > 0
	LOCAL:1 -= 1
ENDIF

IF LOCAL:1 == 0
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 비명을 억누르며 고통에 견뎠다.
ELSEIF LOCAL:1 == 1
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 격렬한 고통\@(TEQUIP:42) ? 을 필사적으로 참는 것처럼 보인다. # 에 악문 이빨 사이로 괴로운 듯한 신음을 흘렸다.\@
ELSEIF LOCAL:1 == 2
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 고통을 견디지 못하고 \@(TEQUIP:42) ? 전신을 요동쳤다. # 비명을 지르며 전신을 요동쳤다.\@
ELSEIF LOCAL:1 == 3
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 고통이 너무 심해 전신에서 식은땀을 흘리며 정신이 오락가락했다.
ELSEIF LOCAL:1 == 4
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 엄청난 고통에 의식이 날아갈 것 같이 되어 \@(TEQUIP:42) ? 반쯤 기절한 것 같다. # 마치 짐승과도 같은 엄청난 볼륨의 비명을 질러댔다.\@
ELSE
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 엄청난 고통에 울부짖으면서 필사적으로 용서를 애걸하고 있다.
ENDIF

;--------------------------------------------------------
;채찍
;--------------------------------------------------------
@TEXT_221
PRINTL 
LOCAL = UP:16

IF LOCAL < 300
	LOCAL:1 = 0
ELSEIF LOCAL < 1000
	LOCAL:1 = 1
ELSEIF LOCAL < 2000
	LOCAL:1 = 2
ELSEIF LOCAL < 3000
	LOCAL:1 = 3
ELSE
	LOCAL:1 = 4
ENDIF
IF TALENT:40
	LOCAL:1 += 1
ELSEIF TALENT:41 && LOCAL:1 > 0
	LOCAL:1 -= 1
ENDIF

IF LOCAL:1 == 0
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 비명을 억누르며 고통에 견뎠다.
ELSEIF LOCAL:1 == 1
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 격렬한 고통\@(TEQUIP:42) ? 을 필사적으로 참는 것처럼 보인다. # 에 악문 이빨 사이로 괴로운 듯한 신음을 흘렸다.\@
ELSEIF LOCAL:1 == 2
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 고통을 견디지 못하고 \@(TEQUIP:42) ? 전신을 요동쳤다. # 비명을 지르며 전신을 요동쳤다.\@
ELSEIF LOCAL:1 == 3
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 고통이 너무 심해 전신에서 식은땀을 흘리며 정신이 오락가락했다.
ELSEIF LOCAL:1 == 4
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 엄청난 고통에 의식이 날아갈 것 같이 되어 \@(TEQUIP:42) ? 반쯤 기절한 것 같다. # 마치 짐승과도 같은 엄청난 볼륨의 비명을 질러댔다.\@
ELSE
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 엄청난 고통에 울부짖으면서 필사적으로 용서를 애걸하고 있다.
ENDIF

;--------------------------------------------------------
;바늘
;--------------------------------------------------------
@TEXT_222
PRINTL 
LOCAL = UP:16

IF LOCAL < 300
	LOCAL:1 = 0
ELSEIF LOCAL < 1000
	LOCAL:1 = 1
ELSEIF LOCAL < 2000
	LOCAL:1 = 2
ELSEIF LOCAL < 3000
	LOCAL:1 = 3
ELSE
	LOCAL:1 = 4
ENDIF
IF TALENT:40
	LOCAL:1 += 1
ELSEIF TALENT:41 && LOCAL:1 > 0
	LOCAL:1 -= 1
ENDIF

IF LOCAL:1 == 0
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 비명을 억누르며 고통에 견뎠다.
ELSEIF LOCAL:1 == 1
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 격렬한 고통\@(TEQUIP:42) ? 을 필사적으로 참는 것처럼 보인다. # 에 악문 이빨 사이로 괴로운 듯한 신음을 흘렸다.\@
ELSEIF LOCAL:1 == 2
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 고통을 견디지 못하고 \@(TEQUIP:42) ? 전신을 요동쳤다. # 비명을 지르며 전신을 요동쳤다.\@
ELSEIF LOCAL:1 == 3
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 고통이 너무 심해 전신에서 식은땀을 흘리며 정신이 오락가락했다.
ELSEIF LOCAL:1 == 4
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 엄청난 고통에 의식이 날아갈 것 같이 되어 \@(TEQUIP:42) ? 반쯤 기절한 것 같다. # 마치 짐승과도 같은 엄청난 볼륨의 비명을 질러댔다.\@
ELSE
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 엄청난 고통에 울부짖으면서 필사적으로 용서를 애걸하고 있다.
ENDIF

;--------------------------------------------------------
;촛농
;--------------------------------------------------------
@TEXT_223
PRINTL 
LOCAL = UP:16

IF LOCAL < 300
	LOCAL:1 = 0
ELSEIF LOCAL < 1000
	LOCAL:1 = 1
ELSEIF LOCAL < 2000
	LOCAL:1 = 2
ELSEIF LOCAL < 3000
	LOCAL:1 = 3
ELSE
	LOCAL:1 = 4
ENDIF
IF TALENT:40
	LOCAL:1 += 1
ELSEIF TALENT:41 && LOCAL:1 > 0
	LOCAL:1 -= 1
ENDIF

IF LOCAL:1 == 0
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 비명을 억누르며 고통에 견뎠다.
ELSEIF LOCAL:1 == 1
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 격렬한 고통\@(TEQUIP:42) ? 을 필사적으로 참는 것처럼 보인다. # 에 악문 이빨 사이로 괴로운 듯한 신음을 흘렸다.\@
ELSEIF LOCAL:1 == 2
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 고통을 견디지 못하고 \@(TEQUIP:42) ? 전신을 요동쳤다. # 비명을 지르며 전신을 요동쳤다.\@
ELSEIF LOCAL:1 == 3
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 고통이 너무 심해 전신에서 식은땀을 흘리며 정신이 오락가락했다.
ELSEIF LOCAL:1 == 4
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 엄청난 고통에 의식이 날아갈 것 같이 되어 \@(TEQUIP:42) ? 반쯤 기절한 것 같다. # 마치 짐승과도 같은 엄청난 볼륨의 비명을 질러댔다.\@
ELSE
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 엄청난 고통에 울부짖으면서 필사적으로 용서를 애걸하고 있다.
ENDIF

;--------------------------------------------------------
;풋잡한다
;--------------------------------------------------------
@TEXT_302
PRINTL 
LOCAL = UP:0

IF LOCAL < 300
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 페니스를 짓밟힐 때마다 눈물을 흘 \@(TEQUIP:42) ? # 리며 비명을 질렀다.\@렸다.
ELSEIF LOCAL < 1000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 페니스를 발로 비빌 때마다 .\@(TEQUIP:42) ? 몸을 움찔거렸다. # 신음을 냈다\@
ELSEIF LOCAL < 3000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 페니스에 오는 자극에 쾌감을 느끼고 몸부림치고 있다.
ELSEIF LOCAL < 6000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 페니스를 밟히니 기쁜 듯이 음탕한 쾌감을 맛보았다.
ELSEIF LOCAL < 10000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 발이 페니스에 가까이 다가오기만 해도 풀린 얼굴로 미소지었다.
ELSE
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 페니스를 짓밟힐 때마다 쾌감에 몸 전체를 미친듯 요동쳤다.
ENDIF

;--------------------------------------------------------
;모유먹이기
;--------------------------------------------------------
@TEXT_319
PRINTL 

IF TALENT:150 && TALENT:170
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 황홀한 얼굴로 %CALLNAME:PLAYER%의 젖을 열정적으로 탐하며 행복해 하고 있다.
ELSEIF TALENT:150
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 아기라도 된 마냥 행복한 얼굴로 %CALLNAME:PLAYER%의 젖을 맛있게 빨고 있다.
ELSEIF TALENT:170
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 음란한 혀놀림으로 %CALLNAME:PLAYER%의 젖을 격렬하게 핥고 빨고 있다.
ELSEIF MARK:2 >= 2
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 체념한 듯 %CALLNAME:PLAYER%의 젖을 묵묵히 빨고 있다.
ELSEIF MARK:2 == 1
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 언짢은 표정을 짓고는 %CALLNAME:PLAYER% 젖을 빨고 있다.
ELSEIF MARK:9 > 0
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 어떻게든 %CALLNAME:PLAYER%의 젖을 빨지 않으려 안간힘을 쓰고 있다.
ELSE
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 이 상황이 굴욕스럽고 불만인 모양이다.
ENDIF

;--------------------------------------------------------
;수음한다
;--------------------------------------------------------
@TEXT_321
PRINTL 
LOCAL = UP:0

IF LOCAL < 300
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 페니스를 꽉 잡고 부비니 다소 아픈 듯한 표정을 지었다.
ELSEIF LOCAL < 1000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 페니스에 가해지는 자극에 \@(TEQUIP:42) ? 몸을 부들부들 떨었다. # 작게 신음을 냈다.\@
ELSEIF LOCAL < 3000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 페니스를 잡고 부비자 쾌감에 전신을 작게 경련하고 있었다.
ELSEIF LOCAL < 6000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 페니스를 잡고 비비자 격렬한 쾌감에 \@(TEQUIP:42) ? # 미친듯 신음을 내면서 \@전신을 떨었다.
ELSEIF LOCAL < 10000
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:PLAYER%의 손을 더 기다릴 수 없다는 듯이 자기 페니스를 들이밀었다.
ELSE
	PRINTFORML %조사처리(CALLNAME:PLAYER,"가")% 격하게 %CALLNAME:TARGET%의 페니스를 움켜쥐고 위아래로 비비자 %조사처리(CALLNAME:TARGET,"는")%
	PRINTFORMW 기절할 것 같은 쾌감에 미친듯 몸부림치며\@(TEQUIP:42) ? #  누구도 막을 수 없는 큰 소리로 신음을\@ 질러댔다.
ENDIF

;--------------------------------------------------------
;보지 닦아주기
;--------------------------------------------------------
@TEXT_336
PRINTL 

IF TALENT:150
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 눈물을 글썽이면서 보지를 닦는 %조사처리(CALLNAME:PLAYER,"를")% 보고 있다.
ELSEIF MARK:2 >= 3
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% \@(TEQUIP:42) ? 기분 좋은 표정을 하 # 신음소리를 크게 내\@면서, 얌전하게 있다.
ELSEIF MARK:9 >= 3
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 몸을 비틀면서 %CALLNAME:PLAYER%의 손으로부터 도망치려 하고 있다.
ELSE
	PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 불쾌한 얼굴을 하면서 보지를 닦는 %조사처리(CALLNAME:PLAYER,"를")% 보고 있다.
ENDIF

;--------------------------------------------------------
;템플릿용
;--------------------------------------------------------
;「(SAMPLE)」내에 해당하는 커맨드 번호를 넣을 것
;@TEXT_(SAMPLE)
;LOCAL = UP:0

;IF LOCAL < 300
;	PRINTFORMW 
;ELSEIF LOCAL < 1000
;	PRINTFORMW 
;ELSEIF LOCAL < 3000
;	PRINTFORMW 
;ELSEIF LOCAL < 6000
;	PRINTFORMW 
;ELSEIF LOCAL < 10000
;	PRINTFORMW 
;ELSE
;	PRINTFORMW 
;ENDIF

;--------------------------------------------------------
;추가 지문의 시추에이션에 따른 비표시 전환용 식중함수
;--------------------------------------------------------
;ARG = SELECTCOM
@NOT_PRINT_ADDITIONAL_TEXT(ARG)
#FUNCTION
;三角木馬乗馬中, 特殊風呂入浴中, 強精神薬反動中, 興奮剤反動中, 浣腸中, 極太氷柱or氷塊挿入中, 吊るし上げ中, 荊で緊縛中の場合
IF TEQUIP:43 || TEQUIP:55 || TEQUIP:13 == 1 || (TEQUIP:14 > 0 && TEQUIP:14 < 5) || TEQUIP:25 == 7 || TEQUIP:26 >= 3 || TEQUIP:40 == 4 || TEQUIP:40 == 5
	;苦痛と恐怖と不快と抑鬱と薬毒の上昇値の合計
	LOCAL = UP:16 + UP:17 + UP:21 + UP:22 + UP:30
	;上の合計が一定以上ならば表示せずに帰る
	SIF LOCAL >= 1000
		RETURNF 0
ENDIF
RETURNF 1
