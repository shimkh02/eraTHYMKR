;========================================================
;밤 이벤트 각 본체 이벤트 부분
;=========================================================
;-----------------------------------------------
;자고 있는 주인에게 이것저것 한다
;-----------------------------------------------
@NIGHT_EVENT_1, ARG, ARG:1, ARG:2, ARG:3
;지문 컷
SIF CFLAG:99 == 0
	PRINTFORMW 밤 2시경……. %조사처리(CALLNAME:TARGET,"가")% 이미 잠들어버린 %CALLNAME:MASTER% 곁에 찾아왔다.
PRINTL 
;확률 1/3로 요바이 실패
IF RAND:3 == 0
	;유치 캐릭터의 곁잠 실패일 경우
	IF ARG:2 == 2
		;지문 컷
		IF CFLAG:99 == 0
			PRINTFORML %조사처리(CALLNAME:MASTER,"과")% 같은 이불 속에서 마음껏 응석부려야겠다 생각했던 %CALLNAME:TARGET%의 눈에 눈물이 가득해졌다.
			PRINTFORML 방에 돌아가선 이불을 끌어안고는, %조사처리(CALLNAME:MASTER,"가")% 없는 침대 안에서 쓸쓸함에 작게 몸을 떨었다.
		ENDIF
		CALL SELF_KOJO, TARGET, 403
		;정조대 장착 중에는 배설행위 불가
		IF CFLAG:42 & 2
			PRINTW 　
			;지문 컷
			SIF CFLAG:99 == 0
				PRINTFORML 그날 밤, %조사처리(CALLNAME:TARGET,"는")% 무서운 꿈을 꾸고 만 것 같다…….
			;17공포의 구슬
			CALL COMMON_UP_JUEL, TARGET, 17, 300+ARG:3*RAND:701, 1
		ELSE
			IF EXP:6 >= 1
				;지문 컷
				SIF CFLAG:99 == 0
				PRINTFORML 그날 밤, 무서운 꿈을 꾸고 만 %조사처리(CALLNAME:TARGET,"는")% 침대에서 실례를 해버린 것 같다……
				LOCAL = 300+ARG:3*RAND:701
				;06방뇨경험
				CALL COMMON_UP_EXP, TARGET, 6, 1, 0
				;15치정의 구슬
				CALL COMMON_UP_JUEL, TARGET, 15, LOCAL, 0
				;17공포의 구슬
				CALL COMMON_UP_JUEL, TARGET, 17, LOCAL, 1
			ENDIF
		ENDIF
	;야습 실패
	ELSE
		;지문 컷
		SIF CFLAG:99 == 0
			PRINTFORML 문 저편에서 허둥지둥 사라져가는 %CALLNAME:TARGET%의 발소리가 멀찍이서 들려왔다…….
		;정조대 장착 중에는 자위에도 실패
		IF CFLAG:42 & 2
			;지문 컷
			IF CFLAG:99 == 0
				PRINTFORML 욕정을 억누르지 못한 %조사처리(CALLNAME:TARGET,"는")% 자위행위를 하려고 다리 사이로 손을 뻗었지만
				PRINTFORML 검게 빛나는 정조대 때문에 그 행위는 저지당하고 말았다….
			ENDIF
			CALL SELF_KOJO, TARGET, 160
			;13욕정의 구슬
			CALL COMMON_UP_JUEL, TARGET, 13, (ARG*150), 0
			;14굴복의 구슬
			CALL COMMON_UP_JUEL, TARGET, 14, (ARG*200), 1
		ELSE
			;지문 컷
			SIF CFLAG:99 == 0
				PRINTFORML 하지만 욕정을 억누르지 못한 %조사처리(CALLNAME:TARGET,"는")% 그만 그 자리에서 자위를 시작해버린 것 같다.
			CALL SELF_KOJO, TARGET, 151
			PRINTL 
			;02절정경험
			CALL COMMON_UP_EXP, TARGET, 2, (ARG/2), 0
			;10자위경험
			CALL COMMON_UP_EXP, TARGET, 10, (ARG/2), 0
			;00쾌C의 구슬
			CALL COMMON_UP_JUEL, TARGET, 0, (ARG*(100*ABL:0)), 0
			;12순종의 구슬
			CALL COMMON_UP_JUEL, TARGET, 12, (ARG*100), 0
			;13욕정의 구슬
			CALL COMMON_UP_JUEL, TARGET, 13, (ARG*200), 1
			PRINTL 
			;지문 컷
			SIF CFLAG:99 == 0
				PRINTFORML {ARG/2}번의 자위 끝에 만족한 %조사처리(CALLNAME:TARGET,"는")% 피곤해진 모습으로 침대에 가 풀썩 쓰러졌다…….
		ENDIF
	ENDIF
ELSE
	;지문 컷
	SIF CFLAG:99 == 0
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% \@(90 >= GET_AGG(TARGET)) ? 조심조심 방으로 침입하더니 # 당당히 방 안으로 침입하더니\@ %CALLNAME:MASTER%의 침대 안으로 파고들었다.
	;애정 있음, 3분의 1로 곁잠
	IF TALENT:150 && RAND:3 == 0
		;지문 컷
		SIF CFLAG:99 == 0
			PRINTFORMW 사랑스러운 %CALLNAME:MASTER%의 잠든 얼굴에 가만히 키스를 한 %조사처리(CALLNAME:TARGET,"는")% \@(RAND:2 == 0) ? 가슴에 뺨을 대고 기쁜 듯이 기대어 잠들었다. # 가슴을 베개삼아 기쁜 듯이 기대어 잠들었다.\@
		;곁잠 이벤트 대사 호출
		CALL SELF_KOJO, TARGET, 400
		PRINTL 
		;21애정경험
		CALL COMMON_UP_EXP, TARGET, 21, ((ARG:1/2)+2), 0
		;12순종의 구슬
		CALL COMMON_UP_JUEL, TARGET, 12, (ARG:1*200), 1
		PRINTL 
		;지문 컷
		SIF CFLAG:99 == 0
			PRINTFORML 그날 밤, %조사처리(CALLNAME:TARGET,"는")% 행복한 꿈을 꾸었다…….
	;맘대로 욕망의 발산을 해버린다
	ELSE
		LOCAL:1 = RAND:4
		;정조대 장착 중에는 맘대로 욕망의 발산을 할 수 없음
		IF CFLAG:42 & 2
			;지문 컷
			SIF CFLAG:99 == 0
				PRINTFORML %CALLNAME:MASTER%의 몸에 발정하고 만 %CALLNAME:TARGET%였으나, 정조대가 그 욕망을 방해했다.

			CALLF SET_NIGHT_KOJO(0, 5)
			;요바이 기승위(정조대 방어) 이벤트 대사 호출
			CALL SELF_KOJO, TARGET, 401
			PRINTL 
			;13욕정의 구슬
			CALL COMMON_UP_JUEL, TARGET, 13, (ARG*150), 0
			;14굴복의 구슬
			CALL COMMON_UP_JUEL, TARGET, 14, (ARG*200), 1
			PRINTL 
			SIF CFLAG:99 == 0
				PRINTFORML %CALLNAME:MASTER%의 곁에서 번민하는 기분을 끌어안은 %조사처리(CALLNAME:TARGET,"는")%, 좀처럼 잠들 수 없었다.
		;奴隷に棒があり, 주인に穴がある場合.夜這いされる.
		;反発LV3と禁断と調合知識があったら, 穴を作ってでも犯しに来る
		;反発関係は移転.よって反発3でない奴隷棒有り, 주인穴のみここになる
		ELSEIF (TALENT:120 || TALENT:121) && TALENT:MASTER:120 == 0
			;지문 컷
			IF CFLAG:99 == 0
				;주인이 처녀の場合素股に変更.処女素股は浪漫らしいので.
				IF TALENT:MASTER:0 >= 1
					;ふたなりなら尻に突っ込む.例え경험が無くとも.
					IF TALENT:MASTER:121
						PRINTFORML %조사처리(CALLNAME:TARGET,"는")%, 잠들어 있는 %CALLNAME:MASTER%의 속옷을 벗기고
						PRINTFORML 보지에 삽입하려고 했지만, %조사처리(CALLNAME:MASTER,"가")% 처녀라는 사실을 알고는 잠시 주저했다.
						PRINTL 하지만 이미 아플 정도로 솟아오른 육봉 때문에, 이대로 그냥 돌아간다니 상상도 할 수 없다.
						PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:MASTER%의 보지를 핥아서, 애액으로 적시게 한 뒤,
						PRINTFORML 그것을 %CALLNAME:MASTER%의 엉덩이 구멍과 자신의 우뚝 선 물건에 바르고 나서
						PRINTL 뜻을 굳히고는 육봉을 장내로 쑤셔넣었다.
						PRINTL 　
						IF EXP:MASTER:1 == 0
							PRINTFORML 돌연한 충격에 눈을 뜬 %조사처리(CALLNAME:MASTER,"는")%, 장내에 쑤셔박힌 이물감에 공포를 느끼면서도
							PRINTFORML 허둥대는 %CALLNAME:TARGET%의 변명을 들었다, 
							PRINTFORML 그 와중에도 장내를 탐하는 것을 멈추려고 하지 않는 %CALLNAME:TARGET%의 모습을 보고
							PRINTL 여기서 무리해서 저항해봐야, 처녀를 빼앗길 뿐이라고 생각해서
							PRINTL 가급적 편한 자세를 취하고, 뒷쪽 구멍으로 처음 경험을 마치도록 했다.
						ELSE
							PRINTFORML 돌연한 충격에 눈이 떠진 %조사처리(CALLNAME:MASTER,"는")% 잠시 멍하니 있다가도
							PRINTFORML %조사처리(CALLNAME:TARGET,"과")%의 행위를 받아들여, 스스로 허리를 흔들기 시작했다.
						ENDIF
					;処女素股
					ELSE
						PRINTFORML %조사처리(CALLNAME:TARGET,"는")%, 잠들어 있는 %CALLNAME:MASTER%의 속옷을 벗기고
						PRINTFORML 보지에 삽입하려고 했지만, %조사처리(CALLNAME:MASTER,"가")% 처녀라는 사실을 알고는 주저하다가
						PRINTL 허벅지에 우뚝 솟은 육봉을 마찰시켜 욕망을 풀기로 했다.
						PRINTL 　
						PRINTFORML 몸에 걸리는 무게에 눈을 뜬 %조사처리(CALLNAME:MASTER,"는")%, 허벅지에 육봉을 비비고 있는 %CALLNAME:TARGET%의 모습에 아연해 하면서도
						PRINTFORML 쿡 하고 웃은 뒤, 스스로가 위가 되어 봉사해주었다.
					ENDIF
				ELSE
					PRINTFORML %조사처리(CALLNAME:TARGET,"는")%, 잠들어 있는 %CALLNAME:MASTER%의 속옷을 벗기고
					PRINTL 얼굴을 묻고 보지를 핥고 빨기 시작했다.
					PRINTL 그리고 충분히 젖은 것을 확인한 뒤, 육봉을 대고 단번에 꿰뚫었다.
					PRINTL 　
					PRINTFORML 하복부에 걸리는 충격에 눈을 뜬 %조사처리(CALLNAME:MASTER,"는")% 잠시 아연해 하면서도
					PRINTFORML %조사처리(CALLNAME:TARGET,"과")%의 행위를 받아들이고, 스스로 허리를 움직이기 시작했다…….

				ENDIF
			ENDIF
			CALLF SET_NIGHT_KOJO(0, 6)
			PRINTL 
			;요바이 기승위(주인이 범해지는 계열) 이벤트 대사 호출
			CALL SELF_KOJO, TARGET, 401
			PRINTL 
			;주인이 처녀
			IF TALENT:MASTER:0
				IF TALENT:MASTER:121
					;Ａ경험(주인)
					CALL COMMON_UP_EXP, MASTER, 1, (ARG*2), 0
				ENDIF
				;03사정경험
				CALL COMMON_UP_EXP, TARGET, 3, ARG, 0
				;LUNATIC 이상은 사정할 때마다 체력이 감소
				CALL EXHAUST_EJAC, TARGET, ARG
				;00쾌C의 구슬
				CALL COMMON_UP_JUEL, TARGET, 0, (ARG*(70*ABL:0*ABL:0)), 0
				;12순종의 구슬
				CALL COMMON_UP_JUEL, TARGET, 12, (ARG:1*60*ABL:10), 0
				;13욕정의 구슬
				CALL COMMON_UP_JUEL, TARGET, 13, (ARG*100*ABL:11), 1
			ELSE
				;00Ｖ경험(주인)
				CALL COMMON_UP_EXP, MASTER, 0, (ARG*2), 0
				;03사정경험
				CALL COMMON_UP_EXP, TARGET, 3, ARG, 0
				;LUNATIC 이상은 사정할 때마다 체력이 감소
				CALL EXHAUST_EJAC, TARGET, ARG
				;00쾌C의 구슬
				CALL COMMON_UP_JUEL, TARGET, 0, (ARG*(70*ABL:0*ABL:0)), 0
				;12순종의 구슬
				CALL COMMON_UP_JUEL, TARGET, 12, (ARG:1*60*ABL:10), 0
				;13욕정의 구슬
				CALL COMMON_UP_JUEL, TARGET, 13, (ARG*100*ABL:11), 1
			ENDIF
			PRINTL 
			;지문 컷
			IF CFLAG:99 == 0
				IF TALENT:MASTER:0 > 0
					PRINTFORML 그리고 %CALLNAME:MASTER%에게 {ARG}번 정액을 토해내고는, %조사처리(CALLNAME:TARGET,"는")% 부끄러운 듯 돌아갔다.
				ELSE
					PRINTFORML 그리고 %조사처리(CALLNAME:TARGET,"는")% 질내에 {ARG}번 정액을 토해내더니, 그대로 연결된 채로 잠에 빠졌다…….
				ENDIF
			ENDIF
		;주인にモノがついていて, 奴隷は非オトコで恋慕もしくは淫乱有り.4分の3でエッチ
		ELSEIF (TALENT:MASTER:120 || TALENT:MASTER:121) && TALENT:120 == 0 && (TALENT:150 || TALENT:170) && LOCAL:1 >= 1
			;지문 컷
			SIF CFLAG:99 == 0
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 자고 있는 %CALLNAME:MASTER%의 물건을 펠라로 일단 세워놓고는 
			IF TALENT:0 == 1 && TALENT:150
				;지문 컷
				IF CFLAG:99 == 0
					PRINTFORMW 자신의 순결을 사랑하는 %CALLNAME:MASTER%에게 바치려고 보지에 귀두를 댔다.
					PRINTFORML 하지만 기대에 푹 젖은 보지가 기둥을 적시자 %조사처리(CALLNAME:TARGET,"는")% 탄식과 함께 %CALLNAME:MASTER%에게 기대 누웠다.
				ENDIF
				CALLF SET_NIGHT_KOJO(0, 1)
				;요바이 기승위(실패) 이벤트 대사 호출
				CALL SELF_KOJO, TARGET, 401
				PRINTL 
				;21애정경험
				CALL COMMON_UP_EXP, TARGET, 21, ((ARG:1/2)+2), 0
				;12순종의 구슬
				CALL COMMON_UP_JUEL, TARGET, 12, (ARG:1*200), 1
				PRINTL 
				;지문 컷
				SIF CFLAG:99 == 0
					PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 언젠가 %CALLNAME:MASTER%에게 순결을 빼앗길 때를 꿈꾸며 잠에 빠져들었다…….
			ELSE
				IF GET_AGG(TARGET) >= 90 && TALENT:0 == 0 && TALENT:170
					;지문 컷
					SIF CFLAG:99 == 0
						PRINTFORML 음부로 마찰하며 물건을 질내로 이끌고는, 음란한 허리놀림과 교성을 내는 통에 %조사처리(CALLNAME:MASTER,"는")% 깨어났다.
					CALLF SET_NIGHT_KOJO(0, 2)
					;요바이 기승위(성공) 이벤트 대사 호출
					CALL SELF_KOJO, TARGET, 401
					PRINTL 
					;00Ｖ경험
					CALL COMMON_UP_EXP, TARGET, 0, ARG, 0
					;02절정경험
					CALL COMMON_UP_EXP, TARGET, 2, (ARG/2), 0
					;04정액경험
					CALL COMMON_UP_EXP, TARGET, 4, (ARG/2), 0
					;42Ｖ조율경험
					CALL COMMON_UP_EXP, TARGET, 42, ARG, 0
					;01쾌V의 구슬
					CALL COMMON_UP_JUEL, TARGET, 1, (ARG*100), 0
					;12순종의 구슬
					CALL COMMON_UP_JUEL, TARGET, 12, (ARG:1*150), 0
					;13욕정의 구슬
					CALL COMMON_UP_JUEL, TARGET, 13, (ARG*50), 1
					PRINTL 
					;지문 컷
					SIF CFLAG:99 == 0
						PRINTFORML 그리고 {ARG}번 행위를 반복하여 절정하더니, 그대로 %CALLNAME:MASTER%의 가슴 위에 기대 잠들어 버렸다.
				ELSE
					;지문 컷
					SIF CFLAG:99 == 0
						PRINTFORML 자신의 보지에 삽입하려고 했지만 못내 부끄러운 듯이 그것을 멈췄다.
					CALLF SET_NIGHT_KOJO(0, 3)
					;요바이 기승위(단념) 이벤트 대사 호출
					CALL SELF_KOJO, TARGET, 401
					PRINTL 
					;04정액경험
					CALL COMMON_UP_EXP, TARGET, 4, 1, 0
					;22펠라경험
					CALL COMMON_UP_EXP, TARGET, 22, ARG, 0
					;11습득의 구슬
					CALL COMMON_UP_JUEL, TARGET, 11, (ARG*150), 0
					;12순종의 구슬
					CALL COMMON_UP_JUEL, TARGET, 12, (ARG:1*150), 1
					PRINTL 
					;지문 컷
					SIF CFLAG:99 == 0
						PRINTFORML 그대로 펠라치오를 계속하여, %조사처리(CALLNAME:MASTER,"를")% 사정시키더니 곁에서 같이 잠들었다…….
				ENDIF
			ENDIF
		;ついてない×ついてない場合, レズッ気3以上で貝あわせ.
		ELSEIF (TALENT:120 == 0 && TALENT:121 == 0) && (TALENT:MASTER:120 == 0 && TALENT:MASTER:121 == 0) && ABL:22 > 2
			;지문 컷
			IF CFLAG:99 == 0
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")%, 잠들어 있던 %CALLNAME:MASTER%의 속옷을 가만히 벗기더니
				PRINTFORML 완전히 푹 젖어버린 자신의 보지를 %CALLNAME:MASTER%의 보지에 마찰시키며 쾌락을 탐했다.
				PRINTL 　
				PRINTFORML 하복부의 위화감에 눈을 뜬 %조사처리(CALLNAME:MASTER,"는")%
				PRINTFORML 갑자기 치밀어오른 쾌락에 곤혹스러워 하면서도, 질 수 없다는 듯 허리를 흔들며 %조사처리(CALLNAME:TARGET,"를")% 공략했다.
			ENDIF
			CALLF SET_NIGHT_KOJO(0, 7)
			;요바이 기승위(貝あわせ) 이벤트 대사 호출
			CALL SELF_KOJO, TARGET, 401
			PRINTL 
			;02절정경험
			CALL COMMON_UP_EXP, TARGET, 2, (ARG/2), 0
			;12레즈경험
			CALL COMMON_UP_EXP, TARGET, 12, (ARG*8), 0
			;00쾌C의 구슬
			CALL COMMON_UP_JUEL, TARGET, 0, (ARG*400), 0
			;12순종의 구슬
			CALL COMMON_UP_JUEL, TARGET, 12, (ARG:1*300), 0
			;13욕정의 구슬
			CALL COMMON_UP_JUEL, TARGET, 13, (ARG*600), 1
			PRINTL 
			;지문 컷
			SIF CFLAG:99 == 0
				PRINTFORML 그리고 {ARG}번 행위를 반복한 %조사처리(CALLNAME:TARGET,"가")% 절정하자, 두 사람은 연결된 채로 잠에 빠졌다.
		;ついていないと, 強制的にオナニー
		ELSE
			;지문 컷
			IF CFLAG:99 == 0
				PRINTFORM %CALLNAME:MASTER%의 손을 잡은 %조사처리(CALLNAME:TARGET,"는")%, 
				IF 90 >= GET_AGG(TARGET)
					PRINTFORML 속옷 위로 부족하나마 보지를 비비기 시작했다.
				ELSE
					PRINTFORML 대담하게도 속옷 안에까지 손을 이끌었다.
				ENDIF
			ENDIF
			CALLF SET_NIGHT_KOJO(0, 4)
			;요바이 기승위(오나니) 이벤트 대사 호출
			CALL SELF_KOJO, TARGET, 401
			PRINTL 
			;02절정경험
			CALL COMMON_UP_EXP, TARGET, 2, 1, 0
			;10자위경험
			CALL COMMON_UP_EXP, TARGET, 10, 1, 0
			;00쾌C의 구슬
			CALL COMMON_UP_JUEL, TARGET, 0, (ARG*(100*ABL:0)), 0
			;12순종의 구슬
			CALL COMMON_UP_JUEL, TARGET, 12, (ARG:1*100), 0
			;13욕정의 구슬
			CALL COMMON_UP_JUEL, TARGET, 13, (ARG*200), 1
			PRINTL 
			;지문 컷
			IF CFLAG:99 == 0
				IF 90 >= GET_AGG(TARGET)
					PRINTFORML 목소리를 억누르며 작게 절정하더니, 그 여운을 남긴 채로 잠에 들었다…….
				ELSE
					PRINTFORML 가감없이 달콤한 목소리를 내며 자위를 계속하더니, 결국 %조사처리(CALLNAME:TARGET,"는")% %조사처리(CALLNAME:MASTER,"를")% 바라보며 절정했다…….
				ENDIF
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-----------------------------------------------
;곁잠 성공
;-----------------------------------------------
;幼稚で反抗的, 素直な場合
@NIGHT_EVENT_2, ARG
;지문 컷
IF CFLAG:99 == 0
	PRINTFORML 밤, %조사처리(CALLNAME:TARGET,"가")% 잠옷을 입고 베개를 끌어안은 채 %CALLNAME:MASTER%의 방으로 들이닥쳤다.
	IF TALENT:11 || TALENT:12 || TALENT:16
		PRINTFORMW 오늘은 %CALLNAME:MASTER%에 대한 선물로, %조사처리(CALLNAME:TARGET,"가")% 같이 자 주겠다는 모양이다.
	ELSE
		PRINTFORMW 혼자서 자는 게 무서웠던 모양인지, 쓸쓸해 보이는 표정으로 %조사처리(CALLNAME:MASTER,"를")% 바라보고 있다.
	ENDIF
ENDIF
;곁잠 성공 이벤트 대사 호출
CALL SELF_KOJO, TARGET, 402
PRINTL 
LOCAL = (200*ABL:0)+(ABL:0*RAND:101)
;21애정경험
CALL COMMON_UP_EXP, TARGET, 21, ((ARG/2)+2), 0
;12순종의 구슬
CALL COMMON_UP_JUEL, TARGET, 12, LOCAL, 1
PRINTL 
;지문 컷
IF CFLAG:99 == 0
	IF TALENT:11 || TALENT:12 || TALENT:16
		PRINTFORM 늘 꿋꿋한 태도를 취하던 %CALLNAME:TARGET%도, 침대 안에서는 
	SIF TALENT:127 || TALENT:128
		PRINTFORM 작은 몸으로 
		PRINTFORML 꾸욱 하고 %CALLNAME:MASTER%에게 안겨왔다.
	ELSE
		PRINTFORML 같이 자 주자, 침대 안에서 %조사처리(CALLNAME:TARGET,"는")% 기쁜 듯이 %CALLNAME:MASTER%에게 안겨들어 왔다.
	ENDIF
ENDIF

;-----------------------------------------------
;색시와의 하룻밤
;-----------------------------------------------
@NIGHT_EVENT_3, ARG, ARG:1
LOCAL = 1 + RAND:3
CALLF SET_NIGHT_KOJO(0, LOCAL)
SELECTCASE LOCAL
	CASE 1
		;지문 컷
		IF CFLAG:99 == 0
		PRINTFORMW %CALLNAME:MASTER%의 반려자가 된 %조사처리(CALLNAME:TARGET,"가")% 야식의 초대로 와 줬다.
		PRINTFORML 진심이 담뿍 담긴 애처요리를 감상하고는, 열정을 품게 된 %조사처리(CALLNAME:MASTER,"는")% %조사처리(CALLNAME:TARGET,"를")% 그대로 덮쳤다.
		ENDIF
		;색시와의 하룻밤 이벤트 대사 호출
		CALL SELF_KOJO, TARGET, 404
		PRINTL 
		;70조리경험
		CALL COMMON_UP_EXP, TARGET, 70, ABL:70, 0
		CALL NIGHT_LOVE, ARG, ARG:1
		PRINTL 
		SIF CFLAG:99 == 0
			PRINTFORML \@(ITEM:44) ? 에이프런 차림의 # 옷을 채 벗기지도 않고, \@ %조사처리(CALLNAME:TARGET,"를")% %CALLNAME:MASTER%의 애정을 그대로 담은 채 더럽혔다…….
	CASE 2
		IF CFLAG:99 == 0
			PRINTFORMW 똑똑, 하고 노크 소리와 %조사처리(CALLNAME:MASTER,"를")% 부르는 사랑스러운 그녀의 목소리.
			PRINTFORML 문을 열자, %조사처리(CALLNAME:TARGET,"는")% 입술을 겹쳐오며 %조사처리(CALLNAME:MASTER,"를")% 덮쳤다.
		ENDIF
		;색시와의 하룻밤 이벤트 대사 호출
		CALL SELF_KOJO, TARGET, 404
		PRINTL 
		CALL NIGHT_LOVE, ARG, ARG:1
		PRINTL 
		;지문 컷
		SIF CFLAG:99 == 0
		PRINTFORML 두 사람은 마치 첫날밤인 양 정열적으로 날이 밝을 때까지 서로 몸을 섞었다.
	CASE 3
		;지문 컷
		SIF CFLAG:99 == 0
			PRINTFORMW 바라던 바를 이루고 하나가 된 %조사처리(CALLNAME:TARGET,"가")%, %조사처리(CALLNAME:MASTER,"과")% 같이 있고 싶다며 찾아왔다.
		;색시와의 하룻밤 이벤트 대사 호출
		CALL SELF_KOJO, TARGET, 404
		PRINTL 
		;21애정경험
		CALL COMMON_UP_EXP, TARGET, 21, (ARG+1), 0
		;23キス경험
		CALL COMMON_UP_EXP, TARGET, 23, (ARG+1), 0
		LOCAL:1 = (600*ABL:0)+(ABL:0*RAND:101)
		;12순종의 구슬
		CALL COMMON_UP_JUEL, TARGET, 12, LOCAL:1, 1
		;23キス경험
		CALL COMMON_UP_EXP, MASTER, 23, (ARG+1), 1
		PRINTL 
		;지문 컷
		SIF CFLAG:99 == 0
			PRINTFORML 두 사람 다 잠들어 버릴 때까지, 서로 꽉 끌어안은 채 행복한 시간을 보냈다…….
ENDSELECT

;-----------------------------------------------
;엿보기 오나니
;-----------------------------------------------
@NIGHT_EVENT_10, ARG, ARG:1, ARG:2, ARG:3, ARG:4, ARG:5
TARGET = ARG:1
IF RAND:6 == 0 && ARG:5 >= 3 && ARG:1 != ARG:2 && ARG:2 != ARG && (CFLAG:(ARG:1):42 & 2) == 0 && (CFLAG:(ARG:2):42 & 2) == 0 && TALENT:(ARG:1):120 == 0 && TALENT:(ARG:2):120 == 0
	PRINTL
	WAIT
	;지문 컷
	IF CFLAG:99 == 0
		PRINTFORML %CALLNAME:MASTER%의 방에서 신음소리를 울리는 %조사처리(CALLNAME:ARG,"를")% 몰래 보고 있던 %조사처리(CALLNAME:(ARG:1),"와")% %조사처리(CALLNAME:(ARG:2),"는")%
		PRINTFORML 뜨거워진 몸을 진정시키기 위해 서로의 신체를 자위했다.
	ENDIF

	;둘이서 엿보며 레즈(레즈하는 게ARG:1,ARG:2)
	CALL SELF_KOJO, TARGET, 485, ARG:2
	CALL SELF_KOJO, ARG:2, 485, TARGET

	PRINTL 
	;12레즈경험
	CALL COMMON_UP_EXP, ARG:1, 12, ARG:3, 0
	CALL COMMON_UP_EXP, ARG:2, 12, ARG:3, 0
	;12순종의 구슬
	CALL COMMON_UP_JUEL, ARG:1, 12, (ARG:3*50), 0
	CALL COMMON_UP_JUEL, ARG:2, 12, (ARG:3*50), 0
	;13욕정의 구슬
	CALL COMMON_UP_JUEL, ARG:1, 13, (ARG:4*150), 0
	CALL COMMON_UP_JUEL, ARG:2, 13, (ARG:4*150), 1
	PRINTL 
	;지문 컷
	IF CFLAG:99 == 0
		PRINTFORML %조사처리(CALLNAME:MASTER,"가")% %조사처리(CALLNAME:(ARG:1),"와")% %조사처리(CALLNAME:(ARG:2),"를")% 눈치 못채고 잠들어 버리자
		PRINTFORML %조사처리(CALLNAME:(ARG:1),"와")% %조사처리(CALLNAME:(ARG:2),"는")% 각자 자기 방으로 들어갔다.
	ENDIF
;ELSEIF RAND:4 == 0
;	PRINTFORM 문득, %조사처리(CALLNAME:MASTER,"는")%
;	PRINTFORML %조사처리(CALLNAME:(ARG:1),"가")% 이쪽의 정사를 딸감으로 자위를 하고 있는 것을 눈치챘다.
;	PRINTFORMW 혼자 절정하는 %조사처리(CALLNAME:(ARG:1),"가")% 그 여운에 잠긴 사이에 붙잡아, %조사처리(CALLNAME:(ARG:1),"와")% 함께 벌을 주기 시작했다…….
;	CALL SELF_KOJO, ARG:1, 
;	JUEL:(ARG:1):17 += ARG:3*200
;	JUEL:(ARG:1):16 += ARG:3*100
;	JUEL:(ARG:1):15 += ARG:3*250
;	PRINTFORML %PALAMNAME:17%의 구슬＋{ARG:3*200}
;	PRINTFORML %PALAMNAME:16%의 구슬＋{ARG:3*100}
;	PRINTFORML %PALAMNAME:15%의 구슬＋{ARG:3*250}
ELSE
	PRINTL 
	WAIT
	;직전의 행위가 자위계라면 플래그 세운다
	TFLAG:229 = (GROUPMATCH(GET_NIGHT_KOJO(0), 19, 119, 219, 319)) ? 1 # 0

	;지문 컷
	SIF CFLAG:99 == 0
		PRINTFORML 몰래 \@(TFLAG:229 == 1) ? %조사처리(CALLNAME:ARG,"가")% %CALLNAME:MASTER% 앞에서 자위하는 것을 # %조사처리(CALLNAME:ARG,"와")% %CALLNAME:MASTER%의 정사를 \@ 엿보고 있던 %조사처리(CALLNAME:(ARG:1),"는")% 
	;정조대 장착 중에는 자위 불가
	IF CFLAG:42 & 2
		;지문 컷
		IF CFLAG:99 == 0
			IF TALENT:(ARG:1):150
				PRINTFORMW 사랑하는 사람\@(TFLAG:229 == 1) ? 에게 자신의 치부를 보여줄 수 있는 # 에게 안긴\@ %조사처리(CALLNAME:ARG,"를")% 부러운 눈으로 보고 있다.	
				PRINTFORML 그리고 \@(TFLAG:229 == 1) ? %조사처리(CALLNAME:ARG,"를")% # %조사처리(CALLNAME:MASTER,"를")%\@ 보고 참을 수 없게 된 %조사처리(CALLNAME:TARGET,"는")% 자위를 하려고 다리 사이에 손을 뻗었지만
				PRINTW 검게 빛나는 정조대에 의해 그 행위는 저지당하고 말았다….
			ELSE
				PRINTFORMW 허겁지겁 방에 돌아가 \@(TFLAG:229 == 1) ? %CALLNAME:ARG%의 자위 # 두 명의 정사\@를 다시 생각하며, 괴로워하고 있다…….
			ENDIF
		ENDIF
		CALLF SET_NIGHT_KOJO(0, 1)
		;엿보기 자위 이벤트 대사 호출
		CALL SELF_KOJO, TARGET, 409
		;13욕정의 구슬
		CALL COMMON_UP_JUEL, TARGET, 13, (ARG:3*90*ABL:11), 1
		;14굴복의 구슬
		CALL COMMON_UP_JUEL, TARGET, 14, (ARG:3*150*ABL:11), 1
		PRINTL 
		;지문 컷
		IF CFLAG:99 == 0
			IF TALENT:(ARG:1):150
				PRINTFORML %조사처리(CALLNAME:MASTER,"가")% %조사처리(CALLNAME:(ARG:1),"를")% 눈치채지 못하고 잠들어 버리자, %조사처리(CALLNAME:(ARG:1),"는")% 울면서 자기 방으로 돌아왔다.
			ELSE
				PRINTFORML %CALLNAME:MASTER%\@(TFLAG:229 == 1) ? 의 앞에서 자위하는 # 에게 사랑받는\@ %CALLNAME:ARG%에게 자신의 모습이 겹쳐져, 
				PRINTFORML %조사처리(CALLNAME:(ARG:1),"는")% 좀처럼 잠들 수 없었다.
			ENDIF
		ENDIF
	ELSE
		;지문 컷
		IF CFLAG:99 == 0
			IF TALENT:(ARG:1):150
				PRINTFORMW 사랑하는 사람\@(TFLAG:229 == 1) ? 에게 자신의 치부를 보여줄 수 있는 # 에게 안긴\@ %조사처리(CALLNAME:ARG,"를")% 부러운 눈으로 보고 있다.
				PRINTFORML 그리고 \@(TFLAG:229 == 1) ? %조사처리(CALLNAME:ARG,"를")% # %조사처리(CALLNAME:MASTER,"를")%\@ 보고 참을 수 없게 된 %조사처리(CALLNAME:TARGET,"는")% 그 자리에서 자위를 시작해 버렸다.
			ELSE
				PRINTFORMW 허겁지겁 방에 돌아가 \@(TFLAG:229 == 1) ? %CALLNAME:ARG%의 자위 # 두 명의 정사\@를 딸감 삼아 자위하기 시작했다…….
			ENDIF
		ENDIF
		;엿보기 자위 이벤트 대사 호출
		CALLF SET_NIGHT_KOJO(0, 2)
		CALL SELF_KOJO, TARGET, 409
		;02절정 경험
		CALL COMMON_UP_EXP, TARGET, 2, 1, 0
		;10자위 경험
		CALL COMMON_UP_EXP, TARGET, 10, 1, 0
		;00쾌C의 구슬
		CALL COMMON_UP_JUEL, TARGET, 0, (ARG:3*(100*ABL:0)), 0
		;03쾌B의 구슬
		CALL COMMON_UP_JUEL, TARGET, 3, (ARG:3*(100*ABL:3)), 0
		;11습득의 구슬
		CALL COMMON_UP_JUEL, TARGET, 11, (ARG:3*10), 0
		;12순종의 구슬
		CALL COMMON_UP_JUEL, TARGET, 12, (ARG:4*60*ABL:10), 0
		;13욕정의 구슬
		CALL COMMON_UP_JUEL, TARGET, 13, (ARG:3*120*ABL:11), 1
		PRINTL 
		;지문 컷
		IF CFLAG:99 == 0
			IF TALENT:(ARG:1):150
				PRINTFORML %조사처리(CALLNAME:MASTER,"가")% %조사처리(CALLNAME:(ARG:1),"를")% 눈치 못채고 잠들어버리자, %조사처리(CALLNAME:(ARG:1),"는")% 울면서 자기 방으로 돌아왔다.
			ELSE
				PRINTFORML %CALLNAME:MASTER%\@(TFLAG:229 == 1) ? 의 앞에서 자위하는 # 에게 사랑받는\@ %CALLNAME:ARG%에게 자신의 모습을 투영하고, %조사처리(CALLNAME:(ARG:1),"는")% 살그머니 절정했다.
			ENDIF
		ENDIF
	ENDIF
ENDIF
TARGET = ARG

;-----------------------------------------------
;도촬된다
;-----------------------------------------------
@NIGHT_TOUSATSU, ARG, ARG:1
PRINTL 
WAIT
;지문 컷
IF CFLAG:99 == 0
	;주인이 보도자를 가지지 않고, 도촬 사람이 보도자를 가지고 있어 욕망이 5 미만의 경우는 도촬되었던 것에 눈치채지 못한다
	IF TALENT:MASTER:196 == 0 && TALENT:(ARG:1):196 && ABL:(ARG:1):11 < 5
		PRINTFORML %조사처리(CALLNAME:MASTER,"와")% %CALLNAME:ARG%의 밤일을 몰래 숨어 도촬하는 사람이 있었다.
		PRINTFORML %CALLNAME:(ARG:1)%의 은폐는 성공적이어서 기분 좋게 도촬을 마무리했다.
		PRINTFORML 촬영된 비디오는 %CALLNAME:(ARG:1)%의 컬렉션에 추가되었고
		PRINTL 자위할 때 이용될 것이다.
	ELSEIF ABL:MASTER:14 < 3
		PRINTDATAL
			DATALIST
				DATA ……
				DATA …………
				DATA !
				DATA 
				DATAFORM %조사처리(CALLNAME:MASTER,"는")% 돌연히 %조사처리(CALLNAME:ARG,"를")% 밀쳐내더니, 방문을 힘차게 열었다.
				DATAFORM 눈을 휘둥그레하게 뜬 %조사처리(CALLNAME:ARG,"를")% 곁눈질로 주변을 살피라 해봤지만, 이미 사람의 기척은 없다.
				DATA ……다만 테이프가 빠져버린 비디오 카메라만이 복도에 남겨져 있을 뿐이었다…….
			ENDLIST
			DATALIST
				DATA ……
				DATA …………
				DATA !
				DATA 
				DATAFORM %조사처리(CALLNAME:MASTER,"는")% 방문 너머에 누군가 있는 기색을 느꼈다.
				DATAFORM %CALLNAME:ARG%도 눈치챈 것 같지만 보여지는 수치를 느끼면서
				DATA 서로 어색하게 행위를 계속했다.
			ENDLIST
		ENDDATA
	ELSE
		PRINTDATAL
			DATALIST
				DATAFORM ……밖에서 촬영하는 %조사처리(CALLNAME:(ARG:1),"는")% 이미 들켰었다.
				DATAFORM %조사처리(CALLNAME:MASTER,"는")% 촬영되고 있다는 사실을 몰래 %CALLNAME:ARG%에게 말하자
				DATA 수치스러운지 얼굴이 빨개졌지만 그로 인해 더 욕정한 것 같다.
				DATAFORM 밖에서 흥분하면서 훔쳐보는 %CALLNAME:(ARG:1)%에게 보여주듯이
				DATAFORM %조사처리(CALLNAME:MASTER,"는")%, %조사처리(CALLNAME:ARG,"와")%의 행위를 한층 더 격렬하게 했다.
			ENDLIST
			DATALIST
			DATA ……아무래도 촬영은 끝난 것 같다.
		DATAFORM %조사처리(CALLNAME:(ARG:1),"는")% 잘 숨었다고 생각한 모양이지만  %조사처리(CALLNAME:MASTER,"는")% 촬영이 시작될 당초부터 알고 있었다.
		DATA 막지 않았던 건 단순히 누가 보는 쪽이 더 욕정하기 때문이었다.
		DATAFORM 그리고 또, 그 비디오가 어떻게 될런지. %조사처리(CALLNAME:(ARG:1),"가")% 뻘건 눈으로 재생하고 있을지, 아니면 팔아서 용돈을 만들건지
		DATAFORM 그런 걸 생각하니 %CALLNAME:MASTER%의 몸은 자연히 열기를 띠기 시작했다…….
			ENDLIST
		ENDDATA
	ENDIF
ENDIF
;도촬의 이벤트 대사 호출
CALL SELF_KOJO, ARG:1, 452
PRINTL 
;테이프의 수를 소비시킨다
IF NOITEM == 0
	ITEM:75 -= 1
	SIF FLAG:13 & 16 && MONEY:0 >= ITEMPRICE:75
		CALL FILL_PLURAL_ITEM, 75
ENDIF

;71촬영 경험
CALL COMMON_UP_EXP, ARG:1, 71, 1, 0
CFLAG:(ARG:1):161 += 1
;눈치채지 못한 경우는 주의 입수는 Through{관통,통과}
IF TALENT:MASTER:196 || TALENT:(ARG:1):196 == 0 || ABL:(ARG:1):11 > 4
	;13욕정의 구슬 혹은 100 부정의 구슬(욕망과 촬영 기능에 의해 변화, 들어가지 않는 것도 있다), 단 질투의 유무로 구별.
	IF (200*ABL:(ARG:1):11)-(50*(5-ABL:(ARG:1):71)) > 0
		;질투가 없으면 비디오로 욕정. 단 촬영 기능 나쁘면 욕정할 수 있는 만큼 잘 비치지 않는다
		IF TALENT:(ARG:1):157 == 0
			CALL COMMON_UP_JUEL, ARG:1, 13, ((200*ABL:(ARG:1):11)-(50*(5-ABL:(ARG:1):71))), 0
		;질투 있으면 비디오로 질투해 반발. 단 촬영 기능 나쁘면 자주(잘) 안보이기 때문에…
		ELSE
			CALL COMMON_UP_JUEL, ARG:1, 100, ((200*ABL:(ARG:1):11)-(50*(5-ABL:(ARG:1):71))), 0
		ENDIF
	ENDIF
ENDIF
;72피사경효험
CALL COMMON_UP_EXP, ARG, 72, 1, 0
CALL COMMON_UP_EXP, MASTER, 72, 1, 1

;=====================================================
;
;노예의 덥침 행동
;
;=====================================================
;-----------------------------------------------
;야습격성공
;-----------------------------------------------
@NIGHT_EVENT_4, ARG, ARG:1
CALLF SET_CHARA_M(MASTER)

;유일한 문제, 처녀 상실 이벤트 약 3%. 조금 인상
;좀처럼은 없잖아 하지만, 서로 사랑함이나 복종, 등을 가지고 있는 경우는 자신으로부터 바치는 일은 하지 않는다.
;마이나스이벤트라고 생각될까, 플러스 이벤트라고 생각될까···. 물론 게임적으로는 마이나스지요, 미안해···
;현조건에서는 의외로 일어나기 너무 쉬운 싫은을 지적되어 왔기 때문에, 조건 변경(자세한 것은 재료 바레 근처를)
;이미지적으로는 실컷 초조하게 해 오는데 전혀 처녀를 빼앗아 주지 않는 주인에 대해서, 노예의 욕구 불만이 모집해 폭발한 느낌
IF (TALENT:MASTER:120 || TALENT:MASTER:121) && (TALENT:0 >= 1 && ABL:1 >= 2) && RAND:100 < 3 && (TALENT:150 && TALENT:170) && TALENT:161 == 0 && TALENT:153 == 0 && TALENT:127 == 0 && (CFLAG:(GET_CHARA_M()):42 & 2) == 0
	CALL NIGHT_SEX_SHOJO, ARG, ARG:1

;처녀 상실을 격리되었으므로 다음.
ELSEIF (ABL:10+ABL:11+ABL:13 > 12) && (TALENT:150 || TALENT:170)
	CALL NIGHT_SEX01, MASTER, 3, ARG, ARG:1
ELSEIF GET_AGG(TARGET) >= 90
	CALL NIGHT_SEX01, MASTER, 2, ARG, ARG:1
ELSE
	CALL NIGHT_SEX02, MASTER, ARG:1
ENDIF

;-----------------------------------------------
;노예가 의욕 만만때
;-----------------------------------------------
@NIGHT_SEX01, ARG, ARG:1, ARG:2, ARG:3

LOCAL = MAX((ARG:2+ARG:3)/2, 3)
SIF TALENT:170
	LOCAL += 3
;지문 컷
SIF CFLAG:99 == 0 && ARG:1== 3
	PRINTFORML %조사처리(CALLNAME:MASTER,"가")% 잠자리에 들려고 할 때, 돌연 %조사처리(CALLNAME:TARGET,"가")% 들어왔다.

;우선 노예의 행동 방침을 결정한다
LOCAL:1 = 0

IF RAND:4 == 0 && TALENT:120 == 0 && TALENT:121 == 0
	LOCAL:1 = 3
ELSEIF RAND:3 == 0 && (TALENT:120 || TALENT:121)
	LOCAL:1 = 0
ELSEIF RAND:2 == 0 && ABL:2 > 1 && (TALENT:ARG:120 || TALENT:ARG:121) && (CFLAG:ARG:42 & 2) == 0
	LOCAL:1 = 2
ELSEIF TALENT:120 == 0 && TALENT:0== 0 && ABL:1 > 1 && (TALENT:ARG:120 || TALENT:ARG:121) && (CFLAG:ARG:42 & 2) == 0
	LOCAL:1 = 1
ELSEIF (TALENT:120 || TALENT:121)
	LOCAL:1 = 0
ELSEIF ABL:2 > 1 && (TALENT:ARG:120 || TALENT:ARG:121) && (CFLAG:ARG:42 & 2) == 0
	LOCAL:1 = 2
ELSE
	LOCAL:1 = 3
ENDIF

;지문 컷
IF CFLAG:99 == 0 && ARG:1 == 2
	IF TALENT:120 && TALENT:ARG:120
		PRINTFORML %조사처리(CALLNAME:MASTER,"가")% 잠자리에 들려고 했을 때, 갑자기 %조사처리(CALLNAME:TARGET,"가")% 방을 방문해 왔다.
		PRINTFORML 그대로 %조사처리(CALLNAME:MASTER,"를")% 넘어뜨리고, 격렬하게 욕망을 부딪치려고 하고 있다.
		PRINTFORMW %CALLNAME:MASTER%도 그 행동에 대답하여 주기로 했다….
	ELSE
		PRINTFORML 밤, 갑자기 %조사처리(CALLNAME:TARGET,"가")% 방에 들어 오더니, %조사처리(CALLNAME:ARG,"를")% 침대에 밀어 넘어뜨렸다.
		PRINTFORM %조사처리(CALLNAME:TARGET,"는")% 
		IF LOCAL:1 == 0
			PRINTFORMW 완전히 발기한 페니스를 꺼내, 욕망의 불똥이 튀는 눈으로%조사처리(CALLNAME:ARG,"를")% 빨며 돌린다…….
		ELSEIF LOCAL:1 == 2
			PRINTFORMW %CALLNAME:ARG%의 옷을 벗겨내더니, 빨개진 얼굴로 자신의 옷도 풀기 시작한다.
		ELSE
			IF TALENT:0 >= 1
				PRINTFORM 순진한 소녀의 꽃잎에 
			ELSE
				PRINTFORM 발정한 암컷의 성기에 
			ENDIF
			PRINTFORMW 애액을 배이게 하고 그걸 %CALLNAME:MASTER%에게 문지르기 시작했다.
		ENDIF
	ENDIF
ENDIF

CALLF SET_NIGHT_KOJO(0, ARG:1)
CALL SELF_KOJO, TARGET, 405
LOCAL:2 = 0

;장대의 경우
IF LOCAL:1 == 0
	;이마라치오
	IF RAND:6 == 0 && (TALENT:ARG:54 == 0 || FLAG:14 & 16)
		LOCAL:2 = 4
	;가랑이 강제
	ELSEIF RAND:5 == 0 && TALENT:ARG:120 == 0 && TALENT:ARG:121 == 0 && (CFLAG:ARG:42 & 2) == 0
		LOCAL:2 = 0
	;젖성교
	ELSEIF RAND:4 == 0 && TALENT:ARG:120 == 0
		LOCAL:2 = 3
	;어널 섹스
	ELSEIF RAND:3 == 0 && ((ARG == MASTER && EXP:ARG:1 > 2) || ABL:ARG:2 > 1) && (CFLAG:ARG:42 & 2) == 0
		LOCAL:2 = 2
	;섹스
	ELSEIF TALENT:ARG:120 == 0 && TALENT:ARG:0 == 0 && EXP:ARG:0 > 0 && (CFLAG:ARG:42 & 2) == 0
		LOCAL:2 = 1
	;재차 어널 섹스
	ELSEIF RAND:2 == 0 && ((ARG == MASTER && EXP:ARG:1 > 12) || ABL:ARG:2 > 1) && (CFLAG:ARG:42 & 2) == 0
		LOCAL:2 = 2
	;재차 가랑이 처리
	ELSEIF RAND:3 == 0 && TALENT:ARG:120 == 0 && TALENT:ARG:121 == 0 && (CFLAG:ARG:42 & 2) == 0
		LOCAL:2 = 0
	;재차젖성교
	ELSEIF RAND:2 == 0 && TALENT:ARG:120 == 0
		LOCAL:2 = 3
	;재차 이마라치오
	ELSEIF TALENT:ARG:54 == 0 || FLAG:14 & 16
		LOCAL:2 = 4
	;자위
	ELSE
		LOCAL:2 = (ARG:1 == 2) ? 19 # 302
	ENDIF
;역강간
ELSEIF LOCAL:1 == 1
	LOCAL:2 = 100
;역어널 강간
ELSEIF LOCAL:1 == 2
	LOCAL:2 = 200
ELSE
	;강제 가랑이 한다
	IF RAND:2 == 0 && (TALENT:ARG:120 || TALENT:ARG:121) && (CFLAG:ARG:42 & 2) == 0
		LOCAL:2 = 300
	;강제조개 대면시키고 한다
	ELSEIF RAND:2 == 0 && (CFLAG:ARG:42 & 2) == 0 && (TALENT:ARG:120 == 0 && TALENT:ARG:121 == 0)
		LOCAL:2 = 301
	;쿠니 강제&자위
	ELSEIF TALENT:ARG:54 == 0 || FLAG:14 & 16
		LOCAL:2 = 302
	;자위
	ELSE
		LOCAL:2 = 19
	ENDIF
ENDIF
;지문 컷
IF CFLAG:99 == 0
	SELECTCASE LOCAL:2
		CASE 0
			IF ARG:1 == 3
				PRINTFORML 그대로 %조사처리(CALLNAME:TARGET,"는")%, %조사처리(CALLNAME:ARG,"를")% 넘어뜨려 속옷을 벗기고
				PRINTFORML %CALLNAME:ARG%의 성기에 음부를 대고는, 스스로를 위로하기 시작했다.
			ELSE
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:ARG%의 가랑이에 성기를 문지르며 격렬하게 허리를 흔들었다.
			ENDIF
		CASE 1
			IF ARG:1 == 3
				PRINTFORML 그대로 %조사처리(CALLNAME:TARGET,"는")% %조사처리(CALLNAME:ARG,"를")% 넘어뜨리고는, 부풀어오른 성기를 질 입구에 가져다 대더니 단숨에 밀어넣었다.
			ELSE
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:ARG%의 다리를 비틀고 질입구를 열어서 성기를 밀어넣었다.
			ENDIF
		CASE 2
			IF ARG:1 == 3
				PRINTFORML 그대로 %조사처리(CALLNAME:TARGET,"는")% %조사처리(CALLNAME:ARG,"를")% 넘어뜨리더니
				PRINTFORML %CALLNAME:ARG%의 고혈에 로션을 바르고는, 부풀어오른 성기로 단숨에 꿰뚫었다.
			ELSE
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %조사처리(CALLNAME:ARG,"를")% 엎드리게 한뒤 고혈에 성기를 쑤셔 넣었다.
			ENDIF
		CASE 3
			IF ARG:1 == 3
				PRINTFORML 그대로%조사처리(CALLNAME:TARGET,"는")% %조사처리(CALLNAME:ARG,"를")% 넘어뜨리더니, 옷을 벗기고 유방을 난폭하게 주물렀다.
			ELSE
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:ARG%의 위에 올라타고 유방으로 성기를 문지르기 시작했다.
			ENDIF
		CASE 4
			IF ARG:1 == 3
				PRINTFORML 그대로 %조사처리(CALLNAME:TARGET,"는")% %조사처리(CALLNAME:ARG,"를")% 넘어뜨리고는, 
				PRINTFORML 항의하려는 %CALLNAME:ARG%의 입에 부풀대로 부푼 성기를 밀어넣었다.
			ELSE
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:ARG%의 입에 성기를 밀어넣으면서 머리를 누르고 허리를 흔들었다.
			ENDIF
		CASE 19
			IF ARG:1 == 3
				PRINTFORML %CALLNAME:ARG% 앞에서 갑작스럽게 %조사처리(CALLNAME:TARGET,"는")% 자위하기 시작했다.
			ELSE
				PRINTFORML %CALLNAME:ARG% 앞에서 갑작스럽게 %조사처리(CALLNAME:TARGET,"는")% 자위하기 시작했다.
			ENDIF
		CASE 100
			IF ARG:1 == 3
				PRINTFORML 그대로 그녀는 %조사처리(CALLNAME:ARG,"를")% 밀어 넘어뜨려, 꿀 단지로 페니스를 단단히 집어삼키고 격렬하게 허리를 부딪히기 시작했다.
			ELSE
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:ARG%의 페니스를 꿀 단지에 넣어두고 스스로 허리를 흔들어 댔다.
			ENDIF
		CASE 200
			IF ARG:1 == 3
				PRINTFORML 그대로 %조사처리(CALLNAME:TARGET,"는")% %조사처리(CALLNAME:ARG,"를")% 밀어 넘어뜨리고 자신의 고혈에 %CALLNAME:ARG%의 페니스를 넣었다.
			ELSE
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:ARG%의 페니스를 고혈에 넣고 스스로 허리를 흔들었다.
			ENDIF
		CASE 300
			IF ARG:1 == 3
				PRINTFORML 그대로 %조사처리(CALLNAME:TARGET,"는")% %조사처리(CALLNAME:ARG,"를")% 밀어 넘어뜨린뒤, 애액이 방울져 떨어지는 음부를 들이대고는
				PRINTFORML %CALLNAME:ARG%의 페니스로 자신의 클리토리스를 자극하기 시작했다.
			ELSE
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:ARG%의 페니스에 음부를 문지르며 클리토리스를 자극하기 시작했다.
			ENDIF
		CASE 301
			IF ARG:1 == 3
				PRINTFORML 그대로 %조사처리(CALLNAME:TARGET,"는")% %조사처리(CALLNAME:ARG,"를")% 넘어뜨려, 애액이 흥건한 음부를 들이대고는
				PRINTFORML 격렬하게 허리를 흔들며 쾌락에 빠져들었다.
			ELSE
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:ARG%의 음부에 자신의 음부를 문지르며 쾌락을 탐냈다.
			ENDIF
		CASE 302
			IF ARG:1 == 3
				PRINTFORML 그대로 %조사처리(CALLNAME:TARGET,"는")% %조사처리(CALLNAME:ARG,"를")% 밀어 넘어뜨려 얼굴 위에 음부를 들이댔다.
			ELSE
				PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:ARG%의 얼굴에 음부를 들이대고 빨게 하하면서 자신은 유방을 비비며 자위에 탐냈다.
			ENDIF
	ENDSELECT
ENDIF

;실은 엿보기 자위로 필요
CALLF SET_NIGHT_KOJO(0, LOCAL:2)
CALL SELF_KOJO, TARGET, 480, LOCAL:2

;지문 컷
IF CFLAG:99 == 0 && ARG:1 == 3
	SELECTCASE LOCAL:2
		CASE 0
			PRINTFORMW 수그러질줄 모르는 성기를 음부에 비비며, 쾌락에 빠져들었다….
		CASE 1
			PRINTFORMW 수그러질줄 모르는 성기로 자궁을 밀어올리며, 질내를 탐욕스럽게 비벼댔다….
		CASE 2
			PRINTFORMW 수그러질줄 모르는 성기로 고혈을 범하며, 장내를 탐욕스럽게 자극했다….
		CASE 3
			PRINTFORMW 수그러질줄 모르는 성기를 유방으로 감싸, 쾌락에 빠져들었다….
		CASE 4
			PRINTFORMW 수그러질줄 모르는 성기로 입안을 범하며, 쾌락에 빠져들었다….
		CASE 19
			PRINTFORMW 오로지 스스로 몸을 만지작거리며, 쾌락에 빠져들었다….
		CASE 100
			PRINTFORMW 굶주린 짐승처럼 침을 흘리며 성기를 집어삼키며, 자궁의 갈증을 달랬다….
		CASE 200
			PRINTFORMW 집요하게 허리를 흔들어대며, 쾌락에 몰두했다….
		CASE 300
			PRINTFORMW 넘치는 꿀물을 윤활제 삼아 쾌락을 계속 탐했다….
		CASE 301
			PRINTFORMW 홀린듯이 허리를 흔들어대며, 난폭하게 자신의 유방을 비벼댔다….
		CASE 302
			PRINTFORMW 애액이 흥건한 자신의 음부를 %CALLNAME:(GET_CHARA_M())%의 입에 꽉 눌러, 교성을 지르며 자위에 빠져들었다….
	ENDSELECT
ENDIF

;처리 호출
CALLFORM NIGHT_SEX_{LOCAL:2}, ARG, LOCAL

IF ARG:1 == 3
	;체격에 의한 얼싸안게 했다(? )(을)를 실시한다.
	LOCAL:3 = 6
	LOCAL:4 = 6

	;남자는 큰
	SIF TALENT:ARG:120
		LOCAL:3 += 1
	SIF TALENT:120
		LOCAL:4 += 1
	;작은 몸집 체형의 유무
	SIF TALENT:ARG:128
		LOCAL:3 -= 2
	SIF TALENT:128
		LOCAL:4 -= 2
	;소인 체형의 유무
	SIF TALENT:ARG:127
		LOCAL:3 -= 4
	SIF TALENT:127
		LOCAL:4 -= 4
	;거구의 유무
	SIF TALENT:ARG:129
		LOCAL:3 += 4
	SIF TALENT:129
		LOCAL:4 -= 4

	;지문 컷
	IF CFLAG:99 == 0
		PRINTFORM {LOCAL}회의 행위 후, 
		IF LOCAL:3 > LOCAL:4
			PRINTFORML %조사처리(CALLNAME:ARG,"는")% %조사처리(CALLNAME:TARGET,"를")% 꼭 껴안으면서 잠이 들었다….
		ELSEIF LOCAL:3 == LOCAL:4
			PRINTFORML %조사처리(CALLNAME:ARG,"와")% %조사처리(CALLNAME:TARGET,"는")% 얼싸안으면서 잠이 들었다….
		ELSE
			PRINTFORML %조사처리(CALLNAME:ARG,"는")% %CALLNAME:TARGET%의 팔에 안겨 잠이 들었다….
		ENDIF
	ENDIF
ELSE
	;지문 컷
	SIF CFLAG:99 == 0
	PRINTFORML {LOCAL}회의 행위를 두 명은 밤 늦게까지 행했다…….
ENDIF

;-----------------------------------------------
;노예가 거기까지 의욕이 아닐 때
;-----------------------------------------------
@NIGHT_SEX02, ARG, ARG:1

LOCAL = MAX(ARG:1, 3)

IF 50 > GET_AGG(TARGET) && (TALENT:150 == 0 && TALENT:170 == 0)
	;지문 컷
	IF CFLAG:99 == 0
		IF TALENT:120 && TALENT:ARG:120
			PRINTFORML %조사처리(CALLNAME:MASTER,"가")% 잠자리에 들려던 차에, 돌연 %조사처리(CALLNAME:TARGET,"가")% 방으로 찾아왔다.
			PRINTFORML 그대로 그는 %조사처리(CALLNAME:MASTER,"를")% 넘어뜨려, 격렬하게 욕망을 풀어내려고 했다.
			PRINTFORMW %CALLNAME:MASTER%도 그 마음에 응하기로 했다….
		ELSE
			PRINTFORML 밤, 돌연 %조사처리(CALLNAME:TARGET,"가")% 방에 들어와서는, %조사처리(CALLNAME:ARG,"를")% 침대에 밀어 넘어뜨렸다.
			IF TALENT:120
				PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 싫은 소리를 하면서도, 페니스는 쾌락을 기대하고 발기했다.
			ELSE
				PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 싫은 소리를 하면서도 %조사처리(CALLNAME:MASTER,"가")% 국부를 문지르자 신음소리를 냈다.
			ENDIF
		ENDIF
	ENDIF
	CALLF SET_NIGHT_KOJO(0, 1)
	CALL SELF_KOJO, TARGET, 405
ELSE
	;지문 컷
	IF CFLAG:99 == 0
		IF TALENT:120 && TALENT:ARG:120
			PRINTFORML %조사처리(CALLNAME:MASTER,"가")% 잠자리에 들려던 차에, 돌연 %조사처리(CALLNAME:TARGET,"가")% 방으로 찾아왔다.
			PRINTFORML 그대로 그는 %조사처리(CALLNAME:MASTER,"를")% 넘어뜨려, 격렬하게 욕망을 풀어내려고 했다.
			PRINTFORMW %CALLNAME:MASTER%도 그 마음에 응하기로 했다….
		ELSE
			PRINTFORML 밤이 깊어졌을 무렵, 부끄러워 하는 %조사처리(CALLNAME:TARGET,"가")% %CALLNAME:ARG%의 방을 방문했다.
			PRINTFORM %조사처리(CALLNAME:ARG,"는")% 아무 말도 하지 않고 %조사처리(CALLNAME:TARGET,"를")% 불러서 
			IF TALENT:120 || TALENT:121
				PRINTFORML 완전히 발기한 페니스를 즐거운 듯이 바라본다.
			ELSE
				IF TALENT:128
					PRINTFORM 벌써 뜨거워진 소녀의 부드러운 
				ELSEIF TALENT:112 || TALENT:113
					PRINTFORM 욕망이 가득찬 풍부한 여자의 
				ELSE
					PRINTFORM 이미 젖기 시작한 그녀의 
				ENDIF
				PRINTFORMW 몸을 애무하기 시작했다.
			ENDIF
		ENDIF
	ENDIF
	CALLF SET_NIGHT_KOJO(0, 2)
	CALL SELF_KOJO, TARGET, 405
ENDIF

;우선 노예의 행동 방침을 결정한다
LOCAL:1 = 0

IF RAND:4 == 0 && TALENT:120 == 0 && TALENT:121 == 0
	LOCAL:1 = 3
ELSEIF RAND:3 == 0 && (TALENT:120 || TALENT:121)
	LOCAL:1 = 0
ELSEIF RAND:2 == 0 && ABL:2 > 1
	LOCAL:1 = 2
ELSEIF TALENT:120 == 0 && TALENT:0== 0 && ABL:1 > 1
	LOCAL:1 = 1
ELSEIF (TALENT:120 || TALENT:121)
	LOCAL:1 = 0
ELSEIF ABL:2 > 1
	LOCAL:1 = 2
ELSE
	LOCAL:1 = 3
ENDIF

LOCAL:2 = 0

;장대의 경우
IF LOCAL:1 == 0
	;자위 당한다
	IF RAND:8 == 0
		LOCAL:2 = 19
	;자위 받는다
	ELSEIF RAND:7 == 0
		LOCAL:2 = 15
	;페라받는다
	ELSEIF RAND:6 == 0 && EXP:ARG:22 > 0 && (TALENT:ARG:54 == 0 || FLAG:14 & 16)
		LOCAL:2 = 14
	;가랑이 받는다
	ELSEIF RAND:5 == 0 && TALENT:ARG:120 == 0 && TALENT:ARG:121 == 0 && (CFLAG:ARG:42 & 2) == 0
		LOCAL:2 = 10
	;파이즈리 받는다
	ELSEIF RAND:4 == 0 && TALENT:ARG:120 == 0 && EXP:ARG:22 > 0
		LOCAL:2 = 13
	;어널 섹스 시켜 준다
	ELSEIF RAND:3 == 0 && ((ARG == MASTER && EXP:ARG:1 > 12) || ABL:ARG:2 > 1) && (CFLAG:ARG:42 & 2) == 0
		LOCAL:2 = 12
	;섹스 시켜 준다
	ELSEIF TALENT:ARG:120 == 0 && TALENT:ARG:0 == 0 && ((ARG == MASTER && EXP:ARG:0 > 12) || ABL:ARG:1 > 1) && (CFLAG:ARG:42 & 2) == 0
		LOCAL:2 = 11
	;재차 어널 섹스 시켜 준다
	ELSEIF RAND:3 == 0 && ((ARG == MASTER && EXP:ARG:1 > 12) || ABL:ARG:2 > 1) && (CFLAG:ARG:42 & 2) == 0
		LOCAL:2 = 12
	;재차 가랑이 받는다
	ELSEIF RAND:5 == 0 && TALENT:ARG:120 == 0 && TALENT:ARG:121 == 0 && (CFLAG:ARG:42 & 2) == 0
		LOCAL:2 = 10
	;재차 파이즈리 받는다
	ELSEIF RAND:4 == 0 && TALENT:ARG:120 == 0
		LOCAL:2 = 13
	;아무것도 없으면 페라받는다
	ELSEIF RAND:3 == 0 && EXP:ARG:22 > 0 && (TALENT:ARG:54 == 0 || FLAG:14 & 16)
		LOCAL:2 = 14
	;재차 자위
	ELSEIF RAND:2 == 0
		LOCAL:2 = 15
	;자위 당한다
	ELSE
		LOCAL:2 = 19
	ENDIF
;질의 경우
ELSEIF LOCAL:1 == 1
	;넣어 받는다
	IF RAND:2 == 0 && (TALENT:ARG:120 || TALENT:ARG:121) && (CFLAG:ARG:42 & 2) == 0
		LOCAL:2 = 110
	;손가락이라든지 바이브라든지
	ELSEIF RAND:2 == 0
		LOCAL:2 = 115
	;자위 당한다
	ELSE
		LOCAL:2 = 119
	ENDIF
;어널의 경우
ELSEIF LOCAL:1 == 2
	;넣어 받는다
	IF RAND:2 == 0 && (TALENT:ARG:120 || TALENT:ARG:121) && (CFLAG:ARG:42 & 2) == 0
		LOCAL:2 = 210
	;손가락이라든지 바이브라든지
	ELSEIF RAND:2 == 0
		LOCAL:2 = 215
	;자위 당한다
	ELSE
		LOCAL:2 = 219
	ENDIF
ELSE
	;상호 애무
	IF RAND:3 == 0 && (CFLAG:ARG:42 & 2) == 0
		LOCAL:2 = 310
	;애무
	ELSEIF RAND:2 == 0
		LOCAL:2 = 311
	;자위 당한다
	ELSE
		LOCAL:2 = 319
	ENDIF
ENDIF
;지문 컷
IF CFLAG:99 == 0
	SELECTCASE LOCAL:2
		CASE 10
			PRINTFORML %조사처리(CALLNAME:ARG,"는")% %CALLNAME:TARGET%의 성기에 음부를 겹치고는, 격렬하게 허리를 흔들었다.
		CASE 11
			PRINTFORML %조사처리(CALLNAME:ARG,"는")% %CALLNAME:TARGET%의 다리를 억지로 벌려, 꿀단지에 성기를 밀어넣었다.
		CASE 12
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %조사처리(CALLNAME:ARG,"를")% 엎드리게 하고는, 그 고형에 성기를 밀어넣었다.
		CASE 13
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:ARG%의 위에 올라타서는, 유방으로 성기를 잡아 마구 비비기 시작했다.
		CASE 14
			PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:ARG%의 입에 성기를 밀어넣어, 머리를 누르며 격렬하게 허리를 흔들었다.
		CASE 15
			PRINTFORML %조사처리(CALLNAME:ARG,"가")% 페니스를 손으로 만져주자, %조사처리(CALLNAME:TARGET,"는")% 열중한 것처럼 숨을 내쉬었다.
		CASE 19
			PRINTFORML %조사처리(CALLNAME:ARG,"는")% 심술궂은 미소를 띠면서, %CALLNAME:TARGET%에게 스스로 하도록 명령했다.
		CASE 110
			PRINTFORML %조사처리(CALLNAME:ARG,"는")%, %CALLNAME:TARGET%의 페니스를 꿀단지로 집어 삼키고는, 스스로 허리를 흔들었다.
		CASE 115
			PRINTFORML %조사처리(CALLNAME:ARG,"가")% %CALLNAME:TARGET%의 꿀 단지에 손가락을 넣어 긁어주자, %조사처리(CALLNAME:TARGET,"는")% 기쁜듯이 교성을 울렸다.
		CASE 119
			PRINTFORML %조사처리(CALLNAME:ARG,"는")% 심술궂은 미소를 띠며, %CALLNAME:TARGET%에게 스스로 하도록 명령했다.
		CASE 210
			PRINTFORML %조사처리(CALLNAME:ARG,"는")% %조사처리(CALLNAME:TARGET,"가")% 넙죽 엎드린 상태가 되자 고혈에 삽입 준비를 하기 시작했다.
		CASE 215
			PRINTFORML %조사처리(CALLNAME:ARG,"가")% 손가락을 고혈에 넣어주자, %조사처리(CALLNAME:TARGET,"는")% 참지 못하고 교성을 울렸다.
		CASE 219
			PRINTFORML %조사처리(CALLNAME:ARG,"는")% 심술궂은 미소를 띠며, %CALLNAME:TARGET%에게 스스로 하도록 명령했다.
		CASE 310
			PRINTFORML %조사처리(CALLNAME:ARG,"와")% %조사처리(CALLNAME:TARGET,"는")% 서로의 몸을 만지작거리며, 서로 위로했다….
		CASE 311
			PRINTFORML %CALLNAME:ARG%의 손가락이 민감한 부분에 닿자, %조사처리(CALLNAME:TARGET,"는")% 몸을 비틀면서 허덕였다.
		CASE 319
			PRINTFORML %조사처리(CALLNAME:ARG,"는")% 심술궂은 미소를 띄며, %CALLNAME:TARGET%에게 스스로 하도록 명령했다.
	ENDSELECT
	PRINTW
ENDIF

;실은 엿보기 자위로 필요
CALLF SET_NIGHT_KOJO(0, LOCAL:2)
CALL SELF_KOJO, TARGET, 481, LOCAL:2

;처리 호출
CALLFORM NIGHT_SEX_{LOCAL:2}, ARG, LOCAL
;지문 컷
SIF CFLAG:99 == 0
	PRINTFORML 두 명은, {LOCAL}회의 행위를 밤 늦게까지 계속하는 것이었다…….

;-----------------------------------------------
;노예가 처녀를 바치는 이벤트( 구NIGHT_EVENT_4)
;-----------------------------------------------
@NIGHT_SEX_SHOJO, ARG, ARG:1
;지문 컷
IF CFLAG:99 == 0
	PRINTL 
	PRINTFORMW 갑자기 %CALLNAME:MASTER%의 빈틈을 노리고 들어오더니, %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:MASTER% 위에 말타듯이 올라탔다.
	PRINTFORML %CALLNAME:MASTER%의 물건을 잡아당기며 피를 몰리게 하고 스스로 서서히 질에 넣기 시작하는 %CALLNAME:TARGET%에게 이제 미혹은 없다…….
ENDIF
CALLF SET_NIGHT_KOJO(0, 4)
CALL SELF_KOJO, TARGET, 405
PRINTL 
CALL SETCOLOR_TORIKOMODE, "RED", 3
PRINTL 【처녀 상실】
RESETCOLOR
;첫경험 기록
IF CFLAG:TARGET:500 == 0
	CFLAG:TARGET:500 = NO:MASTER + 1
	CSTR:TARGET:5 = %CALLNAME:MASTER%
ENDIF
;50이상 경험
SIF TALENT:0 == 1
	CALL COMMON_UP_EXP, TARGET, 50, 2, 0
;덥침으로 처녀를 바친 플래그
CFLAG:153 |= 2
CFLAG:MASTER:153 |= 8
;처녀 깔봐 회수 카운트
FLAG:400 += 1
TALENT:0 = 0
;00V경험
CALL COMMON_UP_EXP, TARGET, 0, 1, 0
;04정액 경험
CALL COMMON_UP_EXP, TARGET, 4, 1, 0
;42V조율 경험
CALL COMMON_UP_EXP, TARGET, 42, 1, 0
;01쾌V의 구슬
CALL COMMON_UP_JUEL, TARGET, 1, 10, 0
;12순종의 구슬
CALL COMMON_UP_JUEL, TARGET, 12, (ARG:1*700), 0
;16고통의 구슬
CALL COMMON_UP_JUEL, TARGET, 16, (ARG*300), 1
PRINTL 
;지문 컷
IF CFLAG:99 == 0
	PRINTFORMW 처녀의 자궁에 %CALLNAME:MASTER%의 반쪽 유전자로 채워지자, %조사처리(CALLNAME:TARGET,"는")% 단 숨소리를 낸다.
	PRINTFORMW 순결을 바칠 수 있어 %조사처리(CALLNAME:TARGET,"는")% 만족하게 미소지었다…….
ENDIF

;=====================================================
;
;노예에 의한 선의의 촉수간
;
;=====================================================
;-----------------------------------------------
;촉수간
;----------------------------------------------
@NIGHT_SHOKUSHU_SOFT, ARG

;행위수F:9의 결정. 기본치는 기교 LV＋촉수 사역 LV(노예의 촉수 사역 LV는 통상은 0)
LOCAL = ABL:12 + ABL:98
;촉수 조작방법을 가지는 경우+3
SIF TALENT:149
	LOCAL += 3
;최저수 3회
SIF LOCAL < 3
	LOCAL = 3
;폭주라면 36+RAND:36
SIF ARG == 0
	LOCAL = 36+RAND:36

SIF ARG == 1 && (CFLAG:42 & 2)
	ARG = 2

;지문 컷
IF CFLAG:99 == 0
	IF ARG == 0
		PRINTL …불길한 예감은 적중했던 모양이다….
		PRINTFORMW %조사처리(CALLNAME:TARGET,"가")% 소환한 촉수는 제어를 잃고 %조사처리(CALLNAME:MASTER,"과")% %조사처리(CALLNAME:TARGET,"를")% 덮쳐왔다.
		PRINTL 
		IF TALENT:MASTER:120 == 0 && TALENT:MASTER:0 > 0
			PRINTFORML 촉수는 막바로 %조사처리(CALLNAME:MASTER,"를")% 붙잡고선 엉덩이와 입에 촉수를 밀어넣고 유두를 비틀어댔다.
			PRINTFORML 촉수가 가져다주는 폭력적이라고까지 할 수 있을 쾌락에 잠시 몸을 맡기고 있던 %조사처리(CALLNAME:MASTER,"였")%지만
			PRINTW 질구에 닿는 촉수의 감촉에, 순간 창백해져 필사적으로 저항하기 시작했다.
			PRINTW 하지만, 손발을 묶은 촉수는 꿈틀거릴 뿐 풀 수가 없었고.결국 서서히, 서서히 촉수가 질내로 진입해 들어왔다….
			PRINTL  
			PRINTL 파과의 피를 애액, 그리고 정제모를 체액에 섞어 집요하게 질을 범하며, 자궁에 또아리를 튼 촉수.
			PRINTFORML 울면서 계속 사죄하는 %CALLNAME:TARGET%의 목소리를 멀게 들으면서
			PRINTFORMW %조사처리(CALLNAME:MASTER,"는")%, 자신을 계속해서 유린하는 그것을, 그저 망연히 바라보고 있었다….
		ELSEIF (CFLAG:42 & 2)
			PRINTFORML 촉수는 정조대 틈새로 가는 촉수를 침입시키더니 %CALLNAME:TARGET%의 음부를 집요하게 범하기 시작했다….
			PRINTFORMW 그 쾌락에 %조사처리(CALLNAME:TARGET,"가")% 신음을 토해내기까지, 그리 시간이 오래 걸리진 않았다….
		ENDIF
	ELSE
		PRINTL 자신에게 위해를 가하는 방향으로는 힘을 사용할 수 없도록, 한정적으로 봉인을 풀어 주자
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 뭔가 주문을 외우더니 촉수를 소환했다.
		PRINTFORML 이걸로 뭘 할 생각인지 묻는 %CALLNAME:MASTER%에게
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 완연한 미소를 짓고 촉수를 %CALLNAME:MASTER%에게 향했다.
		PRINTL  
		PRINTFORML 확실히 위해를 가하는 방향은 아닌가.
		SIF TALENT:MASTER:0 == 0 && TALENT:MASTER:120 == 0 && EXP:MASTER:0 > 0
			PRINTFORML 질내에서 자궁까지 밀려들어와 돌며, 민감한 부분을 교묘하게 자극하는 촉수.
		SIF EXP:MASTER:1 > 0
			PRINTL 장내에 가득 들어와, 배설물을 탐닉하는 촉수
		SIF TALENT:MASTER:120 || TALENT:MASTER:121
			PRINTL 페니스에 휘감겨서, 사정을 재촉하는 촉수
		SIF TALENT:MASTER:120 == 0
			PRINTL 유방을 휘감고서는 쥐어짜며, 유두를 달콤하게 죄어오는 촉수
		PRINTL 입 안을 점유하고, 혀와 치열을 계속해서 애무하는 촉수
		PRINTL 등과 옆구리, 넓적다리와 겨드랑이에 이르는 모든 성감대를 찾아다니는 촉수
		PRINTL 사지를 부드럽게 속박해서, 자유를 빼앗으며 손가락 사이를 절묘하게 비벼대는 촉수
		PRINTFORML 그런 촉수들이 가져다주는 쾌락에 취하면서, 때로는 이렇게 빠져보는 것도 나쁘지 않다고 말하는 %CALLNAME:MASTER%에게
		PRINTFORMW %조사처리(CALLNAME:TARGET,"는")% 만면의 미소로 대답했다….
	ENDIF
	PRINTL 
ENDIF
;노예에 의한 선의의 촉수간 이벤트 대사 호출
CALL SELF_KOJO, TARGET, 417, ARG

REPEAT 2
	LOCAL:5 = 2
	IF COUNT == 0
		LOCAL:1 = MASTER
		LOCAL:2 = TARGET
	ELSE
		LOCAL:1 = TARGET
		LOCAL:2 = MASTER
		SIF ARG == 2
			LOCAL:5 -= 1
	ENDIF
	
	;경험 취득 처리
	;00V, 01 A, 02 절정, 03 사정, 04 정액, 22 페라, 31 긴박 ,
	;40약물, 43 A쾌락, 44 B쾌락, 50 이상 경험, 53 촉수 경험
	;00V경험
	IF TALENT:(LOCAL:1):120 == 0 && ((TALENT:(LOCAL:1):0 == 0 && ((LOCAL:1 == MASTER && EXP:(LOCAL:1):0 > 0) || (LOCAL:1 != MASTER && EXP:(LOCAL:1):0 > 0 && ARG != 2))) || ARG == 0) 
		LOCAL:5 += 1
		IF TALENT:(LOCAL:1):0
			CALL SETCOLOR_TORIKOMODE, "RED", 3
			PRINTFORML 【처녀 상실】(%CALLNAME:(LOCAL:1)%)
			RESETCOLOR
			;첫경험 기록
			IF CFLAG:(LOCAL:1):500 == 0
				CFLAG:(LOCAL:1):500 = 1003
				CSTR:(LOCAL:1) = 촉수
			ENDIF
			TALENT:(LOCAL:1):0 = 0
		ENDIF
		CALL COMMON_UP_EXP, LOCAL:1, 0, LOCAL, 0
	ENDIF
	;01A경험
	IF (LOCAL:1 == MASTER && EXP:(LOCAL:1):1 > 0) || (LOCAL:1 != MASTER && ABL:(LOCAL:1):2 > 1 && ARG != 2) || ARG == 0
		LOCAL:5 += 1
		CALL COMMON_UP_EXP, LOCAL:1, 1, LOCAL, 0
	ENDIF
	;02절정 경험
	CALL COMMON_UP_EXP, LOCAL:1, 2, (LOCAL*LOCAL:5)/2, 0
	;03사정 경험
	SIF (TALENT:(LOCAL:1):120 || TALENT:(LOCAL:1):121) && (CFLAG:(LOCAL:1):42 & 2) == 0
		CALL COMMON_UP_EXP, LOCAL:1, 3, LOCAL/2, 0
	;04정액 경험
	CALL COMMON_UP_EXP, LOCAL:1, 4, (LOCAL*(LOCAL:5+2))/2, 0
	;22페라 경험
	CALL COMMON_UP_EXP, LOCAL:1, 22, LOCAL, 0
	;31긴박 경험
	CALL COMMON_UP_EXP, LOCAL:1, 31, 2*LOCAL, 0
	;40약물 경험
	SIF TALENT:(LOCAL:1):44 == 0
		CALL COMMON_UP_EXP, LOCAL:1, 40, LOCAL/6, 0
	;43A쾌락 경험
	SIF (LOCAL:1 == MASTER && EXP:(LOCAL:1):1 > 0) || (LOCAL:1 != MASTER && ABL:(LOCAL:1):2 > 1 && ARG != 2) || ARG == 0
		CALL COMMON_UP_EXP, LOCAL:1, 43, LOCAL*ABL:(LOCAL:1):2, 0
	;44B쾌락 경험
	CALL COMMON_UP_EXP, LOCAL:1, 44, LOCAL*ABL:(LOCAL:1):3, 0
	;50이상 경험
	LOCAL:6 = MAX(LOCAL/6, 1)
	CALL COMMON_UP_EXP, LOCAL:1, 50, LOCAL:6, 0
	;53촉수 경험
	CALL COMMON_UP_EXP, LOCAL:1, 53, LOCAL*(LOCAL:5+3), 0
	
	;주인 능력 상승 처리는 폐지
	IF LOCAL:1 == MASTER
	;노예의 능력과 구슬 취득 처리
	ELSE
		;제어 성공이라면 촉수 조작 취득. 확률로 촉수 사역 Lvp
		IF ARG > 0 && TALENT:149 == 0
			TALENT:149 = 1
			PRINTFORML %조사처리(CALLNAME:(LOCAL:1),"는")% 촉수제어에 성공하여, %TALENTNAME:149%을 습득했다.
		ELSEIF ARG > 0 && RAND:10 < 1 && ABL:(LOCAL:1):12 > ABL:(LOCAL:1):98
			ABL:(LOCAL:1):98 += 1
			PRINTFORML %조사처리(CALLNAME:(LOCAL:1),"는")% 촉수를 능숙하게 다루는게 가능해져서, %ABLNAME:98%이 1 상승했다.
		ENDIF
		;구슬은 00 C, 01 V, 02 A, 03 B, 11 습득, 12 순종, 13 욕망, 17 공포, 32 선도
		LOCAL:6 = (ABL:12+ABL:98)/2
		;00쾌C
		SIF LOCAL:1 == MASTER || (CFLAG:(LOCAL:1):42 & 2) == 0
			CALL COMMON_UP_JUEL, LOCAL:1, 0, 40*LOCAL*(1+ABL:(LOCAL:1):0)*(1+LOCAL:6), 0
		;01쾌V
		SIF TALENT:(LOCAL:1):120 == 0 && ((TALENT:(LOCAL:1):0 == 0 && ((LOCAL:1 == MASTER && EXP:(LOCAL:1):0 > 0) || (LOCAL:1 != MASTER && EXP:(LOCAL:1):0 > 0 && ARG != 2))) || ARG == 0) 
			CALL COMMON_UP_JUEL, LOCAL:1, 1, 40*LOCAL*(1+ABL:(LOCAL:1):1)*(1+LOCAL:6), 0
		;02쾌A
		SIF (LOCAL:1 == MASTER && EXP:(LOCAL:1):1 > 0) || (LOCAL:1 != MASTER && ABL:(LOCAL:1):2 > 1 && ARG != 2) || ARG == 0
			CALL COMMON_UP_JUEL, LOCAL:1, 2, 40*LOCAL*(1+ABL:(LOCAL:1):2)*(1+LOCAL:6), 0
		;03쾌B
		IF TALENT:(LOCAL:1):120 == 0
			CALL COMMON_UP_JUEL, LOCAL:1, 3, 40*LOCAL*(1+ABL:(LOCAL:1):3)*(1+LOCAL:6), 0
		ELSE
			CALL COMMON_UP_JUEL, LOCAL:1, 3, 20*LOCAL*(1+ABL:(LOCAL:1):3)*(1+LOCAL:6), 0
		ENDIF
		;11습득
		SIF ARG > 0
			CALL COMMON_UP_JUEL, LOCAL:1, 11, 80*LOCAL*(1+LOCAL:6), 0
		;12순종
		SIF ARG > 0
			CALL COMMON_UP_JUEL, LOCAL:1, 12, 20*LOCAL*(1+ABL:(LOCAL:1):10)*(1+ABL:(LOCAL:1):13), 0
		;13욕망
		CALL COMMON_UP_JUEL, LOCAL:1, 13, 15*LOCAL*(1+ABL:(LOCAL:1):11)*(1+LOCAL:6), 0
		;17공포
		SIF ARG == 0
			CALL COMMON_UP_JUEL, LOCAL:1, 17, 100*LOCAL, 0
		;31침식
		CALL COMMON_UP_JUEL, LOCAL:1, 31, 50*LOCAL*(1+ABL:(LOCAL:1):35), 0
		;32선도
		SIF ARG > 0
			CALL COMMON_UP_JUEL, LOCAL:1, 32, 30*LOCAL*(1+LOCAL:6), 0
	ENDIF
	PRINTW 　
REND

;지문 컷
IF CFLAG:99 == 0
	IF ARG == 0
		PRINTL 주입된 마력이 다한 것일까.
		PRINTFORML %조사처리(CALLNAME:MASTER,"과")% %조사처리(CALLNAME:TARGET,"를")% 범하며, 정체 불명의 액체를 계속 토해내던 촉수도
		PRINTL 날이 밝을 무렵에는 소멸해 버리고
		PRINTFORML 그 뒤엔 아연히 체액의 바다에 널브러져 있는 %조사처리(CALLNAME:MASTER,"과")% %CALLNAME:TARGET%만이 남아있었다….
	ELSE
		PRINTFORML 이윽고 %CALLNAME:TARGET%의 마력이 다했는지 촉수는 송환되었다.
		PRINTFORML 상상 이상의 쾌락에 심신 공히 피로에 찌든 %CALLNAME:MASTER%에게 무릎베개를 해주며
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% 다음엔 어떤 식으로 기쁘게 해줄까를 생각하고 있었다….
	ENDIF
ENDIF

;=====================================================
;
;조교 주인이는인 체해졌을 경우의 야행동
;
;=====================================================
;-----------------------------------------------
;습격 레즈비언 플레이
;-----------------------------------------------
@NIGHT_EVENT_8, ARG, ARG:1, ARG:2, ARG:3
;레즈비언 플레이이므로 어느 쪽인지가 남자의 경우는 여기서 연주한다
;조건으로 연주했다
;어느 쪽인지가 정조대 장착 중에는 불가
;전제로 연주했다

;지문 컷
IF CFLAG:99 == 0
	PRINTFORML 그날 밤, 상대할 사람이 강하게 필요해진 %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:ARG%의 방에 들이닥쳤다
	IF ABL:ARG:22 >= 2
		PRINTFORMW %조사처리(CALLNAME:ARG,"는")% %조사처리(CALLNAME:TARGET,"를")% 맞이하곤 침대로 유혹해, 서로의 뜨거운 몸을 겹쳤다……
	ELSE
		PRINTFORMW 당혹해 하는 %조사처리(CALLNAME:ARG,"를")% 끌어안고는, %조사처리(CALLNAME:TARGET,"는")% 욕정한 암컷의 몸을 덮쳐 눌렀다……
	ENDIF
ENDIF
LOCAL = 0

IF (TALENT:(ARG:1):120 || TALENT:(ARG:1):121) && TALENT:ARG:0 == 0 && EXP:ARG:0 >= 1 && RAND:3 == 0
	LOCAL = 1
ELSEIF (TALENT:ARG:120 || TALENT:ARG:121) && TALENT:(ARG:1):0 == 0 && EXP:(ARG:1):0 >= 1 && RAND:2 == 0
	LOCAL = 2
ENDIF
;습격 레즈비언 플레이의 이벤트 말, 407이 습격측, 413이 피습격측
CALL SELF_KOJO, TARGET, 407, 0
;습격측의 말로 TFLAG:99를 세우지 않으면, 피습격측의 말을 호출한다
SIF TFLAG:99 == 0
	CALL SELF_KOJO, ARG, 413
PRINTL 
;12:12
CALL COMMON_UP_EXP, TARGET, 12, (ARG:2/2)+2, 0
CALL COMMON_UP_EXP, ARG, 12, (ARG:2/2)+2, 0
;12:12의 구슬
CALL COMMON_UP_JUEL, TARGET, 12, ARG:3*150, 0
;13:욕정의 구슬
CALL COMMON_UP_JUEL, TARGET, 13, ARG:2*250, 0
;11:습득의 구슬
CALL COMMON_UP_JUEL, TARGET, 11, ARG:2*100, 0
IF ABL:ARG:22 >= 2
	;12:12의 구슬
	CALL COMMON_UP_JUEL, ARG, 12, ARG:3*100, 0
	;13:욕정의 구슬
	CALL COMMON_UP_JUEL, ARG, 13, ARG:2*200, 0
	;11:습득의 구슬
	CALL COMMON_UP_JUEL, ARG, 11, ARG:2*70, 0
ELSE
	;12:12의 구슬
	CALL COMMON_UP_JUEL, ARG, 12, ARG:3*40, 0
	;13:욕정의 구슬
	CALL COMMON_UP_JUEL, ARG, 13, ARG:2*160, 0
ENDIF
;물건이 붙어 있어, 넣을 수 있는 측이 처녀는 아니고 V경험 1이상이라면 사정 경험, 및 V경험＋
IF LOCAL == 1
	;03:사정 경험
	CALL COMMON_UP_EXP, TARGET, 3, (ARG:2/2), 0
	;LUNATIC 이상은 사정할 때마다 체력이 감소
	CALL EXHAUST_EJAC, TARGET, ARG:2 / 2
	;00:쾌C의 구슬
	CALL COMMON_UP_JUEL, TARGET, 0, ARG:2*(70*ABL:0*ABL:0), 0
	PRINTL 
	;00:V경험
	CALL COMMON_UP_EXP, ARG, 0, ARG:2, 0
	;04:정액 경험
	CALL COMMON_UP_EXP, ARG, 4, (ARG:2/2), 0
	;01:쾌V의 구슬
	CALL COMMON_UP_JUEL, ARG, 1, ARG:2*(80*ABL:ARG:1*ABL:ARG:1), 0
	;지문 컷
	SIF CFLAG:99 == 0
		PRINTFORML %CALLNAME:ARG%의 질내를 {ARG:2}번 범한 %조사처리(CALLNAME:TARGET,"는")% 만족한 듯이 방을 뒤로했다.
ELSEIF LOCAL == 2
	;00:V경험
	CALL COMMON_UP_EXP, ARG:1, 0, ARG:2, 0
	;04:정액 경험
	CALL COMMON_UP_EXP, ARG:1, 4, (ARG:2/2), 0
	;01:쾌V의 구슬
	CALL COMMON_UP_JUEL, ARG:1, 1, ARG:2*(80*ABL:(ARG:1) :1*ABL:(ARG:1):1), 0
	PRINTL 
	;03:사정 경험
	CALL COMMON_UP_EXP, ARG, 3, (ARG:2/2), 0
	;LUNATIC 이상은 사정할 때마다 체력이 감소
	CALL EXHAUST_EJAC, ARG, ARG:2/2
	;00:쾌C의 구슬
	CALL COMMON_UP_JUEL, ARG, 0, ARG:2*(70*ABL:ARG:0*ABL:ARG:0), 0
	;지문 컷
	SIF CFLAG:99 == 0
		PRINTFORML %조사처리(CALLNAME:ARG,"를")% 역강간해 {ARG:2/2}번 질내를 가득 채우더니, %조사처리(CALLNAME:TARGET,"는")% 만족한 듯 방을 뒤로했다.
ELSE
	;00:쾌C의 구슬
	CALL COMMON_UP_JUEL, TARGET, 0, ARG:2*100, 0
	;03:3의 구슬
	CALL COMMON_UP_JUEL, TARGET, 3, ARG:2*100, 0
	;00:쾌C의 구슬
	CALL COMMON_UP_JUEL, ARG, 0, ARG:2*80, 0
	;03:3의 구슬
	CALL COMMON_UP_JUEL, ARG, 3, ARG:2*80, 0
	;지문 컷
	SIF CFLAG:99 == 0
		PRINTFORML 그 밤, %조사처리(CALLNAME:ARG,"와")% {(ARG:2/2)+2}번 레즈 플레이를 한 %조사처리(CALLNAME:TARGET,"는")% 푹 잠들었다.
ENDIF

;-----------------------------------------------
;습격 BL플레이
;-----------------------------------------------
@NIGHT_EVENT_12, ARG, ARG:1, ARG:2
;BL플레이이므로 어느 쪽인지가 남자가 아닌 경우는 여기서 연주한다
;어느 쪽인지가 정조대 장착 중에는 불가
;조건으로 연주했다

;지문 컷
IF CFLAG:99 == 0
	PRINTFORML 그날 밤, 찰진 사람이 강하게 필요해진 %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:ARG%의 방에 들이닥쳤다.
	IF ABL:ARG:23 >= 2
		PRINTFORMW %조사처리(CALLNAME:ARG,"는")% %조사처리(CALLNAME:TARGET,"를")% 맞이하곤 침대로 유혹해, 서로의 뜨거운 몸을 겹쳤다…….
	ELSE
		PRINTFORMW 당혹해 하는 %조사처리(CALLNAME:ARG,"를")% 엎드리게 하곤, %조사처리(CALLNAME:TARGET,"는")% 욕정한 수컷의 몸을 덮쳐 눌렀다…….
	ENDIF
ENDIF
;습격 BL플레이의 이벤트 말, 414가 습격측, 415가 피습격측
CALL SELF_KOJO, TARGET, 414, 0
;습격측의 말로 TFLAG:99를 세우지 않으면, 피습격측의 말을 호출한다
SIF TFLAG:99 == 0
	CALL SELF_KOJO, ARG, 415
;13:BL경험
CALL COMMON_UP_EXP, TARGET, 13, (ARG:1/2)+2, 0
CALL COMMON_UP_EXP, ARG, 13, (ARG:1/2)+2, 0
;12:12의 구슬
CALL COMMON_UP_JUEL, TARGET, 12, ARG:2*150, 0
;13:욕정의 구슬
CALL COMMON_UP_JUEL, TARGET, 13, ARG:1*250, 0
;11:습득의 구슬
CALL COMMON_UP_JUEL, TARGET, 11, ARG:1*100, 0
IF ABL:ARG:23 >= 2
	;12:12의 구슬
	CALL COMMON_UP_JUEL, ARG, 12, ARG:2*100, 0
	;13:욕정의 구슬
	CALL COMMON_UP_JUEL, ARG, 13, ARG:1*200, 0
	;11:습득의 구슬
	CALL COMMON_UP_JUEL, ARG, 11, ARG:1*70, 0
ELSE
	;12:12의 구슬
	CALL COMMON_UP_JUEL, ARG, 12, ARG:2*40, 0
	;13:욕정의 구슬
	CALL COMMON_UP_JUEL, ARG, 13, ARG:1*160, 0
ENDIF
;00:쾌C의 구슬
CALL COMMON_UP_JUEL, TARGET, 0, ARG:1*100, 0
CALL COMMON_UP_JUEL, ARG, 0, ARG:1*80, 0
;지문 컷
SIF CFLAG:99 == 0
	PRINTFORML 그 밤, %조사처리(CALLNAME:ARG,"와")% {(ARG:1/2)+2}번 ＢＬ플레이를 하곤 %조사처리(CALLNAME:TARGET,"는")% 푹 잠들었다.

;-----------------------------------------------
;이성 사이
;-----------------------------------------------
@NIGHT_SEX_9, ARG, ARG:1
;지문 컷
IF CFLAG:99 == 0
	PRINTFORML 그날 밤, 몸이 불타고 있고 가라앉지 않아 %조사처리(CALLNAME:(ARG:1),"는")% %CALLNAME:ARG%의 방으로 들이닥쳤다.
	IF ABL:ARG:11 > 2
		PRINTFORMW %조사처리(CALLNAME:ARG,"는")% %조사처리(CALLNAME:(ARG:1),"를")% 맞이하곤 침대로 유혹해, 서로의 뜨거운 몸을 겹쳤다…….
	ELSE
		PRINTFORMW 당황하는 %조사처리(CALLNAME:ARG,"를")% 덮치고는 %조사처리(CALLNAME:TARGET,"는")% 욕정한 \@(TALENT:(ARG:1):120) ? 수컷 # 암컷 \@의 몸을 덮쳐 눌렀다…….
	ENDIF
	PRINTL 
ENDIF
;습격 이성 사이 플레이의 이벤트 말, 418이 습격측, 419가 피습격측
CALL SELF_KOJO, TARGET, 418, 0
;습격측의 말로 TFLAG:99를 세우지 않으면, 피습격측의 말을 호출한다
SIF TFLAG:99 == 0
	CALL SELF_KOJO, ARG, 419
;남자를 LOCAL:1에 여자를 LOCAL:2로 한다
IF TALENT:(ARG:1):120
	LOCAL:1 = ARG:1
	LOCAL:2 = ARG
ELSE
	LOCAL:1 = ARG
	LOCAL:2 = ARG:1
ENDIF

;행위수지정
LOCAL = MAX(ABL:(LOCAL:1) :12+ABL:(LOCAL:2):12, 4)

;LOCAL:2가 처녀로, LOCAL:1 LOCAL:2함께 반발 각인이 있는 경우. 확률1/3로 처녀 상실
IF RAND:3 == 0 && TALENT:(LOCAL:2):0 > 0 && MARK:(LOCAL:2):9 > 0 && MARK:(LOCAL:1):9 > 0
	LOCAL:5 = 0
;LOCAL:2에 막대기가 있어, LOCAL:1의 엉덩이를 사용할 수 있는 경우
ELSEIF RAND:4 == 0 && TALENT:(LOCAL:2):121 && ABL:(LOCAL:1):2 > 1
	LOCAL:5 = 1
;LOCAL:2의 엉덩이를 사용할 수 있는 경우
ELSEIF RAND:3 == 0 && ABL:(LOCAL:2):2 > 1
	LOCAL:5 = 2
;LOCAL:2의 질을 사용할 수 있는 경우
ELSEIF TALENT:(LOCAL:2):0 == 0 && EXP:(LOCAL:2):0 > 0
	LOCAL:5 = 3
;재차 LOCAL:2의 엉덩이 판정
ELSEIF ABL:(LOCAL:2):2 > 1
	LOCAL:5 = 2
;재차 LOCAL:1의 엉덩이 판정
ELSEIF TALENT:(LOCAL:2):121 && ABL:(LOCAL:1):2 > 1
	LOCAL:5 = 1
;서로의 구멍을 사용할 수 없는 경우
ELSE
	LOCAL:5 = 4
ENDIF

;지문 컷
IF CFLAG:99 == 0
	SELECTCASE LOCAL:5
		CASE 0
		PRINTFORML %조사처리(CALLNAME:(LOCAL:2),"는")% %조사처리(CALLNAME:(LOCAL:1),"를")% 침대에 눕히곤, 스스로 허리를 내려 %CALLNAME:(LOCAL:1)%의 페니스를 자신의 바기나로 이끌었다.
		PRINTFORMW %조사처리(CALLNAME:(LOCAL:1),"는")% 흘러 떨어지는 파과의 피에 동요했지만, 그것을 표할 틈도 없이 %조사처리(CALLNAME:(LOCAL:2),"는")% 고통을 참으며 계속해서 허리를 흔들었다….
		CASE 1
			PRINTFORML %조사처리(CALLNAME:(LOCAL:2),"는")% %CALLNAME:(LOCAL:1)% 앞에 개처럼 엎드리고는, 엉덩이 구멍에 자신의 애액을 바르고 한번에 꿰뚫게 했다.
			PRINTFORMW 그리고 엉덩이를 들락날락하는 페니스가 가져다주는 쾌락에 기뻐하는 남자를, 계속해서 공략했다….
		CASE 2
			PRINTFORML %조사처리(CALLNAME:(LOCAL:1),"는")% %조사처리(CALLNAME:(LOCAL:2),"를")% 개처럼 엎드리게 하곤, 엉덩이 구멍에 %CALLNAME:(LOCAL:2)%의 애액을 칠하고, 단숨에 꿰뚫었다.
			PRINTFORMW 그리고 엉덩이를 들락날락하는 페니스가 가져다주는 쾌락에 기뻐하는 여자를, 계속해서 공략했다….
		CASE 3
			PRINTFORML %조사처리(CALLNAME:(LOCAL:1),"는")% 빳빳해진 육봉을 잡아 %CALLNAME:(LOCAL:2)%의 꿀항아리를 계속해서 유린했다.
			PRINTFORMW 그리고 자궁을 밀어제치고 들어오는 육봉이 가져다주는 쾌락에 좋아하는 여자를, 끊임없이 공략했다….
		CASE 4
			IF TALENT:(LOCAL:2):121
				PRINTFORMW %조사처리(CALLNAME:(LOCAL:2),"와")% %조사처리(CALLNAME:(LOCAL:1),"는")% 서로의 페니스를 하루종일 핥고 빨았다….
			ELSE
				PRINTFORMW %조사처리(CALLNAME:(LOCAL:2),"와")% %조사처리(CALLNAME:(LOCAL:1),"는")% 밤새 얼싸안았다….
			ENDIF
		CASE 5
			PRINTFORML %조사처리(CALLNAME:(LOCAL:2),"는")% %조사처리(CALLNAME:(LOCAL:1),"를")% 침대에 눕히곤, 꿀을 가득 머금은 보지를 거기에 부볐다.
			PRINTFORMW 그리고 %조사처리(CALLNAME:(LOCAL:1),"가")% 사정하자, 그 정액도 윤활유 삼아 한층 더 격렬하게 봉사를 계속했다….
	ENDSELECT
	PRINTL 
ENDIF

REPEAT 2
	LOCAL:11 = (COUNT == 0) ? LOCAL:1 # LOCAL:2
	LOCAL:12 = (COUNT == 0) ? LOCAL:2 # LOCAL:1

	;경험의 취득
	;00V경험
	IF LOCAL:11 == LOCAL:2 && (LOCAL:5 == 0 || LOCAL:5 == 3)
		IF TALENT:(LOCAL:11):0 > 0
			CALL SETCOLOR_TORIKOMODE, "RED", 3
			PRINTFORML 【처녀 상실】(%CALLNAME:(LOCAL:11)%)
			RESETCOLOR
			;첫경험 기록
			IF CFLAG:(LOCAL:11):500 == 0
				CFLAG:(LOCAL:11):500 = NO:(LOCAL:12) + 1
				CSTR:(LOCAL:11) = %CALLNAME:(LOCAL:12)%
			ENDIF
			TALENT:(LOCAL:11):0 = 0
		ENDIF
		CALL COMMON_UP_EXP, LOCAL:11, 0, (LOCAL/2), 0
	ENDIF
	;01A경험
	SIF (LOCAL:11 == LOCAL:2 && LOCAL:5 == 2) ||(LOCAL:11 == LOCAL:1 && LOCAL:5 == 1)
		CALL COMMON_UP_EXP, LOCAL:11, 1, (LOCAL/2), 0
	;02절정 경험
	CALL COMMON_UP_EXP, LOCAL:11, 2, (LOCAL/4), 0
	;03사정 경험
	SIF TALENT:(LOCAL:11):120 || TALENT:(LOCAL:11):121
		CALL COMMON_UP_EXP, LOCAL:11, 3, (LOCAL/2), 0
	;04정액 경험
	SIF TALENT:(LOCAL:12):120 || TALENT:(LOCAL:12):121
		CALL COMMON_UP_EXP, LOCAL:11, 4, (LOCAL/2), 0
	;20봉사 쾌락 경험
	SIF TALENT:(LOCAL:12):120 || TALENT:(LOCAL:12):121
		CALL COMMON_UP_EXP, LOCAL:11, 20, (LOCAL*ABL:(LOCAL:11):13)/4, 0
	;22페라 경험
	IF TALENT:(LOCAL:12):120 || TALENT:(LOCAL:12):121
		IF LOCAL:5 == 4 && TALENT:(LOCAL:12):121
			CALL COMMON_UP_EXP, LOCAL:11, 22, (2*LOCAL)/3, 0
		ELSE
			CALL COMMON_UP_EXP, LOCAL:11, 22, (LOCAL/4), 0
		ENDIF
	ENDIF
	;23키스 경험
	CALL COMMON_UP_EXP, LOCAL:11, 23, (LOCAL/3), 0
	;43A쾌락 경험
	SIF (LOCAL:11 == LOCAL:2 && LOCAL:5 == 2) ||(LOCAL:11 == LOCAL:1 && LOCAL:5 == 1)
		CALL COMMON_UP_EXP, LOCAL:11, 43, (LOCAL*ABL:(LOCAL:11):2)/2, 0
	;44B쾌락 경험
	IF LOCAL:11 == LOCAL:2
		CALL COMMON_UP_EXP, LOCAL:11, 44, (LOCAL*ABL:(LOCAL:11):3)/2, 0
	ELSE
		CALL COMMON_UP_EXP, LOCAL:11, 44, (LOCAL*ABL:(LOCAL:11):3)/4, 0
	ENDIF
	
	;구슬 취득
	;00쾌C
	CALL COMMON_UP_JUEL, LOCAL:11, 0, 20*LOCAL*(1+ABL:(LOCAL:11):0)*(1+ABL:(LOCAL:12):12), 0
	;01쾌V
	SIF LOCAL:11 == LOCAL:2 && (LOCAL:5 == 0 || LOCAL:5 == 3)
		CALL COMMON_UP_JUEL, LOCAL:11, 1, 20*LOCAL*(1+ABL:(LOCAL:11):1)*(1+ABL:(LOCAL:12):12), 0
	;02쾌A
	SIF (LOCAL:11 == LOCAL:2 && LOCAL:5 == 2) ||(LOCAL:11 == LOCAL:1 && LOCAL:5 == 1)
		CALL COMMON_UP_JUEL, LOCAL:11, 2, 20*LOCAL*(1+ABL:(LOCAL:11):2)*(1+ABL:(LOCAL:12):12), 0
	;03쾌B
	IF LOCAL:11 == LOCAL:2
		CALL COMMON_UP_JUEL, LOCAL:11, 3, 20*LOCAL*(1+ABL:(LOCAL:11):3)*(1+ABL:(LOCAL:12):12), 0
	ELSE
		CALL COMMON_UP_JUEL, LOCAL:11, 3, 10*LOCAL*(1+ABL:(LOCAL:11):3)*(1+ABL:(LOCAL:12):12), 0
	ENDIF
	;11습득
	CALL COMMON_UP_JUEL, LOCAL:11, 11, 80*LOCAL*(1+ABL:(LOCAL:11):12), 0
	;13욕정
	CALL COMMON_UP_JUEL, LOCAL:11, 13, 15*LOCAL*(1+ABL:(LOCAL:11):11)*(1+ABL:(LOCAL:12):12), 0
	;32선도
	SIF (LOCAL:11 == LOCAL:2 && (LOCAL:5 == 0 || LOCAL:5 == 1)) || (LOCAL:11 == LOCAL:1 && (LOCAL:5 == 2 || LOCAL:5 == 3))
		CALL COMMON_UP_JUEL, LOCAL:11, 32, 30*LOCAL*(1+ABL:(LOCAL:11):12), 0
	PRINTW 　
REND

;지문 컷
SIF CFLAG:99 == 0
	PRINTFORML 새벽녘, %조사처리(CALLNAME:(ARG:1),"는")% 아쉬움을 남기면서도, %CALLNAME:ARG%의 방을 뒤로했다….

;=====================================================
;
;SM행위에 의한 야행동
;
;=====================================================
;-----------------------------------------------
;매저키스트 노예의 덥침( 구@NIGHT_EVENT_11)
;-----------------------------------------------
@NIGHT_EVENT_SM_M, ARG
;ARG에 두드리는 (분)편이 넣어져서 보내져 온다. (주인이나 사이가 좋은 노예이다)
;매저키스트 노예는 G라도 있으므로 말구운것은 이 근처도 이용한다

;채찍 지참의 경우
IF ITEM:32 || NOITEM
	;지문 컷
	IF CFLAG:99 == 0
		PRINTFORML 그날 밤, %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:ARG%의 방에 찾아와, 채찍으로 자신을 후려쳐달라고 애원했다.
		IF ABL:ARG:20 < 3
			PRINTFORML %조사처리(CALLNAME:ARG,"가")% 곤혹스러워 하면서도 몇 번 채찍으로 후려치자, %조사처리(CALLNAME:TARGET,"는")% 만족한 듯하다….
		ELSE
			PRINTFORML %조사처리(CALLNAME:ARG,"가")% 몇 번 채찍으로 후려치자, %조사처리(CALLNAME:TARGET,"는")% 진심으로 만족한 듯하다….
		ENDIF
	ENDIF
	CALLF SET_NIGHT_KOJO(0, 1)
	CALL SELF_KOJO, TARGET, 411
ELSE
;채찍이 없으면 스팬 킹
	;지문 컷
	IF CFLAG:99 == 0
		PRINTFORML 그날 밤, %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:ARG%의 방에 찾아와, 엉덩이를 때려달라고 애원했다.
		IF ABL:ARG:20 < 3
			PRINTFORML %조사처리(CALLNAME:ARG,"가")% 곤혹스러워 하면서도 몇 번 엉덩이를 손바닥으로 때리자, %조사처리(CALLNAME:TARGET,"는")% 만족한 듯하다….
		ELSE
			PRINTFORML %조사처리(CALLNAME:ARG,"가")% 몇 번 엉덩이를 때리자, %조사처리(CALLNAME:TARGET,"는")% 진심으로 만족한 듯하다….
		ENDIF
	ENDIF
	CALLF SET_NIGHT_KOJO(0, 2)
	CALL SELF_KOJO, TARGET, 411
ENDIF

CALL NIGHT_EVENT_SM_M_2, ARG

;-----------------------------------------------
;습격 SM플레이( 구@NIGHT_EVENT_9)
;-----------------------------------------------
@NIGHT_EVENT_SM_S, ARG
;ARG에 당하는 측이 들어와 보내져 온다

;상대가 매저키스트라면 보다 효과가 오른다
;지문 컷
IF CFLAG:99 == 0
	PRINTFORML 그날 밤, 상대할 사람이 강하게 필요해진 %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:ARG%의 방에 들이닥쳤다.
	IF ARG == MASTER
		PRINTFORML %조사처리(CALLNAME:ARG,"는")% 채찍을 든 %조사처리(CALLNAME:TARGET,"를")% 보고 당황했지만, 곧 몸속 깊은 곳에서 열기가 오르는 것을 느끼고
		PRINTFORML %조사처리(CALLNAME:TARGET,"를")% 받아들이더니, 차례차례 옷을 벗고 벌을 줄 것을 애원해왔다.
		PRINTFORMW 그 모습을 보고 정욕이 더욱 듫끓은 %조사처리(CALLNAME:TARGET,"는")% 만면에 미소를 띄며 채찍을 휘두르기 시작했다…….
	ELSEIF ABL:ARG:21 >= 2 || TALENT:ARG:82 || TALENT:ARG:178
		PRINTFORML %조사처리(CALLNAME:ARG,"는")% %조사처리(CALLNAME:TARGET,"를")% 맞이하더니 기쁜듯 옷을 모조리 벗어버렸다.
		PRINTFORMW 그 모습을 보고 정욕이 더욱 듫끓은 %조사처리(CALLNAME:TARGET,"는")% 만면에 미소를 띄며 채찍을 휘두르기 시작했다…….
	ELSE
		PRINTFORMW 곤혹스러워 하는 %CALLNAME:ARG%의 옷을 벗긴 뒤, %조사처리(CALLNAME:TARGET,"는")% 욕정이 이끄는 대로 채찍을 마구 휘둘렀다…….
	ENDIF
ENDIF
;습격 SM플레이의 이벤트 대사 호출 습격측(410) 피습격측(412)
CALL SELF_KOJO, TARGET, 410, 0
;습격측의 말로 TFLAG:99를 세우지 않으면, 피습격측의 말을 호출한다
SIF TFLAG:99 == 0 && ARG != MASTER
	CALL SELF_KOJO, ARG, 412
PRINTL 

CALL NIGHT_EVENT_SM_S_2, ARG
;지문 컷
SIF CFLAG:99 == 0
	PRINTFORML %조사처리(CALLNAME:ARG,"를")% 채찍으로 {RESULT}번 정도 후려친 뒤, %조사처리(CALLNAME:TARGET,"는")% 만족한 듯 방을 뒤로했다.
;=====================================================
;
;많은 사람으로 행해지는 성행위에 대해(난교, 연회)
;
;=====================================================
;-----------------------------------------------
;3 P( 구NIGHT_EVENT_6)
;-----------------------------------------------
@NIGHT_3P, ARG, ARG:1, ARG:2, ARG:3
;회수 결정하자 ARG 2-7 ARG:1 2-7로4-12회에 어떨까요. 14라도 좋은 생각도 들지만.
LOCAL = MIN(ARG+ARG:1, 12)

IF TALENT:(ARG:2):150 || TALENT:(ARG:2):170
	;지문 컷
	IF CFLAG:99 == 0
		IF TALENT:170 && TALENT:(ARG:2):170
			IF TALENT:120 == 0 && TALENT:121 == 0 && TALENT:(ARG:2):120 == 0 && TALENT:(ARG:2):121 == 0
				PRINTFORML 상기한 얼굴로 이미 애액을 흘리기 시작한 두 사람을, %조사처리(CALLNAME:MASTER,"는")% 급히 침대까지 데리고 갔다…….
			ELSEIF (TALENT:120 || TALENT:121) && (TALENT:(ARG:2):120 || TALENT:(ARG:2):121)
				PRINTFORML 다리 사이가 가득 부풀어, 눈이 시뻘개진 두 사람을 %조사처리(CALLNAME:MASTER,"는")% 급히 침대까지 데리고 갔다…….
			ELSE
				PRINTFORML 서로의 성기를 애무하며, 당장에라도 서로 섞일 것 같은 두 사람을 %조사처리(CALLNAME:MASTER,"는")% 급히 침대까지 데리고 갔다…….
			ENDIF
		ELSE
			PRINTFORML 볼을 붉히면서도 오늘 밤은 셋이서 같이 즐기고 싶다는 두 사람을, %조사처리(CALLNAME:MASTER,"는")% 침대까지 데리고 갔다…….
		ENDIF
	ENDIF
	LOCAL:1 = 1
	CALLF SET_NIGHT_KOJO(0, 1)
	;3 P의 이벤트 대사 호출
	CALL SELF_KOJO, TARGET, 406
	PRINTL 
ELSE
	;지문 컷
	IF CFLAG:99 == 0
		PRINTFORML 보아하니 %조사처리(CALLNAME:TARGET,"가")% %CALLNAME:(ARG:2)%에게도 성의 즐거움을 가르쳐 주었으면 좋겠다고 데려 온 것 같다.
		PRINTFORML %조사처리(CALLNAME:(ARG:2),"는")% 억지로 끌려왔기 때문에 불만스러워 보였지만, %조사처리(CALLNAME:MASTER,"는")% 두 사람을 맞아들였다.
	ENDIF
	LOCAL:1 = 2
ENDIF
;3 P였던 경우
IF LOCAL:1 == 1
	CALL NIGHT_STATUS_UP_3P, ARG:2, LOCAL
	;지문 컷
	SIF CFLAG:99 == 0
		PRINTFORMW 그날 밤은 %조사처리(CALLNAME:TARGET,"과")% %조사처리(CALLNAME:(ARG:2),"와")% 마음껏 3 P를 즐겼다.
;보고 있을 뿐(만큼)이었던 경우
ELSEIF LOCAL:1 == 2
	PRINTL 
	;지문 컷
	SIF CFLAG:99 == 0
		PRINTFORML %조사처리(CALLNAME:MASTER,"와")% %조사처리(CALLNAME:TARGET,"는")% %조사처리(CALLNAME:(ARG:2),"가")% 있는 건 잊었다는 듯 일심불란 섹스를 계속하고 있다…….
	TARGET = ARG:2
	;지문 컷
	IF CFLAG:99 == 0
		IF GET_AGG(TARGET) >= 50
			PRINTFORMW %조사처리(CALLNAME:(ARG:3),"가")% 치태로 어지럽혀진 모습을 %조사처리(CALLNAME:(ARG:2),"는")% 침을 꿀꺽 삼키며, 탐하듯이 바라보고 있다.
		ELSE
			PRINTFORMW 보통과는 전혀 다른 %CALLNAME:(ARG:3)%의 달콤한 교성에, %조사처리(CALLNAME:(ARG:2),"는")% 빨간 얼굴로 이쪽을 보고 있다.
		ENDIF
	ENDIF
	;3 P의 이벤트 대사 호출
	TARGET = ARG:3
	CALLF SET_NIGHT_KOJO(0, 2)
	CALL SELF_KOJO, TARGET, 406
	PRINTL 
	IF ABL:(ARG:2):11 >= 3
		;지문 컷
		IF CFLAG:99 == 0
			PRINTFORML ……
			PRINTFORML 이윽고 아무래도 참을 수 없게 되어버린 %조사처리(CALLNAME:(ARG:2),"는")%
			PRINTFORMW 불붙은 몸을 진정시켜 줬음 한다며, 눈물섞인 소리로 %CALLNAME:MASTER%에게 애원해왔다…….
		ENDIF
	ENDIF
	PRINTL 
	
	CALL NIGHT_STATUS_UP_2P, LOCAL
	
	PRINTFORML %CALLNAME:(ARG:2)%
	TARGET = ARG:2
	IF ABL:11 >= 3
		;00쾌C의 구슬
		CALL COMMON_UP_JUEL, TARGET, 0, ABL:0*100, 0
		;03쾌B의 구슬
		CALL COMMON_UP_JUEL, TARGET, 3, ABL:3*100, 0
	ENDIF
	;12순종의 구슬
	CALL COMMON_UP_JUEL, TARGET, 12, ARG:1*20, 0
	;13욕정의 구슬
	IF ABL:11 >= 3
		CALL COMMON_UP_JUEL, TARGET, 13, ARG*150, 0
	ELSE
		CALL COMMON_UP_JUEL, TARGET, 13, ARG*50, 0
	ENDIF
	;15치정의 구슬
	CALL COMMON_UP_JUEL, TARGET, 15, ARG*200, 0
	TARGET = ARG:3
	;지문 컷
	SIF CFLAG:99 == 0
		PRINTFORML %조사처리(CALLNAME:TARGET,"가")% %CALLNAME:MASTER%의 품 속에서 잠들자, %조사처리(CALLNAME:(ARG:2),"는")% 초췌해진 얼굴로 방을 뒤로했다.
ENDIF

;-----------------------------------------------
;신사들의 욕망, 많은 사람 PT 난교( 구NIGHT_EVENT_7)
;-----------------------------------------------
@NIGHT_RANKOU, ARG
;누군가가 정조대 장착 중에는 불가→조건으로 연주했으므로, k
;LOCAL=12. 이것은 나눗셈의 사정.
LOCAL = 12

;지문 컷
IF CFLAG:99 == 0
	PRINTL 
	PRINTFORML %조사처리(CALLNAME:MASTER,"가")% 옷을 벗으라고 하자, 네명은 앞을 다투듯이 옷을 벗어던지고는
	PRINTFORMW 누가 먼저라 할 거 없이 쾌락과 애정을 구해, %CALLNAME:MASTER%에게 몰려들어 자신들의 몸을 밀어붙이며 행위를 유혹해온다.
ENDIF
PRINTL 
;많은 사람 PT난교의 이벤트 대사 호출
REPEAT 4
	CALL SELF_KOJO, GET_CHARA_G(COUNT), 408, ARG
REND
PRINTL 

;지문 컷
IF CFLAG:99 == 0
	IF RAND:2 == 0
		PRINTFORML %조사처리(CALLNAME:MASTER,"는")% 네 사람의 몸의 차이를 즐기면서 그녀들과 가지가지 성행위를 즐겼다…….
	ELSEIF RAND:1 == 0
		PRINTFORML %조사처리(CALLNAME:MASTER,"는")% 네 명의 얼굴을 나란히 모아서 전원 자신의 성기에 봉사하도록 명했다…….
		PRINTFORMW 서로 경쟁하듯이 봉사가 시작되었고, 따뜻한 숨결과 요염한 혀놀림이 %조사처리(CALLNAME:MASTER,"를")% 흥분하게 만든다.
		IF TALENT:MASTER:120 || TALENT:MASTER:121
			PRINTFORML %조사처리(CALLNAME:MASTER,"가")% 기세 좋게 사정하니, 네 사람의 얼굴은 멍한 표정인 채로 하얗게 더럽혀져갔다.
		ELSE
			PRINTFORML %조사처리(CALLNAME:MASTER,"가")% 절정을 맞이해 애액을 뿜자, 네 사람은 서로의 얼굴에 묻은 애액을 핥기 시작했다.
		ENDIF
	ENDIF
	PRINTL 
	;남근의 수를 카운트 한다
	LOCAL:1 = 0
	SIF TALENT:MASTER:120 || TALENT:MASTER:121
		LOCAL:1 += 1
	SIF TALENT:(GET_CHARA_G(0)):120 || TALENT:(GET_CHARA_G(0)):121
		LOCAL:1 += 1
	SIF TALENT:(GET_CHARA_G(1)):120 || TALENT:(GET_CHARA_G(1)):121
		LOCAL:1 += 1
	SIF TALENT:(GET_CHARA_G(2)):120 || TALENT:(GET_CHARA_G(2)):121
		LOCAL:1 += 1
	SIF TALENT:(GET_CHARA_G(3)):120 || TALENT:(GET_CHARA_G(3)):121
		LOCAL:1 += 1
		
	;용 끝난 R를 이용해 남자의 수를 센다
	LOCAL:2 = 0
	SIF TALENT:MASTER:120
		LOCAL:2 += 1
	SIF TALENT:(GET_CHARA_G(0)):120
		LOCAL:2 += 1
	SIF TALENT:(GET_CHARA_G(1)):120
		LOCAL:2 += 1
	SIF TALENT:(GET_CHARA_G(2)):120
		LOCAL:2 += 1
	SIF TALENT:(GET_CHARA_G(3)):120
		LOCAL:2 += 1
	
	;전원 남자의 경우
	IF LOCAL:2 == 5
		PRINTFORML 그리고 남자들은 자신의 욕망을 그대로 내보이며 격렬하게 기차놀이를 시작했다…….
	;막대기 4개
	ELSEIF LOCAL:1 == 4
		;주인이 막대기 없음. 어쩐지 너무 노력한 것 같다
		IF TALENT:MASTER:120 == 0 && TALENT:MASTER:121 == 0
			;양구멍 사용가능
			IF RAND:2 == 0 && TALENT:MASTER:0 == 0 && EXP:MASTER:0 > 12 && EXP:MASTER:1 > 12
				IF RAND:2 == 0
					PRINTFORML %조사처리(CALLNAME:MASTER,"는")% 두 개의 육봉을 입에 물고 봉사해가면서
					PRINTFORMW 앞뒤의 구멍으로도 탐욕스레 육봉을 먹어치우고 있었다…….
				ELSE
					PRINTFORML %조사처리(CALLNAME:MASTER,"는")%, 자기가 먼저라며 주인의 질과 항문, 입에 육봉을 밀어대는 노예들을 보고 쓴웃음을 지으면서도
					PRINTFORML 충분히 그들의 육봉을 탐하며, 동시에 유두와 클리토리스도 만져달라며
					PRINTFORMW 전신으로 쾌락을 받아들이고 욕망대로 허리를 흔들어댔다…….
				ENDIF
			;V사용가능
			ELSEIF TALENT:MASTER:0 == 0 && EXP:MASTER:0 > 12
				IF RAND:2 == 0
					PRINTFORML %조사처리(CALLNAME:MASTER,"는")%, 가지가지 체위로 성교를 즐기면서 차례차례 노예들의 정을 탐했다.
					PRINTFORML 이미 질내도 자궁도 네 명의 노예들의 정액으로 가득 차 있었지만, 
					PRINTFORML %CALLNAME:MASTER%의 욕망을 채우기엔 아직 부족하다.
					PRINTFORML %조사처리(CALLNAME:MASTER,"는")%, 자신의 위에 올라타 있던 노예가 정액을 토하는 걸 끝내자 후딱 옆으로 밀어버리고
					PRINTFORMW 다음 노예의 육봉을 자신의 꿀항아리에 품었다.
				ELSE
					PRINTFORML %CALLNAME:MASTER%의 제안을 듣자, 노예들은 곤혹스러운 표정으로 서로를 마주보다가
					PRINTFORML 다음순간 뻘개진 눈으로, 차례차례 %CALLNAME:MASTER%에게 덮쳐들었다.
					PRINTFORML 단지 자신의 욕망이 이끄는 대로 여체를 탐하며, 정액을 토하는 노예들.
					PRINTFORMW 거친 행위 중에 배려조차 잊어버린 노예들에 둘러싸여, %조사처리(CALLNAME:MASTER,"는")% 지복의 한때를 보냈다.
				ENDIF
			;A는 사용가능 V는 안돼
			ELSEIF EXP:MASTER:1 > 12
				PRINTFORML %CALLNAME:MASTER%의 꿀항아리에 넣을 수 없다는 걸 처음엔 낙담하고 있던 노예들이었지만
				PRINTFORML 한사람, 또 한사람 엉덩이 구멍으로 정을 탐하다 보니, 어느새인가 앞다투어 자비를 바라는 듯한 얼굴이 되었다.
				PRINTFORML %조사처리(CALLNAME:MASTER,"는")% 애널 섹스에 매료된 노예들을 쓴웃음을 지으며 돌아보면서도
				PRINTFORMW 다음엔 누구의 정액을 탐해볼까 생각했다.
			;穴はダメ
			ELSE
				PRINTFORML %CALLNAME:MASTER%의 꿀항아리에 넣을 수 없다는 걸 처음엔 낙담하고 있던 노예들이었지만
				PRINTFORML %CALLNAME:MASTER%의 기교 앞에, 차례차례 정액을 토해냈다.
				PRINTFORML 새삼 격의 차이를 느꼈는지, 자비를 바란다는 듯한 노예들을 쓴웃음을 지으면서 바라보며
				PRINTFORMW %조사처리(CALLNAME:MASTER,"는")% 다음엔 누구의 정액을 탐해볼까 생각했다.
			ENDIF
		;주인막대기 있어
		ELSE
			IF RAND:2 == 0
				PRINTFORML %조사처리(CALLNAME:MASTER,"는")%, 한 마리의 암컷과 그 암컷에 육봉을 비벼대며 욕망을 풀려고 드는 세 명의 노예를 바라보며
				PRINTFORMW 만족스레 끄덕이더니 자신의 욕망도 풀기 위해 그 무리에 가세했다.
			ELSE
				PRINTFORML %조사처리(CALLNAME:MASTER,"가")% 암컷을 자위시키면서 우뚝 솟은 자기 물건을 입으로 애무하게 두니
				PRINTFORML 세 명의 노예가 충혈된 눈으로, 어떤 자는 암컷의 몸을 자신의 것으로 부벼대고, 어떤 자는 유두를 비틀어댔다.
				PRINTFORML 쾌락에 번민하면서도 필사적으로 %CALLNAME:MASTER%에게 봉사하는 암컷의 모습에 욕정한 %조사처리(CALLNAME:MASTER,"는")%
				PRINTFORMW 암컷의 머리를 꽉 붙잡은 다음, 그 부드러운 입을 목구멍 깊숙히까지 격렬히 범하기 시작했다.
			ENDIF
		ENDIF
	ELSEIF ARG == 1
		;주인막대기 있어
		IF TALENT:MASTER:120 || TALENT:MASTER:121
			IF RAND:2 == 0
				PRINTFORML %조사처리(CALLNAME:MASTER,"는")%, 자신의 육봉을 빼앗으려는 듯 다투는 노예들을 만족스럽게 둘러보며
				PRINTFORMW 누구에게 자비를 베풀어 줄까를 생각했다.
			ELSE
				PRINTFORML 네 명의 노예들은 %CALLNAME:MASTER%의 육봉을 중심으로 몸을 부비며, 서로를 애무해주고 있다.
				PRINTFORML %조사처리(CALLNAME:MASTER,"는")% 시시때때로 정액을 토해서 그녀들을 만족시켜주며
				PRINTFORMW 그녀들의 교성을 자장가삼아 기분좋은 잠에 빠져들었다.
			ENDIF
		;주인막대기 없음
		ELSE
			IF RAND:2 == 0
				PRINTFORML %조사처리(CALLNAME:MASTER,"는")%, 자신도 다른 암컷들과 같이 한 개의 육봉에 몰려들어선
				PRINTFORML 다른 여성의 몸을 즐겼다.
			ELSE
				PRINTFORML 한 개의 육봉을 서로 빼앗으려는 듯이 몰려든 세 마리의 암컷들.
				PRINTFORML 그 고리에서 소외되어 있던 %조사처리(CALLNAME:MASTER,"는")%, 쓸쓸히 방 구석에서 자위에 빠져 있었지만
				PRINTFORML 그걸 알아챈 건지, 암컷들을 밀치고서 육봉이 눈앞에 들이밀어졌다.
				PRINTFORML %조사처리(CALLNAME:MASTER,"는")% 무구한 소녀처럼 미소짓더니, 
				IF ABL:MASTER:12 > 1
					PRINTFORMW 육봉에 혀를 대고, 그간 연마한 기술로 봉사를 시작했다
				;普通でない.暴行されて技術下がったのを放置しないと出ない
				ELSE
					PRINTFORMW 육봉에 혀를 대고, 서투르나마 열심히 봉사를 시작했다
				ENDIF
			ENDIF
		ENDIF
	ELSEIF ARG == 0
		PRINTFORMW 여자들은 자신의 욕망이 이끄는 대로 요염하게 몸을 구해왔다……
	ELSE
		PRINTFORMW 주인과 노예들은, 욕망이 가는 대로 차례차례 상대를 바꿔가며 몸을 섞었다……
	ENDIF
	PRINTL 
ENDIF

CALL NIGHT_STATUS_UP_5P, LOCAL, GET_CHARA_G(0), GET_CHARA_G(1), GET_CHARA_G(2), GET_CHARA_G(3)

;지문 컷
SIF CFLAG:99 == 0
	PRINTFORML 노예들과의 격렬한 행위는 밤이 하얗게 밝아올 때까지 계속되었고, 다섯명은 힘이 다 떨어진 듯 잠에 빠져들었다…….

;-----------------------------------------------
;플랑쨩난교( 구NIGHT_EVENT_7)
;-----------------------------------------------
@NIGHT_RANKOU_FLAN, ARG, ARG:1
;분신 할 수 있는 사람의 경우 즉 포오브아카인드. 편애가 아니어요, 예
IF CFLAG:99 == 0
	PRINTFORML 그날 밤, 네 명으로 분신한 %조사처리(CALLNAME:TARGET,"가")% %CALLNAME:MASTER%의 방으로 찾아왔다.
	PRINTFORML %CALLNAME:TARGET%들은 순수한 미소를 지으며 침대 위의 %CALLNAME:MASTER%에게 쇄도했다.
ENDIF
;많은 사람 PT난교의 이벤트 대사 호출
CALL SELF_KOJO, TARGET, 408, ARG:1
PRINTL 
;지문 컷
IF CFLAG:99 == 0
	IF RAND:4 == 0
		PRINTFORMW 네 명의 %조사처리(CALLNAME:TARGET,"는")% 일렬로 서더니, Ｍ자로 다리를 벌리고 %조사처리(CALLNAME:MASTER,"를")% 유혹했다.
	ELSEIF RAND:3 == 0
		PRINTFORMW %조사처리(CALLNAME:MASTER,"가")% 네 명을 동시에 기분좋게 해 주자, %CALLNAME:TARGET%의 달콤한 목소리가 몇 번이고 방에 울렸다.
	ELSEIF RAND:2 == 0
		PRINTFORML %CALLNAME:TARGET% 일행은 %조사처리(CALLNAME:MASTER,"를")% 덮치더니, 각각 자기들 맘대로 핥거나 부비거나 해왔다
		PRINTFORMW %조사처리(CALLNAME:MASTER,"는")% 네 명의 %CALLNAME:TARGET%의 봉사에 기분이 좋아졌다.
	ELSE
		PRINTFORML %조사처리(CALLNAME:MASTER,"가")% 한 명의 %CALLNAME:TARGET%만을 상대하자, 남은 세 명은 지루한 건지 
		PRINTFORMW 등에 올라타거나 국부를 눌러오거나, 보란 듯이 눈앞에서 자위를 하기 시작했다.
	ENDIF
ENDIF
;02절정 경험
CALL COMMON_UP_EXP, TARGET, 2, 4, 0
;11습득의 구슬
CALL COMMON_UP_JUEL, TARGET, 11, 495, 0
;12순종의 구슬
CALL COMMON_UP_JUEL, TARGET, 12, 495, 0
;13욕정의 구슬
CALL COMMON_UP_JUEL, TARGET, 13, 495, 0
;15치정의 구슬
CALL COMMON_UP_JUEL, TARGET, 15, 495, 0
;지문 컷
SIF CFLAG:99 == 0
	PRINTFORML {ARG}번의 행위 끝에, 네 명의 %조사처리(CALLNAME:TARGET,"는")% %CALLNAME:MASTER%에게 기대어 잠들어 버렸다…….

;-----------------------------------------------
;연회
;----------------------------------------------
@NIGHT_ENKAI, ARG
PRINTL 
;지문 컷
SIF CFLAG:99 == 0
	PRINTFORMW %조사처리(CALLNAME:MASTER,"는")%, 집안에 있는 술을 모조리 긁어모아 연회를 열기로 했다.

;주류 소비(합계 10개 소비, 높은 녀석으로부터 차례로)
REPEAT 10
	;스피리타스
	IF ITEM:604 > 0
		ITEM:604 -= 1
		CONTINUE
	;대음양주
	ELSEIF ITEM:603 > 0
		ITEM:603 -= 1
		CONTINUE
	;와인
	ELSEIF ITEM:602 > 0
		ITEM:602 -= 1
		CONTINUE
	;맥주
	ELSEIF ITEM:601 > 0
		ITEM:601 -= 1
		CONTINUE
	;카르아
	ELSE
		ITEM:600 -= 1
	ENDIF
REND


IF ARG == 1
	;많은 사람 술잔치의 이벤트 대사 호출
	REPEAT 4
		CALL SELF_KOJO, GET_CHARA_G(COUNT), 450
	REND
	REPEAT 4
		LOCAL = GET_CHARA_G(COUNT)

		;12순종의 구슬
		CALL COMMON_UP_JUEL, LOCAL, 12, 280*ABL:LOCAL:10*ABL:LOCAL:13, 0
		;취기의 처리
		;귀신이나 황혼의 괴물
		IF TALENT:COUNT:214 || TALENT:COUNT:221
			BASE:COUNT:8 += 500
		;갓빠나 인형
		ELSEIF TALENT:COUNT:208 || TALENT:COUNT:210
			BASE:COUNT:8 += 1500
		;그 외
		ELSE
			BASE:COUNT:8 += 2000
		ENDIF
	REND
ELSE
	;3명이 술잔치의 이벤트 대사 호출
	CALL SELF_KOJO, GET_CHARA_G(0), 451
	CALL SELF_KOJO, GET_CHARA_M(), 451
	REPEAT 2
		LOCAL = (COUNT == 0) ? TARGET # GET_CHARA_M()
		;12순종의 구슬
		CALL COMMON_UP_JUEL, LOCAL, 12, 280*ABL:LOCAL:10*ABL:LOCAL:13, 0
		;취기의 처리
		;귀신이나 황혼의 괴물
		IF TALENT:COUNT:214 || TALENT:COUNT:221
			BASE:COUNT:8 += 500
		;갓빠나 인형
		ELSEIF TALENT:COUNT:208 || TALENT:COUNT:210
			BASE:COUNT:8 += 1500
		;그 외
		ELSE
			BASE:COUNT:8 += 2000
		ENDIF
	REND
ENDIF
PRINTL 
;지문 컷
SIF CFLAG:99 == 0
	PRINTW 연잔치는 밤 늦게까지 계속되었고, 모두 마음껏 잔치를 즐겼다…….

;=====================================================
;
;조수와의 밤이벤트
;
;=====================================================
;-----------------------------------------------
;조수와 섹스
;----------------------------------------------
;ARG：구G
@NIGHT_ASSI_SEX, ARG
;지문 컷
SIF CFLAG:99 == 0
	PRINTFORML %조사처리(CALLNAME:MASTER,"는")% %조사처리(CALLNAME:TARGET,"를")% 안기로 했다….
;조수와 섹스의 이벤트 대사 호출
CALL SELF_KOJO, TARGET, 461
;지문 컷
IF CFLAG:99 == 0
	PRINT 그 날은 밤 늦게까지 
	;어떤지에서도 좋은 구애됨.
	IF ABL:MASTER:11 > ABL:11 || (RAND:2 == 0 && ABL:MASTER:11 == ABL:11)
		PRINTFORML %조사처리(CALLNAME:MASTER,"는")% %조사처리(CALLNAME:TARGET,"와")%의 행위를 즐겼다.
	ELSE
		PRINTFORML %조사처리(CALLNAME:TARGET,"는")% %조사처리(CALLNAME:MASTER,"와")%의 행위를 즐겼다.
	ENDIF
ENDIF

LOCAL = MAX((ABL:TARGET:11+ABL:MASTER:11)/2, 3)

CALL NIGHT_STATUS_UP_2P, LOCAL

;반발 노예에 의한 사후 습격 판정.
LOCAL = 0

SIF ARG != ASSI && MARK:ARG:9 > 2
	LOCAL += 4
SIF TALENT:MASTER:120 == 0
	LOCAL += 1
SIF TALENT:ASSI:120 == 0
	LOCAL += 2
SIF LOCAL < 5
	LOCAL = 0

IF LOCAL > 4
	IF LOCAL == 6 || (RAND:2 == 0 && LOCAL == 7)
		LOCAL:1 = ASSI
		LOCAL:2 = MASTER
	ELSE
		LOCAL:1 = MASTER
		LOCAL:2 = ASSI
	ENDIF
	CALL NIGHT_ASSI_SEX_EX, ARG, LOCAL:1, LOCAL:2
	RETURN 1
ENDIF
RETURN 0

;-----------------------------------------------
;조수와 둘이서 술을 마신다
;----------------------------------------------
@NIGHT_ASSI_SAKE

SIF CFLAG:99 == 0
	PRINTFORML  그날 밤은 %조사처리(CALLNAME:ASSI,"과")% 둘이서 술을 마시며 이야기하며 보내기로 했다….

;주류 소비(합계 1개 소비, 높은 녀석으로부터 차례로)
;스피리타스
IF ITEM:604 > 0
	ITEM:604 -= 1
;대음양주
ELSEIF ITEM:603 > 0
	ITEM:603 -= 1
;와인
ELSEIF ITEM:602 > 0
	ITEM:602 -= 1
;맥주
ELSEIF ITEM:601 > 0
	ITEM:601 -= 1
;카르아
ELSE
	ITEM:600 -= 1
ENDIF
;조수와 술을 마시는 것 이벤트 대사 호출
CALL SELF_KOJO, TARGET, 462

;LOCAL 하지 않아도 괜찮지만, 많은 사람에서의 연회와 서식을 배합한다.
LOCAL = TARGET
;12순종의 구슬
CALL COMMON_UP_JUEL, LOCAL, 12, 280*ABL:LOCAL:10*ABL:LOCAL:13, 0

RETURN 0

;-----------------------------------------------
;조수와의 SM행위 조수가 S
;----------------------------------------------
@NIGHT_ASSI_SM_S
;지문 컷
IF CFLAG:99 == 0
	PRINTFORML %CALLNAME:ASSI%에게 채찍을 건내주자 순간적으로 곤란한 얼굴을 했지만
	PRINTFORML 그 다음 %조사처리(CALLNAME:MASTER,"를")% 밀어 넘어뜨리고 옷을 벗긴 뒤, 등짝에 채찍질을 했다.
ENDIF
;조수와 SM플레이 하는(조수가 S)의 이벤트 대사 호출
CALL SELF_KOJO, TARGET, 463

CALL NIGHT_EVENT_SM_S_2, MASTER

RETURN 0

;-----------------------------------------------
;조수와의 SM행위 조수가 M
;----------------------------------------------
@NIGHT_ASSI_SM_M
;지문 컷
IF CFLAG:99 == 0
	PRINTFORML %CALLNAME:ASSI%에게 벌을 주겠다고 말하자 순간적으로 곤란한 얼굴을 했지만
	IF ABL:21 < 3
		PRINTFORML 살살 하겠다고 하자 옷을 벗어 등을 내보였다.
	ELSE
		PRINTFORML 곧바로 옷을 벗으면서 등을 내보이며 벌을 졸랐다.
	ENDIF
ENDIF
;조수와 SM플레이 하는(조수가 M)의 이벤트 대사 호출
CALL SELF_KOJO, TARGET, 464

CALL NIGHT_EVENT_SM_M_2, MASTER

RETURN 0

;-----------------------------------------------
;조수와 밤의 마을에서 우후후
;----------------------------------------------
@NIGHT_ASSI_SEX_PH
;지문 컷
IF CFLAG:99 == 0
	PRINTFORML 밤길은 위험하지만, %조사처리(CALLNAME:ASSI,"가")% 있으면 안심일 것이다.
	PRINTFORMW %조사처리(CALLNAME:MASTER,"와")% %조사처리(CALLNAME:ASSI,"는")% 밤에 마을로 향해 걸어 나갔다.
ENDIF

;어떻게 할까. 주인조수 순서로
;남남이면, 여자아이 덮쳐 강간(보쌈하지는 않는다. 귀찮아서)+공원의 벤치에 있는 사람을 발견해…
;여여는 역강간이나 대난교 플레이
;그 외는 여자아이 강간만

LOCAL = 0

IF TALENT:120 && TALENT:MASTER:120
	LOCAL = (RAND:2) ? 9 # 1
ELSEIF !TALENT:120 && !TALENT:121 && !TALENT:MASTER:120 && !TALENT:MASTER:121
	LOCAL = (RAND:2) ? 3 # 2
ELSE
	LOCAL = 1
ENDIF
;지문 컷
IF CFLAG:99 == 0
	PRINTL 
	IF LOCAL == 1
		PRINTFORML %조사처리(CALLNAME:MASTER,"와")% %조사처리(CALLNAME:TARGET,"가")% 그늘로 다니면서 돌아다니고 있는데
		PRINT 부주의하게도 한 
		PRINTDATA
			DATA 여성이 
			DATA 아가씨가 
			DATA 소녀가 
		ENDDATA
		PRINTL 걸어 왔다.
		PRINTL 달빛에 비친 얼굴을 보니 꽤 반반했다.
		PRINTFORML %조사처리(CALLNAME:MASTER,"와")% %조사처리(CALLNAME:TARGET,"는")% 서로 고개를 끄덕이고 사냥감을 덮쳤다.
		PRINTL 
		PRINTL ………
		PRINTL ……
		PRINTL …
		PRINTW 
		SELECTCASE RAND:3
			CASE 0
				PRINTL 「아, 안 돼, 그만…! 나, 오늘 위험한 날…!」
			CASE 1
				PRINTL 「힉…?! 다, 당신들 누구……, 읍?! 으읍……!」
			CASE 2
				PRINTL 「제겐, 마음에 둔 사람이 있어요…! 제발……!」
		ENDSELECT
	ELSEIF LOCAL == 2
		PRINTFORM %조사처리(CALLNAME:MASTER,"와")% %조사처리(CALLNAME:TARGET,"가")% 큰길을 걷고 있는데 저쪽에서 한 
		PRINTDATA
			DATA 멋진 남자가 
			DATA 젊은이가 
			DATA 소년이 
		ENDDATA
		PRINTL 걸어 왔다.
		PRINTFORML %조사처리(CALLNAME:MASTER,"와")% %조사처리(CALLNAME:TARGET,"는")% 서로 고개를 끄덕이고는, 요염한 몸짓으로 사냥감을 뒷골목으로 꾀어냈다.
	ELSEIF LOCAL == 3
		PRINTFORM %조사처리(CALLNAME:MASTER,"와")% %조사처리(CALLNAME:TARGET,"가")% 조금 외진 골목길을 걷고 있는데
		PRINT 여러 명의 
		PRINTDATA
			DATA 남자들이 
			DATA 젊은이 무리가 
		ENDDATA
		PRINTL 불러 세웠다.
		PRINTL 달빛에 비친 그 얼굴은 천박한 미소와 함께 욕망에 이글거리는 눈으로 둘의 몸을 핥듯이 보고 있다.
		PRINTFORML %조사처리(CALLNAME:MASTER,"와")% %조사처리(CALLNAME:TARGET,"는")% 서로 끄덕이고는, 사냥감들을 인기척 없는 곳으로 꾀어냈다.
	ELSEIF LOCAL == 9
		PRINTFORML 우연히도 마을 한켠에 왠지 멋진♂남자가.
		PRINTFORML %조사처리(CALLNAME:MASTER,"와")% %조사처리(CALLNAME:TARGET,"는")% 뭔가에 빨려들듯이 남자 쪽으로 걸어갔다….
	ENDIF
	PRINTW 
ENDIF
;조수와 마을로 누군가와 엣찌를 하는 이벤트 대사 호출
CALL SELF_KOJO, TARGET, 465, LOCAL

CALLFORM NIGHT_ASSI_SEX_PH_{LOCAL}, MASTER, MAX(RAND:4+ABL:MASTER:11+ABL:11, 5)

RETURN 0

;-----------------------------------------------
;반발 노예에 의한, 사후 습격
;----------------------------------------------
@NIGHT_ASSI_SEX_EX, ARG, ARG:1, ARG:2
;습격자 ARG, 피해자 ARG:1, 발견자 ARG:2
IF CFLAG:99 == 0
	PRINTL …
	PRINTL ……
	PRINTFORML 일을 보러 간 %CALLNAME:(ARG:1)%의 귀가가 늦다.
	PRINTFORML 갑자기 불안하게 된 %조사처리(CALLNAME:(ARG:2),"는")% 상황을 보러 가봤다.
	WAIT
ENDIF

IF (TALENT:ARG:120 || TALENT:ARG:121) && !(CFLAG:ARG:42 & 2)
	IF CFLAG:99 == 0
		IF RAND:4 == 0
			PRINTL 근처에 가보니, 여자의 흐느껴 우는 목소리가 들린다….
			PRINTFORML %조사처리(CALLNAME:(ARG:2),"가")% 당황해서 안을 확인하자, 거기에는 정액 투성이가 된 %CALLNAME:(ARG:1)%의 모습이 보인다.
		ELSEIF RAND:3 == 0
			PRINTL 근처에 가보니, 반쯤 열린 문에서 등불이 새나오고 있다.
			PRINTFORML %조사처리(CALLNAME:(ARG:2),"가")% 안을 확인하니, 거기에는 정액 투성이가 되서 멍하게 있는 %CALLNAME:(ARG:1)%의 모습이 보인다.
		ELSEIF RAND:2 == 0
			PRINTL 근처에 가는중에 문이 살짝 열린 방에서 흐느껴 우는 소리가 들린다.
			PRINTFORML 사용하지 않느 방에서 소리가 나서 당황한 %조사처리(CALLNAME:(ARG:2),"가")% 안을 들여다 보니
			PRINTFORML 거기에는 정액 투성이가 된 %CALLNAME:(ARG:1)%의 모습이 보인다.
		ELSE
			PRINTL 근처에 가는중 문이 열린 방이 보인다.
			PRINTFORML 사용하지 않는 방인데 열려있어 %조사처리(CALLNAME:(ARG:2),"가")% 안을 들여다보니
			PRINTFORML 거기에는 정액 투성이가 되서 멍하게 있는 %CALLNAME:(ARG:1)%의 모습이 있었다….
		ENDIF
	ENDIF
	
	;행위수결정
	LOCAL = 2 + ABL:ARG:11
	
	;피해자측 처리.
	IF TALENT:(ARG:1):0
		TALENT:(ARG:1):0 = 0
		CALL SETCOLOR_TORIKOMODE, "RED", 3
		PRINTFORML 【처녀 상실】(%CALLNAME:(ARG:1)%)
		RESETCOLOR
		;첫경험 기록
		IF CFLAG:(ARG:1):500 == 0
			CFLAG:(ARG:1):500 = NO:ARG + 1
			CSTR:(ARG:1):5 = %CALLNAME:ARG%
		ENDIF
		;주인의 처녀를 빼앗은 플래그
		IF ARG:1 == MASTER
			CFLAG:ARG:153 |= 1
			CFLAG:(ARG:1):153 |= 4
		ENDIF
	ENDIF
	;00 V경험
	CALL COMMON_UP_EXP, ARG:1, 0, LOCAL, 0
	;02절정 경험
	CALL COMMON_UP_EXP, ARG:1, 2, (LOCAL*(1+ABL:(ARG:1):1)/4), 0
	;04정액 경험
	CALL COMMON_UP_EXP, ARG:1, 4, (LOCAL*(1+ABL:ARG:0)/4), 0
	;81피강간 경험
	CALL COMMON_UP_EXP, ARG:1, 81, 1, 0
	;83피간쾌락 경험
	SIF LOCAL*(1+ABL:(ARG:1):1)/4 >= 1
		CALL COMMON_UP_EXP, ARG:1, 83, LOCAL, 0

	IF (ARG:1) == MASTER
		;타락매번 상승
		CFLAG:MASTER:66 += LOCAL * 5
	ELSE
		;01쾌V의 구슬
		CALL COMMON_UP_JUEL, ARG:1, 1, 10*LOCAL*ABL:(ARG:1) :1*ABL:(ARG:1):1, 0
		;13욕망의 구슬
		SIF LOCAL*(1+ABL:(ARG:1):1)/4 >= 1
			CALL COMMON_UP_JUEL, ARG:1, 13, 10*LOCAL*ABL:(ARG:1) :1*ABL:(ARG:1):11, 0
		;16고통의 구슬
		SIF ABL:(ARG:1):1 < 3
			CALL COMMON_UP_JUEL, ARG:1, 16, 100*LOCAL*(3-ABL:(ARG:1):1), 0
		;17공포의 구슬
		CALL COMMON_UP_JUEL, ARG:1, 17, 10*LOCAL*(5-ABL:(ARG:1):1)*(5-ABL:(ARG:1):11), 0
		PRINTL 
	ENDIF
	
	;가해자측
	;02절정 경험
	CALL COMMON_UP_EXP, ARG, 2, (LOCAL*(1+ABL:ARG:0)/4), 0
	;03사정 경험
	CALL COMMON_UP_EXP, ARG, 3, (LOCAL*(1+ABL:ARG:0)/4), 0
	;80강간 경험
	CALL COMMON_UP_EXP, ARG, 80, 1, 0
	;00쾌C의 구슬
	CALL COMMON_UP_JUEL, ARG, 0, 10*LOCAL*(1+ABL:ARG:0)*(1+ABL:ARG:0), 0
	;13욕망의 구슬
	CALL COMMON_UP_JUEL, ARG, 13, 10*LOCAL*(1+ABL:ARG:0)*(1+ABL:ARG:11), 0
	;100부정의 구슬
	CALL COMMON_DOWN_JUEL, ARG, 100, 500*(2+MARK:ARG:9), 0
	PRINTFORML 평소의 울분을 풀었기에, %PALAMNAME:100%의 구슬은 줄어들었습니다. (%CALLNAME:ARG%)
ELSE
	PRINTFORML %조사처리(CALLNAME:(ARG:2),"는")%, 다쳐 넘어져 있는 %조사처리(CALLNAME:(ARG:1),"를")% 발견했다.
	IF ABL:(ARG:1):12 > 0
		ABL:(ARG:1):12 -= 1
		PRINTFORML %조사처리(CALLNAME:(ARG:1),"는")% 상처를 입었다. (%ABLNAME:12%－1 레벨)
	ENDIF
ENDIF

;-----------------------------------------------
;반발 노예들에 의한, 윤간
;----------------------------------------------
@NIGHT_ASSI_SEX_LUNA, ARG, ARG:1, ARG:2, ARG:3
;지문 컷
IF CFLAG:99 == 0
	PRINTFORML %조사처리(CALLNAME:MASTER,"가")% %조사처리(CALLNAME:ASSI,"과")% 어떻게 보낼까 생각하고 있는데 갑자기 손님이 찾아왔다.
	PRINTFORML 즐거움을 방해받아 기분이 나쁜 %조사처리(CALLNAME:MASTER,"가")% 누군지 확인하지 않고 문을 열자
	PRINTFORML 거기에는 %조사처리(CALLNAME:ARG,"과")% %조사처리(CALLNAME:(ARG:1),"와")% %조사처리(CALLNAME:(ARG:2),"와")% %조사처리(CALLNAME:(ARG:3),"가")% 있었다.
	PRINTFORML %CALLNAME:MASTER%의 정지 명령을 무시하고 네 명은 방에 침입해 %조사처리(CALLNAME:ARG,"가")% %조사처리(CALLNAME:ASSI,"를")% 깔아 뭉갰다.
	PRINTL  
ENDIF

REPEAT 4
	CALL SELF_KOJO, ARG:COUNT, 468
REND
;지문 컷
IF CFLAG:99 == 0
	PRINTFORML 네 명은 %조사처리(CALLNAME:MASTER,"와")% %CALLNAME:ASSI%의 옷을 벗기고
	PRINTFORML 각각 두 명의 몸에 페니스를 비벼댔다.
ENDIF

PRINTL  

LOCAL = 4 + ABL:ARG:11 + ABL:(ARG:1):11 + ABL:(ARG:2):11 + ABL:(ARG:3):11

;피해자측 처리
REPEAT 2
	TARGET = (COUNT == 0) ? MASTER # ASSI
	IF TALENT:0
		TALENT:0 = 0		
		CALL SETCOLOR_TORIKOMODE, "RED", 3
		PRINTFORML 【처녀 상실】(%CALLNAME:(TARGET)%)
		RESETCOLOR
		;첫경험 기록
		IF CFLAG:TARGET:500 == 0
			CFLAG:TARGET:500 = NO:ARG + 1
			CSTR:TARGET:5 = %CALLNAME:ARG%
		ENDIF
		;주인의 처녀를 빼앗은 플래그
		IF TARGET == MASTER
			CFLAG:153 |= 4
			CFLAG:ARG:153 |= 1
			CFLAG:(ARG:1):153 |= 1
			CFLAG:(ARG:2):153 |= 1
			CFLAG:(ARG:3):153 |= 1
		ENDIF
	ENDIF
	;00V경험
	SIF !TALENT:120
		CALL COMMON_UP_EXP, TARGET, 0, LOCAL, 0
	;01A경험
	CALL COMMON_UP_EXP, TARGET, 1, LOCAL*2/3, 0
	;02절정 경험
	LOCAL:1 = 0
	LOCAL:1 += LOCAL*(1+ABL:0)/8
	SIF !TALENT:120
		LOCAL:1 += LOCAL*(1+ABL:1)/4
	LOCAL:1 += LOCAL*(1+ABL:2)*2/3
	LOCAL:1 += LOCAL*(1+ABL:3)/8
	CALL COMMON_UP_EXP, TARGET, 2, LOCAL:1, 0
	;03사정 경험
	SIF TALENT:120 || TALENT:121
		CALL COMMON_UP_EXP, TARGET, 3, (LOCAL*(1 + ABL:0)/8), 0
	;04정액 경험
	CALL COMMON_UP_EXP, TARGET, 4, (LOCAL*(4 + ABL:ARG:0 + ABL:(ARG:1):0 + ABL:(ARG:2):0 + ABL:(ARG:3):0)/16), 0
	;23키스 경험
	LOCAL:1 = RAND:(LOCAL)
	CALL COMMON_UP_EXP, TARGET, 23, LOCAL:1, 0
	;43A쾌락 경험
	CALL COMMON_UP_EXP, TARGET, 43, LOCAL*ABL:2*2/3, 0
	;44B쾌락 경험
	CALL COMMON_UP_EXP, TARGET, 43, LOCAL*ABL:2/2, 0
	;82피윤간 경험
	CALL COMMON_UP_EXP, TARGET, 82, 1, 0
	;83피간쾌락 경험
	LOCAL:1 = 0
	SIF LOCAL*(1+ABL:0)/8 >=1
		LOCAL:1 += LOCAL/2
	SIF LOCAL*(1+ABL:1)/4 >=1 && !TALENT:120
		LOCAL:1 += LOCAL
	SIF LOCAL*(1+ABL:2)*2/3 >= 1
		LOCAL:1 += LOCAL*2/3
	SIF LOCAL*(1+ABL:3)/8 >= 1
		LOCAL:1 += LOCAL/2
	CALL COMMON_UP_EXP, TARGET, 83, LOCAL:1, 0

	IF GET_CHARA_M() == MASTER
		;타락매번 상승
		CFLAG:MASTER:66 += LOCAL * 8
	ELSE
		;00쾌C의 구슬
		CALL COMMON_UP_JUEL, TARGET, 0, 10*LOCAL*ABL:0*ABL:0/2, 0
		;01쾌V의 구슬
		SIF !TALENT:120
			CALL COMMON_UP_JUEL, TARGET, 1, 10*LOCAL*ABL:1*ABL:1, 0
		;02쾌A의 구슬
		CALL COMMON_UP_JUEL, TARGET, 2, 10*LOCAL*ABL:2*ABL:2*2/3, 0
		;03쾌B의 구슬
		CALL COMMON_UP_JUEL, TARGET, 3, 10*LOCAL*ABL:3*ABL:3/2, 0
		;13욕망의 구슬
		SIF LOCAL:1 >= 1
			CALL COMMON_UP_JUEL, TARGET, 13, 10*LOCAL*ABL:11*ABL:11, 0
		;16고통의 구슬
		SIF ABL:1 < 3 && ABL:2 < 3
			CALL COMMON_UP_JUEL, TARGET, 16, 10*LOCAL*(3-ABL:1)*(3-ABL:2), 0
		;17공포의 구슬
		CALL COMMON_UP_JUEL, TARGET, 17, 10*LOCAL*(5-ABL:1)*(5-ABL:2), 0
		PRINTW  
	ENDIF
REND

;가해자측
REPEAT 4
	TARGET = ARG:COUNT
	;가해자측
	;02절정 경험
	CALL COMMON_UP_EXP, TARGET, 2, (LOCAL*(1+ABL:0)/16), 0
	;03사정 경험
	CALL COMMON_UP_EXP, TARGET, 3, (LOCAL*(1+ABL:0)/16), 0
	;23키스 경험
	LOCAL:1 = RAND:(LOCAL/3)
	CALL COMMON_UP_EXP, TARGET, 23, LOCAL:1, 0
	;80강간 경험
	CALL COMMON_UP_EXP, TARGET, 80, 1, 0
	;00쾌C의 구슬
	CALL COMMON_UP_JUEL, TARGET, 0, 10*LOCAL*(1+ABL:0)*(1+ABL:0)/4, 0
	;13욕망의 구슬
	CALL COMMON_UP_JUEL, TARGET, 13, 10*LOCAL*(1+ABL:0)*(1+ABL:11)/4, 0
	;100부정의 구슬
	CALL COMMON_DOWN_JUEL, TARGET, 100, 500*(2+MARK:9), 0
	PRINTFORML 평소의 울분을 풀었기에, %PALAMNAME:100%의 구슬은 줄어들었습니다. (%CALLNAME:TARGET%)
REND

PRINTW  

REPEAT 4
	CALL SELF_KOJO, ARG:COUNT, 469
REND

IF CFLAG:99 == 0
	PRINTL 네 명이 돌아가자, 방에는 모든 구멍에서 정액이 흘러나오는
	PRINTFORML 얼빠진 눈을 한 %조사처리(CALLNAME:MASTER,"와")% %CALLNAME:ASSI%만 남겨졌다.
ENDIF

;=====================================================
;
;여기에서 반발 노예의 턴
;
;=====================================================
;-----------------------------------------------
;반발 노예에 의한, 강간
;----------------------------------------------
@NIGHT_GOUKAN_N, ARG, ARG:1
;ARG:1이 강간하는 사람
;ARG가 강간되는 사람
;행위수지정. 주인 상대라면 8. 노예의 경우, 음란하지 않으면 4
LOCAL = 8
SIF ARG != MASTER && TALENT:(ARG:1):170 == 0
	LOCAL = 4

LOCAL:1 = 0

;지문 컷
SIF CFLAG:99 == 0
	PRINTFORML %조사처리(CALLNAME:ARG,"가")% 잠들려고 하던 때, 갑자기 %조사처리(CALLNAME:(ARG:1),"가")% 방을 방문해 왔다.
;대상이 남자이다. 여기까지의 조건으로 당연히 조제 금단은 가지고 있다
IF TALENT:ARG:120
	CALL SHIFT_TO_F, ARG
	;지문 컷
	SIF CFLAG:99 == 0
		PRINTFORML 억지로 %CALLNAME:ARG%의 입에 〔무지개사탕〕을 밀어넣었다.
	PRINTFORML [놀랍게도 %조사처리(CALLNAME:ARG,"는")% 여성의 몸이 되어버렸다]
	;지문 컷
	SIF CFLAG:99 == 0
		PRINTFORM 그리고 %조사처리(CALLNAME:(ARG:1),"는")%, 당혹해 하는 %조사처리(CALLNAME:ARG,"를")% 쓰러트리고, 
	;정조대 부착
	IF (CFLAG:(ARG:1):42 & 2)
		LOCAL:1 = 1
		;지문 컷
		IF CFLAG:99 == 0
			SIF TALENT:(ARG:1):150
				PRINTFORML \@(ARG == MASTER) ? 사랑스럽게 %조사처리(CALLNAME:ARG,"를")% 응시하고 # 질투를 드러내며 %조사처리(CALLNAME:ARG,"를")% 째려보면서\@
			PRINTFORML \@(EXP:ARG:0 == 0) ? 남자를 모르는 # 아직 젖지도 않은\@ 보지에 주먹을 쑤셔박았다.
		ENDIF
	;막대기가 없는 경우 기른다
	ELSE
		IF TALENT:120 == 0 && TALENT:121 == 0
			TALENT:121 = 1
			CALL CHANGE_FUTA_COMMON, TARGET
			;지문 컷
			IF CFLAG:99 == 0
				PRINTL 그녀에겐 있을리 없는 것을 꺼내들더니
				SIF TALENT:(ARG:1):150
					PRINTFORML \@(ARG == MASTER) ? 몹시 사랑스럽게 %조사처리(CALLNAME:ARG,"를")% 바라보고 # 질투를 드러내며 %조사처리(CALLNAME:ARG,"를")% 째려보면서\@
				PRINTFORML \@(EXP:ARG:0 == 0) ? 남자를 모르는 # 아직 젖지도 않은\@ 보지에 우뚝 솟은 것을 밀어넣었다.
			ENDIF
		;지문 컷
		ELSEIF CFLAG:99 == 0
			SIF TALENT:(ARG:1):150
			PRINTFORML \@(ARG == MASTER) ? 몹시 사랑스럽게 %조사처리(CALLNAME:ARG,"를")% 바라보고 # 질투를 드러내며 %조사처리(CALLNAME:ARG,"를")% 째려보면서\@
			PRINTFORML \@(EXP:ARG:0 == 0) ? 남자를 모르는 # 아직 젖지도 않은\@ 보지에 발기된 것을 쑤셔넣었다.
		ENDIF
	ENDIF
;대상이 여자이다
ELSE
	;지문 컷
	IF CFLAG:99 == 0
		PRINTFORML 수상하게 생각한 %조사처리(CALLNAME:ARG,"가")% 내보내려고 했지만
		PRINTFORML %조사처리(CALLNAME:(ARG:1),"는")% 억지로 방으로 들이닥쳐, %조사처리(CALLNAME:ARG,"를")% 덮치더니
		PRINTFORML 반항하는 %CALLNAME:ARG%의 옷을 갈기갈기 찢고, 
	ENDIF
	;정조대 붙어, 막대기는 없지만 상대가 여자 노예, 막대기가 없게 금단도 조제도 없고 여주인인
	IF (CFLAG:(ARG:1):42 & 2) || ((TALENT:120 == 0 && TALENT:121 == 0) && (ARG != MASTER || (ARG == MASTER &&(TALENT:55 == 0 && TALENT:56 == 0))))
		LOCAL:1 = 1
		;지문 컷
		IF CFLAG:99 == 0
			SIF TALENT:(ARG:1):150
				PRINTFORML \@(ARG == MASTER) ? 몹시 사랑스럽게 %조사처리(CALLNAME:ARG,"를")% 바라보고 # 질투를 드러내며 %조사처리(CALLNAME:ARG,"를")% 째려보면서\@
			PRINTFORML \@(EXP:ARG:0 == 0) ? 남자를 모르는 # 아직 젖지도 않은\@ 보지에 주먹을 쑤셔박았다.
		ENDIF
	;주인 상대로 막대기가 없는 경우 기른다
	ELSE
		IF ARG == MASTER && (TALENT:120 == 0 && TALENT:121 == 0)
			TALENT:121 = 1
			CALL CHANGE_FUTA_COMMON, TARGET
			;지문 컷
			IF CFLAG:99 == 0
				PRINTL 그녀에겐 있을리 없는 것을 꺼내들더니
				SIF TALENT:(ARG:1):150
					PRINTFORML 몹시 사랑스럽게 %조사처리(CALLNAME:ARG,"를")% 바라보며
				PRINTFORML \@(EXP:ARG:0 == 0) ? 남자를 모르는 # 아직 젖지도 않은\@ 보지에 우뚝 솟은 것을 밀어넣었다.
			ENDIF
		;그 이외 보통으로 막대기는 있을 것.
		;지문 컷
		ELSEIF CFLAG:99 == 0
			SIF TALENT:(ARG:1):150
				PRINTFORML 몹시 사랑스럽게 %조사처리(CALLNAME:ARG,"를")% 바라보며
			PRINTFORML \@(EXP:ARG:0 == 0) ? 남자를 모르는 # 아직 젖지도 않은\@ 보지에 성기를 밀어넣었다.
		ENDIF
	ENDIF
ENDIF
IF ARG == MASTER
	;주인이 자고 있는 곳(중)에 찾아온 노예의 이벤트 대사 호출
	CALL SELF_KOJO, TARGET, 422
ELSE
	;다른 노예를 범하는 이벤트 대사 호출
	CALL SELF_KOJO, (ARG:1), 434
	;다른 노예에 범해지는 이벤트 대사 호출
	CALL SELF_KOJO, ARG, 431
ENDIF
;80강간 경험
CALL COMMON_UP_EXP, (ARG:1), 80, 1, 0
;81피강간 경험
CALL COMMON_UP_EXP, ARG, 81, 1, 0
;83피간쾌락 경험
IF LOCAL:1 == 0
	SIF ABL:ARG:1 > 2
		CALL COMMON_UP_EXP, ARG, 83, LOCAL, 0
ELSE
	SIF ABL:ARG:1 > 4
		CALL COMMON_UP_EXP, ARG, 83, LOCAL, 0
ENDIF
IF TALENT:ARG:0 >= 1
	;지문 컷
	IF CFLAG:99 == 0
		IF LOCAL:1 == 0
			PRINTFORML %조사처리(CALLNAME:(ARG:1),"는")% 한 번 %CALLNAME:ARG%의 질내를 즐기더니만
			PRINTFORMW 정액과 애액, 그리고 파괴의 피로 더럽혀진 자신의 육봉을 %CALLNAME:ARG%의 입에 밀어넣고, 목구멍 깊숙히까지 집요하게 범해댔다.
		ELSE
			PRINTFORML %조사처리(CALLNAME:(ARG:1),"는")% 주먹이 끝까지 박혀 고통에 소리를 지르는 %CALLNAME:ARG%의 모습을 충분히 즐긴 뒤 
			PRINTFORMW 애액과 파과의 피로 더럽혀진 손을 %CALLNAME:ARG%의 입에 쑤셔넣고, 혀를 마음껏 희롱했다.
		ENDIF
	ENDIF
	TALENT:ARG:0 = 0
	CALL SETCOLOR_TORIKOMODE, "RED", 3
	PRINTFORML 【처녀 상실】(%CALLNAME:ARG%)
	RESETCOLOR
	;첫경험 기록
	IF CFLAG:ARG:500 == 0
		CFLAG:ARG:500 = NO:(ARG:1) + 1
		CSTR:ARG:5 = %CALLNAME:(ARG:1)%
	ENDIF
	IF LOCAL:1 == 0
		;00V경험
		CALL COMMON_UP_EXP, ARG, 0, LOCAL/2, 0
		IF ARG != MASTER
			;04정액 경험
			CALL COMMON_UP_EXP, ARG, 4, LOCAL/2, 0
		ENDIF
	ELSE
		;00V경험
		CALL COMMON_UP_EXP, ARG, 0, LOCAL*5/2, 0
		;32V확장 경험
		CALL COMMON_UP_EXP, ARG, 32, LOCAL, 0
	ENDIF
	IF ARG == MASTER
		;주인의 처녀를 빼앗은 플래그
		CFLAG:153 |= 1
		CFLAG:ARG:153 |= 4
		;정조 관념 소유이다면 타락매번 증가. 드 M와 매저키스트가 있으면 소멸, 광기 취득
		IF TALENT:ARG:30
			CFLAG:ARG:66 += 2
			TALENT:ARG:82 = 0
			TALENT:ARG:178 = 0
			TALENT:ARG:158 = 1
		;주인이 정조무구애도 정조 관념도 없으면 광기 취득
		ELSEIF TALENT:ARG:31 == 0 && TALENT:ARG:158 == 0
			TALENT:ARG:158 = 1
		ENDIF
	ENDIF
ELSE
	IF LOCAL:1 == 0
		;지문 컷
		IF CFLAG:99 == 0
			PRINTFORML 억지로 범해져 몇 번이고 사정당하던 중 언제부터인가 스스로 허리를 움직이고 있던 \@(TALENT:(ARG:1):150 && ARG == MASTER) ? %CALLNAME:ARG%에게 기쁜 미소를 보내며 # %조사처리(CALLNAME:ARG,"를")% 매도하면서\@
			PRINTFORMW %조사처리(CALLNAME:(ARG:1),"는")% 집요하게 질내를 계속 범했다.
		ENDIF
		;00V경험
		CALL COMMON_UP_EXP, ARG, 0, LOCAL, 0
		IF ARG != MASTER
			;02절정 경험
			CALL COMMON_UP_EXP, ARG, 2, LOCAL/4, 0
			;04정액 경험
			CALL COMMON_UP_EXP, ARG, 4, LOCAL/2, 0
		ENDIF
	ELSE
		;지문 컷
		IF CFLAG:99 == 0
			PRINTFORML 괴로워하는 %조사처리(CALLNAME:ARG,"를")% 바라보며 \@(TALENT:(ARG:1):150 && ARG == MASTER) ? 도취한 미소를 띠더니 # 잔인한 웃음을 띠더니 \@
			PRINTFORMW %조사처리(CALLNAME:(ARG:1),"는")% 집요하게 질내를 유린해댔다.
		ENDIF
		;00V경험
		CALL COMMON_UP_EXP, ARG, 0, LOCAL*5, 0
		;32V확장 경험
		CALL COMMON_UP_EXP, ARG, 32, LOCAL*2, 0
	ENDIF
ENDIF
IF LOCAL:1 == 0
	IF ARG == MASTER
		;타락매번 상승
		CFLAG:MASTER:66 += LOCAL * 10
	ELSE
		;01쾌V의 구슬
		CALL COMMON_UP_JUEL, ARG, 1, LOCAL*80*ABL:ARG:1, 0
		;13욕정의 구슬
		CALL COMMON_UP_JUEL, ARG, 13, LOCAL*50*ABL:ARG:1, 0
		;100부정의 구슬
		CALL COMMON_UP_JUEL, ARG, 100, LOCAL*500, 0
	ENDIF
	;03사정 경험
	CALL COMMON_UP_EXP, (ARG:1), 3, LOCAL/2, 0
	;LUNATIC 이상은 사정할 때마다 체력이 감소
	CALL EXHAUST_EJAC, ARG:1, LOCAL/2
	;00쾌C의 구슬
	CALL COMMON_UP_JUEL, ARG:1, 0, LOCAL*400, 0
ELSE
	IF ARG != MASTER
		;16고통의 구슬
		CALL COMMON_UP_JUEL, ARG, 16, LOCAL*800, 0
		;17공포의 구슬
		CALL COMMON_UP_JUEL, ARG, 17, LOCAL*500, 0
		;100부정의 구슬
		CALL COMMON_UP_JUEL, ARG, 100, LOCAL*1000, 0
	ENDIF
ENDIF
;13욕정의 구슬
CALL COMMON_UP_JUEL, ARG:1, 13, LOCAL*250, 0
;100부정의 구슬
CALL COMMON_DOWN_JUEL, ARG:1, 100, 500*(2+MARK:(ARG:1):9), 0
PRINTFORML 그간의 울분이 풀렸기 때문에, %PALAMNAME:100%의 구슬은 줄어들었다. (%CALLNAME:(ARG:1)%)
PRINTL 
;습격 후의 버리기 대사의 이벤트 대사 호출
LOCAL = (ARG == MASTER) ? 420 # 430
CALL SELF_KOJO, ARG:1, LOCAL
PRINTL 
;지문 컷
IF CFLAG:99 == 0
	PRINTFORM %조사처리(CALLNAME:(ARG:1),"가")% 만족하고 방을 떠나자, 
	IF LOCAL:1 == 0
		PRINTFORMW 거기에는 전신에 정액을 뒤집어쓴채 텅 빈 눈동자를 한 %조사처리(CALLNAME:ARG,"가")% 남겨져 있을 뿐이었다…….
	ELSE
		PRINTFORMW 거기엔 흐느끼고 있는 %조사처리(CALLNAME:ARG,"가")% 남겨져 있을 뿐이었다…….
	ENDIF
ENDIF

;-----------------------------------------------
;반발 노예에 의한, 남자 노예 농락
;----------------------------------------------
@NIGHT_NTR_M, ARG, ARG:1
;다른 것과는 틀려, 여기는 대충이니까 편한 것이다
;(이)라고 생각하면(자) 문장 길어졌다
;행위수지정. 실로 적당.
;어차피 다음에 4로 나눈다면 처음부터 1으로 좋았다
;기본치 12
LOCAL = 12

;습격 사람이 음란하면+4
SIF TALENT:(ARG:1):170
	LOCAL += 4
;습격 사람이 작은 악마라면+4
SIF TALENT:(ARG:1):93
	LOCAL += 4
;습격 사람이음 단지라면+4
SIF TALENT:(ARG:1):172
	LOCAL += 4
;피해자가 음란하면+4
SIF TALENT:ARG:170
	LOCAL += 4
;피해자가 음경이라면+4
SIF TALENT:ARG:171
	LOCAL += 4
;피해자가 친애라면―4
SIF TALENT:ARG:152
	LOCAL -= 4
;피해자가 정조 관념 소유라면―4
SIF TALENT:ARG:30
	LOCAL -= 4

;지문 컷
IF CFLAG:99 == 0
	PRINTFORML 밤 2시 경. %조사처리(CALLNAME:MASTER,"는")% 갑자기 화장실에 가고 싶어, 일을 마치고 돌아오던 길이었다.
	PRINTFORML 문득 %CALLNAME:ARG%의 방에서 불빛이 새어나오는 것이 보였다.
	PRINTFORML %조사처리(CALLNAME:ARG,"는")% 아직 일어나 있는 건가?
	PRINTFORML 혹시나 %조사처리(CALLNAME:MASTER,"를")% 생각하며 자위라도 하고 있는 건가？
	PRINTL 
	PRINTFORML %조사처리(CALLNAME:MASTER,"는")% 싱글싱글 웃으며 방에 다가가보았다.
	PRINTFORML 그랬더니 놀랍게도 방안에선 여성의 교성과 음란한 물소리가 들려오는 것이 아닌가.
	PRINTFORML 두근하고 심장이 빠르게 뛰는 것을 느끼면서 발소리를 죽이고, 열린 문틈으로 안을 훔쳐봤다.
	IF LOCAL > 12
		PRINTFORML 거기에는 맹렬히 여성의 몸에 붙어서 허리를 흔들어대는 %조사처리(CALLNAME:ARG,"과")%
		PRINTFORML 그 우뚝 솟은 물건을 탐하며, 요염하게 허리를 꿈틀거리는 %CALLNAME:(ARG:1)%의 모습이 있었다.
	ELSEIF LOCAL > 4
		PRINTFORML 거기에는 남자 위에 올라타, 정액을 마구 짜내는 %조사처리(CALLNAME:(ARG:1),"와")%
		PRINTFORML 상기된 얼굴로 아래서 여자를 찔러올리는 %CALLNAME:(ARG:1)%의 모습이 있었다.
	ELSE
		PRINTFORML 거기에는 남자 위에 올라타, 정액을 마구 짜내는 %조사처리(CALLNAME:(ARG:1),"와")%
		PRINTFORML 그저 쾌락을 받아들이고 있는 %CALLNAME:(ARG:1)%의 모습이 있었다.
	ENDIF
	PRINTFORMW 상상도 못한 엄청난 광경에 %조사처리(CALLNAME:MASTER,"는")%, 서로 교접을 계속하는 두 사람을 훔쳐볼 수밖에 없었다.
ENDIF

;M의 경험 및 주증감 및 순종 및 봉사 정신 저하 처리. 연모, 망신, 질투, 친애, 서로 사랑함, 복종, 예속 소거
;요컨데 다른 여자에게 네토라레
;(수정) NORMAL/EASY에 대해 서로 사랑함과 예속이 있으면 그것은 사라지지 않고 여러가지 완화된다(일단 둘과도 굉장히 조건 어렵기 때문에)
;02절정, 03 사정 경험 증가
CALL COMMON_UP_EXP, ARG, 2, LOCAL/4, 0
CALL COMMON_UP_EXP, ARG, 3, LOCAL/4, 0
;LUNATIC 이상은 사정할 때마다 체력이 감소
CALL EXHAUST_EJAC, TARGET, LOCAL/4
;주처리. 00쾌C·13 욕망·100 부정의 구슬증가, 12 순종 0
CALL COMMON_UP_JUEL, ARG, 0, LOCAL*250, 0
JUEL:ARG:12 = 0
PRINTFORML %PALAMNAME:12%의 구슬(%CALLNAME:ARG%)는 없어졌다
CALL COMMON_UP_JUEL, ARG, 13, LOCAL*150, 0
CALL COMMON_UP_JUEL, ARG, 100, LOCAL*200, 0
;네토라레플래그
CFLAG:ARG:153 |= 16
;LOCAL>12라면 반발 3 취득(NORMAL/EASY에 대해 완화)
IF LOCAL > 12
	IF FLAG:3 > 2
		CALL COMMON_MARK_UP, ARG, 9, 3, 0
	ELSEIF MARK:ARG:9 < 2
		CALL COMMON_MARK_UP, ARG, 9, 2, 0
	ENDIF
ENDIF
;LOCAL > 12라면 각 소질과 순종을 0으로 한다. 그 이외라면 순종-1
IF LOCAL > 12
	IF ABL:ARG:10 > 0
		ABL:ARG:10 = 0
		PRINTFORML %ABLNAME:10%(%CALLNAME:ARG%)은 0 레벨이 되었다.
	ENDIF
	IF TALENT:ARG:150
		TALENT:ARG:150 = 0
		PRINTFORML %조사처리(CALLNAME:ARG,"는")% %TALENTNAME:150%를 잃었다.
		CFLAG:ARG:14 -= 1
		CFLAG:ARG:3115 = 0
		CFLAG:MASTER:98 -= 1
		SIF TALENT:ARG:200 && TALENT:ARG:160 == 0 && TALENT:ARG:170 == 0
			FLAG:32 -= 1
	ENDIF
	IF TALENT:ARG:151
		TALENT:ARG:151 = 0
		PRINTFORML %조사처리(CALLNAME:ARG,"는")% %TALENTNAME:151%를 잃었다.
		CFLAG:ARG:14 -= 2
	ENDIF
	IF TALENT:ARG:152
		TALENT:ARG:152 = 0
		PRINTFORML %조사처리(CALLNAME:ARG,"는")% %TALENTNAME:152%를 잃었다.
		CFLAG:ARG:14 -= 4
	ENDIF
	IF TALENT:ARG:153 && FLAG:3 > 2
		TALENT:ARG:153 = 0
		PRINTFORML %조사처리(CALLNAME:ARG,"는")% %TALENTNAME:153%를 잃었다.
		CFLAG:ARG:14 -= 8
		CFLAG:ARG:3113 = 0
	ENDIF
	IF TALENT:ARG:157
		TALENT:ARG:157 = 0
		CFLAG:ARG:12 -= 4096
		PRINTFORML %조사처리(CALLNAME:ARG,"는")% %TALENTNAME:157%를 잃었다.
	ENDIF
	IF TALENT:ARG:160
		TALENT:ARG:160 = 0
		PRINTFORML %조사처리(CALLNAME:ARG,"는")% %TALENTNAME:160%를 잃었다.
		CFLAG:ARG:15 -= 1
		CFLAG:ARG:3115 = 0
		CFLAG:MASTER:98 -= 1
		SIF TALENT:ARG:200 && TALENT:ARG:150 == 0 && TALENT:ARG:170 == 0
			FLAG:32 -= 1
	ENDIF
	IF TALENT:ARG:162 && FLAG:3 > 2
		TALENT:ARG:162 = 0
		PRINTFORML %조사처리(CALLNAME:ARG,"는")% %TALENTNAME:162%를 잃었다.
		CFLAG:ARG:15 -= 4
		CFLAG:ARG:3114 = 0
	ENDIF
ELSEIF LOCAL > 4
	IF ABL:ARG:10 > 0
		ABL:ARG:10 -= 1
		PRINTFORML %ABLNAME:10%(%CALLNAME:ARG%)－1 레벨
	ENDIF
	IF ABL:ARG:10 < 4 && TALENT:ARG:152
		TALENT:ARG:152 = 0
		PRINTFORML %조사처리(CALLNAME:ARG,"는")% %TALENTNAME:152%를 잃었다.
	ENDIF
;정조, 친밀만이라면 저속해지지 않는다
ENDIF
;범용 말용 플래그
CFLAG:ARG:43 = 0
;습격자측의 경험 처리와 주처리
;00V경험, 02 절정 경험, 04 정액 경험 증가
CALL COMMON_UP_EXP, ARG:1, 0, LOCAL/2, 0
CALL COMMON_UP_EXP, ARG:1, 2, LOCAL/4, 0
CALL COMMON_UP_EXP, ARG:1, 4, LOCAL/4, 0
;구슬 취득. 01쾌V·13 욕망 증가, 100 부정 감소
CALL COMMON_UP_JUEL, ARG:1, 1, LOCAL*250, 0
CALL COMMON_UP_JUEL, ARG:1, 13, LOCAL*150, 0
CALL COMMON_DOWN_JUEL, ARG:1, 100, 500*(2+MARK:(ARG:1):9), 0
PRINTFORML 평소의 울분을 풀었기에, %PALAMNAME:100%의 구슬은 줄어들었다. (%CALLNAME:(ARG:1)%)
;잠 취한 플래그
CFLAG:(ARG:1):153 |= 32
CFLAG:MASTER:153 |= 4096
PRINTL 
;지문 컷
IF CFLAG:99 == 0
	PRINTFORML %조사처리(CALLNAME:MASTER,"가")% 선정적인 광경에서 눈을 돌리지 못하고 있자 
	PRINTFORML 본의아니게 %조사처리(CALLNAME:(ARG:1),"와")% 눈이 마주치게 되었다.
	PRINTFORMW 생긋 웃는 %CALLNAME:(ARG:1)%에게, %조사처리(CALLNAME:MASTER,"는")% 엄청난 비명을 지르며 그 자리에서 도망쳐나갔다.
;	PRINTL 
ENDIF

;LOCAL:1 = 0
;;A경험로 어널 바이브 소지
;SIF EXP:MASTER:1 > 12 && (ITEM:9 || NOITEM)
;	LOCAL:1 += 1
;;비처녀 한편 바이브 소지
;SIF TALENT:MASTER:0 == 0 && (ITEM:3 || NOITEM)
;	LOCAL:1 += 2
;;가슴이 있다
;SIF TALENT:MASTER:120 == 0
;	LOCAL:1 += 4
;;막대기가 있다
;SIF TALENT:MASTER:120 || TALENT:MASTER:121
;	LOCAL:1 += 8
;
;IF CFLAG:99 == 0
;	PRINTFORML 방에 도망간%조사처리(CALLNAME:MASTER,"는")% 침대에 뛰어들어, 빨리 자려고 했다
;	PRINTFORML 그러나, 두 명의 정사를 본 탓으로, 전혀 몸의 앙는 들어가지 않는다.
;;	;전부 있어
;	IF LOCAL:1 == 15
;		PRINTFORML 어쩔 수 없이%조사처리(CALLNAME:MASTER,"는")%, 2개의 바이브로 비밀의 틈을 문지르고 애액을 친숙해 지게 하면(자)
;		PRINTFORML 그것들을 2개의 구멍의 안쪽까지 밀어넣어, , 최대로 한 진동의 재쾌락에 번민이면서
;		PRINTFORML 장대를 잡아당겨, 유두를 궁리해 위로
;	;A미숙 양성
;	ELSEIF LOCAL:1 == 14
;		PRINTFORML 어쩔 수 없이%조사처리(CALLNAME:MASTER,"는")%, 바이브로 비밀의 틈을 문지르고 애액을 친숙해 지게 하면(자)
;		PRINTFORML 그것을 꿀 단지의 최안쪽까지 밀어넣어, 최대로 한 진동이 가져오는 쾌락에 번민이면서
;		PRINTFORML 장대를 잡아당겨, 유두를 궁리해 위로
;	;V미숙 양성
;	ELSEIF LOCAL:1 == 13
;		PRINTFORML 어쩔 수 없이%조사처리(CALLNAME:MASTER,"는")%, 바이브로 비밀의 틈을 문지르고 애액을 친숙해 지게 하면(자)
;		PRINTFORML 그것을 벌렁거리는고혈에 밀어넣어, 최대로 한 진동이 가져오는 쾌락에 번민이면서
;		PRINTFORML 장대를 잡아당겨, 유두를 궁리해 위로
;	;구멍 미숙 양성
;	ELSEIF LOCAL:1 == 12
;		PRINTFORML 어쩔 수 없이%조사처리(CALLNAME:MASTER,"는")%, 자신의  애액을 윤활제로 해 장대를 잡아당기면서 유두를 궁리해 위로
;	;V는 남자에게는 없기 때문에 11으로 10은 결번
;	;A숙련남
;	ELSEIF LOCAL:1 == 9
;		PRINTFORML 어쩔 수 없이%조사처리(CALLNAME:MASTER,"는")%, 바이브에 로션을 발라 빼앗으면(자)
;		PRINTFORML 그것을 벌렁거리는고혈에 밀어넣어, 최대로 한 진동이 가져오는 쾌락에 번민이면서
;		PRINTFORML 장대를 잡아당기면서도, 어널 바이브로 격렬하게 당신을 공격해대었다
;	;A미숙남
;	ELSEIF LOCAL:1 == 8
;		PRINTFORML 어쩔 수 없이%조사처리(CALLNAME:MASTER,"는")%, 두 명의 정사를 간식에 장대를 호된 훈련 냈다
;	;두 구멍 숙련녀
;	ELSEIF LOCAL:1 == 7
;		PRINTFORML 어쩔 수 없이%조사처리(CALLNAME:MASTER,"는")%, 2개의 바이브로 비밀의 틈을 문지르고, 애액을 친숙해 지게 하면(자)
;		PRINTFORML 그것들을 2개의 구멍의 안쪽까지 밀어넣어, , 최대로 한 진동의 재쾌락에 번민이면서
;		PRINTFORML 유방을 비비어 클리토리스를 문지르고 계속했다
;	;A미숙녀
;	ELSEIF LOCAL:1 == 6
;		PRINTFORML 어쩔 수 없이%조사처리(CALLNAME:MASTER,"는")%, 바이브로 비밀의 틈을 문지르고 애액을 친숙해 지게 하면(자)
;		PRINTFORML 그것을 꿀 단지의 최안쪽까지 밀어넣어, 최대로 한 진동이 가져오는 쾌락에 번민이면서
;		PRINTFORML 유방을 비비어 클리토리스를 문지르고 계속했다
;	;V미숙녀
;	ELSEIF LOCAL:1 == 5
;		PRINTFORML 어쩔 수 없이%조사처리(CALLNAME:MASTER,"는")%, 바이브로 비밀의 틈을 문지르고 애액을 친숙해 지게 하면(자)
;		PRINTFORML 그것을 벌렁거리는고혈에 밀어넣어, 최대로 한 진동이 가져오는 쾌락에 번민이면서
;		PRINTFORML 유방을 비비어 클리토리스를 문지르고 계속했다
;	;구멍 미숙녀
;	ELSEIF LOCAL:1 == 4
;		PRINTFORML 어쩔 수 없이%조사처리(CALLNAME:MASTER,"는")%, 두 명의 정사를 간식에 유방을 비비어, 클리토리스를 문지르고 냈다
;	;가슴도 막대기도 없는 녀석은 몰라
;	ELSE
;		PRINTFORML 어쩔 수 없이%조사처리(CALLNAME:MASTER,"는")%, 두 명의 정사를 간식에 신체 안을 켰다
;	ENDIF
;	PRINTW  
;	PRINTL 
;	PRINTFORML 그러나, 몇 번 절정을 맞이할려고도%CALLNAME:MASTER%의 마음이 채워지는 일은 없었다…….
;ENDIF

;-----------------------------------------------
;반발 노예에 의한, 여자 노예 농락
;----------------------------------------------
@NIGHT_NTR_F, ARG, ARG:1
;다른 것과는 틀려, 여기는 대충이니까 편한 것이다
;(이)라고 생각하면(자) 문장 길어졌다
;행위수지정. 실로 적당.
;어차피 다음에 4로 나눈다면 처음부터 1으로 좋았다
;기본치 12
LOCAL = 12

;습격 사람이 음란하면+4
SIF TALENT:(ARG:1):170
	LOCAL += 4
;습격 사람이 작은 악마라면+4
SIF TALENT:(ARG:1):93
	LOCAL += 4
;습격 사람이음경이라면+4
SIF TALENT:(ARG:1):171
	LOCAL += 4
;피해자가 음란하면+4
SIF TALENT:ARG:170
	LOCAL += 4
;피해자가 음 단지라면+4
SIF TALENT:ARG:172
	LOCAL += 4
;피해자가 친애라면―4
SIF TALENT:ARG:152
	LOCAL -= 4
;피해자가 정조 관념 소유라면―4
SIF TALENT:ARG:30
	LOCAL -= 4

;지문 컷
IF CFLAG:99 == 0
	PRINTFORML 밤 2시 경. %조사처리(CALLNAME:MASTER,"는")% 갑자기 화장실에 가고 싶어, 일을 마치고 돌아오던 길이었다.
	PRINTFORML 문득 %CALLNAME:ARG%의 방에서 불빛이 새어나오는 것이 보였다.
	PRINTFORML %조사처리(CALLNAME:ARG,"는")% 아직 일어나 있는 건가?
	PRINTFORML 혹시나 %조사처리(CALLNAME:MASTER,"를")% 생각하며 자위라도 하고 있는 건가?
	PRINTL 
	PRINTFORML %조사처리(CALLNAME:MASTER,"는")% 싱글싱글 웃으며 방에 다가가보았다.
	PRINTFORML 그랬더니 놀랍게도 방안에선 자위라고는 생각할 수 없는 여성의 교성과 음란한 물소리가 들려오는 것이 아닌가.
	PRINTFORML 두근하고 심장이 빠르게 뛰는 것을 느끼면서 발소리를 죽이고, 열린 문틈으로 안을 훔쳐봤다.
	IF LOCAL > 12
		PRINTFORML 거기에는 스스로 허리를 격렬히 흔들며, 암컷 그 자체의 표정으로 쾌락을 탐하는 %조사처리(CALLNAME:ARG,"과")%
		PRINTFORML %CALLNAME:ARG%의 몸이 밀려버릴 만큼이나 맹렬한 기세로 찔러대는 %CALLNAME:(ARG:1)%의 모습이 있었다.
	ELSEIF LOCAL > 4
		PRINTFORML 거기에는 맹렬히 꿀항아리를 탐하며 허리를 미친듯이 부딪쳐대는 %조사처리(CALLNAME:(ARG:1),"와")%
		PRINTFORML %CALLNAME:(ARG:1)%의 머리를 감싸안고, 스스로도 허리를 꿈틀대는 %CALLNAME:ARG%의 모습이 있었다.
	ELSE
		PRINTFORML 거기에는 여자의 몸을 뒤에서 격렬히 범하고 있는 %조사처리(CALLNAME:(ARG:1),"와")%
		PRINTFORML 그 쾌락을 받아들이며 교성을 지르는 %CALLNAME:ARG%의 모습이 있었다.
	ENDIF
	PRINTFORMW 상상도 못한 엄청난 광경에 %조사처리(CALLNAME:MASTER,"는")%, 서로 교접을 계속하는 두 사람을 훔쳐볼 수밖에 없었다.
ENDIF

;M의 경험 및 주증감 및 순종 및 봉사 정신 저하 처리. 연모, 망신, 질투, 친애, 서로 사랑함, 복종, 예속 소거
;요컨데 다른 막대기 첨부에 네토라레
;(수정) NORMAL/EASY에 대해 서로 사랑함과 예속이 있으면 그것은 사라지지 않고 여러가지 완화된다(일단 둘과도 굉장히 조건 어렵기 때문에)
;00V경험, 02 절정 경험, 04 정액 경험 증가
CALL COMMON_UP_EXP, ARG, 0, LOCAL/2, 0
CALL COMMON_UP_EXP, ARG, 2, LOCAL/4, 0
CALL COMMON_UP_EXP, ARG, 4, LOCAL/4, 0
;주처리. 01쾌V·13 욕망·100 부정의 구슬증가, 12 순종 0
CALL COMMON_UP_JUEL, ARG, 1, LOCAL*250, 0
JUEL:ARG:12 = 0
PRINTFORML %PALAMNAME:12%의 구슬(%CALLNAME:ARG%)은 없어졌다.
CALL COMMON_UP_JUEL, ARG, 13, LOCAL*150, 0
CALL COMMON_UP_JUEL, ARG, 100, LOCAL*200, 0
;네토라레플래그
CFLAG:ARG:153 |= 16
;LOCAL>12라면 반발 3 취득(NORMAL/EASY에 대해 완화)
IF LOCAL > 12
	IF FLAG:3 > 2
		CALL COMMON_MARK_UP, ARG, 9, 3, 0
	ELSEIF MARK:ARG:9 < 2
		CALL COMMON_MARK_UP, ARG, 9, 2, 0
	ENDIF
ENDIF
;LOCAL > 12라면 각 소질과 순종을 0으로 한다. 그 이외라면 순종-1
IF LOCAL > 12
	IF ABL:ARG:10 > 0
		ABL:ARG:10 = 0
		PRINTFORML %ABLNAME:10%(%CALLNAME:ARG%)은 0 레벨이 되었다.
	ENDIF
	IF TALENT:ARG:150
		TALENT:ARG:150 = 0
		PRINTFORML %조사처리(CALLNAME:ARG,"는")% %TALENTNAME:150%를 잃었다.
		CFLAG:ARG:14 -= 1
		CFLAG:ARG:3115 = 0
		CFLAG:MASTER:98 -= 1
		SIF TALENT:ARG:200 && TALENT:ARG:160 == 0 && TALENT:ARG:170 == 0
			FLAG:32 -= 1
	ENDIF
	IF TALENT:ARG:151
		TALENT:ARG:151 = 0
		PRINTFORML %조사처리(CALLNAME:ARG,"는")% %TALENTNAME:151%를 잃었다.
		CFLAG:ARG:14 -= 2
	ENDIF
	IF TALENT:ARG:152
		TALENT:ARG:152 = 0
		PRINTFORML %조사처리(CALLNAME:ARG,"는")% %TALENTNAME:152%를 잃었다.
		CFLAG:ARG:14 -= 4
	ENDIF
	IF TALENT:ARG:153 && FLAG:3 > 2
		TALENT:ARG:153 = 0
		PRINTFORML %조사처리(CALLNAME:ARG,"는")% %TALENTNAME:153%를 잃었다.
		CFLAG:ARG:14 -= 8
		CFLAG:ARG:3113 = 0
	ENDIF
	IF TALENT:ARG:157
		TALENT:ARG:157 = 0
		CFLAG:ARG:12 -= 4096
		PRINTFORML %조사처리(CALLNAME:ARG,"는")% %TALENTNAME:157%를 잃었다.
	ENDIF
	IF TALENT:ARG:160
		TALENT:ARG:160 = 0
		PRINTFORML %조사처리(CALLNAME:ARG,"는")% %TALENTNAME:160%를 잃었다.
		CFLAG:ARG:15 -= 1
		CFLAG:ARG:3115 = 0
		CFLAG:MASTER:98 -= 1
		SIF TALENT:ARG:200 && TALENT:ARG:150 == 0 && TALENT:ARG:170 == 0
			FLAG:32 -= 1
	ENDIF
	IF TALENT:ARG:162 && FLAG:3 > 2
		TALENT:ARG:162 = 0
		PRINTFORML %조사처리(CALLNAME:ARG,"는")% %TALENTNAME:162%를 잃었다.
		CFLAG:ARG:15 -= 4
		CFLAG:ARG:3114 = 0
	ENDIF
ELSEIF LOCAL > 4
	IF ABL:ARG:10 > 0
		ABL:ARG:10 -= 1
		PRINTFORML %ABLNAME:10%(%CALLNAME:ARG%)－1 레벨
	ENDIF
	IF ABL:ARG:10 < 4 && TALENT:ARG:152
		TALENT:ARG:152 = 0
		PRINTFORML %조사처리(CALLNAME:ARG,"는")% %TALENTNAME:152%를 잃었다.
		CFLAG:ARG:14 -= 4
	ENDIF
ENDIF
;범용 말용 플래그
CFLAG:ARG:43 = 0
;습격자측의 경험 처리와 주처리
;02절정, 03 사정 경험 증가
CALL COMMON_UP_EXP, ARG:1, 2, LOCAL/4, 0
CALL COMMON_UP_EXP, ARG:1, 3, LOCAL/4, 0
;LUNATIC 이상은 사정할 때마다 체력이 감소
CALL EXHAUST_EJAC, ARG:1, LOCAL/4
;구슬 취득 00쾌C·13 욕망 증가, 100 부정 감소
CALL COMMON_UP_JUEL, ARG:1, 0, LOCAL*250, 0
CALL COMMON_UP_JUEL, ARG:1, 13, LOCAL*150, 0
CALL COMMON_DOWN_JUEL, ARG:1, 100, 500*(2+MARK:(ARG:1):9), 0
PRINTFORML 평소의 울분을 풀었기에, %PALAMNAME:100%의 구슬은 줄어들었다. (%CALLNAME:(ARG:1)%)
PRINTL 
;잠 취한 플래그
CFLAG:ARG:153 |= 32
CFLAG:MASTER:153 |= 4096

;지문 컷
IF CFLAG:99 == 0
	IF TALENT:120 == 0 && TALENT:121 == 0
		PRINTFORML 그럴 수가… %CALLNAME:(ARG:1)%한텐 남근 따위 나있지 않았을 텐데
		PRINTFORML 그렇게 생각하면서도 %조사처리(CALLNAME:MASTER,"가")% 선정적인 광경에서 눈을 돌리지 못하고 있자
	ELSE
		PRINTFORML %조사처리(CALLNAME:MASTER,"가")% 선정적인 광경에서 눈을 돌리지 못하고 있자 
	ENDIF
	PRINTFORML 본의아니게 %조사처리(CALLNAME:(ARG:1),"와")% 눈이 마주치게 되었다.
	PRINTFORMW 생긋 웃는 %CALLNAME:(ARG:1)%에게, %조사처리(CALLNAME:MASTER,"는")% 엄청난 비명을 지르며 그 자리에서 도망쳐나갔다.
;	PRINTL 
ENDIF

;붙지 않았어들 기른다
IF TALENT:120 == 0 && TALENT:121 == 0
	TALENT:121 = 1
	CALL CHANGE_FUTA_COMMON, TARGET
ENDIF

;LOCAL:1 = 0
;;A경험로 어널 바이브 소지
;SIF EXP:MASTER:1 > 12 && (ITEM:9 || NOITEM)
;	LOCAL:1 += 1
;;비처녀 한편 바이브 소지
;SIF TALENT:MASTER:0 == 0 && (ITEM:3 || NOITEM)
;	LOCAL:1 += 2
;;가슴이 있다
;SIF TALENT:MASTER:120 == 0
;	LOCAL:1 += 4
;;막대기가 있다
;SIF TALENT:MASTER:120 || TALENT:MASTER:121
;	LOCAL:1 += 8
;
;IF CFLAG:99 == 0
;	PRINTFORML 방에 도망간%조사처리(CALLNAME:MASTER,"는")% 침대에 뛰어들어, 빨리 자려고 했다
;	PRINTFORML 그러나, 두 명의 정사를 본 탓으로, 전혀 몸의 앙는 들어가지 않는다.
;	;전부 있어
;	IF LOCAL:1 == 15
;		PRINTFORML 어쩔 수 없이%조사처리(CALLNAME:MASTER,"는")%, 2개의 바이브로 비밀의 틈을 문지르고 애액을 친숙해 지게 하면(자)
;		PRINTFORML 그것들을 2개의 구멍의 안쪽까지 밀어넣어, , 최대로 한 진동의 재쾌락에 번민이면서
;		PRINTFORML 장대를 잡아당겨, 유두를 궁리해 위로
;	;A미숙 양성
;	ELSEIF LOCAL:1 == 14
;		PRINTFORML 어쩔 수 없이%조사처리(CALLNAME:MASTER,"는")%, 바이브로 비밀의 틈을 문지르고 애액을 친숙해 지게 하면(자)
;		PRINTFORML 그것을 꿀 단지의 최안쪽까지 밀어넣어, 최대로 한 진동이 가져오는 쾌락에 번민이면서
;		PRINTFORML 장대를 잡아당겨, 유두를 궁리해 위로
;	;V미숙 양성
;	ELSEIF LOCAL:1 == 13
;		PRINTFORML 어쩔 수 없이%조사처리(CALLNAME:MASTER,"는")%, 바이브로 비밀의 틈을 문지르고 애액을 친숙해 지게 하면(자)
;		PRINTFORML 그것을 벌렁거리는고혈에 밀어넣어, 최대로 한 진동이 가져오는 쾌락에 번민이면서
;		PRINTFORML 장대를 잡아당겨, 유두를 궁리해 위로
;	;구멍 미숙 양성
;	ELSEIF LOCAL:1 == 12
;		PRINTFORML 어쩔 수 없이%조사처리(CALLNAME:MASTER,"는")%, 자신의  애액을 윤활제로 해 장대를 잡아당기면서 유두를 궁리해 위로
;	;V는 남자에게는 없기 때문에 11으로 10은 결번
;	;A숙련남
;	ELSEIF LOCAL:1 == 9
;		PRINTFORML 어쩔 수 없이%조사처리(CALLNAME:MASTER,"는")%, 바이브에 로션을 발라 빼앗으면(자)
;		PRINTFORML 그것을 벌렁거리는고혈에 밀어넣어, 최대로 한 진동이 가져오는 쾌락에 번민이면서
;		PRINTFORML 장대를 잡아당기면서도, 어널 바이브로 격렬하게 당신을 공격해대었다
;	;A미숙남
;	ELSEIF LOCAL:1 == 8
;		PRINTFORML 어쩔 수 없이%조사처리(CALLNAME:MASTER,"는")%, 두 명의 정사를 간식에 장대를 호된 훈련 냈다
;	;두 구멍 숙련녀
;	ELSEIF LOCAL:1 == 7
;		PRINTFORML 어쩔 수 없이%조사처리(CALLNAME:MASTER,"는")%, 2개의 바이브로 비밀의 틈을 문지르고, 애액을 친숙해 지게 하면(자)
;		PRINTFORML 그것들을 2개의 구멍의 안쪽까지 밀어넣어, , 최대로 한 진동의 재쾌락에 번민이면서
;		PRINTFORML 유방을 비비어 클리토리스를 문지르고 계속했다
;	;A미숙녀
;	ELSEIF LOCAL:1 == 6
;		PRINTFORML 어쩔 수 없이%조사처리(CALLNAME:MASTER,"는")%, 바이브로 비밀의 틈을 문지르고 애액을 친숙해 지게 하면(자)
;		PRINTFORML 그것을 꿀 단지의 최안쪽까지 밀어넣어, 최대로 한 진동이 가져오는 쾌락에 번민이면서
;		PRINTFORML 유방을 비비어 클리토리스를 문지르고 계속했다
;	;V미숙녀
;	ELSEIF LOCAL:1 == 5
;		PRINTFORML 어쩔 수 없이%조사처리(CALLNAME:MASTER,"는")%, 바이브로 비밀의 틈을 문지르고 애액을 친숙해 지게 하면(자)
;		PRINTFORML 그것을 벌렁거리는고혈에 밀어넣어, 최대로 한 진동이 가져오는 쾌락에 번민이면서
;		PRINTFORML 유방을 비비어 클리토리스를 문지르고 계속했다
;	;구멍 미숙녀
;	ELSEIF LOCAL:1 == 4
;		PRINTFORML 어쩔 수 없이%조사처리(CALLNAME:MASTER,"는")%, 두 명의 정사를 간식에 유방을 비비어, 클리토리스를 문지르고 냈다
;	;가슴도 막대기도 없는 녀석은 몰라
;	ELSE
;		PRINTFORML 어쩔 수 없이%조사처리(CALLNAME:MASTER,"는")%, 두 명의 정사를 간식에 신체 안을 켰다
;	ENDIF
;	PRINTW  
;	PRINTL 
;	PRINTFORML 그러나, 몇 번 절정을 맞이할려고도%CALLNAME:MASTER%의 마음이 채워지는 일은 없었다…….
;ENDIF

;-----------------------------------------------
;반발 노예에 의한, 본래의 덥침
;----------------------------------------------
@NIGHT_GOUKAN_S, ARG, ARG:1, ARG:2
;ARG:1이 강간하는 사람
;ARG가 강간되는 사람
;행위수지정. 주인 상대라면 8. 노예의 경우, 음란하지 않으면 4
LOCAL = 8
SIF ARG != MASTER && TALENT:(ARG:1):170 == 0
	LOCAL = 4

;지문 컷
SIF CFLAG:99 == 0
	PRINTFORMW 밤 2시 경……%조사처리(CALLNAME:(ARG:1),"가")% 이미 잠들어버린 %CALLNAME:ARG%에게 찾아왔다.
;지문 컷
IF CFLAG:99 == 0
	PRINTFORM %조사처리(CALLNAME:(ARG:1),"는")% 
	IF 90 >= GET_AGG(ARG:1)
		PRINT 주저주저하며 방으로 침입하더니 
	ELSE
		PRINT 당당히 방안으로 들어오더니 
	ENDIF
	PRINTFORML %CALLNAME:ARG%의 침대 속으로 파고들었다.
ENDIF
PRINTL 
;없으면 기른다
IF TALENT:120 == 0 && TALENT:121 == 0
	TALENT:121 = 1
	CALL CHANGE_FUTA_COMMON, TARGET
	;지문 컷
	SIF CFLAG:99 == 0
		PRINTL 이날을 위해 약으로 나게 만든 물건을 손으로 비벼 크게 하며 
ENDIF
;조건적으로 주인이다
IF TALENT:ARG:120
	CALL SHIFT_TO_F, MASTER
	;지문 컷
	SIF CFLAG:99 == 0
		PRINTFORML %조사처리(CALLNAME:(ARG:1),"는")%, 자고 있는 %CALLNAME:ARG%의 입 안에 〔무지개 사탕〕을 넣었다.
	PRINTFORML [놀랍게도 %조사처리(CALLNAME:ARG,"는")% 여성의 몸이 되어버렸다]
	;지문 컷
	SIF CFLAG:99 == 0
		PRINTFORML %조사처리(CALLNAME:(ARG:1),"는")%, 사탕이 효과를 발휘했다는 것을 확인하고는 속옷을 벗기고 
ELSE
	;지문 컷
	SIF CFLAG:99 == 0
		PRINTFORML %조사처리(CALLNAME:(ARG:1),"는")%, 자고 있는 %CALLNAME:ARG%의 속옷을 옆으로 치우고 
ENDIF
;지문 컷
IF CFLAG:99 == 0
	;주인이 처녀
	IF TALENT:ARG:0 >= 1
		PRINTFORML 바기나를 관찰하는 듯이 살짝 벌려보고, 
		IF TALENT:150
			;재생 처녀의 경우
			IF TALENT:ARG:0 > 1
				PRINTL 자신을 위해서 다시 재생한것이겠지 라고 생각하며
				PRINTL 바기나에 우뚝 솟은 물건을 대고, 단숨에 꿰뚫었다.
				PRINTL 　
				PRINTFORML 하복부의 격렬한 통증에 눈을 뜬 %조사처리(CALLNAME:ARG,"는")%
			ELSE
				PRINTFORML 사랑스러운 사람의 순결을 오늘이야말로 받을 수 있는 것이라고 생각하며 도취한 눈으로 %조사처리(CALLNAME:ARG,"를")% 응시하고
				PRINTL 더러움을 모르는 보지에 우뚝 솟은 물건을 대더니, 단숨에 꿰뚫었다.
				PRINTL 　
				PRINTFORML 느껴본 적 없는 격통에 눈을 뜬 %조사처리(CALLNAME:ARG,"는")% 
			ENDIF
		ELSE
			;재생 처녀의 경우
			IF TALENT:ARG:0 > 1
				PRINTL 약에 의지해서라도 잊고 싶었을거라 라고 생각하며
				PRINTL 바기나에 우뚝 솟은 물건을 대고, 단숨에 꿰뚫었다.
				PRINTL 　
				PRINTFORML 하복부의 격렬한 통증에 눈을 뜬 %조사처리(CALLNAME:ARG,"는")%
			ELSE
				PRINTL 더러움을 모르는 보지에 우뚝 솟은 물건을 대더니, 단숨에 꿰뚫었다.
				PRINTL 　
				PRINTFORML 느껴본 적 없는 격통에 눈을 뜬 %조사처리(CALLNAME:ARG,"는")% 
			ENDIF
		ENDIF
		PRINTFORML %CALLNAME:TARGET%의 맹렬함에 공포를 느끼고 필사적으로 그만둘 것을 애원해봤지만
		PRINTFORML 오히려 반대로 그 행동은 %CALLNAME:TARGET%의 가학심을 부추켜, 행위는 더욱 격렬함을 늘려갈 뿐이었다…….
	ELSE
		SIF TALENT:(ARG:1):150
			PRINTFORML 몹시 사랑스럽게 %조사처리(CALLNAME:ARG,"를")% 보더니
		PRINTL 아직 젖지도 않은 보지에 자지를 대더니, 단숨에 꿰뚫었다.
		PRINTL 　
		PRINTFORML 하복부의 격통에 눈을 뜬 %조사처리(CALLNAME:MASTER,"는")% 필사적으로 저항해봤지만
		IF TALENT:120
			PRINTFORML 남자의 강한 힘에 짓눌려서, 그 저항은 오히려 %CALLNAME:TARGET%의 욕정을 부추킬 뿐이었다.
		ELSE
			PRINTFORML 교묘하게 눌려 있어서, 그 저항은 오히려 %CALLNAME:TARGET%의 욕정을 부추킬 뿐이었다.
		ENDIF
		PRINTFORML 이윽고 저항을 포기한 %조사처리(CALLNAME:MASTER,"는")%, 잠시 %CALLNAME:TARGET%에게 몸을 맡기기로 했다…….
	ENDIF
ENDIF
PRINTL 
IF ARG == MASTER
	;자고 있는 곳(중)을 습격하는 이벤트 대사 호출
	CALL SELF_KOJO, ARG:1, 421
ELSE
	;다른 노예를 범하는 이벤트 대사 호출
	CALL SELF_KOJO, ARG:1, 434
	;다른 노예에 범해지는 이벤트 대사 호출
	CALL SELF_KOJO, ARG, 431
ENDIF
PRINTL 
;80강간 경험
CALL COMMON_UP_EXP, ARG:1, 80, 1, 0
;81피강간 경험
CALL COMMON_UP_EXP, ARG, 81, 1, 0
;83피간쾌락 경험
SIF ABL:ARG:1 > 2
	CALL COMMON_UP_EXP, ARG, 83, LOCAL, 0
;처녀
IF TALENT:ARG:0
	TALENT:ARG:0 = 0
	CALL SETCOLOR_TORIKOMODE, "RED", 3
	PRINTFORML 【처녀 상실】(%CALLNAME:ARG%)
	RESETCOLOR
	;첫경험 기록
	IF CFLAG:ARG:500 == 0
		CFLAG:ARG:500 = NO:(ARG:1) + 1
		CSTR:ARG:5 = %CALLNAME:(ARG:1)%
	ENDIF
	IF ARG == MASTER
		;주인의 처녀를 빼앗은 플래그
		CFLAG:153 |= 1
		CFLAG:ARG:153 |= 4
		;정조관념 소유이다면 마조끼 감소 새드끼 증가. 초M와 마조가 있으면 소멸, 광기 취득
		IF TALENT:ARG:30
			ABL:ARG:21 = MAX(ABL:ARG:21 - 1, 0)
			ABL:ARG:20 = MIN(ABL:ARG:20 + 1, 5)
			TALENT:ARG:82 = 0
			TALENT:ARG:178 = 0
			TALENT:ARG:158 = 1
		;주인이 정조무구애도 정조 관념도 없으면 광기 취득
		ELSEIF TALENT:ARG:31 == 0 && TALENT:ARG:158 == 0
			TALENT:ARG:158 = 1
		ENDIF
	ENDIF
ENDIF
;00V경험
CALL COMMON_UP_EXP, ARG, 0, LOCAL, 0

IF ARG != MASTER
	;02절정 경험
	CALL COMMON_UP_EXP, ARG, 2, LOCAL/4, 0
	CALL COMMON_UP_EXP, ARG, 4, LOCAL/2, 0
ENDIF
IF ARG == MASTER
	;타락매번 상승
	CFLAG:MASTER:66 += LOCAL * 10
ELSE
	;01쾌V의 구슬
	CALL COMMON_UP_JUEL, ARG, 1, LOCAL*80*ABL:ARG:1, 0
	;13욕정의 구슬
	CALL COMMON_UP_JUEL, ARG, 13, LOCAL*50*ABL:ARG:1, 0
	;100부정의 구슬
	CALL COMMON_UP_JUEL, ARG, 100, LOCAL*500, 0
ENDIF
;03사정 경험
CALL COMMON_UP_EXP, ARG:1, 3, LOCAL/2, 0
;LUNATIC 이상은 사정할 때마다 체력이 감소
CALL EXHAUST_EJAC, TARGET, LOCAL/2
;00쾌C의 구슬
CALL COMMON_UP_JUEL, ARG:1, 0, LOCAL*400, 0
;13욕정의 구슬
CALL COMMON_UP_JUEL, ARG:1, 13, LOCAL*250, 0
;100부정의 구슬
CALL COMMON_DOWN_JUEL, ARG:1, 100, 500*(2+MARK:(ARG:1):9), 0
PRINTL 
;습격자의 버리기 대사의 이벤트 대사 호출
LOCAL = (ARG == MASTER) ? 420 # 430
CALL SELF_KOJO, ARG:1, LOCAL
;지문 컷
SIF CFLAG:99 == 0
	PRINTFORML 그리고 %CALLNAME:ARG%의 질내에 {ARG:2}번 정액을 토해내고는, 만족한 듯한 모습으로 돌아갔다…….

;-----------------------------------------------
;반발 노예에 의한, 윤간
;----------------------------------------------
@NIGHT_RINKAN, ARG, ARG:1, ARG:2, ARG:3, ARG:4, ARG:5, ARG:6
;계속 피해자는 ARG
;습격자는 ARG:1 ARG:3 ARG:4 + (ARG:2)
;피해자는이 ARG:6 = 1이라면 노예, 0이라면 주인
;프랑 짱은 노예 윤간은 하지 않는다. 성격적으로는 할 것 같지만, 뭐라 할까 처리의 사정으로.
IF ARG:6 == 1
	;즉시 붙이면 찬다. 붉지 않아도 행위수 3배
	;(이었)였던 것이지만, 밤새 걸어 이것이라면 지금 이순간(? )이므로 10배.
	;너무 할지도 모르지만, 뭐의 것은 시험이라고 하는 것으로.
	ARG:5 = ARG:5*10
	;지문 컷
	IF CFLAG:99 == 0
		IF ARG == MASTER
			PRINTFORML 그날 밤, 돌연 %조사처리(CALLNAME:(ARG:1),"와")% %조사처리(CALLNAME:(ARG:3),"와")% %조사처리(CALLNAME:(ARG:2),"와")% %조사처리(CALLNAME:(ARG:4),"가")% 방으로 왔다.
		ELSE
			PRINTFORML 그날 밤, 돌연 %조사처리(CALLNAME:(ARG:1),"와")% %조사처리(CALLNAME:(ARG:3),"와")% %조사처리(CALLNAME:(ARG:4),"가")%%CALLNAME:ARG%의 방으로 왔다.
		ENDIF
		PRINTFORML %조사처리(CALLNAME:ARG,"는")% 방문자들의 우호적이라고는 보이지 않는 태도에 어서 내쫓으려고 했지만
		PRINTFORMW %조사처리(CALLNAME:(ARG:1),"가")% 방 안에 억지로 밀고 들어온 것을 신호탄으로, 일제히 몰려들어와 포위되고 말았다.
	ENDIF
	IF ARG == MASTER
		;복수 노예에 의한 윤간 이벤트 대사 호출
		REPEAT 4
			CALL SELF_KOJO, ARG:(COUNT + 1), 423
		REND
	ELSE
		;다른 노예를 윤간하는(435)/다른 노예에 윤간되는(432) 이벤트 대사 호출
		CALL SELF_KOJO, ARG:1, 435
		CALL SELF_KOJO, ARG:3, 435
		CALL SELF_KOJO, ARG:4, 435
		CALL SELF_KOJO, ARG, 432
	ENDIF
	PRINTL 
	;지문 컷
	IF CFLAG:99 == 0
		IF TALENT:ARG:0 > 0
			;실제로 플레이 해 보았더니 생각한 만큼 에로틱 없었기 때문에 수정.
			PRINTFORML \@(ARG == MASTER) ? 네 # 세\@ 사람은, 공포에 떨며 저항하는 %조사처리(CALLNAME:ARG,"를")% 짓누르고 의복을 찢어내더니 
			PRINTL 처녀의 증거를 확인하고 일제히 잔인한 표정을 짓고는 
			PRINTFORML 손에 든 특대 사이즈의 바이브로 용서 없이 %조사처리(CALLNAME:ARG,"를")% 꿰뚫었다.
			PRINTFORML 그리고 %조사처리(CALLNAME:ARG,"가")% 고통에 의식을 잃으려고 하자, 때려서 깨우더니
			PRINTFORML %CALLNAME:ARG%의 질내를 서로 차례를 바꿔가며 마구잡이로 유린했다.
		ELSE
			PRINTFORML \@(ARG == MASTER) ? 네 # 세\@ 사람은, 공포에 떨며 저항하는 %조사처리(CALLNAME:ARG,"를")% 짓누르고 의복을 찢어내더니 
			PRINTFORML 질이나 어널이나 입을 마구 범하고, 유방이나 \@(TALENT:ARG:121) ? 페니스 # 클리토리스\@를 만지작거려 억지로 절정까지 이끌었다.
			PRINTFORML 그리고 부정의 말을 입에 담으면서도 쾌락에 날뛰는 %조사처리(CALLNAME:ARG,"를")% 비웃으면서
			PRINTL 자기들의 정력이 말라버릴 때까지 몇 번이고 몇 번이고 욕망을 방출시켰다.
		ENDIF
		PRINTL 
	ENDIF
	;80강간 경험
	CALL COMMON_UP_EXP, ARG:1, 80, 1, 0
	CALL COMMON_UP_EXP, ARG:3, 80, 1, 0
	CALL COMMON_UP_EXP, ARG:4, 80, 1, 0
	SIF ARG == MASTER
		CALL COMMON_UP_EXP, ARG:2, 80, 1, 0
	;82피윤간 경험
	CALL COMMON_UP_EXP, ARG, 82, 10, 0
	;83피간쾌락 경험
	IF TALENT:ARG:0 > 0
		SIF ABL:ARG:1 > 2
			CALL COMMON_UP_EXP, ARG, 83, ARG:5*4, 0
	ELSE
		SIF ABL:ARG:0 > 2 || ABL:ARG:1 > 2 || ABL:ARG:2 > 2 || ABL:ARG:3 > 2
			CALL COMMON_UP_EXP, ARG, 83, ARG:5*4, 0
	ENDIF
	IF TALENT:ARG:0 > 0
		TALENT:ARG:0 = 0
		CALL SETCOLOR_TORIKOMODE, "RED", 3
		PRINTFORML 【처녀 상실】(%CALLNAME:ARG%)
		RESETCOLOR
		;첫경험 기록
		IF CFLAG:ARG:500 == 0
			CFLAG:ARG:500 = NO:(ARG:1) + 1
			CSTR:ARG:5 = %CALLNAME:(ARG:1)%
		ENDIF
		;00V경험
		CALL COMMON_UP_EXP, ARG, 0, ARG:5*4, 0
		IF ARG == MASTER
			;타락매번 상승
			CFLAG:MASTER:66 += ARG:5 * 20
			;주인의 처녀를 빼앗은 플래그
			CFLAG:ARG:253 |= 1
			CFLAG:(ARG:3):153 |= 1
			CFLAG:(ARG:2):153 |= 1
			CFLAG:(ARG:4):153 |= 1
			CFLAG:ARG:153 |= 4
			;prot8가 된다고 하는 일이므로, 여기의 부분은 망가진 느낌으로 할까하고.
			;뭐 고정적의 광기와 겁장이, 정신 붕괴 근처에서. 가지고 있을 생각키, 반항적, 거만함은 소멸.
			;……일부러 CSV 만지작거려 소질 붙인 다음, 리로드를 반복해 이 이벤트를 보는 사람은 있었던 것일까.
			IF TALENT:ARG:30
				TALENT:ARG:10 = 1
				TALENT:ARG:158 = 1
				TALENT:ARG:167 = 1
				TALENT:ARG:11 = 0
				TALENT:ARG:12 = 0
				TALENT:ARG:15 = 0
			;주인이 정조무구애도 정조 관념도 없으면 광기 취득
			ELSEIF TALENT:ARG:31 == 0 && TALENT:ARG:158 == 0
				TALENT:ARG:158 = 1
			ENDIF
		ELSE
			;02절정 경험
			CALL COMMON_UP_EXP, ARG, 2, ARG:5, 0
			;04정액 경험
			CALL COMMON_UP_EXP, ARG, 4, ARG:5*3, 0
			;100부정의 구슬
			CALL COMMON_UP_JUEL, ARG, 100, 10000, 0
		ENDIF

	ELSE
		IF ARG == MASTER
			;타락매번 상승
			CFLAG:MASTER:66 += ARG:5 * 10
			;귀찮아서 메인 덥침 대상이라는 보고 기교 판정한다.
			;00V경험
			CALL COMMON_UP_EXP, ARG, 0, ARG:5*2, 0
			;01A경험
			CALL COMMON_UP_EXP, ARG, 1, ARG:5*2, 0
			IF TALENT:ARG:66
				;08정음절정 경험
				CALL COMMON_UP_EXP, ARG, 8, ARG:5, 0
			ENDIF
		ELSE
			;00V경험
			CALL COMMON_UP_EXP, ARG, 0, ARG:5*2, 0
			;01A경험
			CALL COMMON_UP_EXP, ARG, 1, ARG:5*2, 0
			;02절정 경험
			CALL COMMON_UP_EXP, ARG, 2, ARG:5, 0
			;04정액 경험
			CALL COMMON_UP_EXP, ARG, 4, ARG:5*3, 0
			IF TALENT:ARG:66
				;08정음절정 경험
				CALL COMMON_UP_EXP, ARG, 8, ARG:5, 0
			ENDIF
			;43A쾌락 경험
			CALL COMMON_UP_EXP, ARG, 43, ARG:5*5, 0
			;44B쾌락 경험
			CALL COMMON_UP_EXP, ARG, 44, ARG:5*5, 0
			;00쾌C의 구슬
			CALL COMMON_UP_JUEL, ARG, 0, ARG:5*300, 0
			;01쾌V의 구슬
			CALL COMMON_UP_JUEL, ARG, 1, ARG:5*300, 0
			;02쾌A의 구슬
			CALL COMMON_UP_JUEL, ARG, 2, ARG:5*300, 0
			;03쾌B의 구슬
			CALL COMMON_UP_JUEL, ARG, 3, ARG:5*300, 0
			;13욕정의 구슬
			CALL COMMON_UP_JUEL, ARG, 13, ARG:5*800, 0
			;17공포의 구슬
			CALL COMMON_UP_JUEL, ARG, 17, ARG:5*400, 0
			;100부정의 구슬
			CALL COMMON_UP_JUEL, ARG, 100, ARG:5*700, 0
		ENDIF
	ENDIF
	;03사정 경험
	CALL COMMON_UP_EXP, ARG:1, 3, ARG:5/2, 0
	SIF ARG == MASTER
		CALL COMMON_UP_EXP, ARG:2, 3, ARG:5/2, 0
	CALL COMMON_UP_EXP, ARG:3, 3, ARG:5/2, 0
	CALL COMMON_UP_EXP, ARG:4, 3, ARG:5/2, 0
	SIF ARG == MASTER
		CALL COMMON_UP_EXP, ARG:2, 3, ARG:5/2, 0
	;LUNATIC 이상은 사정할 때마다 체력이 감소
	CALL EXHAUST_EJAC, TARGET, ARG:5/2
	CALL EXHAUST_EJAC, ARG:3, ARG:5/2
	SIF ARG == MASTER
		CALL EXHAUST_EJAC, ARG:2, ARG:5/2
	CALL EXHAUST_EJAC, ARG:4, ARG:5/2
	;00쾌C의 구슬
	CALL COMMON_UP_JUEL, ARG:1, 0, ARG:5*300, 0
	;13욕정의 구슬
	CALL COMMON_UP_JUEL, ARG:1, 13, ARG:5*700, 0
	CALL COMMON_DOWN_JUEL, ARG:1, 100, 500*(2+MARK:(ARG:1):9), 0
	IF ARG == MASTER
		;00쾌C의 구슬
		CALL COMMON_UP_JUEL, ARG:2, 0, ARG:5*300, 0
		;13욕정의 구슬
		CALL COMMON_UP_JUEL, ARG:2, 13, ARG:5*700, 0
		;100부정의 구슬
		CALL COMMON_DOWN_JUEL, ARG:2, 100, 500*(2+MARK:(ARG:2):9), 0
	ENDIF
	;00쾌C의 구슬
	CALL COMMON_UP_JUEL, ARG:3, 0, ARG:5*300, 0
	;13욕정의 구슬
	CALL COMMON_UP_JUEL, ARG:3, 13, ARG:5*700, 0
	;100부정의 구슬
	CALL COMMON_DOWN_JUEL, ARG:3, 100, 500*(2+MARK:(ARG:3):9), 0
	;00쾌C의 구슬
	CALL COMMON_UP_JUEL, ARG:4, 0, ARG:5*300, 0
	;13욕정의 구슬
	CALL COMMON_UP_JUEL, ARG:4, 13, ARG:5*700, 0
	;100부정의 구슬
	CALL COMMON_DOWN_JUEL, ARG:4, 100, 500*(2+MARK:(ARG:4):9), 0
	PRINTL 
	IF ARG == MASTER
		;버리기 대사의 이벤트 대사 호출
		REPEAT 4
			CALL SELF_KOJO, ARG:(COUNT + 1), 420
		REND
	ELSE
		;습격자의 버리기 대사의 이벤트 대사 호출
		CALL SELF_KOJO, ARG:1, 430
		CALL SELF_KOJO, ARG:3, 430
		CALL SELF_KOJO, ARG:4, 430
	ENDIF
	PRINTL 
	;지문 컷
	IF CFLAG:99 == 0
		PRINTFORML 영원히 계속될 것만 같았던 능욕극도, 날이 밝을 무렵이 되자 끝을 맞이하고
		PRINTFORML 방 안에는 전신이 허옇게 물들어, 실이 끊어진 인형같이 되어버린 %CALLNAME:ARG%만이 남아 있었다…….
	ENDIF
;그 이외. 즉 플랑쨩의 「혼자서 윤간할 수 있는 걸」. 그렇다고 하면서 완구로 할 뿐.
;결코 편애는 아니다. Rev 프랑에는 상당히 빠뜨릴 수 있어 받았지만, 결코 편애는 아니다
;(이)나후아 넣고 있으면, 프랑의 반발3=사망 플래그, 였던 것 같은 생각이 들지 않는 것도 아니겠지만. 뭐 신경쓰지 말고 좋지요?
ELSE
	;플랑쨩 노력해 10배.
	ARG:5 = ARG:5*10
	;지문 컷
	IF CFLAG:99 == 0
		PRINTFORML 그날 밤, 갑자기 %조사처리(CALLNAME:TARGET,"가")% 방으로 찾아왔다.
		PRINTFORML %조사처리(CALLNAME:MASTER,"는")% 밤도 늦었겠다, 게다가 뭔가 불길한 예감도 들기에 내보내려고 했지만
		PRINTFORMW %조사처리(CALLNAME:TARGET,"과")% 눈이 마주치자, 갑자기 몸이 돌처럼 굳어 움직일 수 없게 되어버렸다.
	ENDIF
	;플랑쨩전용의 윤간 이벤트 대사 호출
	CALL SELF_KOJO, TARGET, 424
	PRINTL 
	;지문 컷
	IF CFLAG:99 == 0
		PRINTFORML 움직일 수 없는 %조사처리(CALLNAME:MASTER,"를")% 곁눈질로 보더니, 넷으로 분신한 %조사처리(CALLNAME:TARGET,"는")% 방을 한 바퀴 둘러보고는
		PRINTFORML 잔인한 미소를 띠며 %CALLNAME:MASTER%의 옷을 천천히 한 장 한 장 벗기기 시작했다.
		PRINTFORML 그리고 실 한오라기 걸치지 않은 모습이 된 %조사처리(CALLNAME:MASTER,"를")% 침대에 눕히더니……
		PRINTL 　
		PRINTFORML 끔찍하게도 옆에 놓여있던 의자 다리를 분질러서 %CALLNAME:MASTER%의 바기나에 냅다 꽃았다.
		PRINTFORML 목소리조차 내지 못하는 %조사처리(CALLNAME:MASTER,"는")% \@(TALENT:MASTER:0 > 0) ? 파과의 아픔 # 전희조차 없이 이물이 삽입\@에 울지도 못하고
		PRINTFORML 그저 계속해서 %CALLNAME:TARGET%의 폭력적인 행위를 받아들일 수밖에 없었다…….
		PRINTL 
	ENDIF
	;80강간 경험
	CALL COMMON_UP_EXP, TARGET, 80, 1, 0
	;81피강간 경험
	CALL COMMON_UP_EXP, MASTER, 81, 1, 0
	;83피간쾌락 경험
	SIF EXP:MASTER:32 > 100
		CALL COMMON_UP_EXP, MASTER, 83, ARG:5, 0
	IF TALENT:MASTER:0 > 0
		TALENT:MASTER:0 = 0
		CALL SETCOLOR_TORIKOMODE, "RED", 3
		PRINTFORML 【처녀 상실】(%CALLNAME:MASTER%)
		RESETCOLOR
		;첫경험 기록
		IF CFLAG:MASTER:500 == 0
			CFLAG:MASTER:500 = NO:TARGET + 1
			CSTR:MASTER:5 = %CALLNAME:TARGET%
		ENDIF
		;주인의 처녀를 빼앗은 플래그
		CFLAG:153 |= 1
		CFLAG:MASTER:153 |= 4
		;prot8가 된다고 하는 일이므로, 여기의 부분은 망가진 느낌으로 할까하고.
		;뭐 고정적의 광기와 겁장이, 정신 붕괴 근처에서. 가지고 있을 생각키, 반항적, 거만함은 소멸.
		;……일부러 CSV 만지작거려 소질 붙인 다음, 리로드를 반복해 이 이벤트를 보는 사람은 있었던 것일까.
		IF TALENT:MASTER:30
			TALENT:MASTER:10 = 1
			TALENT:MASTER:158 = 1
			TALENT:MASTER:167 = 1
			TALENT:MASTER:11 = 0
			TALENT:MASTER:12 = 0
			TALENT:MASTER:15 = 0
		;주인이 정조무구애도 정조 관념도 없으면 광기 취득
		ELSEIF TALENT:MASTER:31 == 0 && TALENT:MASTER:158 == 0
			TALENT:MASTER:158 = 1
		ENDIF
	ENDIF
	;00V경험
	CALL COMMON_UP_EXP, MASTER, 0, (ARG:5*20), 0
	;32V확장 경험(의자 외에 무엇을 넣을 수 있었는가는 상상에 맡깁니다)
	CALL COMMON_UP_EXP, MASTER, 32, (ARG:5*4), 0
	;50이상 경험
	CALL COMMON_UP_EXP, MASTER, 50, (ARG:5*4), 0
	;13욕정의 구슬
	CALL COMMON_UP_JUEL, TARGET, 13, (ARG:5*700), 0
	;100부정의 구슬
	CALL COMMON_DOWN_JUEL, TARGET, 100, 500*(2+MARK:9), 0
	PRINTFORML 평소의 울분을 풀었기에, %PALAMNAME:100%의 구슬은 줄어들었습니다.
	;버리기 대사의 이벤트 대사 호출
	CALL SELF_KOJO, TARGET, 420
	;지문 컷
	IF CFLAG:99 == 0
		PRINTFORML 세 사람이 차례로 박혀있는 의자다리를 쑤시고, 또 한 사람이 %CALLNAME:MASTER%의 질을 이물로 계속해서 유린했다.
		PRINTFORML 사람의 것이라고는 생각할 수 없을만큼 흉악한 놀이는, 흡혈귀의 활동시간이 끝나는 새벽녘까지 끊임없이 계속되었다…….
	ENDIF
	IF ARG == MASTER
		;타락매번 상승
		CFLAG:MASTER:66 += ARG:5 * 20
	ENDIF
ENDIF

;-----------------------------------------------
;반발 노예에 의한, 폭행
;----------------------------------------------
@NIGHT_BOUKOU, ARG, ARG:1, ARG:2, ARG:3, ARG:4, ARG:5
;짧고 미안. 분명히 지쳐 왔다
;ARG:1+ARG:4 ARG:3+ARG:2가 폭행하는 사람
;ARG가 폭행되는 사람
;ARG:5가 7 or8라면 집단 폭행이 된다
;행위수지정. 주인 상대라면 8. 노예의 경우, 새디즘이 아니면 4
LOCAL = 8
SIF ARG != MASTER && TALENT:(ARG:1):81 == 0
	LOCAL = 4

;지문 컷
IF CFLAG:99 == 0
	PRINTFORML 그날 밤, %조사처리(CALLNAME:ARG,"는")% 복도를 걷다가 갑자기 뒤에서 구타당했다.
	PRINTFORML 겨우겨우, 손을 대서 얼굴로 엎어지는 걸 피하긴 했지만
	PRINTFORML 딜러\@(ARG:5 > 6) ? 들 #\@의 극딜은 더욱 심해져 쓰러진 %조사처리(CALLNAME:ARG,"를")% 때리고 밟고 마구 폭행을 해댔다.
	PRINTL 
	PRINTFORML 전신을 격통이 휘감는 중에 %CALLNAME:ARG%의 의식은 급속히 멀어져갔다…….
ENDIF
IF ARG:5 > 6
	;13욕정의 구슬
	CALL COMMON_UP_JUEL, ARG:1, 13, (LOCAL*250), 0
	CALL COMMON_UP_JUEL, ARG:3, 13, (LOCAL*250), 0
	CALL COMMON_UP_JUEL, ARG:4, 13, (LOCAL*250), 0
	SIF ARG == MASTER
		CALL COMMON_UP_JUEL, ARG:2, 13, (LOCAL*250), 0
	;100부정의 구슬
	CALL COMMON_DOWN_JUEL, ARG:1, 100, 500*(2+MARK:(ARG:1):9), 0
	CALL COMMON_DOWN_JUEL, ARG:3, 100, 500*(2+MARK:(ARG:3):9), 0
	CALL COMMON_DOWN_JUEL, ARG:4, 100, 500*(2+MARK:(ARG:4):9), 0
	SIF ARG == MASTER
		CALL COMMON_DOWN_JUEL, ARG:2, 100, 500*(2+MARK:(ARG:2):9), 0
	PRINTFORML 그간의 울분이 풀렸기 때문에, %PALAMNAME:100%의 구슬은 사라졌습니다. (공대원들)
	IF ARG == MASTER
		;주인을 폭행한 플래그
		CFLAG:(ARG:1):153 |= 64
		CFLAG:(ARG:3):153 |= 64
		CFLAG:(ARG:4):153 |= 64
		CFLAG:(ARG:2):153 |= 64
		CFLAG:ARG:153 |= 128
	ENDIF
ELSE
	;13욕정의 구슬
	CALL COMMON_UP_JUEL, TARGET, 13, (LOCAL*250), 0
	;100부정의 구슬
	CALL COMMON_DOWN_JUEL, TARGET, 100, 500*(2+MARK:9), 0
	PRINTFORML 그간의 울분이 풀렸기 때문에, %PALAMNAME:100%의 구슬은 사라졌습니다. (공대원들)
		;주인 폭행 플래그
		CFLAG:153 |= 64
		CFLAG:ARG:153 |= 128
ENDIF
IF ABL:ARG:12 > 0
	IF ARG:5 > 6
		ABL:ARG:12 = 0
		PRINTFORML %조사처리(CALLNAME:ARG,"는")% 중상을 입었다. (%ABLNAME:12%가 0 레벨이 되었다)
	ELSE
		ABL:ARG:12 -= 1
		PRINTFORML %조사처리(CALLNAME:ARG,"는")% 상처를 입었다. (%ABLNAME:12%－1 레벨)
	ENDIF
ELSE
	PRINTFORML %조사처리(CALLNAME:ARG,"는")% \@(ARG:5 > 6) ? 중상을 # 상처를\@ 입었다.
ENDIF

;-----------------------------------------------
;반발 노예에 의한, 촉수 능욕
;----------------------------------------------
@NIGHT_SHOKUSHU, ARG, ARG:1
;습격자는 ARG:1
;습격당하는 것은 ARG
;행위 회수=밤새. 5분 일어나 6시간으로서 72회. 어떻게 생각해도 너무 주고.
LOCAL = 72
;노예가 작은 악마 소지라면 회수 보너스
SIF TALENT:93
	LOCAL += 24
;노예가 새디즘을 습득
TALENT:81 = 1
;금단의 지식 소유의 경우, 촉수 조작방법을 습득. 실용적인 의미는 없다
SIF TALENT:56
	TALENT:149 = 1
;지문 컷
IF CFLAG:99 == 0
	PRINTFORML 밤도 깊었기에, 이제 슬슬 %조사처리(CALLNAME:ARG,"가")% 잠을 자 볼까 하고 누우니
	PRINTFORMW 갑자기 천장이 부서지면서, %조사처리(CALLNAME:(ARG:1),"와")% 이형의 촉수가 실내로 닥쳐왔다.
ENDIF
PRINTL 
IF ARG:1 != MASTER
	;다른 노예를 촉수로 능욕하는(436)/다른 노예에 촉수로 능욕되는(433) 이벤트 대사 호출
	CALL SELF_KOJO, ARG:1, 436
	CALL SELF_KOJO, ARG, 433
ELSE
	;조교 주인을 촉수로 강간하는 이벤트 대사 호출
	CALL SELF_KOJO, ARG:1, 425
ENDIF
PRINTL 
;지문 컷
IF CFLAG:99 == 0
	PRINTFORML %조사처리(CALLNAME:ARG,"는")% 어쩔 도리도 없이 붙잡혀, 옷이 모조리 다 벗겨져 버렸다.
	IF TALENT:ARG:0 == 1
		PRINTFORML %조사처리(CALLNAME:(ARG:1),"는")% %CALLNAME:ARG%의 더러움을 모르는 보지를 싱글싱글 웃으면서
		PRINTL 그중에서도 가장 큰 촉수를 삽입시켰다.
		PRINTFORML 너무나도 큰 아픔에 비명을 지르려고 하는 %조사처리(CALLNAME:ARG,"였")%으나, 그 입도 극태의 촉수에 의해 막혀 숨도 제대로 쉴 수 없다.
		PRINTL 더 나아가, 가는 촉수들이 차례차례 항문으로 진입하여 장내를 마구 긁듯이 휘저어댔다.
		PRINTL 질내를 격하게 탐하는 촉수의 기세에 몰려 몸을 마구 펄떡거리면서
		PRINTFORML %조사처리(CALLNAME:ARG,"는")% 그저 행위가 끝나기만을 기도할 수밖에 없었다…….
	ELSEIF TALENT:ARG:0 == 2
		PRINTFORML %조사처리(CALLNAME:(ARG:1),"는")%, 거짓 처녀성에 기대는 %조사처리(CALLNAME:ARG,"를")% 비웃더니
		PRINTFORML 가는 촉수들을 조금씩 조금씩 진입시켜갔다.
		PRINTFORML 처녀막이 서서히 밀리는 공포와, 질내에서 아우성치는 촉수들이 가져오는 쾌락을 견디지 못하고
		PRINTFORML %조사처리(CALLNAME:ARG,"가")% 빨리 처녀를 빼앗아 달라고 애원할 때까지
		PRINTFORML %조사처리(CALLNAME:(ARG:1),"는")% 그 광경을 계속 즐기고 있었다.
		PRINTL 
		PRINTFORML 애원을 들어준 데에 안도하는 %조사처리(CALLNAME:ARG,"였")%지만
		PRINTFORML %조사처리(CALLNAME:(ARG:1),"가")% 선택한 가장 거대한 촉수를 보고 창백해져 필사적으로 저항했으나 사지는 이미 촉수에 붙잡혀 움직일 수조차 없었고
		PRINTL 이윽고 천천히 바기나에 다가온 그것을, 그저 바라볼 수밖에 없었다…….
	ELSE
		PRINTFORML %조사처리(CALLNAME:(ARG:1),"는")% 촉수들에게 %CALLNAME:ARG%의 전신을 집요하게 애무시켰다.
		PRINTFORML 그리고, 엄청난 쾌락에 %조사처리(CALLNAME:ARG,"가")% 절정에 달해버리기 직전에 행위를 멈춰버렸다.
		PRINTFORML 반복되는 쾌락과, 가고 싶어도 갈 수 없는 애닲픔에
		PRINTFORML 이윽고 %조사처리(CALLNAME:ARG,"가")% 넣어달라고 말을 꺼내자, %조사처리(CALLNAME:(ARG:1),"는")% 심술궃은 미소를 띠고는
		PRINTL 가장 가는 촉수 한 개만을 질내에 삽입시켰다.
		PRINTFORML 그런 거 가지곤 아무래도 만족할 수 없는 %조사처리(CALLNAME:ARG,"는")%
		PRINTFORML 지금까지는 단 한번도 내본 적이 없는 끈적한 목소리로 %CALLNAME:ARG%에게 애원을 계속했다…….
		PRINTL 그리고 부탁은 결국 이루어져, 가장 큰 교성을 지르며 스스로 허리를 흔들어 쾌락을 탐했다.
	ENDIF
	PRINTL 
	PRINTFORMW %조사처리(CALLNAME:(ARG:1),"는")%, 무수한 촉수에 전신이 농락당하고 쾌락에 계속해서 신음하는 %조사처리(CALLNAME:ARG,"를")% 질리지도 않고 지켜봤다.
	PRINTL 
ENDIF
;80강간 경험
CALL COMMON_UP_EXP, ARG:1, 80, 1, 0
;81피강간 경험
CALL COMMON_UP_EXP, ARG, 81, 1, 0
;83피간쾌락 경험
SIF ABL:ARG:0 > 2 || ABL:ARG:1 > 2 || ABL:ARG:2 > 2 || ABL:ARG:3 > 2
	CALL COMMON_UP_EXP, ARG, 83, LOCAL, 0
;처녀 판정
IF TALENT:ARG:0 > 0
	TALENT:ARG:0 = 0
	CALL SETCOLOR_TORIKOMODE, "RED", 3
	PRINTFORML 【처녀 상실】(%CALLNAME:ARG%)
	RESETCOLOR
	;첫경험 기록
	IF CFLAG:ARG:500 == 0
		CFLAG:ARG:500 = 1003
		CSTR:ARG:5 = 촉수
	ENDIF
	;이미 정조라든지 관계없이 광기
	TALENT:ARG:158 = 1
	;주인의 경우
	IF ARG == MASTER
		CFLAG:153 |= 1
		CFLAG:MASTER:153 |= 4
	ELSE
		CFLAG:ARG:12 |= 1
		CFLAG:ARG:17 |= 1
	ENDIF
ENDIF
;00V경험
CALL COMMON_UP_EXP, ARG, 0, (LOCAL*2), 0
;01A경험
CALL COMMON_UP_EXP, ARG, 1, (LOCAL*2), 0
;50이상 경험
CALL COMMON_UP_EXP, ARG, 50, 6, 0
;53촉수 경험
CALL COMMON_UP_EXP, ARG, 53, (LOCAL*6), 0
;약물 내성 소유가 아니면, 40 약물 경험도 취득
SIF TALENT:ARG:44 == 0
	CALL COMMON_UP_EXP, ARG, 40, LOCAL, 0
;노예의 경우 이하를 취득
IF ARG != MASTER
	;02절정 경험
	CALL COMMON_UP_EXP, ARG, 2, LOCAL, 0
	;04정액 경험
	CALL COMMON_UP_EXP, ARG, 4, LOCAL, 0
	;31긴박 경험
	CALL COMMON_UP_EXP, ARG, 31, (LOCAL*5), 0
	;00쾌C의 구슬
	CALL COMMON_UP_JUEL, ARG, 0, (LOCAL*300), 0
	;01쾌V의 구슬
	CALL COMMON_UP_JUEL, ARG, 1, (LOCAL*300), 0
	;02쾌A의 구슬
	CALL COMMON_UP_JUEL, ARG, 2, (LOCAL*300), 0
	;03쾌B의 구슬
	CALL COMMON_UP_JUEL, ARG, 3, (LOCAL*300), 0
	;13욕정의 구슬
	CALL COMMON_UP_JUEL, ARG, 13, (LOCAL*1000), 0
	;17공포의 구슬
	CALL COMMON_UP_JUEL, ARG, 17, (LOCAL*400), 0
	;100부정의 구슬
	CALL COMMON_UP_JUEL, ARG, 100, (LOCAL*2000), 1
	;촉수 중독 강제 상승. Lv5로(촉수에[체내 적응력 강화]가 없으면 실패할지도) 기생
	IF ABL:ARG:35 == 5 && TALENT:ARG:146 == 0 && ((FLAG:75 & 8192) || (TALENT:ARG:49 == 0 && NO:ARG != 33 && NO:ARG != 39))
		TALENT:ARG:146 = 1
		PRINTFORML %조사처리(CALLNAME:ARG,"는")% 촉수에 기생당하고 말았다.
		;첫체험 때 이상 경험+1
		IF (CFLAG:ARG:24 & 1) == 0
			;50이상 경험
			CALL COMMON_UP_EXP, ARG, 50, 1, 0
			CFLAG:ARG:24 |= 1
		ENDIF
		LOCAL:1 = TARGET
		TARGET = ARG
		CALL CALC_TENTACLE_GAUGE
		TARGET = LOCAL:1
	ELSEIF ABL:ARG:35 < 5
		ABL:ARG:35 += 1
		PRINTFORML %ABLNAME:35%(%CALLNAME:ARG%)＋1 레벨
	ENDIF
;주인의 경우
ELSE
	;주인이 촉수에 습격당한 플래그
	CFLAG:ARG:153 |= 512
	CFLAG:(ARG:1):153 |= 256

	;타락매번 상승
	CFLAG:MASTER:66 += LOCAL * 30
ENDIF

;노예의 욕망의 구슬, 습득의 구슬증가. 부정의 구슬의 소거
;11습득의 구슬
CALL COMMON_UP_JUEL, TARGET, 11, (LOCAL*100), 0
;13욕정의 구슬
CALL COMMON_UP_JUEL, TARGET, 13, (LOCAL*100), 0
;100부정의 구슬
CALL COMMON_DOWN_JUEL, TARGET, 100, 500*(2+MARK:9), 0
PRINTFORML 평소의 울분을 풀었기에, %PALAMNAME:100%의 구슬은 줄어들었습니다. (%CALLNAME:TARGET%)
;강간시의 버리기 대사 이벤트 대사 호출
PRINTL 
LOCAL = (ARG == MASTER) ? 420 # 430
CALL SELF_KOJO, TARGET, LOCAL
PRINTL 
;지문 컷
IF CFLAG:99 == 0
	PRINTFORML 어느새 %조사처리(CALLNAME:(ARG:1),"와")% 촉수는 사라져 있고
	PRINTFORML 방에 남아있는 건, 정체불명의 체액을 덮어쓰고 아연히 주저앉아 있는 %CALLNAME:ARG%뿐이었다…….
ENDIF
