;≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡
;eratohoYM Mk-2 rev.6용 분할형 대사 템플릿
;10을 캐릭터 번호 치환해 사용해주세요(레이무라면 1, 렌코라면 49, 미마라면 100).
;
;이 파일에는 조수시의 대사용 함수가 마련되어 있습니다.
;≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡
;==================================================
;레밀리아 대사
;레미랴 모드(유아퇴행)는 고려하지 않음. 왜냐하면 다른 캐릭터 대사의 레밀리아 이미지와 다를 수 있으므로…
;==================================================
;==================================================
;@KOJO_MESSAGE_PLAYERCHANGE 관계
;조수 전환 시에 출력됩니다.
;조수와 교대했을 때, 조수에서 돌아왔을 때에 조수로서 말하기 위한 기능
;==================================================
@KOJO_MESSAGE_PLAYERCHANGE_10
;만약 유아퇴행 중이면 돌아감
;SIF TALENT:ASSI:152 == 1
;	RETURN 1
;주인→레밀리아
;IF ASSIPLAY == 0
;	PRINTFORMW 『어머, 내 차례?』
;레밀리아→주인
;ELSE
;	PRINTFORMW 『벌써 끝? %조사처리(CALLNAME:TARGET,"를")% 더 지분거리고 싶었는데』
;ENDIF

;==================================================
;@KOJO_MESSAGE_CHANGEASSI 관계
;조수 전환 시에 출력됩니다.
;조수와 교대했을 때, 조수에서 돌아왔을 때에 대상으로서 말하기 위한 기능
;==================================================
@KOJO_MESSAGE_CHANGEASSI_10
;만약 유아퇴행 중이면 돌아감
;SIF TALENT:152 == 1
;	RETURN 1
;주인→조수
;IF ASSIPLAY == 0
;	PRINTFORMW 「%조사처리(CALLNAME:ASSI,"가")% 상대야…?」
;조수→주인
;ELSE
;	PRINTFORMW 「미안하지만 %CALLNAME:ASSI%보다는 역시 주인님이 좋아」
;ENDIF

;==================================================
;@KOJO_MESSAGE_COM_ASSI 관계
;조수 조교 중의 커맨드 실행시에 출력됩니다。
;==================================================
@KOJO_MESSAGE_COM_ASSI_10
;만약 유아퇴행 중이면 돌아감
;SIF TALENT:ASSI:152 == 1
;	RETURN 1
;IF SELECTCOM == 
;	PRINTFORMW 『너의 피는… 무슨 맛일까』

