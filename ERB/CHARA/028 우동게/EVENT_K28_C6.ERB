;≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡
;eratohoYM Mk-2 rev.6用 分割型口上テンプレート
;28をキャラ番号に置換して使用してください(霊夢なら1, 蓮子なら49, 魅魔なら100). 
;
;このファイルでは600～699番のコマンド(派生コマンド)の口上用関数が置いてあります. 
;≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡
;==================================================
;레이센 대사
;==================================================
;==================================================
;コマンド口上
;初回判定にCFLAG:9000～9099までの100個を使用する
;初回かどうかはRESULT:1が1かどうかで判別
;==================================================
;-------------------------------------------------
;Ｇスポット刺激
;-------------------------------------------------
@K28_MESSAGE_COM_600
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用Ｇスポット刺激口上です」
;２回目以降
ELSE
	;PRINTFORMW 「Ｇスポット刺激口上です」
ENDIF

;-------------------------------------------------
;潜望鏡
;-------------------------------------------------
@K28_MESSAGE_COM_601
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用潜望鏡口上です」
;２回目以降
ELSE
	;PRINTFORMW 「潜望鏡口上です」
ENDIF

;-------------------------------------------------
;シックスナイン
;-------------------------------------------------
@K28_MESSAGE_COM_602
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用シックスナイン口上です」
;２回目以降
ELSE
	;PRINTFORMW 「シックスナイン口上です」
ENDIF

;-------------------------------------------------
;６９パイズリ
;-------------------------------------------------
@K28_MESSAGE_COM_603
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用６９パイズリ口上です」
;２回目以降
ELSE
	;PRINTFORMW 「６９パイズリ口上です」
ENDIF

;-------------------------------------------------
;雁が首
;-------------------------------------------------
@K28_MESSAGE_COM_604
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用雁が首口上です」
;２回目以降
ELSE
	;PRINTFORMW 「雁が首口上です」
ENDIF

;-------------------------------------------------
;指でＶを撫でる
;-------------------------------------------------
@K28_MESSAGE_COM_605
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用指でＶを撫でる口上です」
;２回目以降
ELSE
	;PRINTFORMW 「指でＶを撫でる口上です」
ENDIF

;-------------------------------------------------
;二人で犯す
;-------------------------------------------------
@K28_MESSAGE_COM_610
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用二人で犯す口上です」
;２回目以降
ELSE
	;PRINTFORMW 「二人で犯す口上です」
ENDIF

;-------------------------------------------------
;岩清水
;-------------------------------------------------
@K28_MESSAGE_COM_611
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用岩清水口上です」
;２回目以降
ELSE
	;PRINTFORMW 「岩清水口上です」
ENDIF

;-------------------------------------------------
;乱れ牡丹
;-------------------------------------------------
@K28_MESSAGE_COM_612
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用乱れ牡丹口上です」
;２回目以降
ELSE
	;PRINTFORMW 「乱れ牡丹口上です」
ENDIF

;-------------------------------------------------
;騎乗乳房自慰
;-------------------------------------------------
@K28_MESSAGE_COM_613
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用騎乗乳房自慰口上です」
;２回目以降
ELSE
	;PRINTFORMW 「騎乗乳房自慰口上です」
ENDIF

;-------------------------------------------------
;挿入Ｇスポ責め
;-------------------------------------------------
@K28_MESSAGE_COM_614
;地の文でつらそうにするパターン
IF EXP:0 <= 30 || (PREVCOM != 24 && EXP:0 <= 50)
	PRINTFORMW 「하앗, 우우읏, 간지, 러웟……」
ELSE
	;恋慕
	IF TALENT:150 == 1
		PRINTFORMW 「%CSTR:92%, 갱장해애, 조아앗! 이런 거, 전혀 몰랐어요!」
		PRINTFORMW 「문질러 주세요오, 좀 더, 거길, 괴롭혀주세요옷!」
	;屈服刻印Lv3
	ELSEIF MARK:2 == 3
		PRINTFORMW 「뭐, 뭐야아아, 좋아아앗……이렇게 문질러대면, 머리가, 머리가 미쳐버려엇!?」
	;それ以外
	ELSE
		PRINTFORMW 「아, 안 돼, 싫어, 너무 너무, 기분 좋아서어엇!」
	ENDIF
ENDIF

;-------------------------------------------------
;挿入子宮口責め
;-------------------------------------------------
@K28_MESSAGE_COM_615
;地の文でつらそうにするパターン
;と言ってもうどんちゃんはデフォでV経験30以上あるのでめったに来ない
IF PREVCOM != 24 && (EXP:0 <= 30 || (EXP:0 >= 31 && EXP:0 <= 50 && (PREVCOM == 21 || PREVCOM == 23 || PREVCOM == 612)))
	PRINTFORMW 「우아, 뭐야 이건, 어딜 찌르는 건가요……!?」
	PRINTFORMW 아직 레이센은 자궁 입구에선 쾌감을 얻기 어려운 것 같다……
ELSE
	;恋慕で騎乗位から派生
	IF TALENT:150 == 1 && PREVCOM == 24
		PRINTFORMW 「앗, 하아, 아아, 기분, 좋은가요. %CSTR:91%……!」
		PRINTFORMW 「언제든지, 싸도, 좋으니까앗! 많이, 해주세요옷」
	;恋慕or屈服刻印Lv3＋V感覚Lv3以上
	ELSEIF (TALENT:150 || MARK:2 == 3) && ABL:1 >= 3
		PRINTFORMW 「앗, 아앗! 좀 더, 강하게 찔러주세요, %CSTR:90%!」
	;恋慕or屈服刻印Lv3
	ELSEIF MARK:2 == 3 || TALENT:150
		PRINTFORMW 「앗, 앗, 좋아서엇, 멈추지 않아, 요옷!」
	;それ以外
	ELSE
		PRINTFORMW 「아아, 이런 게, 기분 좋아서엇, 끝없이…」
	ENDIF
ENDIF

;-------------------------------------------------
;後背位愛撫
;-------------------------------------------------
@K28_MESSAGE_COM_616
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用後背位愛撫口上です」
;２回目以降
ELSE
	;PRINTFORMW 「後背位愛撫口上です」
ENDIF

;-------------------------------------------------
;対面愛撫
;-------------------------------------------------
@K28_MESSAGE_COM_617
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用対面愛撫口上です」
;２回目以降
ELSE
	;PRINTFORMW 「対面愛撫口上です」
ENDIF

;-------------------------------------------------
;後背位アナル胸愛撫
;-------------------------------------------------
@K28_MESSAGE_COM_618
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用後背位アナル胸愛撫口上です」
;２回目以降
ELSE
	;PRINTFORMW 「後背位アナル胸愛撫口上です」
ENDIF

;-------------------------------------------------
;対面アナル胸愛撫
;-------------------------------------------------
@K28_MESSAGE_COM_619
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用対面アナル胸愛撫口上です」
;２回目以降
ELSE
	;PRINTFORMW 「対面アナル胸愛撫口上です」
ENDIF

;-------------------------------------------------
;強制放尿
;-------------------------------------------------
@K28_MESSAGE_COM_620
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用強制放尿口上です」
;２回目以降でその他
ELSE
	;PRINTFORMW 「強制放尿口上です」
ENDIF

;-------------------------------------------------
;口移し
;-------------------------------------------------
@K28_MESSAGE_COM_621
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用口移し口上です」
;２回目以降でその他
ELSE
	;PRINTFORMW 「口移し口上です」
ENDIF

;-------------------------------------------------
;四肢緊縛
;-------------------------------------------------
@K28_MESSAGE_COM_622
;外したとき
IF TEQUIP:40 == 0
	;PRINTFORMW 「四肢緊縛解除口上です」
;初回のとき
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「初回用四肢緊縛口上です」
;２回目以降
ELSE
	;PRINTFORMW 「四肢緊縛口上です」
ENDIF

;-------------------------------------------------
;亀甲縛り
;-------------------------------------------------
@K28_MESSAGE_COM_623
;外したとき
IF TEQUIP:40 == 0
	;PRINTFORMW 「亀甲縛り解除口上です」
;初回のとき
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「初回用亀甲縛り口上です」
;２回目以降
ELSE
	;PRINTFORMW 「亀甲縛り口上です」
ENDIF

;-------------------------------------------------
;吊り下げ
;-------------------------------------------------
@K28_MESSAGE_COM_624
;外したとき
IF TEQUIP:40 == 0
	;PRINTFORMW 「吊り下げ解除口上です」
;初回のとき
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「初回用吊り下げ口上です」
;２回目以降
ELSE
	;PRINTFORMW 「吊り下げ口上です」
ENDIF

;-------------------------------------------------
;ローションプレイ
;-------------------------------------------------
@K28_MESSAGE_COM_625
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用ローションプレイ口上です」
;２回目以降でその他
ELSE
	;PRINTFORMW 「ローションプレイ口上です」
ENDIF

;-------------------------------------------------
;搾乳
;-------------------------------------------------
@K28_MESSAGE_COM_626
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用搾乳口上です」
;２回目以降でその他
ELSE
	;PRINTFORMW 「搾乳口上です」
ENDIF

;-------------------------------------------------
;乳首甘噛み
;-------------------------------------------------
@K28_MESSAGE_COM_627
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用乳首甘噛み口上です」
;２回目以降でその他
ELSE
	;PRINTFORMW 「乳首甘噛み口上です」
ENDIF

;-------------------------------------------------
;相互自慰
;-------------------------------------------------
@K28_MESSAGE_COM_628
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用相互自慰口上です」
;２回目以降でその他
ELSE
	;PRINTFORMW 「相互自慰口上です」
ENDIF

;-------------------------------------------------
;サンドイッチ
;-------------------------------------------------
@K28_MESSAGE_COM_629
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用サンドイッチ口上です」
;２回目以降でその他
ELSE
	;PRINTFORMW 「サンドイッチ口上です」
ENDIF

;-------------------------------------------------
;後背位手淫
;-------------------------------------------------
@K28_MESSAGE_COM_630
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用後背位手淫口上です」
;２回目以降でその他
ELSE
	;PRINTFORMW 「後背位手淫口上です」
ENDIF

;-------------------------------------------------
;相互秘貝開帳
;-------------------------------------------------
@K28_MESSAGE_COM_631
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用相互秘貝開帳口上です」
;２回目以降でその他
ELSE
	;PRINTFORMW 「相互秘貝開帳口上です」
ENDIF

;-------------------------------------------------
;イきつけ三杯
;-------------------------------------------------
@K28_MESSAGE_COM_649
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用イきつけ三杯口上です」
;２回目以降でその他
ELSE
	;PRINTFORMW 「イきつけ三杯口上です」
ENDIF

;-------------------------------------------------
;浄玻璃の鏡
;-------------------------------------------------
@K28_MESSAGE_COM_650
;外したとき
IF TEQUIP:56 == 0
	;PRINTFORMW 「浄玻璃の鏡解除口上です」
;初回のとき
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「初回用浄玻璃の鏡口上です」
;２回目以降
ELSE
	;PRINTFORMW 「浄玻璃の鏡口上です」
ENDIF

;-------------------------------------------------
;御所車(逆レイプ)
;-------------------------------------------------
@K28_MESSAGE_COM_651
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用御所車(逆レイプ)口上です」
;２回目以降でその他
ELSE
	;PRINTFORMW 「御所車(逆レイプ)口上です」
ENDIF

;-------------------------------------------------
;フォーオブアカインド(ふたなり)
;-------------------------------------------------
@K28_MESSAGE_COM_652
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用フォーオブアカインド(ふたなり)口上です」
;２回目以降でその他
ELSE
	;PRINTFORMW 「フォーオブアカインド(ふたなり)口上です」
ENDIF

;-------------------------------------------------
;蜘蛛の巣
;-------------------------------------------------
@K28_MESSAGE_COM_653
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用蜘蛛の巣口上です」
;２回目以降でその他
ELSE
	;PRINTFORMW 「蜘蛛の巣口上です」
ENDIF

;-------------------------------------------------
;蜘蛛脚六点責め
;-------------------------------------------------
@K28_MESSAGE_COM_654
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用蜘蛛脚六点責め口上です」
;２回目以降でその他
ELSE
	;PRINTFORMW 「蜘蛛脚六点責め口上です」
ENDIF

;-------------------------------------------------
;蜘蛛の巣姦
;-------------------------------------------------
@K28_MESSAGE_COM_655
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用蜘蛛の巣姦口上です」
;２回目以降でその他
ELSE
	;PRINTFORMW 「蜘蛛の巣姦口上です」
ENDIF

;-------------------------------------------------
;蜘蛛の巣肛門姦
;-------------------------------------------------
@K28_MESSAGE_COM_656
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用蜘蛛の巣肛門姦口上です」
;２回目以降でその他
ELSE
	;PRINTFORMW 「蜘蛛の巣肛門姦口上です」
ENDIF

;-------------------------------------------------
;蓑虫プレイ
;-------------------------------------------------
@K28_MESSAGE_COM_657
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用蓑虫プレイ口上です」
;２回目以降でその他
ELSE
	;PRINTFORMW 「蓑虫プレイ口上です」
ENDIF

;-------------------------------------------------
;拘束∇
;-------------------------------------------------
@K28_MESSAGE_COM_658
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用拘束∇口上です」
;２回目以降でその他
ELSE
	;PRINTFORMW 「拘束∇口上です」
ENDIF
