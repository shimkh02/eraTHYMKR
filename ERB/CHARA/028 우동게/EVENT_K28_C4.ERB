;≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡
;eratohoYM Mk-2 rev.6用 分割型口上テンプレート
;28をキャラ番号に置換して使用してください(霊夢なら1, 蓮子なら49, 魅魔なら100). 
;
;このファイルでは400～499番のコマンド(キャラ専用コマンド)の口上用関数が置いてあります. 
;≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡
;==================================================
;레이센 대사
;==================================================
;==================================================
;コマンド口上
;初回判定にCFLAG:9000～9099までの100個を使用する
;初回かどうかはRESULT:1が1かどうかで判別
;==================================================
;-------------------------------------------------
;吸血
;使用キャラ…TALENT:吸血鬼
;-------------------------------------------------
@K28_MESSAGE_COM_400
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用吸血口上です」
;２回目以降
ELSE
	;PRINTFORMW 「吸血口上です」
ENDIF

;-------------------------------------------------
;暗闇
;使用キャラ…ルーミア
;TEQUIP:70 == 0で解除口上
;狂気の目は暗闇では発動する模様. 
;-------------------------------------------------
@K28_MESSAGE_COM_401
;終了
IF TEQUIP:70 == 0
	;PRINTFORMW 「暗闇解除口上です」
;初回のみ
ELSEIF RESULT:1 == 1
	;恋慕
	IF TALENT:150 == 1
		PRINTFORMW 「아…… 주인님, 보기 어렵지 않으신가요……?」
	;それ以外
	ELSE
		PRINTFORMW (이 정도로 어둡다면…… 보기 힘들지만, 아직 눈은 쓸 수 있어)
	ENDIF
ELSE
	;恋慕
	IF TALENT:150 == 1
		PRINTFORMW 「주인님, 발밑을 조심해주세요……」
	;屈服刻印３
	ELSEIF MARK:2 == 3
		PRINTFORMW 「아직…… 보이, 지만」
	;それ以外
	ELSE
		PRINTFORMW (어떻게든 허를 찌를 수 없을까……)
	ENDIF
ENDIF

;-------------------------------------------------
;尻子玉抜き
;使用キャラ…にとり, TALENT:河童
;-------------------------------------------------
@K28_MESSAGE_COM_402
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用尻子玉抜き口上です」
;２回目以降
ELSE
	;PRINTFORMW 「尻子玉抜き口上です」
ENDIF

;-------------------------------------------------
;焼芋責め
;使用キャラ…秋姉妹
;-------------------------------------------------
@K28_MESSAGE_COM_403
;終了
IF TEQUIP:20 == 0
	;PRINTFORMW 「焼芋責め解除口上です」
;初回のみ
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「初回用焼芋責め口上です」
;２回目以降
ELSE
	;PRINTFORMW 「焼芋責め口上です」
ENDIF

;-------------------------------------------------
;巨峰責め
;使用キャラ…秋姉妹
;-------------------------------------------------
@K28_MESSAGE_COM_404
;終了
IF TEQUIP:25 == 0
	;PRINTFORMW 「巨峰責め解除口上です」
;初回のみ
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「初回用巨峰責め口上です」
;２回目以降
ELSE
	;PRINTFORMW 「巨峰責め口上です」
ENDIF

;-------------------------------------------------
;石勺柏責め
;使用キャラ…秋姉妹
;-------------------------------------------------
@K28_MESSAGE_COM_405
;終了
IF TEQUIP:32 == 0
	;PRINTFORMW 「石勺柏責め解除口上です」
;初回のみ
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「初回用石勺柏責め口上です」
;２回目以降
ELSE
	;PRINTFORMW 「石勺柏責め口上です」
ENDIF

;-------------------------------------------------
;荊で緊縛
;使用キャラ…幽香
;-------------------------------------------------
@K28_MESSAGE_COM_406
;終了
IF TEQUIP:40 == 0
	;PRINTFORMW 「荊で緊縛解除口上です」
;初回のみ
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「初回用荊で緊縛口上です」
;２回目以降
ELSE
	;PRINTFORMW 「荊で緊縛口上です」
ENDIF

;-------------------------------------------------
;荊の鞭
;使用キャラ…幽香
;-------------------------------------------------
@K28_MESSAGE_COM_407
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用荊の鞭口上です」
;２回目以降
ELSE
	;PRINTFORMW 「荊の鞭口上です」
ENDIF

;-------------------------------------------------
;蜘蛛の糸
;使用キャラ…ヤマメ
;-------------------------------------------------
@K28_MESSAGE_COM_408
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用蜘蛛の糸口上です」
;２回目以降
ELSE
	;PRINTFORMW 「蜘蛛の糸口上です」
ENDIF

;-------------------------------------------------
;独楽回し
;使用キャラ…ヤマメ
;-------------------------------------------------
@K28_MESSAGE_COM_409
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用独楽回し口上です」
;２回目以降
ELSE
	;PRINTFORMW 「独楽回し口上です」
ENDIF

;-------------------------------------------------
;蜘蛛脚愛撫
;使用キャラ…ヤマメ
;-------------------------------------------------
@K28_MESSAGE_COM_410
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用蜘蛛脚愛撫口上です」
;２回目以降
ELSE
	;PRINTFORMW 「蜘蛛脚愛撫口上です」
ENDIF

;-------------------------------------------------
;人形愛撫
;使用キャラ…アリス(EX化時不可)
;-------------------------------------------------
@K28_MESSAGE_COM_411
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用人形愛撫口上です」
;２回目以降
ELSE
	;PRINTFORMW 「人形愛撫口上です」
ENDIF

;-------------------------------------------------
;操りの糸
;使用キャラ…アリス(EX化時不可)
;-------------------------------------------------
@K28_MESSAGE_COM_412
;終了
IF TEQUIP:77 == 0
	;PRINTFORMW 「操りの糸解除口上です」
;初回のみ
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「初回用操りの糸口上です」
;２回目以降
ELSE
	;PRINTFORMW 「操りの糸口上です」
ENDIF

;-------------------------------------------------
;豆炙り
;使用キャラ…妹紅(EX化時不可)
;-------------------------------------------------
@K28_MESSAGE_COM_413
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用豆炙り口上です」
;２回目以降
ELSE
	;PRINTFORMW 「豆炙り口上です」
ENDIF

;-------------------------------------------------
;乳豆炙り
;使用キャラ…妹紅(EX化時不可)
;-------------------------------------------------
@K28_MESSAGE_COM_414
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用乳豆炙り口上です」
;２回目以降
ELSE
	;PRINTFORMW 「乳豆炙り口上です」
ENDIF

;-------------------------------------------------
;読心
;使用キャラ…TALENT:覚 = 1(こいしは2なので不可)
;対象不可…TALENT:覚 = 2
;-------------------------------------------------
@K28_MESSAGE_COM_415
;終了
IF TEQUIP:74 == 0
	;PRINTFORMW 「読心解除口上です」
;初回のみ
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「初回用読心口上です」
;２回目以降
ELSE
	;PRINTFORMW 「読心口上です」
ENDIF

;-------------------------------------------------
;思姦
;使用不可キャラ…TALENT:覚 = 2
;対象…TALENT:覚 = 1(こいしは2なので不可)
;-------------------------------------------------
@K28_MESSAGE_COM_416
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用思姦口上です」
;２回目以降
ELSE
	;PRINTFORMW 「思姦口上です」
ENDIF

;-------------------------------------------------
;筆責め
;使用キャラ…阿求
;-------------------------------------------------
@K28_MESSAGE_COM_417
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用筆責め口上です」
;２回目以降
ELSE
	;PRINTFORMW 「筆責め口上です」
ENDIF

;-------------------------------------------------
;時間停止
;ところで普通, 時間止まった瞬間って喋れなくね？
;使用キャラ…咲夜・輝夜
;-------------------------------------------------
@K28_MESSAGE_COM_418
;終了
IF TEQUIP:71 == 0
	PRINTFORMW 　――되…… 읏!?」
ELSE
	PRINTFORMW 「? 공간이 왜곡――
ENDIF

;-------------------------------------------------
;狂気の目
;使用キャラ…TALENT:狂気の目
;対象不可…TALENT:狂気
;-------------------------------------------------
@K28_MESSAGE_COM_419
;終了
IF TEQUIP:73 == 0
	;PRINTFORMW 「狂気の目解除口上です」
;初回のみ
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「初回用狂気の目口上です」
;２回目以降
ELSE
	;PRINTFORMW 「狂気の目口上です」
ENDIF

;-------------------------------------------------
;鈴蘭の毒
;使用キャラ…メディスン
;-------------------------------------------------
@K28_MESSAGE_COM_420
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用鈴蘭の毒口上です」
;２回目以降
ELSE
	;PRINTFORMW 「鈴蘭の毒口上です」
ENDIF

;-------------------------------------------------
;昆虫採集セットの薬
;使用キャラ…永琳
;-------------------------------------------------
@K28_MESSAGE_COM_421
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用昆虫採集セットの薬口上です」
;２回目以降
ELSE
	;PRINTFORMW 「昆虫採集セットの薬口上です」
ENDIF

;-------------------------------------------------
;媚薬漬け
;使用キャラ…キスメ(調教対象でも可)
;-------------------------------------------------
@K28_MESSAGE_COM_422
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用媚薬漬け口上です」
;２回目以降
ELSE
	;PRINTFORMW 「媚薬漬け口上です」
ENDIF

;-------------------------------------------------
;酒漬け
;使用キャラ…キスメ(調教対象でも可)
;-------------------------------------------------
@K28_MESSAGE_COM_423
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用酒漬け口上です」
;２回目以降
ELSE
	;PRINTFORMW 「酒漬け口上です」
ENDIF

;-------------------------------------------------
;わかめ酒
;使用キャラ…萃香, 勇儀(調教対象でも可)
;-------------------------------------------------
@K28_MESSAGE_COM_424
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用わかめ酒口上です」
;２回目以降
ELSE
	;PRINTFORMW 「わかめ酒口上です」
ENDIF

;-------------------------------------------------
;かりちゅまがーど
;使用キャラ…レミリア
;-------------------------------------------------
@K28_MESSAGE_COM_425
;初回ではない
IF RESULT:1 == 0
	;PRINTFORMW 「下段ガード口上です」
;その他
ELSE
	;PRINTFORMW 「初回用下段ガード口上です」
ENDIF

;-------------------------------------------------
;陰毛炙り
;使用キャラ…妹紅(EX化時不可)
;-------------------------------------------------
@K28_MESSAGE_COM_427
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用陰毛炙り口上です」
;２回目以降
ELSE
	;PRINTFORMW 「陰毛炙り口上です」
ENDIF

;-------------------------------------------------
;御所車
;使用キャラ…雛
;-------------------------------------------------
@K28_MESSAGE_COM_430
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用御所車口上です」
;２回目以降
ELSE
	;PRINTFORMW 「御所車口上です」
ENDIF

;-------------------------------------------------
;フォーオブアカインド
;使用キャラ…フランちゃん
;-------------------------------------------------
@K28_MESSAGE_COM_431
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用四身の拳口上です」
;２回目以降
ELSE
	;PRINTFORMW 「フォーオブアカインド口上です」
ENDIF

;-------------------------------------------------
;氷柱責め
;使用キャラ…チルノ
;-------------------------------------------------
@K28_MESSAGE_COM_432
;終了
IF TEQUIP:26 == 0
	;PRINTFORMW 「氷柱責め解除口上です」
;初回のみ
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「初回用氷柱責め口上です」
;２回目以降
ELSE
	;PRINTFORMW 「氷柱責め口上です」
ENDIF

;-------------------------------------------------
;アナル卒塔婆
;使用キャラ…映姫
;-------------------------------------------------
@K28_MESSAGE_COM_433
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用アナル卒塔婆口上です」
;２回目以降
ELSE
	;PRINTFORMW 「アナル卒塔婆口上です」
ENDIF

;-------------------------------------------------
;角愛撫
;対象…TALENT:鬼またはハクタク化したTALENT:半白澤
;-------------------------------------------------
@K28_MESSAGE_COM_434
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用角愛撫口上です」
;２回目以降
ELSE
	;PRINTFORMW 「角愛撫口上です」
ENDIF

;-------------------------------------------------
;角挿入
;使用キャラ…TALENT:鬼またはハクタク化したTALENT:半白澤
;-------------------------------------------------
@K28_MESSAGE_COM_435
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用角挿入口上です」
;２回目以降
ELSE
	;PRINTFORMW 「角挿入口上です」
ENDIF

;-------------------------------------------------
;角アナル挿入
;使用キャラ…TALENT:鬼またはハクタク化したTALENT:半白澤
;-------------------------------------------------
@K28_MESSAGE_COM_436
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用角アナル挿入口上です」
;２回目以降
ELSE
	;PRINTFORMW 「角アナル挿入口上です」
ENDIF

;-------------------------------------------------
;ヴァギナ要石
;使用キャラ…天子
;-------------------------------------------------
@K28_MESSAGE_COM_437
;抜いたら地震発生！
IF TEQUIP:20 == 0
	;PRINTFORMW 「ヴァギナ要石解除口上です」
;初回のみ
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「初回用ヴァギナ要石口上です」
;２回目以降
ELSE
	;PRINTFORMW 「ヴァギナ要石口上です」
ENDIF

;-------------------------------------------------
;アナル要石
;使用キャラ…天子
;-------------------------------------------------
@K28_MESSAGE_COM_438
;抜いたら地震発生！
IF TEQUIP:25 == 0
	;PRINTFORMW 「アナル要石解除口上です」
;初回のみ
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「初回用アナル要石口上です」
;２回目以降
ELSE
	;PRINTFORMW 「アナル要石口上です」
ENDIF

;-------------------------------------------------
;両穴要石
;使用キャラ…天子
;-------------------------------------------------
@K28_MESSAGE_COM_439
;抜いたら地震発生！
IF TEQUIP:20 == 0 && TEQUIP:25 == 0
	;PRINTFORMW 「両穴要石解除口上です」
;初回のみ
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「初回用両穴要石口上です」
;２回目以降
ELSE
	;PRINTFORMW 「両穴要石口上です」
ENDIF

;-------------------------------------------------
;半霊を犯す
;対象…妖夢, 妖忌
;調教可能キャラにそれぞれの半霊がいないと使えない
;-------------------------------------------------
@K28_MESSAGE_COM_440
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用半霊を犯す口上です」
;２回目以降
ELSE
	;PRINTFORMW 「半霊を犯す口上です」
ENDIF

;-------------------------------------------------
;半霊を挿入(前)
;使用キャラ…妖夢, 妖忌
;調教可能キャラにそれぞれの半霊がいないと使えない
;-------------------------------------------------
@K28_MESSAGE_COM_441
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用半霊を挿入(前)口上です」
;２回目以降
ELSE
	;PRINTFORMW 「半霊を挿入(前)口上です」
ENDIF

;-------------------------------------------------
;半霊を挿入(後)
;使用キャラ…妖夢, 妖忌
;調教可能キャラにそれぞれの半霊がいないと使えない
;-------------------------------------------------
@K28_MESSAGE_COM_442
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用半霊を挿入(後)口上です」
;２回目以降
ELSE
	;PRINTFORMW 「半霊を挿入(後)口上です」
ENDIF

;-------------------------------------------------
;幽明の苦輪
;対象…妖夢, 妖忌
;調教可能キャラにそれぞれの半霊がいないと使えない
;-------------------------------------------------
@K28_MESSAGE_COM_443
;終了
IF TEQUIP:72 == 0
	;PRINTFORMW 「幽明の苦輪解除口上です」
;初回のみ
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「初回用幽明の苦輪口上です」
;２回目以降
ELSE
	;PRINTFORMW 「幽明の苦輪口上です」
ENDIF

;-------------------------------------------------
;半霊再構成
;使用キャラ…妖忌
;調教可能キャラに半霊(忌)がいないと使えない
;-------------------------------------------------
@K28_MESSAGE_COM_444
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用半霊再構成口上です」
;２回目以降
ELSE
	;PRINTFORMW 「半霊再構成口上です」
ENDIF

;-------------------------------------------------
;薀蓄語り
;使用キャラ…霖之助(対象でも可)
;-------------------------------------------------
@K28_MESSAGE_COM_445
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用薀蓄語り口上です」
;２回目以降
ELSE
	;PRINTFORMW 「薀蓄語り口上です」
ENDIF

;-------------------------------------------------
;説教を受ける
;使用キャラ…映姫(対象でも可)
;-------------------------------------------------
@K28_MESSAGE_COM_446
;연모
IF TALENT:150
	PRINTFORMW 「옛날의 일이나, 이 이후의 일은 아무래도 좋습니다……지금은, 주인님만 있다면……♪」
ELSE
	PRINTFORMW 「이, 이제 그만, 그 이야기는……!」
ENDIF

;-------------------------------------------------
;傘を愛撫する
;対象…小傘
;-------------------------------------------------
@K28_MESSAGE_COM_447
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用傘を愛撫する口上です」
;２回目以降
ELSE
	;PRINTFORMW 「傘を愛撫する口上です」
ENDIF

;-------------------------------------------------
;房中術
;使用キャラ…美鈴
;-------------------------------------------------
@K28_MESSAGE_COM_448
;終了
IF TEQUIP:79 == 0
	;PRINTFORMW 「房中術解除口上です」
;初回のみ
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「初回用房中術口上です」
;２回目以降
ELSE
	;PRINTFORMW 「房中術口上です」
ENDIF

;-------------------------------------------------
;フィーバー！
;使用キャラ…衣玖(対象でも可)
;-------------------------------------------------
@K28_MESSAGE_COM_449
;初回ではない
IF RESULT:1 == 0
	;PRINTFORMW 「フィーバー！口上です」
;その他
ELSE
	;PRINTFORMW 「初回用フィーバー！口上です」
ENDIF

;-------------------------------------------------
;儀式「とりあえずピッチャー二つで」
;使用キャラ…天治
;-------------------------------------------------
@K28_MESSAGE_COM_450
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用ピッチャー二つ口上です」
;２回目以降
ELSE
	;PRINTFORMW 「ピッチャー二つ口上です」
ENDIF

;-------------------------------------------------
;酔生夢死「空飛ぶ呑んべぇのレムリア」
;使用キャラ…天治
;-------------------------------------------------
@K28_MESSAGE_COM_451
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用酔生夢死口上です」
;２回目以降
ELSE
	;PRINTFORMW 「酔生夢死口上です」
ENDIF

;-------------------------------------------------
;超凝視
;使用キャラ…ユウゲンマガン
;-------------------------------------------------
@K28_MESSAGE_COM_452
;終了
IF TEQUIP:80 == 0
	;PRINTFORMW 「超凝視解除口上です」
ELSE
	PRINTFORMW 「이 눈……어두, 워……!」
ENDIF

;-------------------------------------------------
;傘に舐めさせる
;使用キャラ…からかさお化け(実質小傘専用)
;-------------------------------------------------
@K28_MESSAGE_COM_453
;初回のみ
IF RESULT:1 == 1
	;PRINTFORMW 「初回用傘に舐めさせる口上です」
;２回目以降
ELSE
	;PRINTFORMW 「傘に舐めさせる口上です」
ENDIF
