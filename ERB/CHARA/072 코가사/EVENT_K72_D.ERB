;-------------------------------------------------
;코가사 말파일 D
;-------------------------------------------------
;-와 YM v1. 0 파일 D 확장 파일

;-와 독자적인 기능을 이용한 말이나, 보다 돌진한 만큼 기로 말을 쓰기 위한 파일입니다
;그만두어, 기분이 접하군?
;사용하는 곳만 써 나머지는 가만히 두어도 움직이므로, 그러한 사용법을 해 주세요.


;-------------------------------------------------
;부르는 법 변경 기능
;-------------------------------------------------
;·주인의 부르는 법을 조건 마다 변경한다. 디폴트에서는 플래그가 조건이지만 소질등으로 바꿀 수도 있다
;·말주가 조교 대상이나 조수로, 조교 개시했을 때와 이벤트 말을 불렀을 때에 부른다. 즉변경하고 싶은 경우는 이 함수를 호출하는 것으로 가능
;·「%MASTERNAME%」 「%LOCALS@MASTER_72%」로 사용할 수 있다
;·말주를 조교중은 조교 커멘드 실행 마다 「%LOCALS@PLAYER_72%」에 조교자의 이름으로서 대입된다(주인 이외의 경우는 CALLNAME가 대입된다)
;-------------------------------------------------
@MASTER_72
IF CFLAG:9001
	MASTERNAME = 주인님
	LOCALS = 주인님
ELSEIF CFLAG:9000
	MASTERNAME = 사부
	CSTR:90 = 사부
	LOCALS = 사부
ELSE
	IF NO:MASTER == 0 && TALENT:MASTER:120
		MASTERNAME = 오빠
		CSTR:90 = 오빠
		LOCALS = 오빠
	ELSEIF NO:MASTER == 0
		MASTERNAME = 언니
		CSTR:90 = 언니
		LOCALS = 언니
	ELSE
		MASTERNAME = %CALLNAME:MASTER%
		CSTR:90 = %CALLNAME:MASTER%
		LOCALS = %CALLNAME:MASTER%
	ENDIF
ENDIF
RETURN 0

;*************************************************
;조교 개시시 말확장 CFLAG:6000 접수대
;*************************************************
;·말주를 TARGET에 조교를 시작했을 때에 표시
;·이 파일 중(안)에서 위에 있는 것(정도)만큼 우선해 표시된다
;-------------------------------------------------

;=================================================
;첫조교시 CFLAG:6000
;=================================================
;·「조교 개시시 말확장」에 속한다
;·특수한 조건으로 말주를 처음으로 조교할 경우에 한 번만 표시
;-------------------------------------------------
@KOJO_D_1_72

;-------------------------------------------------
;첫조교·기억 소거 후
;-------------------------------------------------
;·기억 소거약을 사용한 후에 조교하면(자) 발생
;-------------------------------------------------
;기입 체크
LOCAL = 1
;-------------------------------------------------

IF LOCAL && CFLAG:5268
	;기억을 잃기 전은 연모였다
	IF CFLAG:5268 == 2
		IF FLAG:11 > 0
			PRINTFORML 「원통하도……」
			PRINTFORMW %CALLNAME:MASTER%의 얼굴을 보고, 코가사는 놀래키기 위해 뛰쳐오르는 자세인 채로 고개를 갸웃했다.
			PRINTFORMW 「……엉뚱한 결과 팥소의씨팥소―？」
			PRINTFORMW 무슨 소리인가.
			PRINTFORMW 「음, 별로 원망스럽지 않아……」
			PRINTFORMW 혼란스러워하고 있다. 기억이 다 없어지지 않은 것인지도 몰랐다.
			PRINTFORMW %조사처리(CALLNAME:MASTER,"는")% 상관하지 않고 코가사를 침대에 눕히고, 손을 대었다.
			PRINTFORMW ――코가사의 신체는 어느 정도 쾌락을 느끼고 있을까?
			PRINTFORMW %조사처리(CALLNAME:MASTER,"는")% 자연히 뺨이 달아오르는 것을 느꼈다….
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORML 「원통하도다―!!」
			PRINTFORMW 예측 대로였다. %조사처리(CALLNAME:MASTER,"는")% 훌쩍 몸을 피한다. 코가사는 사랑스럽게 굴렀다.
			PRINTFORMW 「……훌쩍훌쩍」
			PRINTFORMW 손을 잡아 침대까지 데리고 간다.
			PRINTFORMW 「원통해……원통하다구……」
			PRINTFORMW 파랑과 빨강의 눈에 눈물이 맺힌 채 혀를 내미는 것도 잊고 칭얼거리는 코가사.
			PRINTFORMW 처음으로 만났을 때와 달리, 지각 없는 진심의, 원망과 증오.
			PRINTFORMW ――코가사의 신체는 어느 정도 쾌락을 느끼고 있을까?
			PRINTFORMW %조사처리(CALLNAME:MASTER,"는")% 자연히 뺨이 달아오르는 것을 느꼈다….
		ENDIF
	ENDIF
	RETURN 10
ENDIF


;-------------------------------------------------
;첫조교·[괴조인격]취득 후
;-------------------------------------------------
;·붕괴계 소질의 최종 단계
;·MASTER에 적당한 인격이 덧쓰기된 상태를 나타낸다
;·조건을 채웠을 경우, 주회 플레이로 취득시킬 수가 있다
;-------------------------------------------------
;기입 체크
LOCAL = 0
;-------------------------------------------------

IF LOCAL && TALENT:169
	IF FLAG:11 > 0
		PRINTFORM 
		PRINTFORM 
	ENDIF
	RETURN 10
ENDIF


;-------------------------------------------------
;첫조교·[즉빠짐]취득 후
;-------------------------------------------------
;·주회 플레이의 보너스 소질
;·조교되면 간단하게 함락 하는 노예로서의 기질을 나타낸다
;·조건을 채웠을 경우, 주회 플레이로 취득시킬 수가 있다
;-------------------------------------------------
;기입 체크
LOCAL = 0
;-------------------------------------------------

IF LOCAL && TALENT:179
	IF FLAG:11 > 0
		PRINTFORM 
		PRINTFORM 
	ENDIF
	RETURN 10
ENDIF


;-------------------------------------------------
;첫조교·주회 플레이 후
;-------------------------------------------------
;·말주를 조수로서 계승해 주회 플레이를 실시했을 경우의 첫조교시에 표시된다
;·조수로서의 능력이 어느 정도 인계되고 있다
;-------------------------------------------------
;기입 체크
LOCAL = 1
;-------------------------------------------------

IF LOCAL && CFLAG:18
	IF FLAG:11 > 0
		PRINTFORML 「원통하도다―!!」
		PRINTFORMW %조사처리(CALLNAME:MASTER,"가")% 코가사의 방에 들어가자, 눈앞에 가지색의 우산이 뛰어들어 왔다.
		PRINTFORMW 「헷헷헤, 코가사입니다. 스승, 오랜만이야!」
		PRINTFORMW 「저기, 놀라버렸어? 훗후, 나를 얕봐선 안돼」
		PRINTFORMW 말하면서 우산으로부터 나 있는 거대한 혀가, %CALLNAME:MASTER%의 얼굴을 핥는다.
		PRINTFORMW 「…응. 전의 코가사는, 행복하게 될 수 있었어?」
		PRINTFORMW 「새로운 코가사, 엄청 귀여워해줘. 새로운 스승!」
	ENDIF
	CFLAG:9000 = 1
	TRYCALL MASTER_72
	RETURN 10
ENDIF

RETURN 0


;=================================================
;조교 단계별 CFLAG:6001~6040
;=================================================
;·「조교 개시시 말확장」에 속한다
;·특정의 소질이나 각인을 취득한 후의 조교 개시시에 표시한다
;·조수나 오나금등의 특수한 개시시 말에 대해서 우선된다
;-------------------------------------------------
@KOJO_D_1_72_1

;=================================================
;함락계 소질 CFLAG:6001~6010
;=================================================
;·「조교 개시시 말확장」 「조교 단계별」에 속한다
;·조교 완료의 고비가 되는 것 같은 소질을 취득한 후의 조교 개시시에 표시한다
;-------------------------------------------------

;-------------------------------------------------
;[복종]취득 후 CFLAG:6004
;-------------------------------------------------
;·복종계 소질의 기본
;·말주가 MASTER에 완전하게 굴복 한 것을 나타낸다
;-------------------------------------------------
;기입 체크
LOCAL = 1
;-------------------------------------------------

;코가사 독자 사양·친애시·Ex엔드 후는 표시하지 않는다
IF LOCAL && TALENT:160 && CFLAG:6004 == 0 && TALENT:152 == 0 && CFLAG:9003 != 7
	IF FLAG:11 > 0
		PRINTFORMW 「스승…」
		PRINTFORMW 코가사의 모습이 이상하다. …어쩐지, 깊은 생각에 빠진 표정을 하고 있다.
		PRINTFORMW 「나, 스승의 물건이야. 지난번에, 어쩐지, 쿵하고 납득해버렸어」
		PRINTFORMW 「스승의 제자도 아니고, 물건. 이상한 착각, 해버릴거 같았어」
		PRINTFORMW 시원스럽고 투명하게 웃는 얼굴로, 코가사는 그런 일을 말한다.
		PRINTFORMW 「 이제는, 스승이라고 부를 수 없어…주인님. 그래, 주인님같아」
		PRINTFORMW 「주인님을 위해서라면, 나. 좋은 아이가 될 거야. 분명하게 도구로 있을 테니까.」
		PRINTFORMW 「코가사를…쭉 쭉, 잘 부탁드립니다」
		PRINTFORMW 「…훌쩍. 슬프지만, 알아 버렸으니까」
		PRINTFORMW 「주인님의 우산이 될 수 있어서, 기뻐. 눈물이 나올 정도로. 진짜야」
		PRINTFORMW 「그러니까, 귀여워해 줘…으응. 이것도 멋대로네」
		PRINTFORMW 「주인님의 것이니까, 나는 주인님의 마음대로 해 주세요」
		PRINTFORMW 「 이제 이기적인 말은 하지 않을테니까. 알아 버렸기 때문에. 사양하지 않아도 좋으니까」
		PRINTFORMW 「 그렇지만, 할 수 있으면 쭉…잘 부탁드립니다」
		PRINTFORMW 「 나, 진짜로 정말로 좋아했습니다. 사 주고, 사용해 줘서, 고마워요…」
		PRINTFORMW …….
		PRINTFORML 　
		PRINTFORML ――코가사는 진심으로 %CALLNAME:MASTER%에 굴복 했습니다――
		PRINTFORMW 　
		PRINTFORMW 「――있지, 스승」
	ENDIF
	;굴종 플래그를 세운다
	CFLAG:9001 = 1
	CFLAG:6004 = 1
	RETURN 4
ENDIF


;-------------------------------------------------
;[망신]취득 후 CFLAG:6005
;-------------------------------------------------
;·애정계 소질의 특수형. [연모][음란][복종]의 모든 것이 필요
;·말주가 MASTER를 믿고 올 때까지 저속해진 것을 나타낸다
;-------------------------------------------------
;기입 체크
LOCAL = 0
;-------------------------------------------------

IF LOCAL && TALENT:151 && CFLAG:6005 == 0
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	CFLAG:6005 = 1
	RETURN 5
ENDIF

;=================================================
;음란계 소질 CFLAG:6011~6020
;=================================================
;·「조교 개시시 말확장」 「조교 단계별」에 속한다
;·특정의 성감대나 버릇에 대응한 소질을 취득한 후의 조교 개시시에 표시한다
;·[음마]취득시를 제외해, 어느쪽이든 1개 밖에 취득할 수 없다
;-------------------------------------------------

;-------------------------------------------------
;[음핵]취득 후 CFLAG:6011
;-------------------------------------------------
;·클리토리스계 조교에 대응한 음란계 소질
;·페니스가 있는 경우는[음경]이 된다
;-------------------------------------------------
;기입 체크
LOCAL = 0
;-------------------------------------------------

IF LOCAL && TALENT:171 && CFLAG:6011 == 0 && TALENT:120 == 0 && TALENT:121 == 0
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	CFLAG:6011 = 1
	RETURN 11
ENDIF


;-------------------------------------------------
;[음경]취득 후 CFLAG:6011
;-------------------------------------------------
;·페니스계 조교에 대응한 음란계 소질
;·페니스가 없는 경우는[음핵]이 된다
;-------------------------------------------------
;기입 체크
LOCAL = 0
;-------------------------------------------------

IF LOCAL && TALENT:171 && CFLAG:6011 == 0 && (TALENT:120 || TALENT:121)
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	CFLAG:6011 = 1
	RETURN 11
ENDIF


;-------------------------------------------------
;[음 단지]취득 후 CFLAG:6012
;-------------------------------------------------
;·보지(섹스) 계 조교에 대응한 음란계 소질
;·주인의 페니스에 몸이 적응한 것을 나타내, 주인의 페니스 이외에는 좋은 효과를 가져오지 않는다
;·또, 주인에 페니스가 없는 경우는 취득할 수 없다
;-------------------------------------------------
;기입 체크
LOCAL = 0
;-------------------------------------------------

IF LOCAL && TALENT:172 && CFLAG:6012 == 0
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	CFLAG:6012 = 1
	RETURN 12
ENDIF


;-------------------------------------------------
;[음고]취득 후 CFLAG:6013
;-------------------------------------------------
;·어널계 조교에 대응한 음란계 소질
;-------------------------------------------------
;기입 체크
LOCAL = 0
;-------------------------------------------------

IF LOCAL && TALENT:173 && CFLAG:6013 == 0
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	CFLAG:6013 = 1
	RETURN 13
ENDIF


;-------------------------------------------------
;[음유]취득 후 CFLAG:6014
;-------------------------------------------------
;·버스트계 조교에 대응한 음란계 소질
;-------------------------------------------------
;기입 체크
LOCAL = 0
;-------------------------------------------------

IF LOCAL && TALENT:174 && CFLAG:6014 == 0
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	CFLAG:6014 = 1
	RETURN 14
ENDIF


;-------------------------------------------------
;[요도광]취득 후 CFLAG:6015
;-------------------------------------------------
;·요도계 조교에 대응한 음란계 소질
;-------------------------------------------------
;기입 체크
LOCAL = 0
;-------------------------------------------------

IF LOCAL && TALENT:175 && CFLAG:6015 == 0
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	CFLAG:6015 = 1
	RETURN 15
ENDIF


;-------------------------------------------------
;[키스마]취득 후 CFLAG:6016
;-------------------------------------------------
;·키스계 조교에 대응한 음란계 소질
;-------------------------------------------------
;기입 체크
LOCAL = 0
;-------------------------------------------------

IF LOCAL && TALENT:176 && CFLAG:6016 == 0
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	CFLAG:6016 = 1
	RETURN 16
ENDIF


;-------------------------------------------------
;[드 S]취득 후 CFLAG:6017
;-------------------------------------------------
;·기학계 조교에 대응한 음란계 소질
;-------------------------------------------------
;기입 체크
LOCAL = 0
;-------------------------------------------------

IF LOCAL && TALENT:177 && CFLAG:6017 == 0
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	CFLAG:6017 = 1
	RETURN 17
ENDIF


;-------------------------------------------------
;[드 M]취득 후 CFLAG:6017
;-------------------------------------------------
;·피학계 조교에 대응한 음란계 소질
;-------------------------------------------------
;기입 체크
LOCAL = 0
;-------------------------------------------------

IF LOCAL && TALENT:178 && CFLAG:6017 == 0
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	CFLAG:6017 = 1
	RETURN 17
ENDIF


;=================================================
;붕괴계 소질·특수한 소질·특수한 각인 CFLAG:6021~6030
;=================================================
;·「조교 개시시 말확장」 「조교 단계별」에 속한다
;·붕괴계의 소질이나 특수한 상황으로 부가되는 소질, 각인을 취득한 뒤의 조교 개시시에 표시한다
;·붕괴계 소질 레벨 4에 해당하는 「괴조인격」은 초기 취득을 위해, 「특수한 첫조교 말」에 기재
;-------------------------------------------------

;-------------------------------------------------
;[사용마]취득 후 CFLAG:6021
;-------------------------------------------------
;·복종계 소질의 특수형
;·MASTER의 마력으로 사는 사용마로서의 계약을 한 것을 나타낸다
;-------------------------------------------------
;기입 체크
LOCAL = 0
;-------------------------------------------------

IF LOCAL && TALENT:164 && CFLAG:6021 == 0
	IF FLAG:11 > 0
		PRINTFORM 
		PRINTFORM 
	ENDIF
	CFLAG:6021 = 1
	RETURN 21
ENDIF


;-------------------------------------------------
;[유아 퇴행]취득 후 CFLAG:6022
;-------------------------------------------------
;·붕괴계 소질의 제일 단계
;·가혹한 조교의 스트레스로 정신이 유아화해 버린 것을 나타낸다
;-------------------------------------------------
;기입 체크
LOCAL = 0
;-------------------------------------------------

IF LOCAL && TALENT:159 && CFLAG:6022 == 0
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	CFLAG:6022 = 1
	RETURN 22
ENDIF


;-------------------------------------------------
;[정신 붕괴]취득 후 CFLAG:6023
;-------------------------------------------------
;·붕괴계 소질의 제2 단계
;·가혹한 조교의 스트레스로 마음이 파괴되어 버린 것을 나타낸다
;-------------------------------------------------
;기입 체크
LOCAL = 0
;-------------------------------------------------

IF LOCAL && TALENT:167 && CFLAG:6023 == 0
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	CFLAG:6023 = 1
	RETURN 23
ENDIF


;-------------------------------------------------
;[괴뢰]취득 후 CFLAG:6024
;-------------------------------------------------
;·붕괴계 소질의 제3 단계
;·가혹한 조교의 스트레스로 자아가 상실해 버린 것을 나타낸다
;-------------------------------------------------
;기입 체크
LOCAL = 0
;-------------------------------------------------

IF LOCAL && TALENT:167 && CFLAG:6024 == 0
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	CFLAG:6024 = 1
	RETURN 24
ENDIF


;-------------------------------------------------
;불사 각인 취득 후 CFLAG:6025
;-------------------------------------------------
;·촉수의 마력으로 활용되고 있는 상태를 나타낸다
;·디폴트에서는 Lv3만 표시한다
;-------------------------------------------------
;기입 체크
LOCAL = 0
;-------------------------------------------------

IF LOCAL && MARK:10
	;Lv3
	IF MARK:10 >= 3 && CFLAG:6025 < 3
		;연모
		IF TALENT:150
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		;그 이외
		ELSE
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		ENDIF
		CFLAG:6025 = 3
		RETURN 25
	ENDIF
ENDIF


;-------------------------------------------------
;약물 각인 취득 후 CFLAG:6026
;-------------------------------------------------
;·미약등의 약물에 대해 중독에 걸리고 있는 상태를 나타낸다
;·디폴트에서는 Lv3만 표시한다
;-------------------------------------------------
;기입 체크
LOCAL = 0
;-------------------------------------------------

IF LOCAL && MARK:3
	;Lv3
	IF MARK:3 >= 3 && CFLAG:6026 < 3
		;연모
		IF TALENT:150
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		;그 이외
		ELSE
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		ENDIF
		CFLAG:6026 = 3
		RETURN 26
	ENDIF
ENDIF

;-------------------------------------------------
;[오줌싸개 버릇]취득 후 CFLAG:6027
;-------------------------------------------------
;·절정시 등에 방뇨를 참을 수 없는 체질을 나타낸다
;·야뇨 이벤트등으로 취득된다
;-------------------------------------------------
;기입 체크
LOCAL = 0
;-------------------------------------------------

IF LOCAL && TALENT:45 && CFLAG:6027 == 0
	IF FLAG:11 > 0
		PRINTFORML 「우우, 미안해, %MASTERNAME%…」
		PRINTFORMW 코가사의 방에 들어가자, 흠뻑 젖은 시트를 들고 있는 코가사에 조우했다.
		PRINTFORMW 실례를 한 것은 알았다. …왜 양손으로 들고 있는 것일까?
		PRINTFORMW 「이러한 편이 놀라 줄 것 같아서」
		PRINTFORMW …버려버릴테다, 이 망가진 우산.
		PRINTFORMW 「하와와~…물이 새는 우산은 존재 가치가 없지. 훌쩍훌쩍」
	ENDIF
	CFLAG:6027 = 1
	RETURN 27
ENDIF


;=================================================
;특수한 조교 개시 말 CFLAG:6041~6050
;=================================================
;·「조교 개시시 말확장」에 속한다
;·일정한 조건을 채운 조교 개시시에 표시된다
;-------------------------------------------------



;-------------------------------------------------
;특정 날짜맡기고 후
;-------------------------------------------------
;·전회 조교하고 나서 특정의 날짜가 경과하고 있는 경우에 표시
;·전회 조교일은 CFLAG5206에 격납되어(DAY-CFLAG:5206)로 방치 날짜를 나타낸다
;·디폴트에서는 3일 이상 방치되었을 경우에 표시. 또, 말주가 전회 조수였던 경우는 표시하지 않는다
;-------------------------------------------------
;기입 체크
LOCAL = 0
;-------------------------------------------------

;3일 이상 방치
IF LOCAL && (DAY-CFLAG:5206) >= 3 && CFLAG:5217 == 0
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
ENDIF



RETURN 0



;-------------------------------------------------
;기브업 말
;-------------------------------------------------
;·체력이 500을 잘라 조교가 강제 종료할 때의 말
;·이 후 체력 한계 메세지를 사이에 두어 뒷정리 말, 조교 종료시라고 표시되지만,
;	각 분기내의 「RETURN 0」을 「RETURN 1」으로 하면(자) 비표시가 된다(이 말만으로 조교 종료한다)
;-------------------------------------------------
@KOJO_A_3_1_72
LOCAL = 1
;바이브 요괴 소멸용
SIF CFLAG:9102 == 9 || (CFLAG:9103 == 4 && SATOKO(ASSI))
	RETURN 2
;사토코 이벤트용
SIF CFLAG:9102 == 9 || (CFLAG:9103 == 4 && SATOKO(ASSI))
	LOCAL = 0

;볼 개그
IF TEQUIP:42 == 1
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 0
;촉수
ELSEIF TEQUIP:90
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 0
;야외 플레이·낮
ELSEIF TEQUIP:52 && TIME == 0
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 0
;야외 플레이·밤
ELSEIF TEQUIP:52
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 0
;관장
ELSEIF TEQUIP:25 == 7
	;배설 미경험
	IF (CFLAG:5304 & 1p10) == 0
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모+음고
	ELSEIF TALENT:150 && TALENT:173
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 0
;이뇨제
ELSEIF TEQUIP:12
	;연모+음핵
	IF TALENT:150 && TALENT:171
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 0
;그 이외
ELSE
	IF FLAG:11 > 0
		PRINTFORML 「………」
		PRINTFORMW 코가사는 완전히 지쳐서 자 버리는 것 같다….
		PRINTFORMW 이래서는 어쩔 수 없기 때문에, 이 다음은 다음번에 하기로 했다.
	ENDIF
	RETURN LOCAL
ENDIF
RETURN LOCAL

;-------------------------------------------------
;뒷정리 말
;-------------------------------------------------
;·특정의 장비품을 남겨 조교 종료했을 경우, 조교 종료시에 표시
;·위에 있는 것(정도)만큼 우선해 표시된다. 강정신약이 남아 있는 경우는 표시되지 않는다
;·볼 개그만, 표시한 후 다른 말도 표시한다(먼저 제외하지 않으면 말할 수 없기 때문에)
;-------------------------------------------------
@KOJO_A_3_2_72
;바이브 요괴 소멸
;이 시점에서 조수가 사라지기 (위해)때문에 코가사이 바이브 요괴의 아이를 잉것은 없다
IF CFLAG:9102 == 9
	IF CFLAG:9013 == 5
		PRINTFORML 『-깨끗이. 좋아? 너와 같은 도구에는 영혼이 있으니까? 』
		PRINTFORMW 코가사를 다 능욕한 후타나리 바이브 요괴는, 손가락을 세우곤 설교를 시작했다.
		PRINTFORMW 『그럼 나는 이만…네? 뭡니까 주인? 』
		PRINTFORML 『그대로 여기서 살지 않겠냐니, 아니아니아니, 그러니까 나 단순한 바이브입니다! 』
		PRINTFORMW 『…그 만큼 이 우산양을 위로해 주세요. 주인』
		PRINTFORMW 『그리고, 또 이 아이 안에 꿈틀꿈틀 넣어 주세요」
		PRINTFORML 『…기분이 좋아요 , 그게 또. 히히, 그럼 나는 이만! 』
		PRINTFORMW ――훗. 부서지듯이 그녀는 사라지고…거기에는 천한 핑크색의 바이브가 떨어지고 있었다.
	ELSE
		PRINTFORM 「후히아아…굉장했다…
		CALL HEARTBW
		PRINTFORMW 『히힛, 그렇지그렇지. 나는 여자를 울려버리니까 』
		PRINTFORMW 『그럼 나는 이만. 또 모이면 부탁해, 우산양』
		PRINTFORMW 『나도 그거 좋아해. 알게 되면 단순한 바이브로는, 돌아갈 수 없겠지만」
		PRINTFORMW 「엄청 좋았어∼…또 와―」
		PRINTFORMW 아니, 오지 않아도 좋을거 같다.
		PRINTFORMW 『그러면 주인, 매번 아무쪼록, 건강하길! 』
		PRINTFORMW ――그렇게 해서 그녀는, 변함 없이 천한 핑크색의 바이브로 돌아가는 것이었다.
	ENDIF
	DELCHARA ASSI
	ASSI = -1
	ASSIPLAY = 0
	PLAYER = MASTER
	ITEM:3 = ITEM:3 ?  ITEM:3 # 1
	CFLAG:9102 = 0
	RETURN 2
ENDIF

;사토코 이벤트
IF CFLAG:9103 == 4 && SATOKO(ASSI)
	PRINTFORMW 조교가 끝난 후, %조사처리(CALLNAME:ASSI,"가")% %조사처리(CALLNAME:MASTER,"를")% 불러 세웠다.
	PRINTFORMW 『미안합니다』
	PRINTFORMW 『이번에는, …둘이서만, 이야기를 하고 싶습니다』
	PRINTFORMW 『아, 아니요 지금은 아직. 결정되지 않았습니다…그리고』
	PRINTFORMW 흘끔, 곁눈질로 %조사처리(CALLNAME:ASSI,"는")% 코가사를 본다.
	PRINTFORMW 코가사가 있으면 재미없을 이야기인 것일까….
	PRINTFORMW 『이번, 부탁합니다』
	CFLAG:9103 = 5
	RETURN 2
ENDIF

;우선 볼 개그를 제외한다
IF TEQUIP:42 == 1
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
ENDIF

;야외 플레이·낮
IF TEQUIP:52 && TIME == 0
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
;야외 플레이·밤
ELSEIF TEQUIP:52
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
;촉수
ELSEIF TEQUIP:90
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
;줄
ELSEIF TEQUIP:10 && TEQUIP:40
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
;욕실
ELSEIF TEQUIP:53
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
;관장
ELSEIF TEQUIP:25 == 7
	;배설 미경험
	IF (CFLAG:5304 & 1p10) == 0
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모+음고
	ELSEIF TALENT:150 && TALENT:173
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
;이뇨제
ELSEIF TEQUIP:12
	;연모+음핵
	IF TALENT:150 && TALENT:171
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
;바이브
ELSEIF TEQUIP:20 == 1
	;연모+음 단지
	IF TALENT:150 && TALENT:172
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
;어널 바이브
ELSEIF TEQUIP:25 == 1
	;연모+음고
	IF TALENT:150 && TALENT:173
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
;미약
ELSEIF TEQUIP:11
	;약물 경험 150이상
	IF CFLAG:5204 >= 150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
;기브업
ELSEIF BASE < 500
	IF FLAG:11 > 0
		PRINTFORML 「……응응…%MASTERNAME%…」
		PRINTFORMW 코가사을 침대에 옮겨, 이불을 걸어 준다.
		PRINTFORMW %조사처리(CALLNAME:MASTER,"는")% 살그머니 방을 뒤로했다….
	ENDIF
	RETURN 1
ENDIF
RETURN 0



;-------------------------------------------------
;추가 기능군A　잡다한 추가 기능
;-------------------------------------------------

;-------------------------------------------------
;※추가 기능 A1『긴박 퇴피 말』
;-------------------------------------------------
;·커멘드 말에 대신해 표시
;·커멘드 실행시, 행동을 저해하는 아이템을 장비 하고 있을 때 표시
;·속박되어 움직이거나 구가 차 말하거나 하는 것을 회피한다
;·말하지 않게 되면 그것으로 OK의 항목은 공난가능
;-------------------------------------------------
@KOJO_D_A1_72
;볼 개그
IF TEQUIP:42 == 1 && SELECTCOM != 226
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
;촉수 관통
ELSEIF TEQUIP:25 == 3 && SELECTCOM != 524
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
;촉수구욕
ELSEIF TEQUIP:42 == 2 && SELECTCOM != 506
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
;촉수 긴박
ELSEIF TEQUIP:40 && SELECTCOM != 507 && TEQUIP:90
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
;촉수 최면
ELSEIF TEQUIP:92 && SELECTCOM != 550
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
;충목욕탕
ELSEIF TEQUIP:55 == 1 && SELECTCOM != 360
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
;뱀장어 목욕탕
ELSEIF TEQUIP:55 && SELECTCOM != 361
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
;줄, 줄계 특수 커멘드
ELSEIF (TEQUIP:40 && SELECTCOM != 224) || (TEQUIP:72 && SELECTCOM != 406) || (TEQUIP:73 && SELECTCOM != 408) || (TEQUIP:10 && (SELECTCOM < 622 || SELECTCOM > 624))
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
;아이마스크, 어두운 곳
ELSEIF (TEQUIP:41 == 1 && SELECTCOM != 225) || (TEQUIP:70 && SELECTCOM != 401)
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
;여체 번화가
ELSEIF TEQUIP:60 && SELECTCOM != 107
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
;야외 플레이
ELSEIF TEQUIP:60 && SELECTCOM != 107
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
;은방울꽃의 독
ELSEIF TEQUIP:18 && SELECTCOM != 420
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
ENDIF
RETURN 0


;-------------------------------------------------
;※추가 기능 A4『조르는 말』
;-------------------------------------------------
;·커멘드 선택전, 특정의 조건·확률로 말을 표시. 너무 길면(자) 파라미터 표시가 화면외로 납니다.
;·각 항목마다의 「A = 0」을 임의의 발생 확률에 고쳐 쓰는 것(100으로 반드시 표시. 디폴트는0=비표시)
;·동시에 조건과 판정을 채우는 경우, 위의 것이 우선 표시된다
;·일회뿐이라고 쓰여진 것을 쓸 때는, 발생 확률 100을 추천(표시되지 않았던 경우 이제(벌써) 나오지 않는다)
;·쓰지 않는 것은 발생 확률 0으로 해 두면 지울 필요는 없다
;-------------------------------------------------
@KOJO_D_A4_72

;-------------------------------------------------
;조교 직전
;-------------------------------------------------
;·그 조교로 최초의 커멘드를 선택할 경우에 표시
;-------------------------------------------------
;LOCAL = 표시된 개시 서두의 번호
;-------------------------------------------------
IF PREVCOM < 0
	;첫조교시(일회뿐)
	IF LOCAL == 10
		;※발생 확률
		RESULT = PERCENT(100)
		IF RESULT
			;통상
			IF CFLAG:18 == 0
				IF FLAG:11 > 0
					PRINTFORMW ――움찔.
					PRINTFORMW 가볍게 어깨를 눌러 주자, 코가사는 사랑스럽게 침대에 엉덩방아를 찧었다.
					PRINTFORMW 「어, 옷 벗는 거야? 뭐하는 거야?」
					PRINTFORMW 밑에 깔려서 억지로 옷을 탈의하면서 두리번두리번 근처를 둘러보는 코가사.
					PRINTFORMW 맨살 보이는 수치보다, 지금부터 무엇을 할지에 대해 신경 쓰고 있는 모습이다.
					PRINTFORMW 「깜짝 놀라게하려고 하고 있어? 놀래키는 것은 나의 일인데!」
					PRINTFORMW 아. 코가사가 말하는 대로, 반드시 놀랄 것이다.
					PRINTFORMW ――그런데, 어디부터 더럽혀 줄까.
				ENDIF
			;주회 플레이
			ELSE
				IF FLAG:11 > 0
					PRINTFORMW 「스승, 닿았어」
					PRINTFORMW 「 나의 신체, 스승이 알고 있는 음란한 신체인 채야」
					PRINTFORMW 「후후후, 한개씩 확인해 줄래?」
				ENDIF
			ENDIF
			RETURN 1
		ENDIF
	;그 이외
	ELSE
		;※발생 확률
		RESULT = PERCENT(0)
		IF RESULT
			;연모
			IF TALENT:150
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			;그 이외
			ELSE
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;첫절정· 첫부위 절정 직후
;-------------------------------------------------
;· 각 절정을 처음으로 경험한 직후의 커멘드전에 표시. 모두 표시 기회는 일회뿐
;· 첫절정은 「조교로 이구의 것이 처음」, 처음○절정은 「다른 부위에서 이연과는 있지만, 그 부위에서의 절정이 처음」
;· 첫절정과 동시에 체력 500 이하가 되었을 경우는 표시되지 않는다(다음의 커멘드 기회가 없기 때문에)
;-------------------------------------------------
IF E
	;첫절정시(일회뿐)
	IF (CFLAG:5202 & 1p1) == 0
		;※발생 확률
		RESULT = PERCENT(0)
		IF RESULT
			;주사 절정
			IF SELECTCOM > 150 && SELECTCOM < 92
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			;V절정
			ELSEIF NOWEX:1
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			;C절정
			ELSEIF NOWEX:0
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			;A절정
			ELSEIF NOWEX:2
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			;B절정
			ELSEIF NOWEX:3
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			ENDIF
			RETURN 1
		ENDIF
	;첫사중 절정 후(일회뿐)
	ELSEIF NOWEX:0 && NOWEX:1 && NOWEX:2 && NOWEX:3 && (CFLAG:5202 & 1p2) == 0
		;※발생 확률
		RESULT = PERCENT(0)
		IF RESULT
			;연모
			IF TALENT:150
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			;그 이외
			ELSE
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			ENDIF
			RETURN 1
		ENDIF
	;첫C절정 후(일회뿐)
	ELSEIF NOWEX:0 && (CFLAG:5202 & 1p3) == 0
		;※발생 확률
		RESULT = PERCENT(0)
		IF RESULT
			;연모
			IF TALENT:150
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			;그 이외
			ELSE
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			ENDIF
			RETURN 1
		ENDIF
	;첫V절정 후(일회뿐)
	ELSEIF NOWEX:1 && (CFLAG:5202 & 1p4) == 0
		;※발생 확률
		RESULT = PERCENT(0)
		IF RESULT
			;질내 사정도 되었다
			IF TFLAG:10
				;연모+주인
				IF TALENT:150 && ASSIPLAY == 0
					IF FLAG:11 > 0
						PRINTFORM 
						PRINTFORM 
					ENDIF
				;그 이외
				ELSE
					IF FLAG:11 > 0
						PRINTFORM 
						PRINTFORM 
					ENDIF
				ENDIF
			;그 이외
			ELSE
				;연모+주인
				IF TALENT:150 && ASSIPLAY == 0
					IF FLAG:11 > 0
						PRINTFORM 
						PRINTFORM 
					ENDIF
				;그 이외
				ELSE
					IF FLAG:11 > 0
						PRINTFORM 
						PRINTFORM 
					ENDIF
				ENDIF
			ENDIF
			RETURN 1
		ENDIF
	;첫A절정 후(일회뿐)
	ELSEIF NOWEX:2 && (CFLAG:5202 & 1p5) == 0
		;※발생 확률
		RESULT = PERCENT(0)
		IF RESULT
			;연모
			IF TALENT:150
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			;그 이외
			ELSE
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			ENDIF
			RETURN 1
		ENDIF
	;첫B절정 후(일회뿐)
	ELSEIF NOWEX:3 && (CFLAG:5202 & 1p6) == 0
		;※발생 확률
		RESULT = PERCENT(0)
		IF RESULT
			;연모
			IF TALENT:150
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			;그 이외
			ELSE
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			ENDIF
			RETURN 1
		ENDIF
	;첫사정 후(일회뿐)
	ELSEIF NOWEX:10 && (CFLAG:5202 & 1p7) == 0
		;※발생 확률
		RESULT = PERCENT(0)
		IF RESULT
			;연모
			IF TALENT:150
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			;그 이외
			ELSE
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			ENDIF
			RETURN 1
		ENDIF
	;첫분유 후(일회뿐)
	ELSEIF NOWEX:11 && (CFLAG:5202 & 1p8) == 0
		;※발생 확률
		RESULT = PERCENT(0)
		IF RESULT
			;연모
			IF TALENT:150
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			;그 이외
			ELSE
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			ENDIF
			RETURN 1
		ENDIF
	;첫방뇨 후(일회뿐)
	ELSEIF NOWEX:12 && (CFLAG:5202 & 1p9) == 0
		;※발생 확률
		RESULT = PERCENT(0)
		IF RESULT
			;연모
			IF TALENT:150
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			;그 이외
			ELSE
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;V삽입중
;-------------------------------------------------
;·삽입없음 플래그가 있을 때 표시
;-------------------------------------------------
IF TFLAG:102 && V
	;처녀 상실했던 바로 직후
	IF TFLAG:101
		;첫회(일회뿐)
		IF (CFLAG:5202 & 1p2) == 0
			CFLAG:5202 |= 1p2
			;※발생 확률
			RESULT = PERCENT(0)
			IF RESULT
				;연모+주인
				IF TALENT:150 && ASSIPLAY == 0
					IF FLAG:11 > 0
						PRINTFORM 
						PRINTFORM 
					ENDIF
				;그 이외
				ELSE
					IF FLAG:11 > 0
						PRINTFORM 
						PRINTFORM 
					ENDIF
				ENDIF
				RETURN 1
			ENDIF
		;그 이외
		ELSE
			;※발생 확률
			RESULT = PERCENT(0)
			IF RESULT
				;연모+주인
				IF TALENT:150 && ASSIPLAY == 0
					IF FLAG:11 > 0
						PRINTFORM 
						PRINTFORM 
					ENDIF
				;그 이외
				ELSE
					IF FLAG:11 > 0
						PRINTFORM 
						PRINTFORM 
					ENDIF
				ENDIF
				RETURN 1
			ENDIF
		ENDIF
	;V이속여
	ELSEIF NOWEX:1
		;이 오면서 질내 사정되었다
		IF TFLAG:10
			;※발생 확률
			RESULT = PERCENT(0)
			IF RESULT
				;연모+주인
				IF TALENT:150 && ASSIPLAY == 0
					IF FLAG:11 > 0
						PRINTFORM 
						PRINTFORM 
					ENDIF
				;그 이외
				ELSE
					IF FLAG:11 > 0
						PRINTFORM 
						PRINTFORM 
					ENDIF
				ENDIF
				RETURN 1
			ENDIF
		;그 이외
		ELSE
			;※발생 확률
			RESULT = PERCENT(0)
			IF RESULT
				;연모+주인
				IF TALENT:150 && ASSIPLAY == 0
					IF FLAG:11 > 0
						PRINTFORM 
						PRINTFORM 
					ENDIF
				;그 이외
				ELSE
					IF FLAG:11 > 0
						PRINTFORM 
						PRINTFORM 
					ENDIF
				ENDIF
				RETURN 1
			ENDIF
		ENDIF
	;V이 기초
	ELSEIF PALAM:1 > 9000
		
		
		
		
		
		
		
		
		
	ENDIF
ENDIF

;-------------------------------------------------
;절정계
;-------------------------------------------------
;·최초의 조교로 최초의 커멘드를 선택할 경우에 표시
;·쓰는 경우는 발생 확률 100을 추천
;-------------------------------------------------
;발생 확률
	RESULT = 0
;-------------------------------------------------
IF (CFLAG:5202 & 1p0) == 0
		RESULT = PERCENT(RESULT)
	IF RESULT == 1
		;주인이 남자(통상)
		IF TALENT:MASTER:120
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		;주인이 여자
		ELSE
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		ENDIF
		CFLAG:5202 |= 1p0
		RETURN 1
	ENDIF
ENDIF






RETURN 0


;-------------------------------------------------
;추가 기능군B　안면 관리 기능
;-------------------------------------------------

;-------------------------------------------------
;※추가 기능 B1『안면 관리 기능』
;-------------------------------------------------
;·초기 안면 플래그의 대입만
;-------------------------------------------------
;원작으로 안면이 있는 캐릭터를 여기서 판별합니다
;적당 코멘트 해제해 개변·추가해 주세요(800+캐릭터 번호)
;예) 말주가 파르스의 경우, 레이무·마리사·키스메·야마메·용의
@KOJO_D_B1_72
;적당 comment out를 해제해 추가하는 것
;SIF CFLAG:5801 == 0
;	CFLAG:5801 = 1
;SIF CFLAG:5802 == 0
;	CFLAG:5802 = 1
;SIF CFLAG:5857 == 0
;	CFLAG:5857 = 1
;SIF CFLAG:5858 == 0
;	CFLAG:5858 = 1
;SIF CFLAG:5860 == 0
;	CFLAG:5860 = 1

;-------------------------------------------------
;※추가 기능 B2『조수 감상 말』
;-------------------------------------------------
;조수로서 처녀 상실이 발생한 후의 조교 개시시 말
;-------------------------------------------------
;·파일 A 「조교 개시시 말」에 대신해 표시
;·CFLAG:5217에 말주가 조수로서 전회 조교한 상대가 들어온다
;-------------------------------------------------
@KOJO_D_B2_72(ARG)
;-------------------------------------------------
;조수로서 말주가 처녀를 상실하고 있었을 경우
;-------------------------------------------------
;주인이 빼앗았다
IF CFLAG:5214 == 1
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	CFLAG:5214 = 0
	RETURN 1
;노예가 빼앗았다
ELSEIF CFLAG:5214 == 2
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	CFLAG:5214 = 0
	RETURN 1
ENDIF

;-------------------------------------------------
;말주가 타인의 처녀를 빼앗았을 경우
;-------------------------------------------------
IF CFLAG:5212
	;처녀식 있고 10명째 기념
	IF CFLAG:5213 == 10
		;연모
		IF TALENT:150
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		;그 이외
		ELSE
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		ENDIF
	;자신의 페니스로 찢었다
	ELSEIF CFLAG:5212 >= 3 && (TALENT:121 || TALENT:121 || TALENT:120)
		;처녀식 있고 첫체험
		IF CFLAG:5213 == 1
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		;연모
		ELSEIF TALENT:150
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		;그 이외
		ELSE
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		ENDIF
	;페니스 이외로 이겼는지, 현재 나지 않았다
	ELSE
		;처녀식 있고 첫체험
		IF CFLAG:5213 == 1
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		;연모
		ELSEIF TALENT:150
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		;그 이외
		ELSE
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		ENDIF
	ENDIF
	CFLAG:5212 = 0
	RETURN 1
ENDIF

;-------------------------------------------------
;전회의 조교로 조수였던 경우의 개시시 말
;-------------------------------------------------
;·파일 A 「조교 개시시 말」에 대신해 표시
;·전회 조수로서 조교한 상대가 조수의 경우와 첫회시, 3일 이상 조교하고 있지 않는 경우는 확실히 표시
;·그 이외는1/2의 확률로 표시
;·전회 조교한 상대의 이름은%STR:200%, 통칭은%STR:201%로 표시 가능
;-------------------------------------------------
;전회 조교한 상대가 조수(보복 시츄)
IF ASSINO() == CFLAG:5217 && (CFLAG:ARG == 3 || CFLAG:ARG >= 5 || CFLAG:5215 >= 1 || CFLAG:ARG >= 6)
	;조수의 처녀를 빼앗았던 적이 있다
	IF CFLAG:ARG >= 6
		;연모
		IF TALENT:150
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		;그 이외
		ELSE
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		ENDIF
	;그 이외
	ELSE
		;연모
		IF TALENT:150
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		;그 이외
		ELSE
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		ENDIF
	ENDIF
	CFLAG:5201 |= 1p12
	RETURN 1
;첫회
ELSEIF (CFLAG:5201 & 1p12) == 0
	;조교를 실시했다
	IF CFLAG:ARG == 3 || CFLAG:ARG >= 5 || CFLAG:5215 >= 1
		;연모
		IF TALENT:150
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		;그 이외
		ELSE
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		ENDIF
	;일이 없었다
	ELSE
		;연모
		IF TALENT:150
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		;그 이외
		ELSE
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		ENDIF
	ENDIF
	CFLAG:5201 |= 1p12
	RETURN 1
;방치 3일 이상
ELSEIF (3+CFLAG:5206) < DAY
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;그 이외(1/2의 확률로 표시)
ELSEIF ! RAND:2
	;조교를 실시했다
	IF CFLAG:5211 == 1 || CFLAG:5215 >= 3
		;상대나 자신도 연모였다(연적 시츄)
		IF TALENT:150 && TALENT:A:150
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		;그 이외
		ELSE
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		ENDIF
	;가벼운 행위만
	ELSEIF CFLAG:5215 == 2
		;연모
		IF TALENT:150
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		;그 이외
		ELSE
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		ENDIF
	;비디오 찍었을 뿐
	ELSEIF CFLAG:5215 == 1
		;연모
		IF TALENT:150
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		;그 이외
		ELSE
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		ENDIF
	;견만
	ELSE
		;연모
		IF TALENT:150
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		;그 이외
		ELSE
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		ENDIF
	ENDIF
	RETURN 1
ENDIF
RETURN 0


;-------------------------------------------------
;※추가 기능 B3『질투 기능』
;-------------------------------------------------
;말주 이외를 조교하고 나서, 말 주로 어떠한 수단으로 다른 아이가 알려졌을 때의 개시시 말
;-------------------------------------------------
;·파일 A 「조교 개시시 말」에 대신해 표시
;·전회 조교한 상대가 조수의 경우는 표시하지 않는다
;·연모시만으로,1/2의 확률로 표시. 말콘피그로 연모시 이외에 발생시키는 일도 가능(확률1/4)
;·전회 조교한 상대의 이름은%STR:200%, 통칭은%STR:201%로 표시 가능
;-------------------------------------------------
@KOJO_D_B3_72(ARG, ARG:1)
IF (TALENT:150 && ARG:1 < 2) || (CFLAG:5222 & 1p2 && ARG:1)
	;첫회
	IF (CFLAG:5201 & 1p13) == 0
		;안면 있어
		IF CFLAG:ARG > 1
			;연모
			IF TALENT:150
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			;그 이외
			ELSE
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			ENDIF
		;원작으로 안면 있어
		ELSEIF CFLAG:ARG == 1
			;연모
			IF TALENT:150
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			;그 이외
			ELSE
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			ENDIF
		;안면 없음
		ELSE
			;연모
			IF TALENT:150
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			;그 이외
			ELSE
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			ENDIF
		ENDIF
		CFLAG:5201 |= 1p13
	;2회째 이후
	ELSE
		;안면 있어
		IF CFLAG:ARG > 1
			;연모
			IF TALENT:150
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			;그 이외
			ELSE
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			ENDIF
		;원작으로 안면 있어
		ELSEIF CFLAG:ARG == 1
			;연모
			IF TALENT:150
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			;그 이외
			ELSE
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			ENDIF
		;안면 없음
		ELSE
			;연모
			IF TALENT:150
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			;그 이외
			ELSE
				IF FLAG:11 > 0
					PRINTFORM 
					PRINTFORM 
				ENDIF
			ENDIF
		ENDIF
	ENDIF
	;안면 판정 갱신
	SIF CFLAG:ARG < 1
		CFLAG:ARG = 1
	RETURN 1
ENDIF



;-------------------------------------------------
;안면별 커멘드 말
;-------------------------------------------------
;·파일 C 「커멘드 말」에 대신해 표시
;·특정의 커멘드를 실행했을 때에 표시
;-------------------------------------------------
@KOJO_D_B4B_72

;-------------------------------------------------
;바이브 CFLAG311
;-------------------------------------------------
;개시시
IF SELECTCOM == 12 && TEQUIP:20 == 1
	;조수에 의한 플레이
	;처음
	IF CFLAG:5311 == 0 && ASSIPLAY
		;처녀
		IF TALENT:0 && EXP:0 == 1
			;※추가 기능 B2『조수 감상 기능』으로 보존한 데이터를 참조
			A = ASSINO() +5800
			;연모+조수의 처녀를 말주가 빼앗고 있다
			IF TALENT:150 && CFLAG:A >= 6
				PRINTFORMW 「복수처럼 바이브로 푹 당했습니다」
			;연모
			ELSEIF TALENT:150
				PRINTFORM 
			;조수의 처녀를 말주가 빼앗고 있다
			ELSEIF CFLAG:A >= 6
				PRINTFORMW 「복수처럼 바이브로 푹 당했습니다」
			;그 이외
			ELSE
				PRINTFORM 
			ENDIF
		;비처녀
		ELSE
			;연모
			IF TALENT:150
				PRINTFORM 
			;그 이외
			ELSE
				PRINTFORM 
			ENDIF
		ENDIF
		CFLAG:5311 = 1
	;2회째 이후
	ELSEIF ASSIPLAY
		;윤활 2 이하
		IF PALAM:10 < PALAMLV:2
			PRINTFORM 
		;연모
		ELSEIF TALENT:150
			PRINTFORM 
		;V감각 Lv3 이상
		ELSEIF ABL:1 >= 3
			PRINTFORM 
		;그 이외
		ELSE
			PRINTFORM 
		ENDIF
	;주인에 의한 플레이
	;처음
	ELSEIF CFLAG:5311 == 0
		;처녀
		IF TALENT:0 && EXP:0 == 1
			;연모
			IF TALENT:150
				PRINTFORM 
			;그 이외
			ELSE
				PRINTFORM 
			ENDIF
		;비처녀
		ELSE
			;연모
			IF TALENT:150
				PRINTFORM 
			;그 이외
			ELSE
				PRINTFORM 
			ENDIF
		ENDIF
		CFLAG:5311 = 1
	;2회째 이후
	ELSE
		;윤활 2 이하
		IF PALAM:10 < PALAMLV:2
			PRINTFORM 
		;연모+정액량 2이상
		ELSEIF TALENT:150 && CFLAG:5231 > 1
			PRINTFORM 
		;연모
		ELSEIF TALENT:150
			PRINTFORM 
		;반발 각인 Lv3+V감각 Lv3 이상
		ELSEIF MARK:9 == 3 && ABL:1 >= 3
			PRINTFORM 
		;반발 각인 Lv3
		ELSEIF MARK:9 == 3
			PRINTFORM 
		;V감각 Lv3 이상
		ELSEIF ABL:1 >= 3
			PRINTFORM 
		;그 이외
		ELSE
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 0
;해제시
ELSEIF SELECTCOM == 12 && TEQUIP:20 == 0
	;처녀였다
	IF CFLAG:5311 == 1
		;연모
		IF TALENT:150
			PRINTFORM 
		;그 이외
		ELSE
			PRINTFORM 
		ENDIF
		CFLAG:5311 = 2
	;처녀는 아니었다
	ELSE
		;연모+정액량 2이상
		IF TALENT:150 && CFLAG:5231 > 1
			PRINTFORM 
		;연모
		ELSEIF TALENT:150
			PRINTFORM 
		;그 이외
		ELSE
			PRINTFORM 
		ENDIF
	ENDIF
	;정액량-1
	SIF CFLAG:5231 > 0
		CFLAG:5231 -= 1
	RETURN 0
ENDIF


;-------------------------------------------------
;정상정도 CFLAG320
;삽입 G스포 꾸짖고·삽입 자궁구 꾸짖고(요점 삽입 국소 고문 패치. 같은 체위를 반복하면(자) 페니스로 G스포트나 자궁구를 집요하게 꾸짖습니다)
;-------------------------------------------------
IF SELECTCOM == 20
	;조수에 의한 플레이
	;처음
	IF CFLAG:5320 == 0 && ASSIPLAY
		;처녀
		IF TALENT:0 && EXP:0 == 1
			;※추가 기능 B2『조수 감상 기능』으로 보존한 데이터를 참조
			A = ASSINO() +5800
			;연모+조수의 처녀를 말주가 빼앗고 있다
			IF TALENT:150 && CFLAG:A >= 6
				PRINTFORMW 「복수같이 처녀 빼앗깁니다」
			;연모
			ELSEIF TALENT:150
				PRINTFORM 
			;조수의 처녀를 말주가 빼앗고 있다
			ELSEIF CFLAG:A >= 6
				PRINTFORMW 「복수같이 처녀 빼앗깁니다」
			;그 이외
			ELSE
				PRINTFORM 
			ENDIF
		;비처녀
		ELSE
			;연모
			IF TALENT:150
				PRINTFORM 
			;그 이외
			ELSE
				PRINTFORM 
			ENDIF
		ENDIF
		CFLAG:5320 = 1
	;2회째 이후
	ELSEIF ASSIPLAY
		;윤활 2 이하
		IF PALAM:10 < PALAMLV:2
			PRINTFORM 
		;연모+조교자가 사정
		ELSEIF TALENT:150 && TFLAG:10
			PRINTFORM 
		;연모
		ELSEIF TALENT:150
			PRINTFORM 
		;반발 각인 Lv3
		ELSEIF MARK:9 == 3
			PRINTFORM 
		;V감각 Lv3 이상+조수가 뚜껑이든지
		ELSEIF ABL:1 >= 3 && TALENT:ASSI:121
			PRINTFORM 
		;그 이외
		ELSE
			PRINTFORM 
		ENDIF
	;주인에 의한 플레이
	;처음
	ELSEIF CFLAG:5320 == 0
		;처녀
		IF TALENT:0 && EXP:0 == 1
			;연모
			IF TALENT:150
				PRINTFORM 
			;반발 각인 Lv3
			ELSEIF MARK:9 == 3
				PRINTFORM 
			;그 이외
			ELSE
				PRINTFORM 
			ENDIF
		;비처녀
		ELSE
			;연모
			IF TALENT:150
				PRINTFORM 
			;반발 각인 Lv3
			ELSEIF MARK:9 == 3
				PRINTFORM 
			;그 이외
			ELSE
				PRINTFORM 
			ENDIF
		ENDIF
			CFLAG:5320 = 1
	;2회째 이후
	ELSE
		;윤활 2 이하
		IF PALAM:10 < PALAMLV:2
			PRINTFORM 
		;연모+조교자가 사정
		ELSEIF TALENT:150 && TFLAG:10
			PRINTFORM 
		;연모
		ELSEIF TALENT:150
			PRINTFORM 
		;반발 각인 Lv3+V감각 Lv3 이상
		ELSEIF MARK:9 == 3 && ABL:1 >= 3
			PRINTFORM 
		;반발 각인 Lv3
		ELSEIF MARK:9 == 3
			PRINTFORM 
		;굴복 각인 Lv3+V감각 Lv3 이상
		ELSEIF MARK:2 == 3 && ABL:1 >= 3
			PRINTFORM 
		;굴복 각인 Lv3
		ELSEIF MARK:2 == 3
			PRINTFORM 
		;그 이외
		ELSE
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 0
;삽입 G스포 고문
ELSEIF SELECTCOM == 614 && PREVCOM == 20
	;첫회
	IF CFLAG:5320 == 1 || CFLAG:5320 == 3
		;연모
		IF TALENT:150
			PRINTFORM 
		;V감각 4이상
		ELSEIF ABL:1 > 3
			PRINTFORM 
		;그 이외
		ELSE
			PRINTFORM 
		ENDIF
		SIF CFLAG:5320 == 1
			CFLAG:5320 = 2
		SIF CFLAG:5320 == 3
			CFLAG:5320 = 4
	;2회째 이후
	ELSE
		;연모
		IF TALENT:150
			PRINTFORM 
		;V감각 4이상
		ELSEIF ABL:1 > 3
			PRINTFORM 
		;그 이외
		ELSE
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 0
;삽입 자궁구 고문
ELSEIF SELECTCOM == 615 && PREVCOM == 20
	;첫회
	IF CFLAG:5320 == 1 || CFLAG:5320 == 2
		;연모
		IF TALENT:150
			PRINTFORM 
		;V감각 4이상
		ELSEIF ABL:1 > 3
			PRINTFORM 
		;그 이외
		ELSE
			PRINTFORM 
		ENDIF
		SIF CFLAG:5320 == 1
			CFLAG:5320 = 3
		SIF CFLAG:5320 == 2
			CFLAG:5320 = 4
	;2회째 이후
	ELSE
		;연모
		IF TALENT:150
			PRINTFORM 
		;V감각 4이상
		ELSEIF ABL:1 > 3
			PRINTFORM 
		;그 이외
		ELSE
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;후배 정도 CFLAG321
;삽입 G스포 꾸짖고·삽입 자궁구 꾸짖고(요점 삽입 국소 고문 패치. 같은 체위를 반복하면(자) 페니스로 G스포트나 자궁구를 집요하게 꾸짖습니다)
;-------------------------------------------------
IF SELECTCOM == 21
	;조수에 의한 플레이
	;처음
	IF CFLAG:5321 == 0 && ASSIPLAY
		;처녀
		IF TALENT:0 && EXP:0 == 1
			;※추가 기능 B2『조수 감상 기능』으로 보존한 데이터를 참조
			A = ASSINO() +5800
			;연모+조수의 처녀를 말주가 빼앗고 있다
			IF TALENT:150 && CFLAG:A >= 6
				PRINTFORM 
			;연모
			ELSEIF TALENT:150
				PRINTFORM 
			;조수의 처녀를 말주가 빼앗고 있다
			ELSEIF CFLAG:A >= 6
				PRINTFORM 
			;그 이외
			ELSE
				PRINTFORM 
			ENDIF
		;비처녀
		ELSE
			;연모
			IF TALENT:150
				PRINTFORM 
			;그 이외
			ELSE
				PRINTFORM 
			ENDIF
		ENDIF
		CFLAG:5321 = 1
	;2회째 이후
	ELSEIF ASSIPLAY
		;윤활 2 이하
		IF PALAM:10 < PALAMLV:2
			PRINTFORM 
		;연모+조수가 동물귀
		ELSEIF TALENT:150 && TALENT:ASSI:121
			PRINTFORMW 「주인 앞에서, 동물에게 뒷치기로 범해진다……」
		;연모+조교자가 사정
		ELSEIF TALENT:150 && TFLAG:10
			PRINTFORM 
		;연모
		ELSEIF TALENT:150
			PRINTFORM 
		;반발 각인 Lv3
		ELSEIF MARK:9 == 3
			PRINTFORM 
		;V감각 Lv3 이상+조수가 뚜껑이든지
		ELSEIF ABL:1 >= 3 && TALENT:ASSI:121
			PRINTFORM 
		;그 이외
		ELSE
			PRINTFORM 
		ENDIF
	;주인에 의한 플레이
	;처음
	ELSEIF CFLAG:5321 == 0
		;처녀
		IF TALENT:0 && EXP:0 == 1
			;연모
			IF TALENT:150
				PRINTFORM 
			;반발 각인 Lv3
			ELSEIF MARK:9 == 3
				PRINTFORM 
			;그 이외
			ELSE
				PRINTFORM 
			ENDIF
		;비처녀
		ELSE
			;연모
			IF TALENT:150
				PRINTFORM 
			;그 이외
			ELSE
				PRINTFORM 
			ENDIF
		ENDIF
		CFLAG:5321 = 1
	;2회째 이후
	ELSE
		;윤활 2 이하
		IF PALAM:10 < PALAMLV:2
			PRINTFORM 
		;연모+조교자가 사정
		ELSEIF TALENT:150 && TFLAG:10
			PRINTFORM 
		;연모
		ELSEIF TALENT:150
			PRINTFORM 
		;반발 각인 Lv3+V감각 Lv3 이상
		ELSEIF MARK:9 == 3 && ABL:1 >= 3
			PRINTFORM 
		;반발 각인 Lv3
		ELSEIF MARK:9 == 3
			PRINTFORM 
		;굴복 각인 Lv3+V감각 Lv3 이상
		ELSEIF MARK:2 == 3 && ABL:1 >= 3
			PRINTFORM 
		;굴복 각인 Lv3
		ELSEIF MARK:2 == 3
			PRINTFORM 
		;그 이외
		ELSE
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 0
;삽입 G스포 고문
ELSEIF SELECTCOM == 614 && PREVCOM == 21
	;첫회
	IF CFLAG:5321 == 1 || CFLAG:5321 == 3
		;연모
		IF TALENT:150
			PRINTFORM 
		;V감각 4이상
		ELSEIF ABL:1 > 3
			PRINTFORM 
		;그 이외
		ELSE
			PRINTFORM 
		ENDIF
		SIF CFLAG:5321 == 1
			CFLAG:5321 = 2
		SIF CFLAG:5321 == 3
			CFLAG:5321 = 4
	;2회째 이후
	ELSE
		;연모
		IF TALENT:150
			PRINTFORM 
		;V감각 4이상
		ELSEIF ABL:1 > 3
			PRINTFORM 
		;그 이외
		ELSE
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 0
;삽입 자궁구 고문
ELSEIF SELECTCOM == 615 && PREVCOM == 21
	;첫회
	IF CFLAG:5321 == 1 || CFLAG:5321 == 2
		;연모
		IF TALENT:150
			PRINTFORM 
		;V감각 4이상
		ELSEIF ABL:1 > 3
			PRINTFORM 
		;그 이외
		ELSE
			PRINTFORM 
		ENDIF
		SIF CFLAG:5321 == 1
			CFLAG:5321 = 3
		SIF CFLAG:5321 == 2
			CFLAG:5321 = 4
	;2회째 이후
	ELSE
		;연모
		IF TALENT:150
			PRINTFORM 
		;V감각 4이상
		ELSEIF ABL:1 > 3
			PRINTFORM 
		;그 이외
		ELSE
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 0
ENDIF


;-------------------------------------------------
;COM23 역강간
;-------------------------------------------------
;·요점 페니스
;·조교자[남자]불가
;-------------------------------------------------
IF SELECTCOM == 25
	;조수에 의한 플레이
	IF ASSIPLAY
		;※추가 기능 B2『조수 감상 기능』용으로 처녀식 있고 경험과 안면 변수를 갱신
		A = ASSINO() +5800
		;조수가 처녀
		IF TALENT:ASSI:0 && EXP:ASSI:0 == 1
			;연모
			IF TALENT:150
				PRINTFORM 
			;처녀식 있고 경험 3이상
			ELSEIF CFLAG:5213 >= 3
				PRINTFORM 
			;그 이외
			ELSE
				PRINTFORM 
			ENDIF
			;처녀식 있고 카운트
			CFLAG:5213 += 1
			SIF CFLAG:A != 5 && CFLAG:A != 3
				CFLAG:A = 6
			SIF CFLAG:A == 5 || CFLAG:A == 3
				CFLAG:A = 7
		;연모
		ELSEIF TALENT:150
			PRINTFORM 
		;그 이외
		ELSE
			PRINTFORM 
		ENDIF
	;주인에 의한 플레이(Ex모드만)
	;처음
	ELSEIF CFLAG:5323 == 0
		;연모
		IF TALENT:150
			PRINTFORM 
		;그 이외
		ELSE
			PRINTFORM 
		ENDIF
		CFLAG:5323 = 1
	;2회째 이후
	ELSE
		;연모
		IF TALENT:150
			PRINTFORM 
		;반발 각인 Lv3
		ELSEIF MARK:9 == 3
			PRINTFORM 
		;그 이외
		ELSE
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 0
ENDIF


;-------------------------------------------------
;COM34 승마정도
;승마 G스포 꾸짖고·승마 자궁구 꾸짖고(요점 삽입 국소 고문 패치. 같은 체위를 반복하면(자) 페니스로 G스포트나 자궁구를 집요하게 꾸짖습니다)
;-------------------------------------------------
IF SELECTCOM == 24
	;조수에 의한 플레이
	;처음
	IF CFLAG:5334 == 0 && ASSIPLAY
		;처녀
		IF TALENT:0 && EXP:0 == 1
			;※추가 기능 B2『조수 감상 기능』으로 보존한 데이터를 참조
			A = ASSINO() +5800
			;연모+조수의 처녀를 말주가 빼앗고 있다
			IF TALENT:150 && CFLAG:A >= 6
				PRINTFORM 
			;연모
			ELSEIF TALENT:150
				PRINTFORM 
			;조수의 처녀를 말주가 빼앗고 있다
			ELSEIF CFLAG:A >= 6
				PRINTFORM 
			;그 이외
			ELSE
				PRINTFORM 
			ENDIF
		;비처녀
		ELSE
			;연모
			IF TALENT:150
				PRINTFORM 
			;그 이외
			ELSE
				PRINTFORM 
			ENDIF
		ENDIF
		CFLAG:5334 = 1
	;2회째 이후
	ELSEIF ASSIPLAY
		;윤활 Lv2 미만
		IF PALAM:10 < PALAMLV:2
			PRINTFORM 
		;연모
		ELSEIF TALENT:150
			PRINTFORM 
		;반발 각인 Lv3
		ELSEIF MARK:9 == 3
			PRINTFORM 
		;그 이외
		ELSE
			PRINTFORM 
		ENDIF
	;주인에 의한 플레이
	;처음
	ELSEIF CFLAG:5334 == 0
		;처녀
		IF TALENT:0 && EXP:0 == 1
			;연모
			IF TALENT:150
				PRINTFORM 
			;반발 각인 Lv3
			ELSEIF MARK:9 == 3
				PRINTFORM 
			;그 이외
			ELSE
				PRINTFORM 
			ENDIF
		;비처녀
		ELSE
			;연모
			IF TALENT:150
				PRINTFORM 
			;그 이외
			ELSE
				PRINTFORM 
			ENDIF
		ENDIF
		CFLAG:5334 = 1
	;2회째 이후
	ELSE
		;연모
		IF TALENT:150
			PRINTFORM 
		;반발 각인 Lv3+미약
		ELSEIF MARK:9 == 3 && TEQUIP:11
			PRINTFORM 
		;반발 각인 Lv3
		ELSEIF MARK:9 == 3
			PRINTFORM 
		;굴복 각인 Lv3+봉사 정신 Lv3 이상+V감각 Lv3 이상
		ELSEIF MARK:2 == 3 && ABL:13 >= 3 && ABL:1 >= 3
			PRINTFORM 
		;V감각 Lv3 이상
		ELSEIF ABL:1 >= 3
			PRINTFORM 
		;그 이외
		ELSE
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 0
;승마 G스포 고문
ELSEIF SELECTCOM == 614 && PREVCOM == 24
	;첫회
	IF CFLAG:5334 == 1 || CFLAG:5334 == 3
		;연모
		IF TALENT:150
			PRINTFORM 
		;V감각 4이상
		ELSEIF ABL:1 > 3
			PRINTFORM 
		;그 이외
		ELSE
			PRINTFORM 
		ENDIF
		SIF CFLAG:5334 == 1
			CFLAG:5334 = 2
		SIF CFLAG:5334 == 3
			CFLAG:5334 = 4
	;2회째 이후
	ELSE
		;연모
		IF TALENT:150
			PRINTFORM 
		;V감각 4이상
		ELSEIF ABL:1 > 3
			PRINTFORM 
		;그 이외
		ELSE
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 0
;삽입 자궁구 고문
ELSEIF SELECTCOM == 615 && PREVCOM == 24
	;첫회
	IF CFLAG:5334 == 1 || CFLAG:5334 == 2
		;연모
		IF TALENT:150
			PRINTFORM 
		;V감각 4이상
		ELSEIF ABL:1 > 3
			PRINTFORM 
		;그 이외
		ELSE
			PRINTFORM 
		ENDIF
		SIF CFLAG:5334 == 1
			CFLAG:5334 = 3
		SIF CFLAG:5334 == 2
			CFLAG:5334 = 4
	;2회째 이후
	ELSE
		;연모
		IF TALENT:150
			PRINTFORM 
		;V감각 4이상
		ELSEIF ABL:1 > 3
			PRINTFORM 
		;그 이외
		ELSE
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 0
ENDIF


;-------------------------------------------------
;추가 기능군C　오나금기능
;-------------------------------------------------

;-------------------------------------------------
;※추가 기능 C1『오나금기능』
;-------------------------------------------------

;-------------------------------------------------
;오나금 3일 이상 계속중의 조교 개시시
;-------------------------------------------------
;·파일 A 「조교 개시시 말」에 대신해 표시
;·오나금 하고 있는 날짜는%CFLAG:5208%로 표시 가능
;-------------------------------------------------
@KOJO_D_C1A_72
;3일 이상 방치
IF (3+CFLAG:5206) < DAY
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
;자위 중독 Lv3 이상
ELSEIF ABL:30 > 2
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
;그 이외
ELSE
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
ENDIF
RETURN 0

;-------------------------------------------------
;오나금 2일 이상 계속중의 조교 종료시
;-------------------------------------------------
;·파일 A 「조교 종료시 말」에 대신해 표시
;·오나금 하고 있는 날짜는%CFLAG:5208%로 표시 가능
;-------------------------------------------------
@KOJO_D_C1B_72
;3일 이상 방치
IF (3+CFLAG:5206) < DAY
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
;자위 중독 Lv3 이상
ELSEIF ABL:30 > 2
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
;그 이외
ELSE
	;연모
	IF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 1
ENDIF
RETURN 0


;-------------------------------------------------
;조교 커멘드별 오나금말
;-------------------------------------------------
;·파일 C 「커멘드 말」에 대신해 표시
;·특정의 커멘드를 실행했을 때에 표시
;·오나금 하고 있는 날짜는%CFLAG:5208%, 과거의 최고 기록은%CFLAG%:207%로 표시 가능
;-------------------------------------------------
@KOJO_D_C1C_72
;-------------------------------------------------
;자위
;-------------------------------------------------
;·오나 궁중만 표시
;·명령으로 자위를 하므로 오나금을 찢었던 것에는 안 된다
;-------------------------------------------------
IF SELECTCOM:3 && CFLAG:5208
	;연모
	IF TALENT:150
		;미약들이
		IF TEQUIP:11
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		;오나금첫날
		ELSEIF CFLAG:5208 == 1
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		;오나금 5일 이상
		ELSEIF CFLAG:5208 > 4
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		;오나 궁중
		ELSE
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		ENDIF
	;그 이외
	ELSE
		;미약들이
		IF TEQUIP:11
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		;오나금첫날
		ELSEIF CFLAG:5208 == 1
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		;오나금 5일 이상
		ELSEIF CFLAG:5208 > 4
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		;오나 궁중
		ELSE
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		ENDIF
	ENDIF
	TRYCALL SELFPLAY_4(72)
	RETURN 1
ENDIF



;-------------------------------------------------
;COM7 아무것도 하지 않는다
;-------------------------------------------------
;줄·아이마스크·어두운 곳·거미실·자·촉수가 닿고 있다고(면) 나무는 입다문다
;자위를 시키는 일이 있기 (위해)때문에
IF SELECTCOM == 9 && TEQUIP:40 == 0 && TEQUIP:41 == 0 && TEQUIP:70 == 0 && TEQUIP:73 == 0 && TEQUIP:72 == 0 && TEQUIP:90 == 0
	;이뇨제 or관장
	;배설계
	IF TEQUIP:12 || TEQUIP:25 == 7
		PRINTFORM 
	;바이브·어널 바이브·어널 비즈·벌룬·어널 전극·거봉·군고구마 장비
	;이물 삽입계
	ELSEIF TEQUIP:20 == 1 || TEQUIP:25 == 1 || TEQUIP:25 == 5 || TEQUIP:25 == 6 || TEQUIP:25 == 4 || TEQUIP:25 == 9 || TEQUIP:20 == 9
		PRINTFORM 
	;구리 캡, 니프르캐프, 착유기 장비
	;클립계
	ELSEIF TEQUIP:30 == 1 || TEQUIP:35 == 1 || TEQUIP:35 == 2
		PRINTFORM 
	;처음
	ELSEIF CFLAG:5307 == 0
		;연모+미약
		IF TALENT:150 && TEQUIP:11
			PRINTFORM 
		;미약들이
		ELSEIF TEQUIP:11
			PRINTFORM 
		;연모+주인이 플레이
		ELSEIF TALENT:150 && ASSIPLAY == 0
			PRINTFORM 
		;그 이외
		ELSE
			PRINTFORM 
		ENDIF
		CFLAG:5307 = 1
	;2회째 이후
	ELSE
		;오나 금지 명령의 조건은 자위 중독 Lv1 이상과 연모일까 반발 각인 Lv2 이하
		;미약+오나금명령할 수 없다
		IF (TALENT:150 == 0 && MARK:9 == 3 && TEQUIP:11) || (ABL:30 == 0 && TEQUIP:11)
			PRINTFORM 
		;명령할 수 없다
		ELSEIF (TALENT:150 == 0 && MARK:9 == 3) || ABL:30 == 0
			PRINTFORM 
		;미약들이+오나 궁중
		ELSEIF TEQUIP:11 && CFLAG:5208 > 0
			;오나금판정을 실시한다
			;보정+50상한 98, 거의 저항할 수 없다
			TRYCALL SELFPLAY(72, 50, 98)
			;역시 참을 수 없었다
			IF RESULT == 1
				PRINTFORMW 「미약의 탓으로 마음껏 자위있습니다」
				TRYCALL SELFPLAY_4(72, 1)
			;기적적으로 참았다
			ELSE
				PRINTFORMW 「미약의 아픔나무를 열심히 참습니다」
				TRYCALL SELFPLAY_4(72)
			ENDIF
		;오나금첫날
		ELSEIF CFLAG:5208 == 1
			PRINTFORMW 「첫날은 아무리 뭐라해도 참을 수 있습니다」
			TRYCALL SELFPLAY_4(72)
		;오나금 10일 이상
		ELSEIF CFLAG:5208 >= 10
			;오나금판정을 실시한다
			;보정+0상한 50, 깜박하면(자) 시라고 끝내는 레벨
			TRYCALL SELFPLAY(72, 0, 50)
			;참을 수 없었다
			IF RESULT == 1
				PRINTFORM 
				TRYCALL SELFPLAY_4(72, 1)
			;참았다
			ELSE
				PRINTFORM 
				TRYCALL SELFPLAY_4(72)
			ENDIF
		;오나 궁중+조수가 보고 있다
		ELSEIF CFLAG:5208 >= 2 && ASSI >= 0
			;오나금판정을 실시한다
			;보정 0 상한 40, 조금 위험한 레벨
			TRYCALL SELFPLAY(72, 0, 40)
			;참을 수 없었다
			IF RESULT == 1
				PRINTFORMW 「조수에 음란한 일 과시할 수 있어, 참지 못하고……」
				TRYCALL SELFPLAY_4(72, 1)
			;참았다
			ELSE
				PRINTFORM 
				TRYCALL SELFPLAY_4(72)
			ENDIF
		;오나 궁중
		ELSEIF CFLAG:5208 >= 2
			;오나금판정을 실시한다
			;보정-5 상한 30, 에로 있고 아이라면 이따금 끝레벨
			TRYCALL SELFPLAY(72, -5, 30)
			;참을 수 없었다
			IF RESULT == 1
				PRINTFORM 
				TRYCALL SELFPLAY_4(72, 1)
			;참았다
			ELSE
				PRINTFORM 
				TRYCALL SELFPLAY_4(72)
			ENDIF
		;미약들이+명령할 수 있다
		ELSEIF (TALENT:150 || MARK:9 < 3) && ABL:30 >= 1 && TEQUIP:11
			PRINTFORMW 「약의 탓으로 자위가 시 싶어서 참을 수 없습니다……」
			PRINTL [0] 방치한다
			PRINTL [1] 금딸을 명한다
			GOTO INPUT_LOOP_72
		;오나금첫회
		ELSEIF (TALENT:150 || MARK:9 < 3) && ABL:30 >= 1 && CFLAG:5307 == 1
			PRINTFORMW 「아무것도 하지 않으면 자위라도 할까―」
			PRINTL [0] 방치한다
			PRINTL [1] 예의 범절이라고 한다
$INPUT_LOOP_72
			INPUT
			SIF RESULT != 0 && RESULT != 1
				GOTO INPUT_LOOP_72
			;오나금 시킨다
			;미약들이
			IF RESULT == 1 && TEQUIP:11
				PRINTFORM 
			;첫회
			ELSEIF RESULT == 1 && CFLAG:5307 == 1
				PRINTFORM 
			;2회째 이후
			ELSEIF RESULT == 1
				PRINTFORM 
			;오나금 시키지 않다
			ELSE
				PRINTFORM 
				RETURN 0
			ENDIF
			CFLAG:5307 = 2
			CFLAG:5370 = 2
			TRYCALL SELFPLAY_4(72, 4)
			RETURN 0
		;2회째 이후
		ELSEIF (TALENT:150 || MARK:9 < 3) && ABL:30 >= 1
			PRINTFORM 
			PRINTL [0] 방치한다
			PRINTL [1] 금딸을 명한다
			GOTO INPUT_LOOP_72
		ENDIF
	ENDIF
	RETURN 0
ENDIF


;-------------------------------------------------
;COM51 미약
;-------------------------------------------------
;·조교자의 기교 3이상이라고 구전으로 먹인다
;·촉수 조교중은 촉수가 최음액을 분비한다
;·그 이외에서는 무리야리 먹인다
;-------------------------------------------------
;통상
IF SELECTCOM == 61 && ABL:PLAYER:12 < 3 && TEQUIP:90 == 0
	;처음
	IF C:1
		;연모
		IF TALENT:150
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		;그 이외
		ELSE
			IF FLAG:11 > 0
				PRINTFORM 
				PRINTFORM 
			ENDIF
		ENDIF
	;2회째 이후
	ELSE
		CFLAG:5351 += 1
		;오나 궁중
		IF CFLAG:5208
			PRINTFORM 
		;미약사용 10번째 이후
		ELSEIF CFLAG:5351 > 9
			PRINTFORMW 「약물 중독 기색에 기쁨응으로 끝냅니다」
		;연모
		ELSEIF TALENT:150
			PRINTFORM 
		;반발 각인 3
		ELSEIF MARK:9 == 3
			PRINTFORM 
		;구전
		ELSEIF ABL:PLAYER:12 >= 3
			PRINTFORM 
		;그 이외
		ELSE
			PRINTFORM 
		ENDIF
	ENDIF
	RETURN 0
ENDIF

;-------------------------------------------------
;※추가 기능 C2『조교 후 오나금판정』
;-------------------------------------------------
@KOJO_D_C1D_72

;-------------------------------------------------
;오나 궁중의 조교 후 자위
;-------------------------------------------------
;·파일 B 「조교 후 자위」에 대신해 표시
;·자위는 해도 이일까 차면 OK
;·오나금 하고 있는 날짜는%CFLAG:5208%로 표시 가능
;-------------------------------------------------
;오나금성공
IF RESULT == 0
	;오나금첫날
	IF CFLAG:5208 == 1
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;오나금 5 일째 이상
	ELSEIF CFLAG:5208 > 4
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;V감각≥B감각
	ELSEIF ABL:1 >= ABL:3
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;V감각＜B감각
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;오나금실패
ELSEIF RESULT == 1
	;오나금첫날
	IF CFLAG:5208 == 1
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;오나금 5 일째 이상
	ELSEIF CFLAG:5208 > 4
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;오나 궁중
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
ENDIF
RETURN 0


;-------------------------------------------------
;※추가 기능 C1『오나금기능』
;일일 오나금이벤트 말
;-------------------------------------------------
;·일자가 바뀌었을 때, 말주를 조교하고 있지 않았을 때에 오나금판정을 해 표시
;·오나금에 실패해 버렸을 경우(자위), 자위는 했지만 급소 직전에서 멈추는 것으로 참을 수 있었을 경우(급소 직전에서 멈추는 것), 하고 싶어졌지만 참을 수 있었을 경우(인내)의 3거리
;·자위는 4 패턴, 급소 직전에서 멈추는 것·인내는 2 패턴까지 말을 쓰는 것이 가능
;-------------------------------------------------

;-------------------------------------------------
;일일시 자위
;-------------------------------------------------
@KOJO_D_C1E_72

;-------------------------------------------------
;·밤의 오나금판정에 실패해, 이 갈퀴 해 버렸을 경우에 표시
;·0~4 패턴의 말을 쓰는 것이 가능(예：2 패턴으로 설정했을 경우, 자위 1으로 자위 2로부터 랜덤으로 선택된다)
;·말콘피그로 「C와 B에 의한 자위」인가 「좋아하는 성감대에 의한 자위」를 선택
;·전자로 설정했을 경우, 「C자위」마셔 기입하면 좋다
;-------------------------------------------------
;자위 말을 무엇 패턴 쓰는지를 설정
;J:2 = 0~4 패턴
J:2 = 0

;오나금이벤트의 지문을 표시하는지를 설정
;J:3 = 0:표시하지 않는,1:표시한다
J:3 = 1

RETURN 0

;-------------------------------------------------
;자위 1
;-------------------------------------------------
@SELFPLAY_72_A
;C자위
IF F:1 <= 1 || (CFLAG:5223 & 1p2) == 0
	;뚜껑이든지
	IF TALENT:121
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모+음핵
	ELSEIF TALENT:150 && TALENT:171
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;V자위
ELSEIF F:1 == 2
	;연모+음 단지
	IF TALENT:150 && TALENT:172
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;A자위
ELSEIF F:1 == 3
	;연모+고혈이상
	IF TALENT:150 && TALENT:173
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;B자위
ELSEIF F:1 == 4
	;연모+음유
	IF TALENT:150 && TALENT:174
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
ENDIF
RETURN 0

;-------------------------------------------------
;자위 2
;-------------------------------------------------
@SELFPLAY_72_B
;C자위
IF F:1 <= 1 || CFLAG:5223 & 1p2
	;뚜껑이든지
	IF TALENT:121
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모+음핵
	ELSEIF TALENT:150 && TALENT:171
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;V자위
ELSEIF F:1 == 2
	;연모+음 단지
	IF TALENT:150 && TALENT:172
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;A자위
ELSEIF F:1 == 3
	;연모+고혈이상
	IF TALENT:150 && TALENT:173
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;B자위
ELSEIF F:1 == 4
	;연모+음유
	IF TALENT:150 && TALENT:174
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
ENDIF
RETURN 0

;-------------------------------------------------
;자위 3
;-------------------------------------------------
@SELFPLAY_72_C
;C자위
IF F:1 <= 1 || CFLAG:5223 & 1p2
	;뚜껑이든지
	IF TALENT:121
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모+음핵
	ELSEIF TALENT:150 && TALENT:171
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;V자위
ELSEIF F:1 == 2
	;연모+음 단지
	IF TALENT:150 && TALENT:172
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;A자위
ELSEIF F:1 == 3
	;연모+고혈이상
	IF TALENT:150 && TALENT:173
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;B자위
ELSEIF F:1 == 4
	;연모+음유
	IF TALENT:150 && TALENT:174
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
ENDIF
RETURN 0

;-------------------------------------------------
;자위 4
;-------------------------------------------------
@SELFPLAY_72_D
;C자위
IF F:1 <= 1 || CFLAG:5223 & 1p2
	;뚜껑이든지
	IF TALENT:121
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모+음핵
	ELSEIF TALENT:150 && TALENT:171
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;V자위
ELSEIF F:1 == 2
	;연모+음 단지
	IF TALENT:150 && TALENT:172
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;A자위
ELSEIF F:1 == 3
	;연모+고혈이상
	IF TALENT:150 && TALENT:173
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;B자위
ELSEIF F:1 == 4
	;연모+음유
	IF TALENT:150 && TALENT:174
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
ENDIF
RETURN 0


;-------------------------------------------------
;일일시 급소 직전에서 멈추는 것 자위
;-------------------------------------------------
@KOJO_D_C1F_72

;-------------------------------------------------
;·밤의 오나금판정에 성공해, 자위는 했지만 이인가 하지않고서 참을 수 있었을 경우에 표시
;·0~2 패턴의 말을 쓰는 것이 가능(예：2 패턴으로 설정했을 경우, 급소 직전에서 멈추는 것 1으로 급소 직전에서 멈추는 것 2로부터 랜덤으로 선택된다)
;·말콘피그로 「C와 B에 의한 자위」인가 「좋아하는 성감대에 의한 자위」를 선택
;·전자로 설정했을 경우, 「C자위」마셔 기입하면 좋다
;-------------------------------------------------
;급소 직전에서 멈추는 것 말을 무엇 패턴 쓰는지를 설정
;J:2 = 0~2 패턴
J:2 = 0

;오나금이벤트의 지문을 표시하는지를 설정
;J:3 = 0:표시하지 않는,1:표시한다
J:3 = 1

RETURN 0

;-------------------------------------------------
;급소 직전에서 멈추는 것 1
;-------------------------------------------------
@SELFPLAY_72_E
;C자위
IF F:1 <= 1 || CFLAG:5223 & 1p2
	;뚜껑이든지
	IF TALENT:121
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모+음핵
	ELSEIF TALENT:150 && TALENT:171
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;V자위
ELSEIF F:1 == 2
	;연모+음 단지
	IF TALENT:150 && TALENT:172
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;A자위
ELSEIF F:1 == 3
	;연모+고혈이상
	IF TALENT:150 && TALENT:173
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;B자위
ELSEIF F:1 == 4
	;연모+음유
	IF TALENT:150 && TALENT:174
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
ENDIF
RETURN 0

;-------------------------------------------------
;급소 직전에서 멈추는 것 2
;-------------------------------------------------
@SELFPLAY_72_F
;C자위
IF F:1 <= 1 || CFLAG:5223 & 1p2
	;뚜껑이든지
	IF TALENT:121
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모+음핵
	ELSEIF TALENT:150 && TALENT:171
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;V자위
ELSEIF F:1 == 2
	;연모+음 단지
	IF TALENT:150 && TALENT:172
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;A자위
ELSEIF F:1 == 3
	;연모+고혈이상
	IF TALENT:150 && TALENT:173
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;B자위
ELSEIF F:1 == 4
	;연모+음유
	IF TALENT:150 && TALENT:174
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
ENDIF
RETURN 0


;-------------------------------------------------
;일일시 인내
;-------------------------------------------------
@KOJO_D_C1G_72

;-------------------------------------------------
;·밤의 오나금판정에 성공해, 자위를 하고 싶은 것을 참을 수 있었을 경우에 표시
;·0~2 패턴의 말을 쓰는 것이 가능(예：2 패턴으로 설정했을 경우, 인내 1으로 인내 2로부터 랜덤으로 선택된다)
;-------------------------------------------------
;인내 말을 무엇 패턴 쓰는지를 설정
;J:2 = 0~2 패턴
J:2 = 0

;오나금이벤트의 지문을 표시하는지를 설정
;J:3 = 0:표시하지 않는,1:표시한다
J:3 = 1

RETURN 0

;-------------------------------------------------
;인내 1
;-------------------------------------------------
@SELFPLAY_72_G
;연모
IF TALENT:150
	IF FLAG:11 > 0
		PRINTFORM 
		PRINTFORM 
	ENDIF
;그 이외
ELSE
	IF FLAG:11 > 0
		PRINTFORM 
		PRINTFORM 
	ENDIF
ENDIF
RETURN 0

;-------------------------------------------------
;인내 2
;-------------------------------------------------
@SELFPLAY_72_H
;연모
IF TALENT:150
	IF FLAG:11 > 0
		PRINTFORM 
		PRINTFORM 
	ENDIF
;그 이외
ELSE
	IF FLAG:11 > 0
		PRINTFORM 
		PRINTFORM 
	ENDIF
ENDIF
RETURN 0


;-------------------------------------------------
;조수시 자위
;-------------------------------------------------
;·말주가 조수로서 뒤따르고 있지만, 조교자가 주인의 경우, 커멘드 실행시에 판정을 실시한다
;·플레이 내용에 응한 확률로 행해져 실패하면(자) 주인과 노예의 행위를 과시할 수 있어 자위를 해 버려 오나금실패
;·말을 쓸 때는 말주는 조수이므로 ASSI로 관리해, 대사는 『』으로 둘러싸는 것을 추천
;·조건을 채웠을 경우, 조수시 커멘드 말은 표시되지 않는다
;-------------------------------------------------
@KOJO_D_C1H_72

;-------------------------------------------------
;·조수시의 오나금판정에 실패해, 이 갈퀴 해 버렸을 경우에 표시
;·0~4 패턴의 말을 쓰는 것이 가능(예：2 패턴으로 설정했을 경우, 조수 자위 1으로 조수 자위 2로부터 랜덤으로 선택된다)
;·말콘피그로 「C와 B에 의한 자위」인가 「좋아하는 성감대에 의한 자위」를 선택
;·전자로 설정했을 경우, 「C자위」마셔 기입하면 좋다
;·자위의 결과 사정했을 경우, 파일 E의 「조수로서 사정했을 때」에 말을 쓰는 것이 가능
;-------------------------------------------------
;자위 말을 무엇 패턴 쓰는지를 설정
;J:2 = 0~4 패턴
J:2 = 0

;오나금이벤트의 지문을 표시하는지를 설정
;J:3 = 0:표시하지 않는,1:표시한다
J:3 = 1

RETURN 0


;-------------------------------------------------
;조수 자위 1
;-------------------------------------------------
@SELFPLAY_72_I
;C자위
IF F:1 <= 1 || (CFLAG:5223 & 1p2) == 0
	;뚜껑이든지
	IF TALENT:121
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모+음핵
	ELSEIF TALENT:150 && TALENT:171
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;V자위
ELSEIF F:1 == 2
	;연모+음 단지
	IF TALENT:150 && TALENT:172
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;A자위
ELSEIF F:1 == 3
	;연모+고혈이상
	IF TALENT:150 && TALENT:173
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;B자위
ELSEIF F:1 == 4
	;연모+음유
	IF TALENT:150 && TALENT:174
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
ENDIF
RETURN 0

;-------------------------------------------------
;조수 자위 2
;-------------------------------------------------
@SELFPLAY_72_J
;C자위
IF F:1 <= 1 || CFLAG:5223 & 1p2
	;뚜껑이든지
	IF TALENT:121
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모+음핵
	ELSEIF TALENT:150 && TALENT:171
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;V자위
ELSEIF F:1 == 2
	;연모+음 단지
	IF TALENT:150 && TALENT:172
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;A자위
ELSEIF F:1 == 3
	;연모+고혈이상
	IF TALENT:150 && TALENT:173
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;B자위
ELSEIF F:1 == 4
	;연모+음유
	IF TALENT:150 && TALENT:174
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
ENDIF
RETURN 0

;-------------------------------------------------
;조수 자위 3
;-------------------------------------------------
@SELFPLAY_72_K
;C자위
IF F:1 <= 1 || CFLAG:5223 & 1p2
	;뚜껑이든지
	IF TALENT:121
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모+음핵
	ELSEIF TALENT:150 && TALENT:171
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;V자위
ELSEIF F:1 == 2
	;연모+음 단지
	IF TALENT:150 && TALENT:172
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;A자위
ELSEIF F:1 == 3
	;연모+고혈이상
	IF TALENT:150 && TALENT:173
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;B자위
ELSEIF F:1 == 4
	;연모+음유
	IF TALENT:150 && TALENT:174
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
ENDIF
RETURN 0

;-------------------------------------------------
;조수 자위 4
;-------------------------------------------------
@SELFPLAY_72_L
;C자위
IF F:1 <= 1 || CFLAG:5223 & 1p2
	;뚜껑이든지
	IF TALENT:121
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모+음핵
	ELSEIF TALENT:150 && TALENT:171
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;V자위
ELSEIF F:1 == 2
	;연모+음 단지
	IF TALENT:150 && TALENT:172
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;A자위
ELSEIF F:1 == 3
	;연모+고혈이상
	IF TALENT:150 && TALENT:173
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
;B자위
ELSEIF F:1 == 4
	;연모+음유
	IF TALENT:150 && TALENT:174
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;연모
	ELSEIF TALENT:150
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	;그 이외
	ELSE
		IF FLAG:11 > 0
			PRINTFORM 
			PRINTFORM 
		ENDIF
	ENDIF
ENDIF
RETURN 0


;-------------------------------------------------
;조수시 인내
;-------------------------------------------------
;·말주가 조수로서 뒤따르고 있지만, 조교자가 주인의 경우, 커멘드 실행시에 판정을 실시한다
;·조수시 자위를 참을 수가 있었지만, 아슬아슬했던 경우에 인내 말을 쓰는 것이 가능
;·말을 쓸 때는 말주는 조수이므로 ASSI로 관리해, 대사는 『』으로 둘러싸는 것을 추천
;·조건을 채웠을 경우, 조수시 커멘드 말은 표시되지 않는다
;-------------------------------------------------
@KOJO_D_C1I_72

;-------------------------------------------------
;·조수시의 오나금판정에 성공했지만, 위험했던 경우(하고 싶어졌지만 참은, 또는 노예에 질투했을 경우)에 표시
;·0~4 패턴의 말을 쓰는 것이 가능(예：2 패턴으로 설정했을 경우, 조수 인내 1으로 조수 인내 2로부터 랜덤으로 선택된다)
;·설정에 관계없이 파라미터 변화는 수반하지 않는, 디테일만의 기능
;·J:2로 J:3 모두 0을 설정하는 것으로 무효로 할 수 있다. 초기치에서는 유효
;-------------------------------------------------
;인내 말을 무엇 패턴 쓰는지를 설정
;J:2 = 0~4 패턴
J:2 = 0

;오나금이벤트의 지문을 표시하는지를 설정
;J:3 = 0:표시하지 않는,1:표시한다
J:3 = 1

RETURN 0


;-------------------------------------------------
;조수 인내 1
;-------------------------------------------------
@SELFPLAY_72_M
;연모
IF TALENT:150
	IF FLAG:11 > 0
		PRINTFORM 
		PRINTFORM 
	ENDIF
;그 이외
ELSE
	IF FLAG:11 > 0
		PRINTFORM 
		PRINTFORM 
	ENDIF
ENDIF
RETURN 0

;-------------------------------------------------
;조수 인내 2
;-------------------------------------------------
@SELFPLAY_72_N
;연모
IF TALENT:150
	IF FLAG:11 > 0
		PRINTFORM 
		PRINTFORM 
	ENDIF
;그 이외
ELSE
	IF FLAG:11 > 0
		PRINTFORM 
		PRINTFORM 
	ENDIF
ENDIF
RETURN 0

;-------------------------------------------------
;조수 인내 3
;-------------------------------------------------
@SELFPLAY_72_O
;연모
IF TALENT:150
	IF FLAG:11 > 0
		PRINTFORM 
		PRINTFORM 
	ENDIF
;그 이외
ELSE
	IF FLAG:11 > 0
		PRINTFORM 
		PRINTFORM 
	ENDIF
ENDIF
RETURN 0

;-------------------------------------------------
;조수 인내 4
;-------------------------------------------------
@SELFPLAY_72_P
;연모
IF TALENT:150
	IF FLAG:11 > 0
		PRINTFORM 
		PRINTFORM 
	ENDIF
;그 이외
ELSE
	IF FLAG:11 > 0
		PRINTFORM 
		PRINTFORM 
	ENDIF
ENDIF
RETURN 0






;-------------------------------------------------
;재즈 악단 말텐프레집
;특정의 순서로 커멘드를 실행했을 경우에 말과 플레이 결과를 변화시킵니다
;엑스트라 모드 구문으로 허가할(CFLAG:5220 > 0) 필요가 있습니다
;-------------------------------------------------
;※추가 기능 D1『 뽑지 않고 우후후 기능』(상급자전용)
;주로 섹스계 커멘드로부터 애무계 커멘드를 동일한 조교자로 실행했을 경우, 특수한 말을 표시합니다
;삽입한 채로의 플레이가 되는 경우, 쾌V나 사정도 발생합니다
;엑스트라 모드 구문을 유효하게 하지 않는 한 디폴트 무효인의로 편집할 필요는 없습니다
;또, 유효하게 했을 경우에서도 V삽입계 이외는 무효가 되어 있습니다. 사용하는 경우는 무효 포위를 해제로.
;조건은 기호에 응해 증량·감량 해 주세요. 편집할 수 있는 분 용입니다

;※주의　※유효하게 하면(자) 그대로 해서는 안됩니다※
;유효하게 하면(자) 기입하지 않으면 통상의 말도 나오지 않습니다, 기입하지 않는 분기는 가능한 한 삭제해 주세요
;혹은, 말을 쓰지 않는 분기는 PRINTFORMW 를 공난으로 해, 「RETURN 1」을 「RETURN 2」에 고쳐 써 주세요.
;공난으로 하지 않았던 경우는 통상의 말의 앞에 표시됩니다.
;RETURN 2로 하면(자) 재즈 악단명을 표시해 재즈 악단 처리를 한 후, 통상의 말이 표시됩니다.
;말의 내용과 모순되기 쉽기 때문에, 분기마다 삭제하는 (분)편이 깨끗이 한다고 생각됩니다

@COMBO_72
LOCAL = 1
;삽없음의 플레이에서는 A=1, 뽑는 경우는 A=0에 되돌린다
;원래 삽입을 수반하는 플레이에서도 A=0으로 한다
;재즈 악단명비표시로 하는 경우는 A=0→2, 1→3으로 변경

;※메모 재즈 악단 마다 설정. A=0·미기입　A=1·말이 바뀔 뿐　A=2·삽입 속행

;V삽입계 디폴트 조건：연모＋V감각 Lv3 이상
IF TALENT:150 && ABL:1 > 2 && ((TFLAG:133 && ASSIPLAY) || (TFLAG:133 == 0 && ASSIPLAY == 0))
	;정상정도
	IF CFLAG:5220 == 20
		;정상정도→애무
		IF CFLAG:5220 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「삽입한채로 애무를 하십니다」
			;↓여기의 1을 2로 하면(자) 재즈 악단 하면서 통상의 말을 표시. 그 경우↑는 공난으로 하는지, 통상의 말에 계속되는 형태로 쓰는 것
			RETURN 1
		;정상정도→가슴 애무
		ELSEIF CFLAG:5220 == 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;정상정도→키스 한다
		ELSEIF CFLAG:5220 == 6
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;정상정도→아무것도 하지 않는다
		ELSEIF CFLAG:5220 == 9
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「삽입한 채로 쉬어 갑니다」
			RETURN 1
		;정상정도→파이즈리
		ELSEIF CFLAG:5220 == 42
			;뽑아 파이즈리로 옮기므로 A=0
			LOCAL = 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;정상정도→승마정도
		ELSEIF CFLAG:5220 == 24
			;체위 변경은 삽입을 수반하므로 A=0
			LOCAL = 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		ENDIF
	;후배 정도
	ELSEIF CFLAG:5220 == 21
		;후배 정도→애무
		IF CFLAG:5220 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;후배 정도→어널 애무
		ELSEIF CFLAG:5220 == 2
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「삽입한채로 엉덩이를…」
			RETURN 1
		;후배 정도→가슴 애무
		ELSEIF CFLAG:5220 == 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;후배 정도→스팬 킹
		ELSEIF CFLAG:5220 == 220
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「삽입엉덩이응입니다」
			RETURN 1
		;후배 정도→가랑이
		ELSEIF CFLAG:5220 == 43
			LOCAL = 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「뽑아 가랑이로 이행합니다」
			RETURN 1
		ENDIF
	;대면자리정도
	ELSEIF CFLAG:5220 == 22
		;대면자리정도→애무
		IF CFLAG:5220 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「귀를은 보고는 보고 하기도 좋지요」
			RETURN 1
		;대면자리정도→어널 애무
		ELSEIF CFLAG:5220 == 2
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;대면자리정도→키스 한다
		ELSEIF CFLAG:5220 == 6
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결된 채로 얼싸안아 키스입니다」
			RETURN 1
		;대면자리정도→아무것도 하지 않는다
		ELSEIF CFLAG:5220 == 9
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;대면자리정도→미약(기교 Lv3 이상)
		ELSEIF CFLAG:5220 == 61 && ABL:PLAYER:12 >= 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결된 채로 키스로 미약을 먹입니다」
			RETURN 1
		;대면자리정도→흡혈
		ELSEIF CFLAG:5220 == 400
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결된 채로 꽉 껴안을 수 있어 목덜미」
			RETURN 1
		ENDIF
	;혼란 모란
	ELSEIF (CFLAG:5220 == 23 && TEQUIP:56) || CFLAG:5220 == 612
		;혼란 모란→애무
		IF CFLAG:5220 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「신체중 만지작거릴 수 있는 모습이군요」
			RETURN 1
		;혼란 모란→어널 애무
		ELSEIF CFLAG:5220 == 2
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;혼란 모란→자위
		ELSEIF CFLAG:5220 == 7
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결된 채로 자위합니다」
			RETURN 1
		;혼란 모란→가슴 애무
		ELSEIF CFLAG:5220 == 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;혼란 모란→키스 한다
		ELSEIF CFLAG:5220 == 6
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「뒤돌아 봐 키스입니다, 있어라」
			RETURN 1
		;혼란 모란→아무것도 하지 않는다
		ELSEIF CFLAG:5220 == 9
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;혼란 모란→로터
		ELSEIF CFLAG:5220 == 10
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;혼란 모란→오나호르
		ELSEIF CFLAG:5220 == 16 && TEQUIP:30 == 2
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;혼란 모란→오나호르 해제
		ELSEIF CFLAG:5220 == 16 && TEQUIP:30 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;혼란 모란→강제 방뇨
		ELSEIF CFLAG:5220 == 620 || (CFLAG:5220 == 111 && SOURCE:0)
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「해-해-의 포즈입니다. 삽입」
			RETURN 1
		ENDIF
	;배면자리정도
	ELSEIF CFLAG:5220 == 23
		;배면자리정도→애무
		IF CFLAG:5220 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「신체중 만지작거릴 수 있는 모습이군요」
			RETURN 1
		;배면자리정도→어널 애무
		ELSEIF CFLAG:5220 == 2
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도→자위
		ELSEIF CFLAG:5220 == 7
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결된 채로 자위합니다」
			RETURN 1
		;배면자리정도→가슴 애무
		ELSEIF CFLAG:5220 == 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도→키스 한다
		ELSEIF CFLAG:5220 == 6
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「뒤돌아 봐 키스입니다, 있어라」
			RETURN 1
		;배면자리정도→아무것도 하지 않는다
		ELSEIF CFLAG:5220 == 9
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도→로터
		ELSEIF CFLAG:5220 == 10
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도→오나호르
		ELSEIF CFLAG:5220 == 16 && TEQUIP:30 == 2
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도→오나호르 해제
		ELSEIF CFLAG:5220 == 16 && TEQUIP:30 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도→강제 방뇨
		ELSEIF CFLAG:5220 == 620 || (CFLAG:5220 == 111 && SOURCE:0)
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「해-해-의 포즈입니다. 삽입」
			RETURN 1
		ENDIF
	;승마정도
	ELSEIF CFLAG:5220 == 24
		;승마정도→애무
		IF CFLAG:5220 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도→자위
		ELSEIF CFLAG:5220 == 7
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「입에 물고 붐비어 자위 피로합니다」
			RETURN 1
		;승마정도→가슴 애무
		ELSEIF CFLAG:5220 == 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도→키스 한다
		ELSEIF CFLAG:5220 == 6
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「허리 털고 있는 곳을 안아 키스, 복근 사용합니다만」
			RETURN 1
		;승마정도→아무것도 하지 않는다
		ELSEIF CFLAG:5220 == 9
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도→비디오 촬영
		ELSEIF CFLAG:5220 == 100 && TEQUIP:50
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「low angle가 끼고 촬영은 녀석입니다」
			RETURN 1
		ENDIF
	ENDIF
ENDIF

;어널계·비삽입계(디폴트 무효)


;~~~~~~~~~~~~~~~~~~~~~~~~~~
;무효화 부분 개시
IF 0 == 1
;~~~~~~~~~~~~~~~~~~~~~~~~~~



;A삽입계 디폴트 조건：연모＋A감각 Lv3 이상
IF TALENT:150 && ABL:2 > 2 && ((TFLAG:133 && ASSIPLAY) || (TFLAG:133 == 0 && ASSIPLAY == 0))
	;정상정도 어널 or어널 섹스
	IF CFLAG:5220 == 30 || CFLAG:5220 == 30
		;정상정도 A→애무
		IF CFLAG:5220 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;정상정도 A→가슴 애무
		ELSEIF CFLAG:5220 == 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;정상정도 A→키스 한다
		ELSEIF CFLAG:5220 == 6
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;정상정도 A→아무것도 하지 않는다
		ELSEIF CFLAG:5220 == 9
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「삽입한 채로 쉬어 갑니다」
			RETURN 1
		;정상정도 A→손가락 삽입
		ELSEIF CFLAG:5220 == 4
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;정상정도 A→G스포트 자극
		ELSEIF CFLAG:5220 == 600
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		ENDIF
	;후배 정도 어널
	ELSEIF CFLAG:5220 == 31
		;후배 정도 A→애무
		IF CFLAG:5220 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;후배 정도 A→가슴 애무
		ELSEIF CFLAG:5220 == 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;후배 정도 A→스팬 킹
		ELSEIF CFLAG:5220 == 220
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「삽입엉덩이응입니다」
			RETURN 1
		;후배 정도 A→가랑이
		ELSEIF CFLAG:5220 == 43
			LOCAL = 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「뽑아 가랑이로 이행합니다」
			RETURN 1
		ENDIF
	;대면자리정도 어널
	ELSEIF CFLAG:5220 == 32
		;대면자리정도 A→애무
		IF CFLAG:5220 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「귀를은 보고는 보고 하기도 좋지요」
			RETURN 1
		;대면자리정도 A→키스 한다
		ELSEIF CFLAG:5220 == 6
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결된 채로 얼싸안아 키스입니다」
			RETURN 1
		;대면자리정도 A→아무것도 하지 않는다
		ELSEIF CFLAG:5220 == 9
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;대면자리정도 A→미약(기교 Lv3 이상)
		ELSEIF CFLAG:5220 == 61 && ABL:PLAYER:12 >= 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결된 채로 키스로 미약을 먹입니다」
			RETURN 1
		;대면자리정도 A→흡혈
		ELSEIF CFLAG:5220 == 400
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결된 채로 꽉 껴안을 수 있어 목덜미」
			RETURN 1
		ENDIF
	;배면자리정도 어널
	ELSEIF CFLAG:5220 == 33
		;배면자리정도 A→애무
		IF CFLAG:5220 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「신체중 만지작거릴 수 있는 모습이군요」
			RETURN 1
		;배면자리정도 A→바이브 자위
		ELSEIF CFLAG:5220 == 7 && TEQUIP:20 == 1
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도 A→자위
		ELSEIF CFLAG:5220 == 7
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결된 채로 자위합니다」
			RETURN 1
		;배면자리정도 A→가슴 애무
		ELSEIF CFLAG:5220 == 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도 A→키스 한다
		ELSEIF CFLAG:5220 == 6
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「뒤돌아 봐 키스입니다, 있어라」
			RETURN 1
		;배면자리정도 A→아무것도 하지 않는다
		ELSEIF CFLAG:5220 == 9
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도 A→손가락 삽입
		ELSEIF CFLAG:5220 == 4
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도 A→G스포트 자극
		ELSEIF CFLAG:5220 == 600
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도 A→로터
		ELSEIF CFLAG:5220 == 10
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도 A→바이브
			ELSEIF CFLAG:5220 == 12 && TEQUIP:20 == 1
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도 A→바이브 해제
			ELSEIF CFLAG:5220 == 12 && TEQUIP:20 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도 A→오나호르
			ELSEIF CFLAG:5220 == 16 && TEQUIP:30 == 2
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도 A→오나호르 해제
			ELSEIF CFLAG:5220 == 16 && TEQUIP:30 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도 A→강제 방뇨
			ELSEIF CFLAG:5220 == 620 || (CFLAG:5220 == 111 && SOURCE:0)
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「해-해-의 포즈입니다. 삽입」
			RETURN 1
		ENDIF
	;승마정도 A
	ELSEIF CFLAG:5220 == 34
		;승마정도 A→애무
		IF CFLAG:5220 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도 A→자위
		ELSEIF CFLAG:5220 == 7
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「입에 물고 붐비어 자위 피로합니다」
			RETURN 1
		;승마정도 A→가슴 애무
		ELSEIF CFLAG:5220 == 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도 A→키스 한다
		ELSEIF CFLAG:5220 == 6
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「허리 털고 있는 곳을 안아 키스, 복근 사용합니다만」
			RETURN 1
		;승마정도 A→아무것도 하지 않는다
		ELSEIF CFLAG:5220 == 9
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도 A→손가락 삽입
		ELSEIF CFLAG:5220 == 4
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도 A→G스포트 자극
		ELSEIF CFLAG:5220 == 600
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도 A→비디오 촬영
		ELSEIF CFLAG:5220 == 100 && TEQUIP:50
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「low angle가 끼고 촬영은 녀석입니다」
			RETURN 1
		ENDIF
	ENDIF
ENDIF


;비삽입계 디폴트 조건：연모
LOCAL = 0
IF TALENT:150 && ((TFLAG:133 && ASSIPLAY) || (TFLAG:133 == 0 && ASSIPLAY == 0))
	;조수를 범한다
	IF CFLAG:5220 == 2026
		;조수를 범하고는 A=1으로 좋다
		LOCAL = 1
		;조수 섹스 애무
		IF CFLAG:5220 == 1
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「조수에 삽입하고 있는 주인이 손가락으로 하고 줍니다」
			RETURN 1
		;조수 섹스 자위
		ELSEIF CFLAG:5220 == 7
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주인에 범해지는 조수를 보면서 자위합니다」
			RETURN 1
		;조수 섹스중 방치
		ELSEIF CFLAG:5220 == 9
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주인에 범해져 기분이야 유혹한데 조수를 보고 있습니다」
			RETURN 1
		;조수 섹스
		ELSEIF CFLAG:5220 == 110
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주인에 범해지는 조수에 자신의 성기를 벌려 과시합니다」
			RETURN 1
		ENDIF
	ENDIF
ENDIF


;~~~~~~~~~~~~~~~~~~~~~~~~~~
;무효화 부분 종료
ENDIF
;~~~~~~~~~~~~~~~~~~~~~~~~~~


RETURN 0

;-------------------------------------------------
;서포트 말텐프레집
;주인→노예가 흘러 나오고 특정의 순서로 커멘드를 실행했을 경우에 말과 플레이 결과를 변화시킵니다
;요점『 뽑지 않고 우후후 기능』
;
;엑스트라 모드 구문으로 허가할(CFLAG:5220 > 0) 필요가 있습니다
;서포트 말유의 플레이를 계속했을 경우, 계속해 서포트 플레이가 생깁니다.
;그대로 주인에 교대했을 경우, 삽입없음에서의 재개가 됩니다.
;-------------------------------------------------
;※추가 기능 D2『서포트 플레이 기능』(상급자전용)
;주로 주인의 섹스계 커멘드로부터 조수가 애무계 커멘드를 실행했을 경우, 특수한 말을 표시합니다
;삽입한 채로의 플레이가 되는 경우, 쾌V나 사정도 발생합니다
;엑스트라 모드 구문을 유효하게 하지 않는 한 디폴트 무효인의로 편집할 필요는 없습니다
;조건은 기호에 응해 증량·감량 해 주세요. 편집할 수 있는 분 용입니다
;몸의 자세적으로 괴로운 것은 독단으로 생략하고 있습니다.
;『 뽑지 않고 우후후 기능』이 온의 경우에서도 디폴트 무효 취급. 무효 포위를 지워 사용해 주세요

;※주의　※유효하게 하면(자) 그대로 해서는 안됩니다※
;유효하게 하면(자) 기입하지 않으면 통상의 말도 나오지 않습니다, 기입하지 않는 분기는 가능한 한 삭제해 주세요
;혹은, 말을 쓰지 않는 분기는 PRINTFORMW 를 공난으로 해, 「RETURN 1」을 「RETURN 2」에 고쳐 써 주세요.
;공난으로 하지 않았던 경우는 통상의 말의 앞에 표시됩니다.
;RETURN 2로 하면(자) 재즈 악단명을 표시해 재즈 악단 처리를 한 후, 통상의 말이 표시됩니다.
;말의 내용과 모순되기 쉽기 때문에, 분기마다 삭제하는 (분)편이 깨끗이 한다고 생각됩니다

@COMBOASSI_72


;~~~~~~~~~~~~~~~~~~~~~~~~~~
;무효화 부분 개시
IF 0 == 1
;~~~~~~~~~~~~~~~~~~~~~~~~~~


LOCAL = 1
;삽없음의 플레이에서는 A=1, 뽑는 경우는 A=0에 되돌린다
;주인 정상정도→조수 후키 정도 등, 물리적으로 무리한 경우등이 A=0

;V삽입계 디폴트 조건：연모＋V감각 Lv3 이상＋조수가 남자는 아니다
IF TALENT:150 && ABL:1 > 2 && TALENT:ASSI:120 == 0
	;주인의 정상정도로부터
	IF CFLAG:5220 == 2020
		;정상정도 서포트 애무
		IF SELECTCOM == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「삽입한채로 조수에 애무됩니다」
			RETURN 1
		;정상정도 서포트 어널 애무
		ELSEIF SELECTCOM == 2
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주인에 범해지고면서 조수에 엉덩이를 요」
			RETURN 1
		;정상정도 서포트가슴 애무
		ELSEIF SELECTCOM == 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;정상정도 서포트 키스
		ELSEIF SELECTCOM == 6
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;정상정도시간(아무것도 하지 않는다)
		ELSEIF SELECTCOM == 9
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결되고 있는 곳을 조수에 빤히 볼 수 있습니다」
			RETURN 1
		;정상정도 서포트 로터
		ELSEIF SELECTCOM == 10
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「결합부 위에 로터를 맞힐 수 있으며……」
			RETURN 1
		;정상정도 서포트전마
		ELSEIF SELECTCOM == 11
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「결합부 위에 E안마사를 맞힐 수 있으며……」
			RETURN 1
		;정상정도 서포트 A바이브
		ELSEIF SELECTCOM == 13 && TEQUIP:25 == 1
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주인과 연결된 채로 어널 바이브를 돌진해지고……」
			RETURN 1
		;정상정도 서포트 A비즈
		ELSEIF SELECTCOM == 172 && TEQUIP:25 == 5
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주인과 연결된 채로 어널 비즈를 1개1개(살)……」
			RETURN 1
		;정상정도 서포트 A비즈 해제
		ELSEIF SELECTCOM == 172 && TEQUIP:25 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주인과 연결된 채로 한숨에 비즈를 뽑아 내지고……」
			RETURN 1
		;정상정도 서포트 자위
		ELSEIF SELECTCOM == 40
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결된 채로 뚜껑이든지 조수의 물건을 잡아당기게 하고……」
			RETURN 1
		;정상정도 서포트 페라
		ELSEIF SELECTCOM == 41
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결된 채로 뚜껑이든지 조수의 물건을 빨게 하고……」
			RETURN 1
		;정상정도 사포트파이즈리
		ELSEIF SELECTCOM == 42
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;정상정도 서포트미약키스(기교 Lv3 이상)
		ELSEIF SELECTCOM == 61 && ABL:PLAYER:12 >= 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「조수가 키스로 미약을 먹입니다」
			RETURN 1
		;정상정도 서포트 강제 쿤닐링구스
		ELSEIF SELECTCOM == 300
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「범해지고면서 조수에 안면기승위되어 빨게 하고……」
			RETURN 1
		;정상정도 사포트이라마치오
		ELSEIF SELECTCOM == 230
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결된 채로 뚜껑이든지 조수의 물건을 빨게 하고……」
			RETURN 1
		;정상정도 서포트 강제 A봉사
		ELSEIF SELECTCOM == 44
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「범해지고면서 조수에 안면기승위되어 빨게 하고……」
			RETURN 1
		;정상정도 사포트하메 촬영
		ELSEIF SELECTCOM == 100 && TEQUIP:50
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		ENDIF
	;후배 정도
	ELSEIF CFLAG:5220 == 2021
		;후배 정도 서포트 애무
		IF SELECTCOM == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;후배 정도 서포트 쿤닐링구스
		ELSEIF SELECTCOM == 1
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「아래로부터 날름날름 빱니다」
			RETURN 1
		;후배 정도 서포트 어널 애무
		ELSEIF SELECTCOM == 2
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「어널에 들어간 조수의 손가락이 엷은 살색 넘어로 주인의 물건을 어루만지고……」
			RETURN 1
		;후배 정도 서포트 페라(말주가 된다)
		ELSEIF SELECTCOM == 5
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「뒤를 범해지고면서 전을 빨아지고……」
			RETURN 1
		;후배 정도 서포트가슴 애무
		ELSEIF SELECTCOM == 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;후배 정도 서포트 키스
		ELSEIF SELECTCOM == 6
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;후배 정도시간
		ELSEIF SELECTCOM == 9
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;후배 정도 서포트 로터
		ELSEIF SELECTCOM == 10
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「결합부 위에 로터를 맞힐 수 있으며……」
			RETURN 1
		;후배 정도 서포트전마
		ELSEIF SELECTCOM == 11
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「결합부 위에 E안마사를 맞힐 수 있으며……」
			RETURN 1
		;후배 정도 서포트 페라
		ELSEIF SELECTCOM == 41
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결된 채로 뚜껑이든지 조수의 물건을 빨게 하고……」
			RETURN 1
		;후배 정도 서포트미약키스(기교 Lv3 이상)
		ELSEIF SELECTCOM == 61 && ABL:PLAYER:12 >= 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「조수가 키스로 미약을 먹입니다」
			RETURN 1
		;후배 정도 서포트 강제 쿤닐링구스
		ELSEIF SELECTCOM == 300
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「눈앞의 조수의 음부를 빨면서 가방으로 찔리고……」
			RETURN 1
		;후배 정도 서포트 69
		ELSEIF SELECTCOM == 602
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「포복아래에 조수가 들어와 범해지고면서 빨아 사랑……」
			RETURN 1
		;후배 정도 사포트이라마치오
		ELSEIF SELECTCOM == 230
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결된 채로 뚜껑이든지 조수의 물건을 빨게 하고……」
			RETURN 1
		;후배 정도 서포트 강제 A봉사
		ELSEIF SELECTCOM == 44
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「눈앞의 조수의 국혈을 빨면서 가방으로 찔리고……」
			RETURN 1
		;후배 정도 사포트하메 촬영
		ELSEIF SELECTCOM == 100 && TEQUIP:50
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		ENDIF
	;대면자리정도
	ELSEIF CFLAG:5220 == 2022
		;대면자리정도 서포트 애무
		IF SELECTCOM == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「귀를은 보고는 보고 하기도 좋지요」
			RETURN 1
		;대면자리정도 서포트 어널 애무
		ELSEIF SELECTCOM == 2
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;대면자리정도 서포트가슴 애무
		ELSEIF SELECTCOM == 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;대면자리정도 서포트 키스
		ELSEIF SELECTCOM == 6
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결된 채로 얼싸안아 키스입니다」
			RETURN 1
		;대면자리정도시간
		ELSEIF SELECTCOM == 9
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;대면자리정도 서포트 로터
		ELSEIF SELECTCOM == 10
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주인과의 사이에 로터를 끼고……」
			RETURN 1
		;대면자리정도 서포트 A바이브
		ELSEIF SELECTCOM == 13 && TEQUIP:25 == 1
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주인과 연결된 채로 어널 바이브를 돌진해지고……」
			RETURN 1
		;대면자리정도 서포트 A비즈
		ELSEIF SELECTCOM == 172 && TEQUIP:25 == 5
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주인과 연결된 채로 어널 비즈를 1개1개(살)……」
			RETURN 1
		;대면자리정도 서포트 A비즈 해제
		ELSEIF SELECTCOM == 172 && TEQUIP:25 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주인과 연결된 채로 한숨에 비즈를 뽑아 내지고……」
			RETURN 1
		;대면자리정도 서포트미약키스(기교 Lv3 이상)
		ELSEIF SELECTCOM == 61 && ABL:PLAYER:12 >= 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「조수가 키스로 미약을 먹입니다」
			RETURN 1
		;대면자리정도 서포트 스팬 킹
		ELSEIF SELECTCOM == 220
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주와 너무 노닥 거려 조수에 등 얻어맞읍니다(무엇)」
			RETURN 1
		;대면자리정도 사포트하메 촬영
		ELSEIF SELECTCOM == 100 && TEQUIP:50
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		ENDIF
	;혼란 모란
	ELSEIF (CFLAG:5220 == 2023 && TEQUIP:56) || CFLAG:5220 == 20612
		;혼란 모란 서포트 애무
		IF SELECTCOM == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「조수에 좋아해 부엌 만지작거려집니다……」
			RETURN 1
		;혼란 모란 서포트 쿤닐링구스
		ELSEIF SELECTCOM == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「결합부를 조수에 빨 수 있습니다」
			RETURN 1
		;혼란 모란 자위시간
		ELSEIF SELECTCOM == 7
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결된 채로 자위하는 것을 조수에 바로 정면으로부터 볼 수 있습니다」
			RETURN 1
		;혼란 모란 서포트 페라(말주가 된다)
		ELSEIF SELECTCOM == 5
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「뒤를 범해지고면서 전을 빨아지고……」
			RETURN 1
		;혼란 모란 서포트가슴 애무
		ELSEIF SELECTCOM == 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;혼란 모란 서포트 키스
		ELSEIF SELECTCOM == 6
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;혼란 모란시간
		ELSEIF SELECTCOM == 9
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;혼란 모란 서포트 로터
		ELSEIF SELECTCOM == 10
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;혼란 모란 서포트전마
		ELSEIF SELECTCOM == 11
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;혼란 모란 사포트오나호르
		ELSEIF SELECTCOM == 16 && TEQUIP:30 == 2
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;혼란 모란 사포트오나호르 해제
		ELSEIF SELECTCOM == 16 && TEQUIP:30 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;혼란 모란 서포트 페라(말주가 한다)
		ELSEIF SELECTCOM == 41
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주인에 범해지고면서 조수의 페니스를 빱니다」
			RETURN 1
		;혼란 모란 사포트파이즈리
		ELSEIF SELECTCOM == 42
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;혼란 모란 서포트다리 코키(말주가 한다)
		ELSEIF SELECTCOM == 47
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주인에 범해지고면서 조수의 페니스를 다리로 비빕니다」
			RETURN 1
		;혼란 모란 서포트미약키스(기교 Lv3 이상)
		ELSEIF SELECTCOM == 61 && ABL:PLAYER:12 >= 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「조수가 키스로 미약을 먹입니다」
			RETURN 1
		;혼란 모란 서포트 강제 쿤닐링구스
		ELSEIF SELECTCOM == 300
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「눈앞에 조수가 립은 지째 당합니다」
			RETURN 1
		;혼란 모란 서포트 짝 맞추기 놀이(어느 쪽인지가 뚜껑이든지라면 불가)
		ELSEIF SELECTCOM == 301 && TALENT:121 == 0 && TALENT:ASSI:121 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「조수가 부러운 듯이  결합부에 문질러 옵니다……」
			RETURN 1
		;혼란 모란 서포트다리 코키(말주가 된다)
		ELSEIF SELECTCOM == 302
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주인에 범해지고면서 조수에 페니스를 다리로 요」
			RETURN 1
		;혼란 모란 서포트 매도
		ELSEIF SELECTCOM == 227
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「거울을 보면서 조수에 좋아해 부엌 말해집니다」
			RETURN 1
		;혼란 모란 서포트 강제 방뇨
		ELSEIF SELECTCOM == 620 || (SELECTCOM == 111 && SOURCE:0)
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「해-해-의 포즈입니다. 삽입」
			RETURN 1
		;혼란 모란 사포트하메 촬영
		ELSEIF SELECTCOM == 100 && TEQUIP:50
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		ENDIF
	;배면자리정도
	ELSEIF CFLAG:5220 == 2023
		;배면자리정도 서포트 애무
		IF SELECTCOM == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「조수에 좋아해 부엌 만지작거려집니다……」
			RETURN 1
		;배면자리정도 서포트 쿤닐링구스
		ELSEIF SELECTCOM == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「결합부를 조수에 빨 수 있습니다」
			RETURN 1
		;배면자리정도 자위시간
		ELSEIF SELECTCOM == 7
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결된 채로 자위하는 것을 조수에 바로 정면으로부터 볼 수 있습니다」
			RETURN 1
		;배면자리정도 서포트 페라(말주가 된다)
		ELSEIF SELECTCOM == 5
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「뒤를 범해지고면서 전을 빨아지고……」
			RETURN 1
		;배면자리정도 서포트가슴 애무
		ELSEIF SELECTCOM == 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도 서포트 키스
		ELSEIF SELECTCOM == 6
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도시간
		ELSEIF SELECTCOM == 9
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도 서포트 로터
		ELSEIF SELECTCOM == 10
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도 서포트전마
		ELSEIF SELECTCOM == 11
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도 사포트오나호르
		ELSEIF SELECTCOM == 16 && TEQUIP:30 == 2
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도 사포트오나호르 해제
		ELSEIF SELECTCOM == 16 && TEQUIP:30 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도 서포트 페라(말주가 한다)
		ELSEIF SELECTCOM == 41
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주인에 범해지고면서 조수의 페니스를 빱니다」
			RETURN 1
		;배면자리정도 사포트파이즈리
		ELSEIF SELECTCOM == 42
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도 서포트다리 코키(말주가 한다)
		ELSEIF SELECTCOM == 47
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주인에 범해지고면서 조수의 페니스를 다리로 비빕니다」
			RETURN 1
		;배면자리정도 서포트미약키스(기교 Lv3 이상)
		ELSEIF SELECTCOM == 61 && ABL:PLAYER:12 >= 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「조수가 키스로 미약을 먹입니다」
			RETURN 1
		;배면자리정도 서포트 강제 쿤닐링구스
		ELSEIF SELECTCOM == 300
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「눈앞에 조수가 립은 지째 당합니다」
			RETURN 1
		;배면자리정도 서포트 짝 맞추기 놀이(어느 쪽인지가 뚜껑이든지라면 불가)
		ELSEIF SELECTCOM == 301 && TALENT:121 == 0 && TALENT:ASSI:121 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「조수가 부러운 듯이  결합부에 문질러 옵니다……」
			RETURN 1
		;배면자리정도 서포트다리 코키(말주가 된다)
		ELSEIF SELECTCOM == 302
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주인에 범해지고면서 조수에 페니스를 다리로 요」
			RETURN 1
		;배면자리정도 서포트 강제 방뇨
		ELSEIF SELECTCOM == 620 || (SELECTCOM == 111 && SOURCE:0)
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「해-해-의 포즈입니다. 삽입」
			RETURN 1
		;배면자리정도 사포트하메 촬영
		ELSEIF SELECTCOM == 100 && TEQUIP:50
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		ENDIF
	;승마정도
	ELSEIF CFLAG:5220 == 2024
		;승마정도 서포트 애무
		IF SELECTCOM == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도 서포트 쿤닐링구스
		ELSEIF SELECTCOM == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「결합부를 조수에 빨 수 있습니다」
			RETURN 1
		;승마정도 자위시간
		ELSEIF SELECTCOM == 7
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도 서포트가슴 애무
		ELSEIF SELECTCOM == 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도 서포트 키스
		ELSEIF SELECTCOM == 6
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도시간
		ELSEIF SELECTCOM == 9
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도 서포트 로터
		ELSEIF SELECTCOM == 10
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도 서포트전마
		ELSEIF SELECTCOM == 11
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도 사포트오나호르
		ELSEIF SELECTCOM == 16 && TEQUIP:30 == 2
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도 사포트오나호르 해제
		ELSEIF SELECTCOM == 16 && TEQUIP:30 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도 서포트 페라(말주가 한다)
		ELSEIF SELECTCOM == 41
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주인에 범해지고면서 조수의 페니스를 빱니다」
			RETURN 1
		;승마정도 사포트파이즈리
		ELSEIF SELECTCOM == 42
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도 서포트미약키스(기교 Lv3 이상)
		ELSEIF SELECTCOM == 61 && ABL:PLAYER:12 >= 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「조수가 키스로 미약을 먹입니다」
			RETURN 1
		;승마정도 서포트 강제 쿤닐링구스
		ELSEIF SELECTCOM == 300
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「눈앞에 조수가 립은 지째 당합니다」
			RETURN 1
		;승마정도 서포트 짝 맞추기 놀이(어느 쪽인지가 뚜껑이든지라면 불가)
		ELSEIF SELECTCOM == 301 && TALENT:121 == 0 && TALENT:ASSI:121 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「조수가 부러운 듯이  결합부에 문질러 옵니다……」
			RETURN 1
		;승마정도 서포트 스팬 킹
		ELSEIF SELECTCOM == 220
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주와 너무 노닥 거려 조수에 등 얻어맞읍니다(무엇)」
			RETURN 1
		;승마정도 서포트 C주사
		ELSEIF SELECTCOM == 80
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결된 채로 주사하셔 허리가 각쿤각쿤 날뜁니다」
			RETURN 1
		;승마정도 사포트하메 촬영
		ELSEIF SELECTCOM == 100 && TEQUIP:50
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		ENDIF
	ENDIF
ENDIF


;A삽입계 디폴트 조건：연모＋A감각 Lv3 이상＋조수가 남자는 아니다
IF TALENT:150 && ABL:2 > 2 && TALENT:ASSI:120 == 0
	;정상정도 어널 or어널 섹스
	IF CFLAG:5220 == 2030 || CFLAG:5220 == 2030
		;정상정도 A서포트 애무
		IF SELECTCOM == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「삽입한채로 조수에 애무됩니다」
			RETURN 1
		;정상정도 A서포트가슴 애무
		ELSEIF SELECTCOM == 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;정상정도 A서포트 키스
		ELSEIF SELECTCOM == 6
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;정상정도 A시간(아무것도 하지 않는다)
		ELSEIF SELECTCOM == 9
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결되고 있는 곳을 조수에 빤히 볼 수 있습니다」
			RETURN 1
		;정상정도 A서포트 로터
		ELSEIF SELECTCOM == 10
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「결합부 위에 로터를 맞힐 수 있으며……」
			RETURN 1
		;정상정도 A서포트전마
		ELSEIF SELECTCOM == 11
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「결합부 위에 E안마사를 맞힐 수 있으며……」
			RETURN 1
		;정상정도 A서포트 자위
		ELSEIF SELECTCOM == 40
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결된 채로 뚜껑이든지 조수의 물건을 잡아당기게 하고……」
			RETURN 1
		;정상정도 A서포트 페라
		ELSEIF SELECTCOM == 41
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결된 채로 뚜껑이든지 조수의 물건을 빨게 하고……」
			RETURN 1
		;정상정도 A사포트파이즈리
		ELSEIF SELECTCOM == 42
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;정상정도 A서포트미약키스(기교 Lv3 이상)
		ELSEIF SELECTCOM == 61 && ABL:PLAYER:12 >= 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「조수가 키스로 미약을 먹입니다」
			RETURN 1
		;정상정도 A서포트 강제 쿤닐링구스
		ELSEIF SELECTCOM == 300
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「범해지고면서 조수에 안면기승위되어 빨게 하고……」
			RETURN 1
		;정상정도 A사포트이라마치오
		ELSEIF SELECTCOM == 230
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결된 채로 뚜껑이든지 조수의 물건을 빨게 하고……」
			RETURN 1
		;정상정도 A서포트 강제 A봉사
		ELSEIF SELECTCOM == 44
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「범해지고면서 조수에 안면기승위되어 빨게 하고……」
			RETURN 1
		;정상정도 A사포트하메 촬영
		ELSEIF SELECTCOM == 100 && TEQUIP:50
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		ENDIF
	;후배 정도 어널
	ELSEIF CFLAG:5220 == 2031
		;후배 정도 A서포트 애무
		IF SELECTCOM == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;후배 정도 A서포트 쿤닐링구스
		ELSEIF SELECTCOM == 1
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「아래로부터 날름날름 빱니다」
			RETURN 1
		;후배 정도 A서포트 페라(말주가 된다)
		ELSEIF SELECTCOM == 5
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「뒤를 범해지고면서 전을 빨아지고……」
			RETURN 1
		;후배 정도 A서포트가슴 애무
		ELSEIF SELECTCOM == 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;후배 정도 A서포트 키스
		ELSEIF SELECTCOM == 6
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;후배 정도 A시간
		ELSEIF SELECTCOM == 9
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;후배 정도 A서포트손가락 삽입
		ELSEIF SELECTCOM == 4
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;후배 정도 A서포트 G스포트 자극
		ELSEIF SELECTCOM == 600
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;후배 정도 A서포트 로터
		ELSEIF SELECTCOM == 10
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「결합부 위에 로터를 맞힐 수 있으며……」
			RETURN 1
		;후배 정도 A서포트 바이브
			ELSEIF SELECTCOM == 12 && TEQUIP:20 == 1
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;후배 정도 A서포트 바이브 해제
			ELSEIF SELECTCOM == 12 && TEQUIP:20 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;후배 정도 A서포트전마
		ELSEIF SELECTCOM == 11
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「결합부 위에 E안마사를 맞힐 수 있으며……」
			RETURN 1
		;후배 정도 A서포트 페라
		ELSEIF SELECTCOM == 41
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결된 채로 뚜껑이든지 조수의 물건을 빨게 하고……」
			RETURN 1
		;후배 정도 A서포트미약키스(기교 Lv3 이상)
		ELSEIF SELECTCOM == 61 && ABL:PLAYER:12 >= 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「조수가 키스로 미약을 먹입니다」
			RETURN 1
		;후배 정도 A서포트 강제 쿤닐링구스
		ELSEIF SELECTCOM == 300
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「눈앞의 조수의 음부를 빨면서 가방으로 찔리고……」
			RETURN 1
		;후배 정도 A서포트 69
		ELSEIF SELECTCOM == 602
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「포복아래에 조수가 들어와 범해지고면서 빨아 사랑……」
			RETURN 1
		;후배 정도 A사포트이라마치오
		ELSEIF SELECTCOM == 230
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결된 채로 뚜껑이든지 조수의 물건을 빨게 하고……」
			RETURN 1
		;후배 정도 A서포트 강제 A봉사
		ELSEIF SELECTCOM == 44
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「눈앞의 조수의 국혈을 빨면서 가방으로 찔리고……」
			RETURN 1
		;후배 정도 A사포트하메 촬영
		ELSEIF SELECTCOM == 100 && TEQUIP:50
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		ENDIF
	;대면자리정도 어널
	ELSEIF CFLAG:5220 == 2032
		;대면자리정도 A서포트 애무
		IF SELECTCOM == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「귀를은 보고는 보고 하기도 좋지요」
			RETURN 1
		;대면자리정도 A서포트가슴 애무
		ELSEIF SELECTCOM == 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;대면자리정도 A서포트 키스
		ELSEIF SELECTCOM == 6
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결된 채로 얼싸안아 키스입니다」
			RETURN 1
		;대면자리정도 A시간
		ELSEIF SELECTCOM == 9
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;대면자리정도 A서포트 로터
		ELSEIF SELECTCOM == 10
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주인과의 사이에 로터를 끼고……」
			RETURN 1
		;대면자리정도 A서포트미약키스(기교 Lv3 이상)
		ELSEIF SELECTCOM == 61 && ABL:PLAYER:12 >= 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「조수가 키스로 미약을 먹입니다」
			RETURN 1
		;대면자리정도 A서포트 스팬 킹
		ELSEIF SELECTCOM == 220
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주와 너무 노닥 거려 조수에 등 얻어맞읍니다(무엇)」
			RETURN 1
		;대면자리정도 A사포트하메 촬영
		ELSEIF SELECTCOM == 100 && TEQUIP:50
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		ENDIF
	;배면자리정도 어널
	ELSEIF CFLAG:5220 == 2033
		;배면자리정도 A서포트 애무
		IF SELECTCOM == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「조수에 좋아해 부엌 만지작거려집니다……」
			RETURN 1
		;배면자리정도 A서포트 쿤닐링구스
		ELSEIF SELECTCOM == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「결합부를 조수에 빨 수 있습니다」
			RETURN 1
		;배면자리정도 A자위시간
		ELSEIF SELECTCOM == 7
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결된 채로 자위하는 것을 조수에 바로 정면으로부터 볼 수 있습니다」
			RETURN 1
		;배면자리정도 A서포트 페라(말주가 된다)
		ELSEIF SELECTCOM == 5
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「뒤를 범해지고면서 전을 빨아지고……」
			RETURN 1
		;배면자리정도 A서포트가슴 애무
		ELSEIF SELECTCOM == 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도 A서포트 키스
		ELSEIF SELECTCOM == 6
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도시간
		ELSEIF SELECTCOM == 9
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도 A서포트 로터
		ELSEIF SELECTCOM == 10
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도 A서포트전마
		ELSEIF SELECTCOM == 11
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도 A사포트오나호르
		ELSEIF SELECTCOM == 16 && TEQUIP:30 == 2
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도 A사포트오나호르 해제
		ELSEIF SELECTCOM == 16 && TEQUIP:30 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도 A서포트 페라(말주가 한다)
		ELSEIF SELECTCOM == 41
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주인에 범해지고면서 조수의 페니스를 빱니다」
			RETURN 1
		;배면자리정도 A사포트파이즈리
		ELSEIF SELECTCOM == 42
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도 A서포트다리 코키(말주가 한다)
		ELSEIF SELECTCOM == 47
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주인에 범해지고면서 조수의 페니스를 다리로 비빕니다」
			RETURN 1
		;배면자리정도 A서포트미약키스(기교 Lv3 이상)
		ELSEIF SELECTCOM == 61 && ABL:PLAYER:12 >= 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「조수가 키스로 미약을 먹입니다」
			RETURN 1
		;배면자리정도 A서포트 강제 쿤닐링구스
		ELSEIF SELECTCOM == 300
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「눈앞에 조수가 립은 지째 당합니다」
			RETURN 1
		;배면자리정도 A서포트 짝 맞추기 놀이(어느 쪽인지가 뚜껑이든지라면 불가)
		ELSEIF SELECTCOM == 301 && TALENT:121 == 0 && TALENT:ASSI:121 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「조수가 부러운 듯이  결합부에 문질러 옵니다……」
			RETURN 1
		;배면자리정도 A서포트다리 코키(말주가 된다)
		ELSEIF SELECTCOM == 302
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주인에 범해지고면서 조수에 페니스를 다리로 요」
			RETURN 1
		;배면자리정도 A서포트 강제 방뇨
		ELSEIF SELECTCOM == 620 || (SELECTCOM == 111 && SOURCE:0)
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「해-해-의 포즈입니다. 삽입」
			RETURN 1
		;배면자리정도 A사포트하메 촬영
		ELSEIF SELECTCOM == 100 && TEQUIP:50
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도 A서포트손가락 삽입
		ELSEIF SELECTCOM == 4
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도 A서포트 G스포트 자극
		ELSEIF SELECTCOM == 600
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도 A서포트 바이브
			ELSEIF SELECTCOM == 12 && TEQUIP:20 == 1
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;배면자리정도 A서포트 바이브 해제
			ELSEIF SELECTCOM == 12 && TEQUIP:20 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		ENDIF
	;승마정도 A
	ELSEIF CFLAG:5220 == 2034
		;승마정도 A서포트 애무
		IF SELECTCOM == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도 A서포트 쿤닐링구스
		ELSEIF SELECTCOM == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「결합부를 조수에 빨 수 있습니다」
			RETURN 1
		;승마정도 자위시간
		ELSEIF SELECTCOM == 7
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도 A서포트가슴 애무
		ELSEIF SELECTCOM == 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도 A서포트 키스
		ELSEIF SELECTCOM == 6
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도시간
		ELSEIF SELECTCOM == 9
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도 A서포트 로터
		ELSEIF SELECTCOM == 10
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도 A서포트 바이브
			ELSEIF SELECTCOM == 12 && TEQUIP:20 == 1
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도 A서포트 바이브 해제
			ELSEIF SELECTCOM == 12 && TEQUIP:20 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도 A서포트전마
		ELSEIF SELECTCOM == 11
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도 A사포트오나호르
		ELSEIF SELECTCOM == 16 && TEQUIP:30 == 2
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도 A사포트오나호르 해제
		ELSEIF SELECTCOM == 16 && TEQUIP:30 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도 A서포트 페라(말주가 한다)
		ELSEIF SELECTCOM == 41
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주인에 범해지고면서 조수의 페니스를 빱니다」
			RETURN 1
		;승마정도 A사포트파이즈리
		ELSEIF SELECTCOM == 42
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도 A서포트미약키스(기교 Lv3 이상)
		ELSEIF SELECTCOM == 61 && ABL:PLAYER:12 >= 3
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「조수가 키스로 미약을 먹입니다」
			RETURN 1
		;승마정도 A서포트 강제 쿤닐링구스
		ELSEIF SELECTCOM == 300
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「눈앞에 조수가 립은 지째 당합니다」
			RETURN 1
		;승마정도 A서포트 짝 맞추기 놀이(어느 쪽인지가 뚜껑이든지라면 불가)
		ELSEIF SELECTCOM == 301 && TALENT:121 == 0 && TALENT:ASSI:121 == 0
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「조수가 부러운 듯이  결합부에 문질러 옵니다……」
			RETURN 1
		;승마정도 A서포트 스팬 킹
		ELSEIF SELECTCOM == 220
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주와 너무 노닥 거려 조수에 등 얻어맞읍니다(무엇)」
			RETURN 1
		;승마정도 A서포트 C주사
		ELSEIF SELECTCOM == 80
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결된 채로 주사하셔 허리가 각쿤각쿤 날뜁니다」
			RETURN 1
		;승마정도 A서포트 V주사
		ELSEIF SELECTCOM == 81
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「연결된 채로 주사하셔 허리가 각쿤각쿤 날뜁니다」
			RETURN 1
		;승마정도 A서포트손가락 삽입
		ELSEIF SELECTCOM == 4
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도 A서포트 G스포트 자극
		ELSEIF SELECTCOM == 600
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		;승마정도 A사포트하메 촬영
		ELSEIF SELECTCOM == 100 && TEQUIP:50
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORM 
			RETURN 1
		ENDIF
	ENDIF
ENDIF

;비삽입계 디폴트 조건：연모＋조수가 남자는 아니다
LOCAL = 0
IF TALENT:150 && TALENT:ASSI:120 == 0
	;조수를 범한다
	IF CFLAG:5220 == 2026
		LOCAL = 1
		;조수 섹스 서포트 쿤닐링구스 강제
		IF SELECTCOM == 300
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주인에 범해지는 조수의 결합부를 빱니다」
			RETURN 1
		;조수 섹스 자위시간
		ELSEIF SELECTCOM == 7
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주인에 범해지는 조수를 보면서 자위합니다」
			RETURN 1
		;조수 섹스중 방치
		ELSEIF SELECTCOM == 9
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주인에 범해져 기분이야 유혹한데 조수에 업신여겨집니다」
			RETURN 1
		;조수 섹스 서포트
		ELSEIF SELECTCOM == 110
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주인에 범해지는 조수에 자신의 성기를 벌려 과시합니다」
			RETURN 1
		;조수 섹스 서포트 A봉사 강제
		ELSEIF SELECTCOM == 44
			TRYCALL COMBOPLAY(LOCAL)
			PRINTFORMW 「주인에 범해지는 조수의 어널을 빱니다」
			RETURN 1
		ENDIF
	ENDIF
ENDIF


;~~~~~~~~~~~~~~~~~~~~~~~~~~
;무효화 부분 종료
ENDIF
;~~~~~~~~~~~~~~~~~~~~~~~~~~


RETURN 0

