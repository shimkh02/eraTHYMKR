;≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡
;eratohoYM Mk-2 rev. 6용 분할형 말템플릿
;13(을)를 캐릭터 번호에 치환해 사용해 주세요(레이무라면 1, 련자라면 49, 매마라면 100).
;
;이 파일에서는 조수시의 말용 함수가 놓여져 있습니다.
;≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡
;==================================================
;등말
;==================================================
;==================================================
;@KOJO_MESSAGE_PLAYERCHANGE 관계
;조수 변환시에 출력됩니다.
;조수에 교대했을 때, 조수로부터 되돌렸을 때에 조수로서 말하기 때문에(위해)의 기능
;==================================================
@KOJO_MESSAGE_PLAYERCHANGE_13
SIF CFLAG:7900 == 0
	RETURN 0
;연모(나온다)
IF TALENT:150 == 1 && CFLAG:7007 == 0
	PRINTFORMW 「여기로부터는, 첸에게 맡겨줘.」
	CFLAG:7007 = 1
;복종(나온다)
ELSEIF TALENT:160 == 1 && CFLAG:7007 == 0
	PRINTFORMW 「네. 첸이 %CALLNAME:MASTER%로 바뀌어, -조교-합니다.」
	CFLAG:7007 = 1
;그 이외(나온다)
ELSEIF CFLAG:7007 == 0
	PRINTFORMW 「자, 여기로부터는 첸-조교-하는군」
	CFLAG:7007 = 1
;연모(돌아온다)
ELSEIF TALENT:150 == 1 && CFLAG:7007 == 1
	PRINTFORMW 「자, 첸은 1회 돌아와. 계속 힘내」
	CFLAG:7007 = 0
;복종(돌아온다)
ELSEIF TALENT:160 == 1&& CFLAG:7007 == 1
	PRINTFORMW 「여기로부터는,%CALLNAME:MASTER%에 직접-조교-받습니다.」
	CFLAG:7007 = 0
;그 이외(돌아온다)
ELSEIF CFLAG:7007 == 1
	PRINTFORMW 「자, 첸은 1회 돌아와」
	CFLAG:7007 = 0
ENDIF

;==================================================
;@KOJO_MESSAGE_CHANGEASSI 관계
;조수 변환시에 출력됩니다.
;조수에 교대했을 때, 조수로부터 되돌렸을 때에 대상으로 해 말하기 때문에(위해)의 기능
;==================================================
@KOJO_MESSAGE_CHANGEASSI_13
SIF CFLAG:7900 == 0
	RETURN 0
;연모(나온다)
IF TALENT:150 == 1 && CFLAG:7008 == 0
	PRINTFORMW 「네~다른 사람으로 되는 것은 조금…」
	CFLAG:7008 = 1
;복종(나온다)
ELSEIF TALENT:160 == 1 && CFLAG:7008 == 0
	PRINTFORMW 「%조사처리(CALLNAME:MASTER,"가")% 그렇게 하라고 한다면, 첸은 따릅니다」
	CFLAG:7008 = 1
;그 이외(나온다)
ELSEIF CFLAG:7008 == 0
	PRINTFORMW 「교대? ~응. 마음대로 하면…」
	CFLAG:7008 = 1
;연모(돌아온다)
ELSEIF TALENT:150 == 1 && CFLAG:7008 == 1
	PRINTFORMW 「역시,%CALLNAME:MASTER%로 해 주는 것이…제일 기쁘고 기분이 좋아」
	CFLAG:7008 = 0
;복종(돌아온다)
ELSEIF TALENT:160 == 1 && CFLAG:7008 == 1
	PRINTFORMW 「이번은%CALLNAME:MASTER%로 해 받을 수 있습니까?」
	CFLAG:7008 = 0
;그 이외(돌아온다)
ELSEIF CFLAG:7008 == 1
	PRINTFORMW 「교대? ~응. 마음대로 하면…」
	CFLAG:7008 = 0
ENDIF

;==================================================
;@KOJO_MESSAGE_COM_ASSI 관계
;조수 조교중의 커맨드 실행시에 출력됩니다.
;==================================================
@KOJO_MESSAGE_COM_ASSI_13
SIF CFLAG:7900 == 0
	RETURN 0
;연모
IF TALENT:150 == 1
	IF RAND:4 == 0
		PRINTFORMW 「하아…첸도 기분 좋아지고 싶다」
	ELSEIF RAND:3 == 0
		;기교가 3 이하
		IF ABL:12 <= 3
			PRINTFORMW 「에잇……잘 되지 않아」
		;기교가 4이상
		ELSEIF ABL:12 >= 4
			PRINTFORMW 「에잇…기분 있고겠지」	
		ENDIF
	ELSEIF RAND:2 == 0
		PRINTFORMW 「%조사처리(CALLNAME:MASTER,"를")% 위해서(때문에) 이 아이를 첸-주거지-하는군」
	ELSE
		PRINTFORMW 「잘 할 수 있었는지?」
	ENDIF
;복종
ELSEIF TALENT:160 == 1
	IF RAND:2 == 0
		PRINTFORMW 「첸이%CALLNAME:MASTER%의 것인지원-주거지-합니다」
	ELSE
		PRINTFORMW 「%CALLNAME:MASTER%에 거역하면 안 됨!」
	ENDIF
;그 이외
ELSE
	PRINTFORMW 
ENDIF 
