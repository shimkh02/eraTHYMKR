;≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡
;eratohoYM Mk-2 rev. 6용 분할형 말템플릿
;X1를 캐릭터 번호에 치환해 사용해 주세요(레이무라면 1, 련자라면 49, 매마라면 100).
;
;이 파일에서는 400~499번의 커맨드(캐릭터 전용 커맨드)의 말용 함수가 놓여져 있습니다.
;≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡
;==================================================
;00말
;==================================================
;==================================================
;커맨드 말
;첫회 판정에 CFLAG:9000~9099까지의 100개를 사용한다
;첫회인가 어떤가는 RESULT:1이 1인가 어떤가로 판별
;==================================================
;-------------------------------------------------
;흡혈
;사용 캐릭터…TALENT:205
;-------------------------------------------------
@KX1_MESSAGE_COM_400
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 흡혈 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「흡혈 말입니다」
ENDIF

;-------------------------------------------------
;어두운 곳
;사용 캐릭터…르미아
;-------------------------------------------------
@KX1_MESSAGE_COM_401
;종료
IF TEQUIP:70 == 0
	;PRINTFORMW 「어두운 곳 해제 말입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 어두운 곳 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「어두운 곳 말입니다」
ENDIF

;-------------------------------------------------
;엉덩이 시교쿠 빼기
;사용 캐릭터…에 있어, TALENT:210
;-------------------------------------------------
@KX1_MESSAGE_COM_402
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 엉덩이 시교쿠 빼기 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「엉덩이 시교쿠 빼기 말입니다」
ENDIF

;-------------------------------------------------
;군고구마 고문
;사용 캐릭터…가을 자매
;-------------------------------------------------
@KX1_MESSAGE_COM_403
;종료
IF TEQUIP:20 == 0
	;PRINTFORMW 「군고구마 고문 해제 말입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 군고구마 고문 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「군고구마 고문 말입니다」
ENDIF

;-------------------------------------------------
;거봉 고문
;사용 캐릭터…가을 자매
;-------------------------------------------------
@KX1_MESSAGE_COM_404
;종료
IF TEQUIP:25 == 0
	;PRINTFORMW 「거봉 고문 해제 말입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 거봉 고문 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「거봉 고문 말입니다」
ENDIF

;-------------------------------------------------
;석작백고문
;사용 캐릭터…가을 자매
;-------------------------------------------------
@KX1_MESSAGE_COM_405
;종료
IF TEQUIP:32 == 0
	;PRINTFORMW 「석작백고문 해제 말입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회 모치시작백고문 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「석작백고문 말입니다」
ENDIF

;-------------------------------------------------
;가시나무로 긴박
;사용 캐릭터…유카
;-------------------------------------------------
@KX1_MESSAGE_COM_406
;종료
IF TEQUIP:40 == 0
	;PRINTFORMW 「가시나무로 긴박 해제 말입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 가시나무로 긴박 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「가시나무로 긴박 말입니다」
ENDIF

;-------------------------------------------------
;가시나무의 채찍
;사용 캐릭터…유카
;-------------------------------------------------
@KX1_MESSAGE_COM_407
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 가시나무의 채찍 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「가시나무의 채찍 말입니다」
ENDIF

;-------------------------------------------------
;거미의 실
;사용 캐릭터…야마메
;-------------------------------------------------
@KX1_MESSAGE_COM_408
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 거미의 실마리상입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「거미의 실마리상입니다」
ENDIF

;-------------------------------------------------
;팽이를 돌림 해
;사용 캐릭터…야마메
;-------------------------------------------------
@KX1_MESSAGE_COM_409
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 팽이를 돌림 해 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「팽이를 돌림 해 말입니다」
ENDIF

;-------------------------------------------------
;거미다리 애무
;사용 캐릭터…야마메
;-------------------------------------------------
@KX1_MESSAGE_COM_410
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 거미다리 애무 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「거미다리 애무 말입니다」
ENDIF

;-------------------------------------------------
;인형 애무
;사용 캐릭터…앨리스(EX화시 불가)
;-------------------------------------------------
@KX1_MESSAGE_COM_411
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회 고용인형 애무 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「인형 애무 말입니다」
ENDIF

;-------------------------------------------------
;조정의 실
;사용 캐릭터…앨리스(EX화시 불가)
;-------------------------------------------------
@KX1_MESSAGE_COM_412
;종료
IF TEQUIP:77 == 0
	;PRINTFORMW 「조정의 실해제 말입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 조정의 실마리상입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「조정의 실마리상입니다」
ENDIF

;-------------------------------------------------
;콩 쬐어
;사용 캐릭터…매홍(EX화시 불가)
;-------------------------------------------------
@KX1_MESSAGE_COM_413
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용콩 쬐어 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「콩 쬐어 말입니다」
ENDIF

;-------------------------------------------------
;유두 쬐어
;사용 캐릭터…매홍(EX화시 불가)
;-------------------------------------------------
@KX1_MESSAGE_COM_414
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용유두 쬐어 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「유두 쬐어 말입니다」
ENDIF

;-------------------------------------------------
;독심
;사용 캐릭터…TALENT:213 = 1(진하고는 2이므로 불가)
;대상 불가…TALENT:213 = 2
;-------------------------------------------------
@KX1_MESSAGE_COM_415
;종료
IF TEQUIP:74 == 0
	;PRINTFORMW 「독심 해제 말입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 독심 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「독심 말입니다」
ENDIF

;-------------------------------------------------
;사간
;사용 불가 캐릭터…TALENT:213 = 2
;대상…TALENT:213 = 1(진하고는 2이므로 불가)
;-------------------------------------------------
@KX1_MESSAGE_COM_416
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용사간말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「사간말입니다」
ENDIF

;-------------------------------------------------
;붓고문
;사용 캐릭터…아구
;-------------------------------------------------
@KX1_MESSAGE_COM_417
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회 용필 고문 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「붓고문 말입니다」
ENDIF

;-------------------------------------------------
;시간 정지
;그런데 보통, 시간 멈춘 순간은 말할 수 없게?
;사용 캐릭터…사쿠야·카구야
;-------------------------------------------------
@KX1_MESSAGE_COM_418
;종료
IF TEQUIP:71 == 0
	;PRINTFORMW 「시간 정지 해제 말입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 시간 정지 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「시간 정지 말입니다」
ENDIF

;-------------------------------------------------
;광기의 눈
;사용 캐릭터…TALENT:125
;대상 불가…TALENT:158
;-------------------------------------------------
@KX1_MESSAGE_COM_419
;종료
IF TEQUIP:73 == 0
	;PRINTFORMW 「광기의 눈해제 말입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 광기의 눈과 입상입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「광기의 눈과 입상입니다」
ENDIF

;-------------------------------------------------
;은방울꽃의 독
;사용 캐릭터…메디슨
;-------------------------------------------------
@KX1_MESSAGE_COM_420
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 은방울꽃의 독설상입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「은방울꽃의 독설상입니다」
ENDIF

;-------------------------------------------------
;곤충 채집 세트의 약
;사용 캐릭터…영림
;-------------------------------------------------
@KX1_MESSAGE_COM_421
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 곤충 채집 세트의 약말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「곤충 채집 세트의 약말입니다」
ENDIF

;-------------------------------------------------
;미약 담그고
;사용 캐릭터…키스메(조교 대상에서도 가능)
;-------------------------------------------------
@KX1_MESSAGE_COM_422
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용미약 담그고 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「미약 담그고 말입니다」
ENDIF

;-------------------------------------------------
;술 담그고
;사용 캐릭터…키스메(조교 대상에서도 가능)
;-------------------------------------------------
@KX1_MESSAGE_COM_423
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용술 담그고 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「술 담그고 말입니다」
ENDIF

;-------------------------------------------------
;젊은 술
;사용 캐릭터…스이카, 용의(조교 대상에서도 가능)
;-------------------------------------------------
@KX1_MESSAGE_COM_424
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 젊은 사카구치상입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「젊은 사카구치상입니다」
ENDIF

;-------------------------------------------------
;가-
;사용 캐릭터…레미리아
;-------------------------------------------------
@KX1_MESSAGE_COM_425
;첫회는 아니다
IF RESULT:1 == 0
	;PRINTFORMW 「하단 가드 말입니다」
;그 외
ELSE
	;PRINTFORMW 「첫회용 하단 가드 말입니다」
ENDIF

;-------------------------------------------------
;음모 쬐어
;사용 캐릭터…매홍(EX화시 불가)
;-------------------------------------------------
@KX1_MESSAGE_COM_427
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 음모 쬐어 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「음모 쬐어 말입니다」
ENDIF

;-------------------------------------------------
;수레 가마
;사용 캐릭터…병아리
;-------------------------------------------------
@KX1_MESSAGE_COM_430
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 수레 가마 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「수레 가마 말입니다」
ENDIF

;-------------------------------------------------
;포오브아카인드
;사용 캐릭터…프랑 짱
;-------------------------------------------------
@KX1_MESSAGE_COM_431
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용4몸의 주먹 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「포오브아카인드 말입니다」
ENDIF

;-------------------------------------------------
;고드름 고문
;사용 캐릭터…치르노
;-------------------------------------------------
@KX1_MESSAGE_COM_432
;종료
IF TEQUIP:26 == 0
	;PRINTFORMW 「고드름 고문 해제 말입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 고드름 고문 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「고드름 고문 말입니다」
ENDIF

;-------------------------------------------------
;어널 졸탑파
;사용 캐릭터…영희
;-------------------------------------------------
@KX1_MESSAGE_COM_433
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 어널 졸탑파 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「어널 졸탑파 말입니다」
ENDIF

;-------------------------------------------------
;모퉁이 애무
;대상…TALENT:214또는 하크타크화한 TALENT:212
;-------------------------------------------------
@KX1_MESSAGE_COM_434
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용각 애무 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「모퉁이 애무 말입니다」
ENDIF

;-------------------------------------------------
;모퉁이 삽입
;사용 캐릭터…TALENT:214또는 하크타크화한 TALENT:212
;-------------------------------------------------
@KX1_MESSAGE_COM_435
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용각 삽입구상입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「모퉁이 삽입구상입니다」
ENDIF

;-------------------------------------------------
;모퉁이 어널 삽입
;사용 캐릭터…TALENT:214또는 하크타크화한 TALENT:212
;-------------------------------------------------
@KX1_MESSAGE_COM_436
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용각 어널 삽입구상입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「모퉁이 어널 삽입구상입니다」
ENDIF

;-------------------------------------------------
;바기나 요석
;사용 캐릭터…천자
;-------------------------------------------------
@KX1_MESSAGE_COM_437
;뽑으면(자) 지진 발생!
IF TEQUIP:20 == 0
	;PRINTFORMW 「바기나 요석 해제 말입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 바기나 요석 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「바기나 요석 말입니다」
ENDIF

;-------------------------------------------------
;어널 요석
;사용 캐릭터…천자
;-------------------------------------------------
@KX1_MESSAGE_COM_438
;뽑으면(자) 지진 발생!
IF TEQUIP:25 == 0
	;PRINTFORMW 「어널 요석 해제 말입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 어널 요석 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「어널 요석 말입니다」
ENDIF

;-------------------------------------------------
;양구멍 요석
;사용 캐릭터…천자
;-------------------------------------------------
@KX1_MESSAGE_COM_439
;뽑으면(자) 지진 발생!
IF TEQUIP:20 == 0 && TEQUIP:25 == 0
	;PRINTFORMW 「 양구멍 요석 해제 말입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 양구멍 요석 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「 양구멍 요석 말입니다」
ENDIF

;-------------------------------------------------
;반령을 범한다
;대상…요몽, 요기
;조교 가능 캐릭터에게 각각의 반령이 없으면 사용할 수 없다
;-------------------------------------------------
@KX1_MESSAGE_COM_440
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용반령을 범하는 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「반령을 범하는 말입니다」
ENDIF

;-------------------------------------------------
;반령을 삽입(전)
;사용 캐릭터…요몽, 요기
;조교 가능 캐릭터에게 각각의 반령이 없으면 사용할 수 없다
;-------------------------------------------------
@KX1_MESSAGE_COM_441
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용반령을 삽입( 전) 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「반령을 삽입( 전) 말입니다」
ENDIF

;-------------------------------------------------
;반령을 삽입( 후 )
;사용 캐릭터…요몽, 요기
;조교 가능 캐릭터에게 각각의 반령이 없으면 사용할 수 없다
;-------------------------------------------------
@KX1_MESSAGE_COM_442
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용반령을 삽입( 후 ) 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「반령을 삽입( 후 ) 말입니다」
ENDIF

;-------------------------------------------------
;유명의 고륜
;대상…요몽, 요기
;조교 가능 캐릭터에게 각각의 반령이 없으면 사용할 수 없다
;-------------------------------------------------
@KX1_MESSAGE_COM_443
;종료
IF TEQUIP:72 == 0
	;PRINTFORMW 「유명의 고륜해제 말입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 유명의 고륜말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「유명의 고륜말입니다」
ENDIF

;-------------------------------------------------
;반령복구성
;사용 캐릭터…요기
;조교 가능 캐릭터에게 반령(기)이 없으면 사용할 수 없다
;-------------------------------------------------
@KX1_MESSAGE_COM_444
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용반령복구성말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「반령복구성말입니다」
ENDIF

;-------------------------------------------------
;온축이야기
;사용 캐릭터…림지조(대상에서도 가능)
;-------------------------------------------------
@KX1_MESSAGE_COM_445
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용온축실마리상입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「온축실마리상입니다」
ENDIF

;-------------------------------------------------
;설교를 받는다
;사용 캐릭터…영희(대상에서도 가능)
;-------------------------------------------------
@KX1_MESSAGE_COM_446
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 설교를 받는 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「설교를 받는 말입니다」
ENDIF

;-------------------------------------------------
;우산을 애무한다
;대상…소우산
;-------------------------------------------------
@KX1_MESSAGE_COM_447
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 우산을 애무하는 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「우산을 애무하는 말입니다」
ENDIF

;-------------------------------------------------
;방중술
;사용 캐릭터…미령
;-------------------------------------------------
@KX1_MESSAGE_COM_448
;종료
IF TEQUIP:79 == 0
	;PRINTFORMW 「방중술 해제 말입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 방중술 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「방중술 말입니다」
ENDIF

;-------------------------------------------------
;피버!
;사용 캐릭터…의구(대상에서도 가능)
;-------------------------------------------------
@KX1_MESSAGE_COM_449
;첫회는 아니다
IF RESULT:1 == 0
	;PRINTFORMW 「피버! 말입니다」
;그 외
ELSE
	;PRINTFORMW 「첫회용 피버! 말입니다」
ENDIF

;-------------------------------------------------
;의식 「우선 피처2개(살)로」
;사용 캐릭터…텐지(연호)
;-------------------------------------------------
@KX1_MESSAGE_COM_450
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 피처2개(살) 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「피처2개(살) 말입니다」
ENDIF

;-------------------------------------------------
;취생몽사 「하늘을 나는 탄의 레무리아」
;사용 캐릭터…텐지(연호)
;-------------------------------------------------
@KX1_MESSAGE_COM_451
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 취생몽사 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「취생몽사 말입니다」
ENDIF

;-------------------------------------------------
;초응시
;사용 캐릭터…유우겐마간
;-------------------------------------------------
@KX1_MESSAGE_COM_452
;종료
IF TEQUIP:80 == 0
	;PRINTFORMW 「초응시 해제 말입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용초응시 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「초응시 말입니다」
ENDIF

;-------------------------------------------------
;우산에 빨게 한다
;사용 캐릭터…지우산 도깨비(실질소우산 전용)
;-------------------------------------------------
@KX1_MESSAGE_COM_453
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 우산에 빨게 하는 말입니다」
;2번째 이후
ELSE
	;PRINTFORMW 「우산에 빨게 하는 말입니다」
ENDIF
