;≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡
;eratohoYM Mk-2 rev.6용 분할형 대사 템플릿
;39을 캐릭터 번호 치환해 사용해주세요(레이무라면 1, 렌코라면 49, 미마라면 100).
;
;이 파일에는 400～499번 커맨드(캐릭터 전용 커맨드)의 대사용 함수가 마련되어 있습니다.
;≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡
;==================================================
;○○ 대사
;==================================================
;==================================================
;커맨드 대사
;첫회 판정에 CFLAG:9000～9099까지의 100개를 사용한다
;첫회인지 아닌지는 RESULT:1이 1인가 아닌가로 판별
;==================================================
;-------------------------------------------------
;흡혈
;사용 캐릭터…TALENT:205 (흡혈귀)
;-------------------------------------------------
@K39_MESSAGE_COM_400
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 흡혈 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「흡혈 대사입니다」
ENDIF

;-------------------------------------------------
;암흑
;사용 캐릭터…루미아
;-------------------------------------------------
@K39_MESSAGE_COM_401
;종료
IF TEQUIP:70 == 0
	;PRINTFORMW 「암흑 해제 대사입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 암흑 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「암흑 대사입니다」
ENDIF

;-------------------------------------------------
;시리코다마 뽑기
;사용 캐릭터…니토리, TALENT:210 (캇파)
;-------------------------------------------------
@K39_MESSAGE_COM_402
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 시리코다마 뽑기 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「시리코다마 뽑기 대사입니다」
ENDIF

;-------------------------------------------------
;고구마 고문
;사용 캐릭터…아키 자매
;-------------------------------------------------
@K39_MESSAGE_COM_403
;종료
IF TEQUIP:20 == 0
	;PRINTFORMW 「고구마 고문 해제 대사입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 고구마 고문 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「고구마 고문 대사입니다」
ENDIF

;-------------------------------------------------
;거봉 고문
;사용 캐릭터…아키 자매
;-------------------------------------------------
@K39_MESSAGE_COM_404
;종료
IF TEQUIP:25 == 0
	;PRINTFORMW 「거봉 고문 해제 대사입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 거봉 고문 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「거봉 고문 대사입니다」
ENDIF

;-------------------------------------------------
;아스파라거스 고문
;사용 캐릭터…아키 자매
;-------------------------------------------------
@K39_MESSAGE_COM_405
;종료
IF TEQUIP:32 == 0
	;PRINTFORMW 「아스파라거스 고문 해제 대사입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 아스파라거스 고문 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「아스파라거스 고문 대사입니다」
ENDIF

;-------------------------------------------------
;가시나무로 긴박
;사용 캐릭터…유카
;-------------------------------------------------
@K39_MESSAGE_COM_406
;종료
IF TEQUIP:40 == 0
	;PRINTFORMW 「가시나무로 긴박 해제 대사입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 가시나무로 긴박 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「가시나무로 긴박 대사입니다」
ENDIF

;-------------------------------------------------
;가시나무 채찍
;사용 캐릭터…유카
;-------------------------------------------------
@K39_MESSAGE_COM_407
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 가시나무 채찍 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「가시나무 채찍 대사입니다」
ENDIF

;-------------------------------------------------
;거미줄
;사용 캐릭터…야마메
;-------------------------------------------------
@K39_MESSAGE_COM_408
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 거미줄 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「거미줄 대사입니다」
ENDIF

;-------------------------------------------------
;팽이 돌리기
;사용 캐릭터…야마메
;-------------------------------------------------
@K39_MESSAGE_COM_409
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 팽이 돌리기 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「팽이 돌리기 대사입니다」
ENDIF

;-------------------------------------------------
;거미다리 애무
;사용 캐릭터…야마메
;-------------------------------------------------
@K39_MESSAGE_COM_410
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 거미다리 애무 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「거미다리 애무 대사입니다」
ENDIF

;-------------------------------------------------
;인형 애무
;사용 캐릭터…앨리스(EX화시 불가)
;-------------------------------------------------
@K39_MESSAGE_COM_411
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 인형 애무 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「인형 애무 대사입니다」
ENDIF

;-------------------------------------------------
;꼭두각시 실
;사용 캐릭터…앨리스(EX화시 불가)
;-------------------------------------------------
@K39_MESSAGE_COM_412
;종료
IF TEQUIP:77 == 0
	;PRINTFORMW 「꼭두각시 실 해제 대사입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 꼭두각시 실 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「꼭두각시 실 대사입니다」
ENDIF

;-------------------------------------------------
;음핵 지지기
;사용 캐릭터…모코우(EX화시 불가)
;-------------------------------------------------
@K39_MESSAGE_COM_413
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 음핵 지지기 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「음핵 지지기 대사입니다」
ENDIF

;-------------------------------------------------
;유두 지지기
;사용 캐릭터…모코우(EX화시 불가)
;-------------------------------------------------
@K39_MESSAGE_COM_414
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 유두 지지기 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「유두 지지기 대사입니다」
ENDIF

;-------------------------------------------------
;독심
;사용 캐릭터…TALENT:213 = 1 (사토리. 코이시는 2이므로 불가)
;대상 불가…TALENT:213 = 2
;-------------------------------------------------
@K39_MESSAGE_COM_415
;종료
IF TEQUIP:74 == 0
	;PRINTFORMW 「독심 해제 대사입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 독심 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「독심 대사입니다」
ENDIF

;-------------------------------------------------
;사간(思姦)
;사용 불가 캐릭터…TALENT:213 = 2 (마음을 닫은 사토리)
;대상…TALENT:213 = 1 (코이시는 2이므로 불가)
;-------------------------------------------------
@K39_MESSAGE_COM_416
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 사간 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「사간 대사입니다」
ENDIF

;-------------------------------------------------
;붓 고문
;사용 캐릭터…아큐
;-------------------------------------------------
@K39_MESSAGE_COM_417
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 붓 고문 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「붓 고문 대사입니다」
ENDIF

;-------------------------------------------------
;시간정지
;그런데 보통 시간 멈춘 순간이면 말 못하지 않나?
;사용 캐릭터…사쿠야 · 카구야
;-------------------------------------------------
@K39_MESSAGE_COM_418
;종료
IF TEQUIP:71 == 0
	;PRINTFORMW 「시간정지 해제 대사입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 시간정지 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「시간정지 대사입니다」
ENDIF

;-------------------------------------------------
;광기의 눈
;사용 캐릭터…TALENT:125 (광기의 눈)
;대상 불가…TALENT:158 (광기)
;-------------------------------------------------
@K39_MESSAGE_COM_419
;종료
IF TEQUIP:73 == 0
	;PRINTFORMW 「광기의 눈 해제 대사입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 광기의 눈 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「광기의 눈 대사입니다」
ENDIF

;-------------------------------------------------
;은방울꽃 독
;사용 캐릭터…메디슨
;-------------------------------------------------
@K39_MESSAGE_COM_420
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 은방울꽃 독 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「은방울꽃 독 대사입니다」
ENDIF

;-------------------------------------------------
;곤충 채집 세트의 약
;사용 캐릭터…에이린
;-------------------------------------------------
@K39_MESSAGE_COM_421
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 곤충 채집 세트의 약 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「곤충 채집 세트의 약 대사입니다」
ENDIF

;-------------------------------------------------
;미약통 담그기
;사용 캐릭터…키스메(조교 대상이라도 가능)
;-------------------------------------------------
@K39_MESSAGE_COM_422
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 미약통 담그기 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「미약통 담그기 대사입니다」
ENDIF

;-------------------------------------------------
;술통 담그기
;사용 캐릭터…키스메(조교 대상이라도 가능)
;-------------------------------------------------
@K39_MESSAGE_COM_423
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 술통 담그기 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「술통 담그기 대사입니다」
ENDIF

;-------------------------------------------------
;계곡주
;사용 캐릭터…스이카, 유기(조교 대상이라도 가능)
;-------------------------------------------------
@K39_MESSAGE_COM_424
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 계곡주 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「계곡주 대사입니다」
ENDIF

;-------------------------------------------------
;카리쮸마 가드
;사용 캐릭터…레밀리아
;-------------------------------------------------
@K39_MESSAGE_COM_425
;첫회가 아니다
IF RESULT:1 == 0
	;PRINTFORMW 「하단 가드 대사입니다」
;その他
ELSE
	;PRINTFORMW 「첫회용 하단 가드 대사입니다」
ENDIF

;-------------------------------------------------
;음모 지지기
;사용 캐릭터…모코우(EX화시 불가)
;-------------------------------------------------
@K39_MESSAGE_COM_427
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 음모 지지기 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「음모 지지기 대사입니다」
ENDIF

;-------------------------------------------------
;수레바퀴
;사용 캐릭터…히나
;-------------------------------------------------
@K39_MESSAGE_COM_430
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 수레바퀴 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「수레바퀴 대사입니다」
ENDIF

;-------------------------------------------------
;포 오브 어 카인드
;사용 캐릭터…플랑쨩
;-------------------------------------------------
@K39_MESSAGE_COM_431
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 사신권 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「포 오브 어 카인드 대사입니다」
ENDIF

;-------------------------------------------------
;고드름 고문
;사용 캐릭터…치르노
;-------------------------------------------------
@K39_MESSAGE_COM_432
;종료
IF TEQUIP:26 == 0
	;PRINTFORMW 「고드름 고문 해제 대사입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 고드름 고문 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「고드름 고문 대사입니다」
ENDIF

;-------------------------------------------------
;애널탑
;사용 캐릭터…에이키
;-------------------------------------------------
@K39_MESSAGE_COM_433
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 애널탑 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「애널탑 대사입니다」
ENDIF

;-------------------------------------------------
;뿔 애무
;대상…TALENT:214 (오니) 또는 백택화한 TALENT:212 (반백택)
;-------------------------------------------------
@K39_MESSAGE_COM_434
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 뿔 애무 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「뿔 애무 대사입니다」
ENDIF

;-------------------------------------------------
;뿔 삽입
;대상…TALENT:214 (오니) 또는 백택화한 TALENT:212 (반백택)
;-------------------------------------------------
@K39_MESSAGE_COM_435
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 뿔 삽입 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「뿔 삽입 대사입니다」
ENDIF

;-------------------------------------------------
;뿔 애널 삽입
;대상…TALENT:214 (오니) 또는 백택화한 TALENT:212 (반백택)
;-------------------------------------------------
@K39_MESSAGE_COM_436
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 뿔 애널 삽입 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「뿔 애널 삽입 대사입니다」
ENDIF

;-------------------------------------------------
;바기나 요석
;사용 캐릭터…텐시
;-------------------------------------------------
@K39_MESSAGE_COM_437
;뽑으면 지진 발생!
IF TEQUIP:20 == 0
	;PRINTFORMW 「바기나 요석해제 대사입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 바기나 요석 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「바기나 요석 대사입니다」
ENDIF

;-------------------------------------------------
;애널 요석
;사용 캐릭터…텐시
;-------------------------------------------------
@K39_MESSAGE_COM_438
;뽑으면 지진 발생!
IF TEQUIP:25 == 0
	;PRINTFORMW 「애널 요석 해제 대사입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 애널 요석 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「애널 요석 대사입니다」
ENDIF

;-------------------------------------------------
;양구멍 요석
;사용 캐릭터…텐시
;-------------------------------------------------
@K39_MESSAGE_COM_439
;뽑으면 지진 발생!
IF TEQUIP:20 == 0 && TEQUIP:25 == 0
	;PRINTFORMW 「양구멍 요석 해제 대사입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 양구멍 요석 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「양구멍 요석 대사입니다」
ENDIF

;-------------------------------------------------
;반령을 범한다
;대상…요우무, 요우키
;조교 가능 캐릭터에 각자의 반령이 없으면 쓸 수 없음
;-------------------------------------------------
@K39_MESSAGE_COM_440
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 반령을 범한다 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「반령을 범한다 대사입니다」
ENDIF

;-------------------------------------------------
;반령을 삽입(앞)
;사용 캐릭터…요우무, 요우키
;조교 가능 캐릭터에 각자의 반령이 없으면 쓸 수 없음
;-------------------------------------------------
@K39_MESSAGE_COM_441
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 반령을 삽입(앞) 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「반령을 삽입(앞) 대사입니다」
ENDIF

;-------------------------------------------------
;반령을 삽입(후)
;사용 캐릭터…요우무, 요우키
;조교 가능 캐릭터에 각자의 반령이 없으면 쓸 수 없음
;-------------------------------------------------
@K39_MESSAGE_COM_442
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 반령을 삽입(후) 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「반령을 삽입(후) 대사입니다」
ENDIF

;-------------------------------------------------
;유명의 고륜
;대상…요우무, 요우키
;조교 가능 캐릭터에 각자의 반령이 없으면 쓸 수 없음
;-------------------------------------------------
@K39_MESSAGE_COM_443
;종료
IF TEQUIP:72 == 0
	;PRINTFORMW 「유명의 고륜 해제 대사입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 유명의 고륜 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「유명의 고륜 대사입니다」
ENDIF

;-------------------------------------------------
;반령 재구성
;사용 캐릭터…요우키
;조교 가능 캐릭터에 반령(요우키)가 없으면 쓸 수 없음
;-------------------------------------------------
@K39_MESSAGE_COM_444
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 반령 재구성 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「반령 재구성 대사입니다」
ENDIF

;-------------------------------------------------
;지식대담
;사용 캐릭터…린노스케(대상이라도 가능)
;-------------------------------------------------
@K39_MESSAGE_COM_445
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 지식대담 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「지식대담 대사입니다」
ENDIF

;-------------------------------------------------
;설교를 받는다
;사용 캐릭터…에이키(대상이라도 가능)
;-------------------------------------------------
@K39_MESSAGE_COM_446
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 설교를 받는다 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「설교를 받는다 대사입니다」
ENDIF

;-------------------------------------------------
;우산을 애무한다
;대상…코가사
;-------------------------------------------------
@K39_MESSAGE_COM_447
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 우산을 애무한다 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「우산을 애무한다 대사입니다」
ENDIF

;-------------------------------------------------
;방중술
;사용 캐릭터…메이링, 청아
;-------------------------------------------------
@K39_MESSAGE_COM_448
;종료
IF TEQUIP:79 == 0
	;PRINTFORMW 「방중술 해제 대사입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 방중술 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「방중술 대사입니다」
ENDIF

;-------------------------------------------------
;피버!
;사용 캐릭터…이쿠(대상이라도 가능)
;-------------------------------------------------
@K39_MESSAGE_COM_449
;첫회가 아니다
IF RESULT:1 == 0
	;PRINTFORMW 「피버! 대사입니다」
;その他
ELSE
	;PRINTFORMW 「첫회용 피버! 대사입니다」
ENDIF

;-------------------------------------------------
;의식 「일단 피처 두 잔으로」
;사용 캐릭터…텐지
;-------------------------------------------------
@K39_MESSAGE_COM_450
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 피처 두 잔 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「피처 두 잔 대사입니다」
ENDIF

;-------------------------------------------------
;취생몽사 「하늘을 나는 술꾼의 레무리아」
;사용 캐릭터…텐지
;-------------------------------------------------
@K39_MESSAGE_COM_451
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 취생몽사 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「취생몽사 대사입니다」
ENDIF

;-------------------------------------------------
;초응시
;사용 캐릭터…유겐마간
;-------------------------------------------------
@K39_MESSAGE_COM_452
;종료
IF TEQUIP:80 == 0
	;PRINTFORMW 「초응시 해제 대사입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 초응시 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「초응시 대사입니다」
ENDIF

;-------------------------------------------------
;우산더러 핥게 한다
;사용 캐릭터…카라카사 귀신(실질 코가사 전용)
;-------------------------------------------------
@K39_MESSAGE_COM_453
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 우산더러 핥게 한다 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「우산더러 핥게 한다 대사입니다」
ENDIF
