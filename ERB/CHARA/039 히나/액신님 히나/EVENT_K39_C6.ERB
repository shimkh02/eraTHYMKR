;≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡
;eratohoYM Mk-2 rev.6용 분할형 대사 템플릿
;39을 캐릭터 번호 치환해 사용해주세요(레이무라면 1, 렌코라면 49, 미마라면 100).
;
;이 파일에는 600～699번 커맨드(파생 커맨드)의 대사용 함수가 마련되어 있습니다.
;≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡
;==================================================
;○○ 대사
;==================================================
;==================================================
;커맨드 대사
;첫회 판정에 CFLAG:9000～9099까지의 100개를 사용한다
;첫회인지 아닌지는 RESULT:1이 1인가 아닌가로 판별
;==================================================
;-------------------------------------------------
;G스팟 자극
;-------------------------------------------------
@K39_MESSAGE_COM_600
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 G스팟 자극 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「G스팟 자극 대사입니다」
ENDIF

;-------------------------------------------------
;잠망경
;-------------------------------------------------
@K39_MESSAGE_COM_601
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 잠망경 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「잠망경 대사입니다」
ENDIF

;-------------------------------------------------
;식스나인
;-------------------------------------------------
@K39_MESSAGE_COM_602
IF CFLAG:8602== 0
	IF TALENT:150 && MARK:9 == 3
			PRINTFORMW 「…응…으응！？」
			PRINTFORMW 봉사를 계속하며 히나의 비밀스러운 곳에 혀를 집어넣자, 히나의 움직임이 갑자기 둔해졌다
			PRINTFORMW 치아가 닿는 것을 느끼고 잠시 손을 늦추기로 했다……
	ELSEIF TALENT:150
			PRINTFORMW 히나는 음핵에의 자극에 때때로 몸을 떨면서도 봉사를 계속한다
			PRINTFORMW 자극 때문인지 점점 봉사가 더뎌져갔다
	ELSEIF MARK:2 >= 1
			PRINTFORMW 「응…후…츄우…응읏！？」
			PRINTFORMW 갑작스런 자극에 히나는 놀라 봉사를 멈추고 이쪽을 돌아본다
			PRINTFORMW 계속하게 만들고, %CALLNAME:MASTER%도 히나의 애무를 계속했다
	ELSE
			PRINTFORMW 「……아……그런 짓을 하시면…할 수 없…크읏……」
			PRINTFORMW %CALLNAME:MASTER%의 음경에서 입을 떼고 히나는 이 쪽을 바라보았다 
			PRINTFORMW 개의치 않고 %조사처리(CALLNAME:MASTER,"는")% 히나를 애무했다
	ENDIF
	CFLAG:8602 = 1
ELSE
		;何度も連打で表示されるとやだ病が
		;別に下の方にたらい回しにしても問題なさそうなのでRANDでざっくりと削っちゃいます
		IF TALENT:150 == 1 && MARK:9 == 3 && CFLAG:8602 <= 4 && RAND:2 == 0
			PRINTFORML 「…으우……주인님……그렇게 하면 이 쪽이……」
			PRINTFORMW 자극에 못 견디고 히나는 호소했다
			;CFLAG:8602 = 5
		ELSEIF TALENT:150 == 1 && CFLAG:8602 <= 3 && RAND:2 == 0
			PRINTFORML 「……으읏……핫……」
			PRINTFORMW 때때로 자극에 견디지 못해 봉사를 멈추면서도 히나는 이쪽의 지시에 따른다
			;CFLAG:8602 = 4
		ELSEIF MARK:2 >= 1 && CFLAG:8602 <= 2 && RAND:2 == 0
			PRINTFORML 「…읏……응……」
			PRINTFORMW 자극을 받으면서도 히나는 혀와 입으로 봉사를 계속했다
			;CFLAG:8602 = 3
		ELSEIF MARK:2 == 0 && CFLAG:8602 <= 1 && RAND:2 == 0
			PRINTFORML 「……으우…」
			PRINTFORMW 자극에 몸을 떨며 히나는 봉사를 계속했다
			;CFLAG:8602 = 2
		ENDIF
		RETURN 0
ENDIF

;-------------------------------------------------
;６９파이즈리
;-------------------------------------------------
@K39_MESSAGE_COM_603
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 ６９파이즈리 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「６９파이즈리 대사입니다」
ENDIF

;-------------------------------------------------
;기러기목
;-------------------------------------------------
@K39_MESSAGE_COM_604
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 기러기목 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「기러기목 대사입니다」
ENDIF

;-------------------------------------------------
;손가락으로 V를 어루만진다
;-------------------------------------------------
@K39_MESSAGE_COM_605
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 손가락으로 V를 어루만진다 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「손가락으로 V를 어루만진다 대사입니다」
ENDIF

;-------------------------------------------------
;둘이서 범한다
;-------------------------------------------------
@K39_MESSAGE_COM_610
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 둘이서 범한다 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「둘이서 범한다 대사입니다」
ENDIF

;-------------------------------------------------
;이와시미즈(岩清水)
;-------------------------------------------------
@K39_MESSAGE_COM_611
IF TALENT:150 && MARK:9 == 3 && CFLAG:8611 <= 6
		PRINTFORML 「……우우」
		PRINTFORML 히나는 거울로 자신과 %조사처리(CALLNAME:MASTER,"를")% 번갈아 바라봤다
		PRINTFORMW 주어지는 쾌락을 순순히 받아들이기엔 아직 시간이 필요한 것 같다
		;CFLAG:8611 = 6
ELSEIF TALENT:150 && ABL:0 >= 3 && ABL:14 >= 3 && CFLAG:611 <= 5
		PRINTFORML 히나는 거울에 비치는 자신의 모습과 %조사처리(CALLNAME:MASTER,"를")% 번갈아 바라본다
		PRINTFORMW 주어진 쾌락에 당황한 듯 하다
		;CFLAG:8611 = 5
ELSEIF TALENT:150 && CFLAG:8611 <= 4
		PRINTFORML %조사처리(CALLNAME:MASTER,"는")% 히나의 표정을 따라했다
		PRINTFORMW 눈치채지 못할 정도로 희미했지만 히나의 표정에 변화가 생겼다
		;CFLAG:8611 = 4
ELSEIF MARK:2 == 3 && ABL:0 >= 3 && ABL:14 >= 3 && CFLAG:8611 <= 3
		PRINTFORML 「아아…그런……」
		PRINTFORMW 히나는 거울에 비치는 자신의 교태에 눈이 휘둥 그레 졌다
		;CFLAG:8611 = 3
ELSEIF MARK:2 >= 1 && CFLAG:8611 <= 2
		PRINTFORML 「아…으……」
		PRINTFORMW 히나는 수치에 뺨을 물들이며 %조사처리(CALLNAME:MASTER,"와")% 자신을 번갈아 보고 있다
		;CFLAG:8611 = 2
ELSEIF CFLAG:8611 <= 1
		PRINTFORML 「…이런 거……부끄러워…」
		PRINTFORMW 눈물을 글썽거리며, 히나는 수치를 견뎌 내고 있다
		;CFLAG:8611 = 1
ENDIF

;-------------------------------------------------
;노니는 모란(乱れ牡丹)
;-------------------------------------------------
@K39_MESSAGE_COM_612
IF CFLAG:8612== 0
	IF TALENT:150 && MARK:9 == 3
				PRINTFORMW 「……아……아아」
				PRINTFORMW 거울 너머의 자신의 모습을 확인한 히나는 얼굴을 손으로 덮어버렸다
				PRINTFORMW 그 손을 뿌리치고, %조사처리(CALLNAME:MASTER,"는")% 거울을 보라고 히나에게 명령했다
	ELSEIF TALENT:150
				PRINTFORMW 히나를 끌어안고, 천천히 %조사처리(CALLNAME:MASTER,"는")% 히나에게 자기자신을 파묻어갔다
				PRINTFORMW 거울을 보는 히나의 눈동자가 흔들리고, 수치에 몸부림쳤다
	ELSEIF MARK:2 >= 1
				PRINTFORMW 히나를 뒤에서 끌어안고, 피부를 맞대었다
				PRINTFORMW 「……아……그만……」
				PRINTFORMW 눈앞의 거울에 비치는 자신의 교태에 히나는 얼굴이 뜨거워졌다
	ELSE
				PRINTFORMW 「……거짓말이야……이런 거……」
				PRINTFORMW 거울 앞에서 히나를 끌어안고 있자 멍한 표정으로 히나가 중얼거렸다
				PRINTFORMW ……히나에게는 예상밖의 일이겠지만 이쪽은 아직 시작도 하지 않았다
	ENDIF
	CFLAG:8612 = 1
ELSE
		IF TALENT:150 == 1 && MARK:9 == 3 && CFLAG:8612 <= 4
			PRINTFORML 「……아아…주인니임……이런 건 부끄러워요……」
			PRINTFORMW 수치로 귀까지 빨개져서, 히나는 손으로 얼굴을 가렸다
			;CFLAG:8612 = 5
		ELSEIF TALENT:150 == 1 && CFLAG:8612 <= 3
			PRINTFORML 히나의 손이 얼떨결에 거울의 %CALLNAME:MASTER%에게 뻗었다
			PRINTFORML 그러나 그 손은 거울에 닿지 않았고, 만일 닿았다고 해도 그건 온기를 주지 않는다 
			PRINTFORMW %조사처리(CALLNAME:MASTER,"는")% 잡은 손을 느슨하게 하고, 히나를 껴안았다
			;CFLAG:8612 = 4
		ELSEIF MARK:2 >= 1 && CFLAG:8612 <= 2
			PRINTFORML 「……우으…싫어어……」
			PRINTFORMW 수치로 붉게 물든 피부에 신경이 쓰여서 %조사처리(CALLNAME:MASTER,"는")% 더욱 세차게 히나를 애무했다
			;CFLAG:8612 = 3
		ELSEIF MARK:2 == 0 && CFLAG:8612 <= 1
			PRINTFORML 「……또……이런 걸」
			PRINTFORMW 히나는 눈물 지으며 화난듯이 거울을 노려보았다……
			;CFLAG:8612 = 2
		ENDIF
		RETURN 0
ENDIF

;-------------------------------------------------
;기승 유방 자위
;-------------------------------------------------
@K39_MESSAGE_COM_613
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 기승 유방 자위 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「기승 유방 자위 대사입니다」
ENDIF

;-------------------------------------------------
;삽입 G스팟 자극
;-------------------------------------------------
@K39_MESSAGE_COM_614
	IF TALENT:150 && MARK:9 == 3
			PRINTFORML 「…우…아？……히야앗」
			PRINTFORML %CALLNAME:MASTER%의 어깨를 잡고 히나의 허리가 크게 젖혀졌다
			PRINTFORMW 그저 애무를 계속하자 히나는 부들부들거리다 이따끔 크게 경련했다
			;뭔가 여기 일본어에 위화감을 느끼다……자신의 어학 실력에서는 원인 불명이지만(땀
	ELSEIF TALENT:150
			PRINTFORML 「히으…읏……」
			PRINTFORMW 작은 비명과 함께 %CALLNAME:MASTER%의 어깨에 히나의 손톱자국이 새겨졌다
	ELSEIF MARK:2 >= 1
			PRINTFORML 「…아……아……앗」
			PRINTFORMW 거칠게 움직이며 질내의 천장을 문질러대자 움찔움찔하며 히나는 경련을 반복했다
	ELSE
			PRINTFORML 「…아……잠깐……그만둬어」
			PRINTFORMW 싫다면서 도리질치던 히나는 %CALLNAME:MASTER%의 손가락에 흐트러지기 시작했다
	ENDIF

;-------------------------------------------------
;삽입 자궁구 자극
;-------------------------------------------------
@K39_MESSAGE_COM_615
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 삽입 자궁구 자극 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「삽입 자궁구 자극 대사입니다」
ENDIF

;-------------------------------------------------
;후배위 애무
;-------------------------------------------------
@K39_MESSAGE_COM_616
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 후배위 애무 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「후배위 애무 대사입니다」
ENDIF

;-------------------------------------------------
;대면 애무
;-------------------------------------------------
@K39_MESSAGE_COM_617
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 대면 애무 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「대면 애무 대사입니다」
ENDIF

;-------------------------------------------------
;후배위 애널 가슴 애무
;-------------------------------------------------
@K39_MESSAGE_COM_618
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 후배위 애널 가슴 애무 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「후배위 애널 가슴 애무 대사입니다」
ENDIF

;-------------------------------------------------
;대면 애널 가슴 애무
;-------------------------------------------------
@K39_MESSAGE_COM_619
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 대면 애널 가슴 애무 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「대면 애널 가슴 애무 대사입니다」
ENDIF

;-------------------------------------------------
;강제방뇨
;-------------------------------------------------
@K39_MESSAGE_COM_620
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 강제방뇨 대사입니다」
;2회째 이후이고 그 외
ELSE
	;PRINTFORMW 「강제방뇨 대사입니다」
ENDIF

;-------------------------------------------------
;입으로 먹이기
;-------------------------------------------------
@K39_MESSAGE_COM_621
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 입으로 먹이기 대사입니다」
;2회째 이후이고 그 외
ELSE
	;PRINTFORMW 「입으로 먹이기 대사입니다」
ENDIF

;-------------------------------------------------
;사지긴박
;-------------------------------------------------
@K39_MESSAGE_COM_622
;떼어냈을 때
IF TEQUIP:40 == 0
	;PRINTFORMW 「사지긴박 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 사지긴박 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「사지긴박 대사입니다」
ENDIF

;-------------------------------------------------
;귀갑 묶기
;-------------------------------------------------
@K39_MESSAGE_COM_623
;떼어냈을 때
IF TEQUIP:40 == 0
	;PRINTFORMW 「귀갑 묶기 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 귀갑 묶기 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「귀갑 묶기 대사입니다」
ENDIF

;-------------------------------------------------
;매달기
;-------------------------------------------------
@K39_MESSAGE_COM_624
;떼어냈을 때
IF TEQUIP:40 == 0
	;PRINTFORMW 「매달기 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 매달기 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「매달기 대사입니다」
ENDIF

;-------------------------------------------------
;로션 플레이
;-------------------------------------------------
@K39_MESSAGE_COM_625
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 로션 플레이 대사입니다」
;2회째 이후이고 그 외
ELSE
	;PRINTFORMW 「로션 플레이 대사입니다」
ENDIF

;-------------------------------------------------
;착유
;-------------------------------------------------
@K39_MESSAGE_COM_626
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 착유 대사입니다」
;2회째 이후이고 그 외
ELSE
	;PRINTFORMW 「착유 대사입니다」
ENDIF

;-------------------------------------------------
;유두 깨물기
;-------------------------------------------------
@K39_MESSAGE_COM_627
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 유두 깨물기 대사입니다」
;2회째 이후이고 그 외
ELSE
	;PRINTFORMW 「유두 깨물기 대사입니다」
ENDIF

;-------------------------------------------------
;상호자위
;-------------------------------------------------
@K39_MESSAGE_COM_628
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 상호자위 대사입니다」
;2회째 이후이고 그 외
ELSE
	;PRINTFORMW 「상호자위 대사입니다」
ENDIF

;-------------------------------------------------
;샌드위치
;-------------------------------------------------
@K39_MESSAGE_COM_629
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 샌드위치 대사입니다」
;2회째 이후이고 그 외
ELSE
	;PRINTFORMW 「샌드위치 대사입니다」
ENDIF

;-------------------------------------------------
;후배위 수음
;-------------------------------------------------
@K39_MESSAGE_COM_630
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 후배위 수음 대사입니다」
;2회째 이후이고 그 외
ELSE
	;PRINTFORMW 「후배위 수음 대사입니다」
ENDIF

;-------------------------------------------------
;상호 보지 벌리기
;-------------------------------------------------
@K39_MESSAGE_COM_631
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 상호 보지 벌리기 대사입니다」
;2회째 이후이고 그 외
ELSE
	;PRINTFORMW 「상호 보지 벌리기 대사입니다」
ENDIF

;-------------------------------------------------
;술 먹이기(イきつけ三杯)
;-------------------------------------------------
@K39_MESSAGE_COM_649
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 술 먹이기 대사입니다」
;2회째 이후이고 그 외
ELSE
	;PRINTFORMW 「술 먹이기 대사입니다」
ENDIF

;-------------------------------------------------
;정파리의 거울
;-------------------------------------------------
@K39_MESSAGE_COM_650
;떼어냈을 때
IF TEQUIP:56 == 0
	;PRINTFORMW 「정파리의 거울 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 정파리의 거울 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「정파리의 거울 대사입니다」
ENDIF

;-------------------------------------------------
;수레바퀴(역강간)
;-------------------------------------------------
@K39_MESSAGE_COM_651
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 수레바퀴(역강간) 대사입니다」
;2회째 이후이고 그 외
ELSE
	;PRINTFORMW 「수레바퀴(역강간) 대사입니다」
ENDIF

;-------------------------------------------------
;포 오브 어 카인드(후타나리)
;-------------------------------------------------
@K39_MESSAGE_COM_652
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 포 오브 어 카인드(후타나리) 대사입니다」
;2회째 이후이고 그 외
ELSE
	;PRINTFORMW 「포 오브 어 카인드(후타나리) 대사입니다」
ENDIF

;-------------------------------------------------
;거미줄
;-------------------------------------------------
@K39_MESSAGE_COM_653
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 거미줄 대사입니다」
;2회째 이후이고 그 외
ELSE
	;PRINTFORMW 「거미줄 대사입니다」
ENDIF

;-------------------------------------------------
;거미다리 6점 자극
;-------------------------------------------------
@K39_MESSAGE_COM_654
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 거미다리 6점 자극 대사입니다」
;2회째 이후이고 그 외
ELSE
	;PRINTFORMW 「거미다리 6점 자극 대사입니다」
ENDIF

;-------------------------------------------------
;거미줄 섹스
;-------------------------------------------------
@K39_MESSAGE_COM_655
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 거미줄 섹스 대사입니다」
;2회째 이후이고 그 외
ELSE
	;PRINTFORMW 「거미줄 섹스 대사입니다」
ENDIF

;-------------------------------------------------
;거미줄 애널 섹스
;-------------------------------------------------
@K39_MESSAGE_COM_656
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 거미줄 애널 섹스 대사입니다」
;2회째 이후이고 그 외
ELSE
	;PRINTFORMW 「거미줄 애널 섹스 대사입니다」
ENDIF

;-------------------------------------------------
;도롱이벌레 플레이
;-------------------------------------------------
@K39_MESSAGE_COM_657
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 도롱이벌레 플레이 대사입니다」
;2회째 이후이고 그 외
ELSE
	;PRINTFORMW 「도롱이벌레 플레이 대사입니다」
ENDIF

;-------------------------------------------------
;구속∇
;-------------------------------------------------
@K39_MESSAGE_COM_658
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 구속∇ 대사입니다」
;2회째 이후이고 그 외
ELSE
	;PRINTFORMW 「구속∇ 대사입니다」
ENDIF
