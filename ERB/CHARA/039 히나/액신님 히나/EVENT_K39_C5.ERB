;≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡
;eratohoYM Mk-2 rev.6용 분할형 대사 템플릿
;39을 캐릭터 번호 치환해 사용해주세요(레이무라면 1, 렌코라면 49, 미마라면 100).
;
;이 파일에는 500～599번 커맨드(촉수 커맨드)의 대사용 함수가 마련되어 있습니다.
;≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡
;==================================================
;○○ 대사
;==================================================
;==================================================
;커맨드 대사
;첫회 판정에 CFLAG:9000～9099까지의 100개를 사용한다
;첫회인지 아닌지는 RESULT:1이 1인가 아닌가로 판별
;==================================================
;-------------------------------------------------
;촉수소환＆촉수송환
;-------------------------------------------------
@K39_MESSAGE_COM_500
;촉수송환(거둘 때)
IF TEQUIP:90 == 0
	;PRINTFORMW 「촉수송환(해제) 대사입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수소환 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수소환 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 삽입
;-------------------------------------------------
@K39_MESSAGE_COM_501
;떼어냈을 때
IF TEQUIP:20 == 0
	;PRINTFORMW 「촉수 삽입 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 삽입 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 삽입 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 애널 삽입
;-------------------------------------------------
@K39_MESSAGE_COM_502
;떼어냈을 때
IF TEQUIP:25 == 0
	;PRINTFORMW 「촉수 애널 삽입 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 애널 삽입 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 애널 삽입 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 클리 고문
;-------------------------------------------------
@K39_MESSAGE_COM_503
;떼어냈을 때
IF TEQUIP:30 == 0
	;PRINTFORMW 「촉수 클리 고문 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 클리 고문 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 클리 고문 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 유두 자극
;-------------------------------------------------
@K39_MESSAGE_COM_504
;떼어냈을 때
IF TEQUIP:35 == 0
	;PRINTFORMW 「촉수 유두 자극 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 유두 자극 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 유두 자극 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 페니스 고문
;-------------------------------------------------
@K39_MESSAGE_COM_505
;떼어냈을 때
IF TEQUIP:30 == 0
	;PRINTFORMW 「촉수 페니스 고문 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 페니스 고문 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 페니스 고문 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 펠라
;-------------------------------------------------
@K39_MESSAGE_COM_506
;떼어냈을 때
IF TEQUIP:42 == 0
	;PRINTFORMW 「촉수 펠라 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 펠라 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 펠라 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 긴박
;-------------------------------------------------
@K39_MESSAGE_COM_507
;떼어냈을 때
IF TEQUIP:40 == 0
	;PRINTFORMW 「촉수 긴박 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 긴박 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 긴박 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 콧구멍 삽입
;-------------------------------------------------
@K39_MESSAGE_COM_508
;떼어냈을 때
IF TEQUIP:44 == 0
	;PRINTFORMW 「촉수 콧구멍 삽입 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 콧구멍 삽입 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 콧구멍 삽입 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 메이든
;-------------------------------------------------
@K39_MESSAGE_COM_509
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 메이든 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 메이든 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 기생
;-------------------------------------------------
@K39_MESSAGE_COM_510
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 기생 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 기생 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 간지르기
;-------------------------------------------------
@K39_MESSAGE_COM_511
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 간지르기 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 간지르기 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 보지 벌리기
;-------------------------------------------------
@K39_MESSAGE_COM_512
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 보지 벌리기 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 보지 벌리기 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 파이즈리
;-------------------------------------------------
@K39_MESSAGE_COM_513
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 파이즈리 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 파이즈리 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 스마타
;-------------------------------------------------
@K39_MESSAGE_COM_514
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 스마타 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 스마타 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 방전 페니스 고문
;-------------------------------------------------
@K39_MESSAGE_COM_515
;떼어냈을 때
IF TEQUIP:30 == 0
	;PRINTFORMW 「촉수 방전 페니스 고문 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 방전 페니스 고문 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 방전 페니스 고문 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 G스팟 자극
;-------------------------------------------------
@K39_MESSAGE_COM_520
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 G스팟 자극 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 G스팟 자극 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 자궁 자극
;-------------------------------------------------
@K39_MESSAGE_COM_521
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 자궁 자극 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 자궁 자극 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 관장
;-------------------------------------------------
@K39_MESSAGE_COM_522
;뗄 때
IF TEQUIP:25 == 0
	;PRINTFORMW 「촉수 관장 해제 대사입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 관장 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 관장 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 소장 고문
;-------------------------------------------------
@K39_MESSAGE_COM_523
;뗄 때
IF TEQUIP:25 == 0
	;PRINTFORMW 「촉수 소장 고문 해제 대사입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 소장 고문 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 소장 고문 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 관통
;-------------------------------------------------
@K39_MESSAGE_COM_524
;뗄 때
IF TEQUIP:25 == 0
	;PRINTFORMW 「촉수 관통 해제 대사입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 관통 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 관통 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 착유
;-------------------------------------------------
@K39_MESSAGE_COM_525
;뗄 때
IF TEQUIP:35 == 0
	;PRINTFORMW 「촉수 착유 해제 대사입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 착유 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 착유 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 유선 삽입
;-------------------------------------------------
@K39_MESSAGE_COM_526
;뗄 때
IF TEQUIP:35 == 0
	;PRINTFORMW 「촉수 유선 삽입 해제 대사입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 유선 삽입 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 유선 삽입 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 카테터
;-------------------------------------------------
@K39_MESSAGE_COM_527
;뗄 때
IF TEQUIP:32 == 0
	;PRINTFORMW 「촉수 카테터 해제 대사입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 카테터 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 카테터 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 채뇨
;-------------------------------------------------
@K39_MESSAGE_COM_528
;뗄 때
IF TEQUIP:32 == 0
	;PRINTFORMW 「촉수 채뇨 해제 대사입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 채뇨 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 채뇨 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 귓구멍 삽입
;-------------------------------------------------
@K39_MESSAGE_COM_529
;뗄 때
IF TEQUIP:45 == 0
	;PRINTFORMW 「촉수 귓구멍 삽입 해제 대사입니다」
;첫회만
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 귓구멍 삽입 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 귓구멍 삽입 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 바기나 확장
;-------------------------------------------------
@K39_MESSAGE_COM_530
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 바기나 확장 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 바기나 확장 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 애널 확장
;-------------------------------------------------
@K39_MESSAGE_COM_531
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 애널 확장 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 애널 확장 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 양구멍 확장
;-------------------------------------------------
@K39_MESSAGE_COM_532
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 양구멍 확장 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 양구멍 확장 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 산란
;-------------------------------------------------
@K39_MESSAGE_COM_533
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 산란 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 산란 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 애널 산란
;-------------------------------------------------
@K39_MESSAGE_COM_534
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 애널 산란 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 애널 산란 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 애액 채집
;-------------------------------------------------
@K39_MESSAGE_COM_535
;뗄 때
IF TEQUIP:20 == 0
	;PRINTFORMW 「촉수 애액 채집 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 애액 채집 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 애액 채집 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 입 안 세정
;-------------------------------------------------
@K39_MESSAGE_COM_540
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 입 안 세정 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 입 안 세정 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 질내 세정
;-------------------------------------------------
@K39_MESSAGE_COM_541
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 질내 세정 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 질내 세정 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 장내 세정
;-------------------------------------------------
@K39_MESSAGE_COM_542
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 장내 세정 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 장내 세정 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 방광 세정
;-------------------------------------------------
@K39_MESSAGE_COM_543
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 방광 세정 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 방광 세정 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 비강 세정
;-------------------------------------------------
@K39_MESSAGE_COM_544
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 비강 세정 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 비강 세정 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 귓구멍 세정
;-------------------------------------------------
@K39_MESSAGE_COM_545
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 귓구멍 세정 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 귓구멍 세정 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 전신 세정
;-------------------------------------------------
@K39_MESSAGE_COM_546
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 전신 세정 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 전신 세정 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 최면
;-------------------------------------------------
@K39_MESSAGE_COM_550
;종료
IF TEQUIP:92 == 0
	;PRINTFORMW 「촉수 최면 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 최면 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 최면 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 채찍질
;-------------------------------------------------
@K39_MESSAGE_COM_551
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 채찍질 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 채찍질 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 바늘 고문
;-------------------------------------------------
@K39_MESSAGE_COM_552
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 바늘 고문 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 바늘 고문 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 방전 
;-------------------------------------------------
@K39_MESSAGE_COM_553
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 방전 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 방전 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 흡혈
;-------------------------------------------------
@K39_MESSAGE_COM_554
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 흡혈 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 흡혈 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 강제 회복
;-------------------------------------------------
@K39_MESSAGE_COM_560
;종료
IF TEQUIP:91 == 0
	;PRINTFORMW 「촉수 강제 회복 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 강제 회복 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 강제 회복 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 배란유발
;-------------------------------------------------
@K39_MESSAGE_COM_561
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 배란유발 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 배란유발 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 피임막 도포
;-------------------------------------------------
@K39_MESSAGE_COM_562
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 피임막 도포 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 피임막 도포 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 발모제
;-------------------------------------------------
@K39_MESSAGE_COM_563
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 발모제 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 발모제 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 탈모제
;-------------------------------------------------
@K39_MESSAGE_COM_564
;첫회만
IF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 탈모제 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 탈모제 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 독심 고문
;-------------------------------------------------
@K39_MESSAGE_COM_580
;종료
IF TEQUIP:74 == 0
	;PRINTFORMW 「촉수 독심 고문 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 독심 고문 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 독심 고문 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 전기 충격
;-------------------------------------------------
@K39_MESSAGE_COM_581
;종료
IF (TEQUIP:95 & 1) == 0
	;PRINTFORMW 「촉수 전기 충격 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 전기 충격 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 전기 충격 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 유령 질경
;-------------------------------------------------
@K39_MESSAGE_COM_582
;종료
IF TEQUIP:21 == 0
	;PRINTFORMW 「촉수 유령 질경 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 유령 질경 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 유령 질경 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 강제 음주
;-------------------------------------------------
@K39_MESSAGE_COM_583
;종료
IF TEQUIP:42 == 0
	;PRINTFORMW 「촉수 강제 음주 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 강제 음주 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 강제 음주 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 정신제어
;-------------------------------------------------
@K39_MESSAGE_COM_584
;종료
IF TEQUIP:77 == 0
	;PRINTFORMW 「촉수 정신제어 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 정신제어 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 정신제어 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 액독 뿜기
;-------------------------------------------------
@K39_MESSAGE_COM_585
;종료
IF (TEQUIP:95 & 2) == 0
	;PRINTFORMW 「촉수 액독 뿜기 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 액독 뿜기 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 액독 뿜기 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 장내 촬영
;-------------------------------------------------
@K39_MESSAGE_COM_586
;종료
IF TEQUIP:25 == 0
	;PRINTFORMW 「촉수 장내 촬영 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 장내 촬영 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 장내 촬영 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 언어 고문
;-------------------------------------------------
@K39_MESSAGE_COM_587
;종료
IF (TEQUIP:95 & 4) == 0
	;PRINTFORMW 「촉수 언어 고문 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 언어 고문 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 언어 고문 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 은방울꽃 미약
;-------------------------------------------------
@K39_MESSAGE_COM_588
;종료
IF (TEQUIP:95 & 8) == 0
	;PRINTFORMW 「촉수 은방울꽃 미약 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 은방울꽃 미약 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 은방울꽃 미약 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 관능도서
;-------------------------------------------------
@K39_MESSAGE_COM_589
;종료
IF (TEQUIP:95 & 16) == 0
	;PRINTFORMW 「촉수 관능도서 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 관능도서 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 관능도서 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 요정안마
;-------------------------------------------------
@K39_MESSAGE_COM_590
;종료
IF TEQUIP:30 == 0
	;PRINTFORMW 「촉수 요정안마 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 요정안마 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 요정안마 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 빙정 수음
;-------------------------------------------------
@K39_MESSAGE_COM_591
;종료
IF TEQUIP:30 == 0
	;PRINTFORMW 「촉수 빙정 수음 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 빙정 수음 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 빙정 수음 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 음맥 탐사
;-------------------------------------------------
@K39_MESSAGE_COM_592
;종료
IF (TEQUIP:95 & 32) == 0
	;PRINTFORMW 「촉수 음맥 탐사 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 음맥 탐사 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 음맥 탐사 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 최종심판 탐사
;-------------------------------------------------
@K39_MESSAGE_COM_593
;종료
IF (TEQUIP:95 & 64) == 0
	;PRINTFORMW 「촉수 최종심판 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 최종심판 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 최종심판 대사입니다」
ENDIF

;-------------------------------------------------
;촉수 백렬단풍
;-------------------------------------------------
@K39_MESSAGE_COM_594
;종료
IF (TEQUIP:95 & 128) == 0
	;PRINTFORMW 「촉수 백렬단풍 해제 대사입니다」
;첫회 때
ELSEIF RESULT:1 == 1
	;PRINTFORMW 「첫회용 촉수 백렬단풍 대사입니다」
;2회째 이후
ELSE
	;PRINTFORMW 「촉수 백렬단풍 대사입니다」
ENDIF

