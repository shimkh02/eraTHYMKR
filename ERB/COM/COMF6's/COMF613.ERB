;=============================================================================
;기승 유방 자위
;=============================================================================
;………………………………………………
;실행 판정
;………………………………………………
;더미 함수(커맨드를 직접 선택할 수는 없음)
@COM_ABLE613
RETURN 0

@EXCOM_ABLE613
;Ｂ계 확장 커맨드를 유효로 하지 않으면 불가
SIF (FLAG:15 & 8) == 0
	RETURN 0
;Ｖ조교 필터가 오프되어 있지 않으면 불가
SIF FLAG:2 & 1
	RETURN 0
;Ｖ경험이 低い場合, 公開自慰を行ったことがない場合のビデオ撮影中は, 
;이상계 조교 필터가 오프되어 있지 않으면 불가
SIF FLAG:2 & 4 && EXP:0 < EXPLV:1 && (CFLAG:21 & 1) == 0 && TEQUIP:50
	RETURN 0
;조교자에게 페니스가 없으면 불가
SIF EXIST_BAR(PLAYER) == 0
	RETURN 0
;남자라면 안 됨
SIF TALENT:120
	RETURN 0
;주인이 [금단의 지식]持ちじゃないとき, 体格差が2段階以内か조교자가 [금단의 지식]持ちじゃないとダメ
SIF TALENT:MASTER:56 == 0 && COMMON_BODYSIZE_CHECK(PLAYER, TARGET) == 0
	RETURN 0
;처녀で, 순종4미만 · 레즈끼4미만의 ASSI일 경우, 조교 대상이 동성이라면 실행할 수 없다(소악마, 또는 망신이나 복종이 있다면 ＯＫ)
IF TALENT:0 == 1 && ASSIPLAY
	SIF (ABL:ASSI:10 < 4 || (TALENT:120 == 0 && TALENT:ASSI:120 == 0 && ABL:ASSI:22 < 4)) && TALENT:ASSI:93 == 0 && TALENT:ASSI:151 == 0 && TALENT:ASSI:160 == 0
		RETURN 0
ENDIF
;수면 중에는 불가
SIF EQUIP:14
	RETURN 0
;Ｖ계 장착구 사용 중에는 불가
SIF TEQUIP:20 || TEQUIP:21
	RETURN 0
;밧줄 사용 중에는 불가
SIF TEQUIP:40
	RETURN 0
;승마 중에는 불가
SIF TEQUIP:43
	RETURN 0
;욕실 플레이 중에는 플레이 매트가 없으면 불가
SIF TEQUIP:53 && ITEM:42 == 0 && NOITEM == 0
	RETURN 0
;샤워 중에는 불가
SIF TEQUIP:54
	RETURN 0
;○○목욕탕 입욕 중에는 불가
SIF TEQUIP:55
	RETURN 0
;기저귀 플레이 중에는 불가
SIF TEQUIP:58
	RETURN 0
;조수가 훈도시 착용 중일 경우 기교4이상 필수
SIF ASSI >= 0 && ASSIPLAY && TEQUIP:ASSI:59 == 17 && ABL:12 < 4
	RETURN 0
;훈도시 착용 중일 경우 조교자의 기교4이상 필수
SIF TEQUIP:59 == 17 && ABL:PLAYER:12 < 4
	RETURN 0
;ウェディングドレス · ゴスロリ着用中の場合기교4이상 필수
SIF (TEQUIP:59 == 19 || TEQUIP:59 == 20) && ABL:PLAYER:12 < 4
	RETURN 0
;나체정식 중에는 불가
SIF TEQUIP:60
	RETURN 0
;시간정지 중에는 무리
SIF TEQUIP:71
	RETURN 0
;촉수 조교 중에는 불가
SIF TEQUIP:90
	RETURN 0
RETURN 1

;--------------------------------------------------
;커맨드 본체
;--------------------------------------------------
;직전의 조교가 삽입으로 뽑지 않는 플래그가 온의 경우에 가슴 자위로부터 파생.
;비디오 촬영, 착유기포함의 때에 내용 변화. 이름은 변화 없음.
@COM613
PRINTL 기승 유방 자위

;-------------------------------------------------
;실행할 수 있을까의 판정
;-------------------------------------------------
IF TFLAG:699 == 0
	CALL COM_ORDER
	;실행할 수 없다
	SIF RESULT == 0
		RETURN 0
ENDIF

;-------------------------------------------------
;실행 결정
;-------------------------------------------------
TSTR:0 = 기승 유방 자위
SELECTCOM =613
CALL TRAIN_MESSAGE_COM613

;V경험을 수반하는 커맨드의 플래그(처녀막 재생 대응)
TFLAG:1 = 1
SIF TALENT:0 && TALENT:150 && ASSIPLAY == 0
	TFLAG:2 = TALENT:0

;-------------------------------------------------
;사정 게이지 체크
;-------------------------------------------------
LOCAL = 0

;ABL:기교를 본다
IF ABL:12 == 0
	LOCAL = 1000
ELSEIF ABL:12 == 1
	LOCAL = 1300
ELSEIF ABL:12 == 2
	LOCAL = 1700
ELSEIF ABL:12 == 3
	LOCAL = 2200
ELSEIF ABL:12 == 4
	LOCAL = 3000
ELSE
	LOCAL = 4500
ENDIF

;윤활을 본다
LOCAL = EJACULATIONCHECK_PALAM_10_3(TARGET, LOCAL)
;욕정을 본다
LOCAL = EJACULATIONCHECK_PALAM_13_1(TARGET, LOCAL)
;순종을 본다
LOCAL = EJACULATIONCHECK_ABL_10_1(TARGET, LOCAL)
;욕망을 본다
LOCAL = EJACULATIONCHECK_ABL_11_1(TARGET, LOCAL)
;봉사 정신을 본다
LOCAL = EJACULATIONCHECK_ABL_13_2(TARGET, LOCAL)

;조교자의 C감각을 본다
LOCAL = EJACULATIONCHECK_ABL_0_1(PLAYER, LOCAL)

;EXP:V경험을 본다
;V경험이 많아지면(자) 조금 내린다
IF EXP:0 < EXPLV:1
	TIMES LOCAL , 1.50
	IF ASSIPLAY && TALENT:PLAYER:120 == 0
		TCVAR:50 += 2
	ELSE
		TCVAR:50 += 1
	ENDIF
ELSEIF EXP:0 < EXPLV:2
	TIMES LOCAL , 1.00
ELSEIF EXP:0 < EXPLV:3
	TIMES LOCAL , 0.90
ELSEIF EXP:0 < EXPLV:4
	TIMES LOCAL , 0.80
ELSEIF EXP:0 < EXPLV:5
	TIMES LOCAL , 0.70
ELSE
	TIMES LOCAL , 0.60
ENDIF

SIF TALENT:PLAYER:120 || TALENT:PLAYER:121
	BASE:PLAYER:2 += LOCAL

;-------------------------------------------------
;실행 결정
;-------------------------------------------------
DOWNBASE:0 += 120
DOWNBASE:1 += 120

SOURCE:12 = 900
SOURCE:31 = 400
LOCAL:1 = 0

;촬영중
IF TEQUIP:50
	SOURCE:19 = 50
	SOURCE:18 = 100
ENDIF

;위 쪽으로 계산한 더러운 데이터
SOURCE:30 = TFLAG:242*10 + 60

;ABL:봉사 정신을 본다
IF ABL:13 == 0
	SOURCE:15 = 200
	SOURCE:16 = 50
	SOURCE:31 += 300
	TIMES SOURCE:30 , 4.00
ELSEIF ABL:13 == 1
	SOURCE:15 = 250
	SOURCE:16 = 200
	SOURCE:31 += 100
	TIMES SOURCE:30 , 2.50
ELSEIF ABL:13 == 2
	SOURCE:15 = 350
	SOURCE:16 = 550
	SOURCE:31 += 30
	TIMES SOURCE:30 , 1.50
ELSEIF ABL:13 == 3
	SOURCE:15 = 450
	SOURCE:16 = 900
	SOURCE:31 += 0
	TIMES SOURCE:30 , 1.00
ELSEIF ABL:13 == 4
	SOURCE:15 = 600
	SOURCE:16 = 1500
	SOURCE:31 += 0
	TIMES SOURCE:30 , 0.50
ELSE
	SOURCE:15 = 750
	SOURCE:16 = 2200
	SOURCE:31 = 0
	TIMES SOURCE:30 , 0.10
ENDIF

;ABL:V감각을 본다
IF ABL:1 == 0
	SOURCE:1 = 20
	SOURCE:11 = 25
	TIMES SOURCE:15 , 0.50
ELSEIF ABL:1 == 1
	SOURCE:1 = 75
	SOURCE:11 = 75
	TIMES SOURCE:15 , 0.80
ELSEIF ABL:1 == 2
	SOURCE:1 = 200
	SOURCE:11 = 125
	TIMES SOURCE:15 , 1.00
ELSEIF ABL:1 == 3
	SOURCE:1 = 500
	SOURCE:11 = 175
	TIMES SOURCE:15 , 1.20
ELSEIF ABL:1 == 4
	SOURCE:1 = 850
	SOURCE:11 = 300
	TIMES SOURCE:15 , 1.50
ELSE
	SOURCE:1 = 1100
	SOURCE:11 = 425
	TIMES SOURCE:15 , 2.00
ENDIF

;EXP:V경험을 본다
IF EXP:0 < EXPLV:1
	TIMES SOURCE:1 , 0.20
	SOURCE:20 = 5000
	SOURCE:12 = 12000
	SOURCE:31 = 120000
ELSEIF EXP:0 < EXPLV:2
	TIMES SOURCE:1 , 0.60
	SOURCE:20 = 220
ELSEIF EXP:0 < EXPLV:3
	TIMES SOURCE:1 , 1.00
	SOURCE:20 = 30
ELSEIF EXP:0 < EXPLV:4
	TIMES SOURCE:1 , 1.20
	SOURCE:20 = 5
ELSEIF EXP:0 < EXPLV:5
	TIMES SOURCE:1 , 1.40
ELSE
	TIMES SOURCE:1 , 1.50
ENDIF

;PALAM:윤활
IF PALAM:10 < PALAMLV:1
	TIMES SOURCE:1 , 0.20
	SOURCE:20 += 900
	TIMES SOURCE:20 , 3.00
	SOURCE:12 += 2000
ELSEIF PALAM:10 < PALAMLV:2
	TIMES SOURCE:1 , 0.60
	SOURCE:20 += 250
	TIMES SOURCE:20 , 1.00
	SOURCE:12 += 400
ELSEIF PALAM:10 < PALAMLV:3
	TIMES SOURCE:1 , 1.00
	TIMES SOURCE:20 , 0.50
ELSEIF PALAM:10 < PALAMLV:4
	TIMES SOURCE:1 , 1.30
	TIMES SOURCE:20 , 0.30
ELSEIF PALAM:10 < PALAMLV:5
	TIMES SOURCE:1 , 1.60
	TIMES SOURCE:20 , 0.20
ELSE
	TIMES SOURCE:1 , 2.00
	TIMES SOURCE:20 , 0.10
ENDIF

;조교자가[남자]
SIF TALENT:PLAYER:120
	TIMES SOURCE:1 , 2.50

;작은 몸집 체형
SIF TALENT:128
	TIMES SOURCE:20 , 2.00
;정조관념
IF TALENT:30
	IF TALENT:0 == 1
		TIMES SOURCE:11 , 0.60
		TIMES SOURCE:31, 15.00
		SOURCE:32 = 10000
	ELSE
		TIMES SOURCE:11 , 0.60
		TIMES SOURCE:31, 15.00
		SOURCE:32 = 1000
	ENDIF
;정조무구애
ELSEIF TALENT:31
	IF TALENT:0 == 1
		TIMES SOURCE:31, 0.50
		SOURCE:32 = 300
	ELSE
		TIMES SOURCE:31, 0.50
	ENDIF
ELSE
	IF TALENT:0 == 1
		SOURCE:32 = 3000
	ENDIF
ENDIF

;PALAM:욕정
IF PALAM:13 < PALAMLV:1
	TIMES SOURCE:1 , 0.60
	TIMES SOURCE:11 , 0.30
ELSEIF PALAM:13 < PALAMLV:2
	TIMES SOURCE:1 , 0.80
	TIMES SOURCE:11 , 0.60
ELSEIF PALAM:13 < PALAMLV:3
	TIMES SOURCE:1 , 1.00
	TIMES SOURCE:11 , 1.00
ELSEIF PALAM:13 < PALAMLV:4
	TIMES SOURCE:1 , 1.20
	TIMES SOURCE:11 , 1.50
ELSEIF PALAM:13 < PALAMLV:5
	TIMES SOURCE:1 , 1.40
	TIMES SOURCE:11 , 1.80
ELSE
	TIMES SOURCE:1 , 1.60
	TIMES SOURCE:11 , 2.20
ENDIF

;ABL:순종을 본다
IF ABL:10 == 0
	TIMES SOURCE:1 , 0.50
	TIMES SOURCE:11 , 0.60
	TIMES SOURCE:32 , 2.00
	SOURCE:22 += 700
ELSEIF ABL:10 == 1
	TIMES SOURCE:1 , 0.80
	TIMES SOURCE:11 , 0.80
	TIMES SOURCE:32 , 1.50
	SOURCE:22 += 500
ELSEIF ABL:10 == 2
	TIMES SOURCE:1 , 1.00
	TIMES SOURCE:11 , 1.00
	TIMES SOURCE:32 , 1.00
	SOURCE:22 += 300
ELSEIF ABL:10 == 3
	TIMES SOURCE:1 , 1.30
	TIMES SOURCE:11 , 1.20
	TIMES SOURCE:32 , 0.80
	SOURCE:22 += 100
ELSEIF ABL:10 == 4
	TIMES SOURCE:1 , 1.60
	TIMES SOURCE:11 , 1.40
	TIMES SOURCE:32 , 0.60
	SOURCE:22 += 30
ELSE
	TIMES SOURCE:1 , 2.00
	TIMES SOURCE:11 , 1.60
	TIMES SOURCE:32 , 0.30
	SOURCE:22 += 0
ENDIF

;ABL:B감각을 본다
IF ABL:3 == 0
	SOURCE:3 = 15
ELSEIF ABL:3 == 1
	SOURCE:3 = 75
ELSEIF ABL:3 == 2
	SOURCE:3 = 375
ELSEIF ABL:3 == 3
	SOURCE:3 = 1100
ELSEIF ABL:3 == 4
	SOURCE:3 = 1800
ELSE
	SOURCE:3 = 2600
ENDIF

;착유기 장착중을 확인
IF TEQUIP:35 == 2
	LOCAL:1 = 1000
	;EXP:분유경험을 본다
	IF EXP:5 <= EXPLV:0
		TIMES LOCAL:1, 1.00
	ELSEIF EXP:5 <= EXPLV:1
		TIMES LOCAL:1, 1.20
	ELSEIF EXP:5 <= EXPLV:2
		TIMES LOCAL:1, 1.40
	ELSEIF EXP:5 <= EXPLV:3
		TIMES LOCAL:1, 1.80
	ELSEIF EXP:5 <= EXPLV:4
		TIMES LOCAL:1, 2.25
	ELSE
		TIMES LOCAL:1, 4.00
	ENDIF
	;ABL:분유중독을 본다
	IF ABL:34 == 0
		TIMES LOCAL:1, 1.00
	ELSEIF ABL:34 == 1
		TIMES LOCAL:1, 1.10
	ELSEIF ABL:34 == 2
		TIMES LOCAL:1, 1.20
	ELSEIF ABL:34 == 3
		TIMES LOCAL:1, 1.30
	ELSEIF ABL:34 == 4
		TIMES LOCAL:1, 1.40
	ELSE
		TIMES LOCAL:1, 1.50
	ENDIF

	;B민감·둔감을 본다
	IF TALENT:106
		TIMES LOCAL:1, 1.50
	ELSEIF TALENT:107
		TIMES LOCAL:1, 0.60
	ENDIF
	;한 번 단독으로 계산
	SOURCE:22 += LOCAL:1
ENDIF

;ABL:기교를 본다
IF ABL:12 == 0
	SOURCE:15 += 100
	TIMES SOURCE:0 , 0.30
	TIMES SOURCE:3 , 0.30
	TIMES SOURCE:1 , 0.30
	TIMES SOURCE:2 , 0.30
	TIMES SOURCE:11 , 0.60
ELSEIF ABL:12 == 1
	SOURCE:15 += 160
	TIMES SOURCE:0 , 0.70
	TIMES SOURCE:3 , 0.70
	TIMES SOURCE:1 , 0.70
	TIMES SOURCE:2 , 0.70
	TIMES SOURCE:11 , 0.80
ELSEIF ABL:12 == 2
	SOURCE:15 += 220
	TIMES SOURCE:0 , 1.00
	TIMES SOURCE:3 , 1.00
	TIMES SOURCE:1 , 1.00
	TIMES SOURCE:2 , 1.00
	TIMES SOURCE:11 , 1.00
ELSEIF ABL:12 == 3
	SOURCE:15 += 280
	TIMES SOURCE:0 , 1.20
	TIMES SOURCE:3 , 1.20
	TIMES SOURCE:1 , 1.20
	TIMES SOURCE:2 , 1.20
	TIMES SOURCE:11 , 1.20
ELSEIF ABL:12 == 4
	SOURCE:15 += 340
	TIMES SOURCE:0 , 1.40
	TIMES SOURCE:3 , 1.40
	TIMES SOURCE:1 , 1.40
	TIMES SOURCE:2 , 1.40
	TIMES SOURCE:11 , 1.40
ELSE
	SOURCE:15 += 400
	TIMES SOURCE:0 , 1.60
	TIMES SOURCE:3 , 1.60
	TIMES SOURCE:1 , 1.60
	TIMES SOURCE:2 , 1.60
	TIMES SOURCE:11 , 1.60
ENDIF

;ABL:자위 중독을 본다
IF ABL:31 == 0
	SOURCE:24 = 0
	TIMES SOURCE:0 , 1.00
	TIMES SOURCE:3 , 1.00
	TIMES SOURCE:1 , 1.00
	TIMES SOURCE:2 , 1.00
ELSEIF ABL:31 == 1
	SOURCE:24 = 100
	TIMES SOURCE:0 , 1.10
	TIMES SOURCE:3 , 1.10
	TIMES SOURCE:1 , 1.10
	TIMES SOURCE:2 , 1.10
ELSEIF ABL:31 == 2
	SOURCE:24 = 300
	TIMES SOURCE:0 , 1.20
	TIMES SOURCE:3 , 1.20
	TIMES SOURCE:1 , 1.20
	TIMES SOURCE:2 , 1.20
ELSEIF ABL:31 == 3
	SOURCE:24 = 800
	TIMES SOURCE:0 , 1.30
	TIMES SOURCE:3 , 1.30
	TIMES SOURCE:1 , 1.30
	TIMES SOURCE:2 , 1.30
ELSEIF ABL:31 == 4
	SOURCE:24 = 1500
	TIMES SOURCE:0 , 1.50
	TIMES SOURCE:3 , 1.50
	TIMES SOURCE:1 , 1.50
	TIMES SOURCE:2 , 1.50
ELSE
	SOURCE:24 = 2500
	TIMES SOURCE:0 , 1.70
	TIMES SOURCE:3 , 1.70
	TIMES SOURCE:1 , 1.50
	TIMES SOURCE:2 , 1.50
ENDIF

;공개 때는, ABL:노출벽을 본다
IF TEQUIP:50
	IF ABL:14 == 0
		SOURCE:24 += 0
		TIMES SOURCE:0 , 1.00
		TIMES SOURCE:3 , 1.00
		TIMES SOURCE:1 , 1.00
		TIMES SOURCE:2 , 1.00
		TIMES SOURCE:12, 1.00
	ELSEIF ABL:14 == 1
		SOURCE:24 += 100
		TIMES SOURCE:0 , 1.10
		TIMES SOURCE:3 , 1.10
		TIMES SOURCE:1 , 1.10
		TIMES SOURCE:2 , 1.10
		TIMES SOURCE:12, 1.20
	ELSEIF ABL:14 == 2
		SOURCE:24 += 300
		TIMES SOURCE:0 , 1.20
		TIMES SOURCE:3 , 1.20
		TIMES SOURCE:1 , 1.20
		TIMES SOURCE:2 , 1.20
		TIMES SOURCE:12, 1.40
	ELSEIF ABL:14 == 3
		SOURCE:24 += 800
		TIMES SOURCE:0 , 1.30
		TIMES SOURCE:3 , 1.30
		TIMES SOURCE:1 , 1.30
		TIMES SOURCE:2 , 1.30
		TIMES SOURCE:12, 1.60
	ELSEIF ABL:14 == 4
		SOURCE:24 += 1500
		TIMES SOURCE:0 , 1.50
		TIMES SOURCE:3 , 1.50
		TIMES SOURCE:1 , 1.50
		TIMES SOURCE:2 , 1.50
		TIMES SOURCE:12, 2.00
	ELSE
		SOURCE:24 += 2500
		TIMES SOURCE:0 , 1.70
		TIMES SOURCE:3 , 1.70
		TIMES SOURCE:1 , 1.70
		TIMES SOURCE:2 , 1.70
		TIMES SOURCE:12, 3.00
	ENDIF
ENDIF
IF TALENT:134
	;ABL:분유중독을 본다
	IF ABL:34 == 0
		TIMES SOURCE:3 , 1.00
		TIMES SOURCE:24 , 1.00
	ELSEIF ABL:34 == 1
		TIMES SOURCE:3 , 1.20
		TIMES SOURCE:24 , 1.00
	ELSEIF ABL:34 == 2
		TIMES SOURCE:3 , 1.20
		TIMES SOURCE:24 , 1.20
	ELSEIF ABL:34 == 3
		TIMES SOURCE:3 , 1.40
		TIMES SOURCE:24 , 1.20
	ELSEIF ABL:34 == 4
		TIMES SOURCE:3 , 1.40
		TIMES SOURCE:24 , 1.40
	ELSE
		TIMES SOURCE:3 , 1.60
		TIMES SOURCE:24 , 1.60
	ENDIF
ENDIF
;음유
IF TALENT:174
	TIMES SOURCE:15 , 1.40
	TIMES SOURCE:16 , 1.40
	TIMES SOURCE:30 , 0.80
	TIMES SOURCE:3 , 1.40
ENDIF

PRINTL

TCVAR:0 += 1
IF PLAYER == MASTER
	TCVAR:42 += 1
	SIF TALENT:MASTER:120 || TALENT:MASTER:121 || TALENT:MASTER:1
		CFLAG:173 += 1
ENDIF
;비디오 촬영시는 경험에 플러스
IF TEQUIP:50
	TCVAR:10 += 2
	TCVAR:11 += 2
ELSE
	TCVAR:10 += 1
	TCVAR:11 += 1
ENDIF
IF TEQUIP:50 && (CFLAG:21 & 1) == 0
	TCVAR:50 += 1
	CFLAG:21 |= 1
ENDIF

;-------------------------------------------------
;사정 체크
;-------------------------------------------------
TFLAG:91 = SAMEN_CHECK(PLAYER)

;사정시의 처리
IF TFLAG:91 >= 1
	;커맨드 마다 고유의 처리는 이 아래에 쓴다
	TIMES SOURCE:15 , 3.00
	TIMES SOURCE:16 , 5.00
	TIMES SOURCE:22 , 1.50

	;ABL:정액 중독을 본다
	IF ABL:31 == 0
		SOURCE:24 += 0
	ELSEIF ABL:31 == 1
		SOURCE:24 += 300
	ELSEIF ABL:31 == 2
		SOURCE:24 += 1000
	ELSEIF ABL:31 == 3
		SOURCE:24 += 1500
	ELSEIF ABL:31 == 4
		SOURCE:24 += 2200
	ELSE
		SOURCE:24 += 3000
	ENDIF

	;사정처 타입의 설정
	TFLAG:703 = 1
	;사정처 선택의 가능·불가
	TFLAG:704 = 0
	;범용적인 처리는 이 함수로 실시한다
	;(사정 게이지의 재계산이나 사정 경험의 상승과 공통 부분의 표시)
	CALL SAMEN_SHOOT
ENDIF

;조교자가 후타나리
SIF TALENT:PLAYER:121
	SOURCE:22 /= 2

;사정에 관계없이 행해지는 처리는 여기로부터
STAIN:3 |= STAIN:PLAYER:2
STAIN:PLAYER:2 |= STAIN:3

;노예의 손가락⇔노예의 B의 불결이 이동
STAIN:1 |= STAIN:5
STAIN:5 |= STAIN:1

;노예가 큰 가슴 또는 폭유때만, 노예의 입⇔노예의 B의 불결이 이동
IF TALENT:112 || TALENT:113
	STAIN:0 |= STAIN:5
	STAIN:5 |= STAIN:0
ENDIF
;주인 경험 플래그
SIF ASSIPLAY == 0
	TFLAG:50 += (ABL:1 >= 3) ?  3 # 2

;촬영시의 조교 내용 체크
SIF TEQUIP:35 == 2
	TFLAG:122 = 90

IF TALENT:120 == 0 && TALENT:PLAYER:120 == 0
	TCVAR:12 += 10
ELSEIF TALENT:120 && TALENT:PLAYER:120
	TCVAR:13 += 10
ENDIF

;봉사 쾌락 경험 입수 판정
TFLAG:100 |= 1
;노출 쾌락 경험 입수 판정
TFLAG:100 |= 2

SIF TFLAG:101
	TFLAG:101 = 2
;처녀 상실의 후에 페니스를 뽑고 있을까
TFLAG:101 = (TFLAG:101 == 2) ?  1 # 0
RETURN 1

;--------------------------------------------------
;명령 거부의 메시지
;--------------------------------------------------
@DENIAL_MESSAGE_COM_613
CALL KOJO_DENIAL_MESSAGE_COM
SIF FLAG:10 == 0 || CFLAG:99 == 1 || RESULT != 0
	RETURN 0

SIF PALAM:0 >= (PALAMLV:4 - 1) ||  PALAM:1 >= (PALAMLV:4 - 1) ||  PALAM:2 >= (PALAMLV:4 - 1) ||  PALAM:10 >= (PALAMLV:4 - 1)
	PRINT 당장이라도 가버릴 것 같으면서도, 
PRINTFORMW %조사선택(CALLNAME:TARGET, "는")% 명령을 거부했다….

;--------------------------------------------------
;조교시 메시지
;--------------------------------------------------
@TRAIN_MESSAGE_COM613
CALL KOJO_TRAIN_MESSAGE_COM
SIF FLAG:10 == 0 || CFLAG:99 == 1 || RESULT != 0
	RETURN 0

PRINTL
IF TEQUIP:68
	SIF PALAM:0 >= (PALAMLV:4 - 1) ||  PALAM:1 >= (PALAMLV:4 - 1) ||  PALAM:2 >= (PALAMLV:4 - 1) ||  PALAM:10 >= (PALAMLV:4 - 1)
		PRINT 당장이라도 가버릴 것 같아하길래, 
	PRINTFORML %조사선택(CALLNAME:TARGET, "가")% 원하는 대로 하게 했다…. 
ENDIF
;전회와 이번 조교자가 같아, 전회의 조교가 기승위나 수레 가마이며, 뽑지 않는 플래그가 있는 경우
IF TFLAG:102 && (PREVCOM == 24 || PREVCOM == 430) && ((ASSIPLAY && TFLAG:133) || (ASSIPLAY == 0 && TFLAG:133 == 0))
	PRINTFORM %조사선택(CALLNAME:TARGET, "는")% 삽입된 채로 페니스를 단단히 조여 
	IF TALENT:111
		PRINT 빈약하기 그지없는 
	ELSEIF TALENT:112
		PRINT 그 큰 
	ELSEIF TALENT:113
		PRINT 그 거대한 
	ENDIF
	SIF TALENT:134
		PRINT 모유가 방울져 떨어지는 
	PRINT 가슴을 
	IF TEQUIP:35 == 2
		PRINT 착유기로 짜면서 
	ELSE
		PRINT 스스로의 손가락으로 만지작거리며 
	ENDIF
	PRINTW 다시 허리를 움직이기 시작했다….
ELSE
	PRINTFORML 질안에서 빼지 않고 그대로 %조사선택(CALLNAME:TARGET, "를")% 안아 올려 아래로부터 허리를 밀어 올렸다…. 
	PRINTFORM %조사선택(CALLNAME:TARGET, "는")% 
	IF TALENT:111
		PRINT 빈약하기 그지없는 
	ELSEIF TALENT:112
		PRINT 그 큰 
	ELSEIF TALENT:113
		PRINT 그 거대한 
	ENDIF
	SIF TALENT:134
		PRINT 모유가 방울져 떨어지는 
	PRINT 가슴을 
	IF TEQUIP:35 == 2
		PRINT 착유기로 짜면서 
	ELSE
		PRINT 스스로의 손가락으로 만지작거리며 
	ENDIF
	PRINTW 허리를 움직이고 있다….
ENDIF

;-------------------------------------------------
;고유의 실행 판정
;-------------------------------------------------
@COM_ORDER_613, ARG
LOCAL:99 = ARG
;ABL:욕망
IF ABL:11
	SIF LOCAL:99
		RESULTS = %RESULTS% + 
	LOCAL:99 = ABL:11
	TFLAG:240 += LOCAL:99 * 3
	RESULTS = %RESULTS% %ABLNAME:11%LV{LOCAL:99}({LOCAL:99 * 3})
ENDIF
;ABL:V감각
IF ABL:1
	SIF LOCAL:99
		RESULTS = %RESULTS% + 
	LOCAL:99 = ABL:1
	TFLAG:240 += LOCAL:99 * 2
	RESULTS = %RESULTS% %ABLNAME:1%LV{LOCAL:99}({LOCAL:99 * 2})
ENDIF
;ABL:봉사 정신
IF ABL:13
	SIF LOCAL:99
		RESULTS = %RESULTS% + 
	LOCAL:99 = ABL:13
	TFLAG:240 += LOCAL:99 * 4
	RESULTS = %RESULTS% %ABLNAME:13%LV{LOCAL:99}({LOCAL:99 * 4})
ENDIF
;ABL:노출벽
IF ABL:14
	SIF LOCAL:99
		RESULTS = %RESULTS% + 
	LOCAL:99 = ABL:14
	TFLAG:240 += LOCAL:99 * 4
	RESULTS = %RESULTS% %ABLNAME:14%LV{LOCAL:99}({LOCAL:99 * 4})
ENDIF
;ABL:자위 중독
IF ABL:30
	SIF LOCAL:99
		RESULTS = %RESULTS% + 
	LOCAL:99 = ABL:30
	TFLAG:240 += LOCAL:99 * 3
	RESULTS = %RESULTS% %ABLNAME:30%LV{LOCAL:99}({LOCAL:99 * 3})
ENDIF
;ABL:정액 중독
IF ABL:31
	SIF LOCAL:99
		RESULTS = %RESULTS% + 
	LOCAL:99 = ABL:11
	TFLAG:240 += LOCAL:99
	RESULTS = %RESULTS% %ABLNAME:11%LV{LOCAL:99}({LOCAL:99})
ENDIF

;모유 체질 한편 ABL:분유중독
IF TALENT:134
	IF ABL:34
		SIF LOCAL:99
			RESULTS = %RESULTS% + 
		LOCAL:99 = ABL:34
		TFLAG:240 += LOCAL:99 * 2
		RESULTS = %RESULTS% %ABLNAME:34%LV{LOCAL:99}({LOCAL:99 * 2})
	ENDIF
ENDIF

;쾌락 각인
IF MARK:1
	SIF LOCAL:99
		RESULTS = %RESULTS% + 
	LOCAL:99 = MARK:1
	TFLAG:240 += LOCAL:99 * 3
	RESULTS = %RESULTS% %MARKNAME:1%LV{LOCAL:99}({LOCAL:99 * 3})
ENDIF

;PALAM:욕정
GETPALAMLV PALAM:13, 5
LOCAL:2 = RESULT
IF LOCAL:2
	SIF LOCAL:99
		RESULTS = %RESULTS% + 
	LOCAL:99 = LOCAL:2 * 3
	TFLAG:240 += LOCAL:99
	RESULTS = %RESULTS% %PALAMNAME:13%LV{LOCAL:2}({LOCAL:99})
ENDIF

;자제심
IF TALENT:20
	RESULTS = %RESULTS% - 
	LOCAL:99 = 5
	TFLAG:240 -= LOCAL:99
	RESULTS = %RESULTS% %TALENTNAME:20%({LOCAL:99})
ENDIF
;부끄럼쟁이
IF TALENT:35
	RESULTS = %RESULTS% - 
	LOCAL:99 = 5
	TFLAG:240 -= LOCAL:99
	RESULTS = %RESULTS% %TALENTNAME:35%({LOCAL:99})
;부끄럼없음
ELSEIF TALENT:36
	SIF LOCAL:99
		RESULTS = %RESULTS% + 
	LOCAL:99 = 2
	TFLAG:240 += LOCAL:99
	RESULTS = %RESULTS% %TALENTNAME:36%({LOCAL:99})
ENDIF
;남자 싫다
IF TALENT:37 && TALENT:PLAYER:120
	RESULTS = %RESULTS% - 
	LOCAL:99 = 12
	TFLAG:240 -= LOCAL:99
	RESULTS = %RESULTS% %TALENTNAME:37%({LOCAL:99})
;여자 증오
ELSEIF TALENT:38 && TALENT:PLAYER:120 == 0
	RESULTS = %RESULTS% - 
	LOCAL:99 = 12
	TFLAG:240 -= LOCAL:99
	RESULTS = %RESULTS% %TALENTNAME:38%({LOCAL:99})
ENDIF
;자위하기 쉽다
IF TALENT:60
	SIF LOCAL:99
		RESULTS = %RESULTS% + 
	LOCAL:99 = 5
	TFLAG:240 += LOCAL:99
	RESULTS = %RESULTS% %TALENTNAME:60%({LOCAL:99})
ENDIF
;쾌감에 솔직
IF TALENT:70
	SIF LOCAL:99
		RESULTS = %RESULTS% + 
	LOCAL:99 = 5
	TFLAG:240 += LOCAL:99
	RESULTS = %RESULTS% %TALENTNAME:70%({LOCAL:99})
;쾌감의 부정
ELSEIF TALENT:71
	RESULTS = %RESULTS% - 
	LOCAL:99 = 5
	TFLAG:240 -= LOCAL:99
	RESULTS = %RESULTS% %TALENTNAME:71%({LOCAL:99})
ENDIF
;연모
IF TALENT:150 && ASSIPLAY == 0
	SIF LOCAL:99
		RESULTS = %RESULTS% + 
	LOCAL:99 = 5
	TFLAG:240 += LOCAL:99
	RESULTS = %RESULTS% %TALENTNAME:150%({LOCAL:99})
ENDIF
;음유
IF TALENT:174
	SIF LOCAL:99
		RESULTS = %RESULTS% + 
	LOCAL:99 = 8
	TFLAG:240 += LOCAL:99
	RESULTS = %RESULTS% %TALENTNAME:174%({LOCAL:99})
ENDIF
;처녀
IF TALENT:0
	RESULTS = %RESULTS% - 
	LOCAL:99 = 20
	SIF TALENT:0 == 1
		LOCAL:99 += 10
	TFLAG:240 -= LOCAL:99
	RESULTS = %RESULTS% %TALENTNAME:0%({LOCAL:99})
ELSEIF EXP:0 < EXPLV:2
	RESULTS = %RESULTS% - 
	LOCAL:99 = 5
	TFLAG:240 -= LOCAL:99
	RESULTS = %RESULTS% %EXPNAME:0%부족({LOCAL:99})
ENDIF

;조교자가 후타나리
IF TALENT:PLAYER:121
	SIF LOCAL:99
		RESULTS = %RESULTS% + 
	LOCAL:99 = 4
	TFLAG:240 += LOCAL:99
	RESULTS = %RESULTS% %조사처리(CALLNAME:PLAYER,"가")% %TALENTNAME:121%({LOCAL:99})
ENDIF

;미약
IF TEQUIP:11
	SIF LOCAL:99
		RESULTS = %RESULTS% + 
	LOCAL:99 = 6
	TFLAG:240 += LOCAL:99
	RESULTS = %RESULTS% %ITEMNAME:61%({LOCAL:99})
ENDIF

;조교자의 P의 더러움
TFLAG:242 = COMORDER_STAIN_CALC(2, 0)
;기승위에서는 더러움의 영향이 적은
TFLAG:242 /= 3

;서로 불결
IF TFLAG:242
	RESULTS = %RESULTS% - 
	TFLAG:240 -= TFLAG:242
	RESULTS = %RESULTS% 서로 불결
	;악취 둔감
	IF TALENT:61
		RESULTS = %RESULTS%, %TALENTNAME:61%
	ELSEIF TALENT:62
		RESULTS = %RESULTS%, %TALENTNAME:62%
	ENDIF
	RESULTS = %RESULTS%({TFLAG:242})
	LOCAL:99 = 1
ENDIF

TFLAG:241 = 50

;난이도 상승
;공개로+10, 착유기로+5
SIF TEQUIP:50
	TFLAG:241 += 10
SIF TEQUIP:35 == 2
	TFLAG:241 += 5
