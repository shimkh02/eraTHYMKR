;=============================================================================
;취생몽사
;=============================================================================
;………………………………………………
;실행 판정
;………………………………………………
@COM_ABLE451
;개별 설정 커맨드 필터
SIF DITEMTYPE:999:451 > 0
	RETURN 0
;조교자奉仕 · ウフフ系커맨드를 유효로 하지 않으면 불가
SIF (FLAG:15 & 2048) == 0
	RETURN 0
;캐릭터 전용 커맨드를 유효로 하지 않으면 불가
SIF (FLAG:15 & 16384) == 0
	RETURN 0
;조교자가 天治でないとダメ
SIF NO:PLAYER != 142
	RETURN 0
;天治の専用料理を3種とも見て, かつ奴隷に『ピッチャー二つ』を実行したことがないとダメ。もしくは対象が伊佐美かレミリアでないとダメ
SIF ((CFLAG:PLAYER:160 & 14) != 14 || (CFLAG:160 & 1) == 0) && NO:TARGET != 10 && NO:TARGET != 139
	RETURN 0
;各種酒を1つずつ持っているか, アイテムなし設定でないとダメ
SIF (ITEM:600 == 0 || ITEM:601 == 0 || ITEM:602 == 0 || ITEM:603 == 0 || ITEM:604 == 0 || ITEM:605 == 0) && NOITEM == 0
	RETURN 0
;主人に対する好感度が100이상 필요
SIF CFLAG:2 < 100
	RETURN 0
;反発刻印が0でないとダメ
SIF MARK:9
	RETURN 0
;수면 중에는 불가
SIF EQUIP:14
	RETURN 0
;猿轡付けていると無理
SIF TEQUIP:42
	RETURN 0
;샤워 중에는 불가
SIF TEQUIP:54
	RETURN 0
;○○목욕탕 입욕 중에는 불가
SIF TEQUIP:55
	RETURN 0
;기저귀 플레이 중에는 불가
SIF TEQUIP:58
	RETURN 0
;촉수 조교 중에는 불가
SIF TEQUIP:90
	RETURN 0
;실신 중에는 불가
SIF TFLAG:899 > 0
	RETURN 0
RETURN 1

;--------------------------------------------------
;커맨드 본체
;--------------------------------------------------
;이쪽도 술을 먹이는 것 환파크리입니다
;각종술을 1개씩 소모하는 것으로써, 바보와 같이 술을 감추게 한다
;효과는 절대이지만 실행시킬 때까지 옥신각신하지 않아 물러나고 말이야 있고
@COM451
PRINTL 취생몽사 「주정뱅이의 레무리아」
TSTR:0 = 취생몽사 「주정뱅이의 레무리아」
CALL TRAIN_MESSAGE_COM451

;-------------------------------------------------
;소스의 계산
;-------------------------------------------------
DOWNBASE:0 += 150
DOWNBASE:1 += 250
SOURCE:40 = 6000
SOURCE:41 = 3200

;모든 술을을 1개씩 소비
IF NOITEM == 0
	ITEM:600 -= 1
	ITEM:601 -= 1
	ITEM:602 -= 1
	ITEM:603 -= 1
	ITEM:604 -= 1
	ITEM:605 -= 1
ENDIF
RETURN 1

;--------------------------------------------------
;조교시 메시지
;--------------------------------------------------
@TRAIN_MESSAGE_COM451
CALL KOJO_TRAIN_MESSAGE_COM
SIF FLAG:10 == 0 || CFLAG:99 == 1 || RESULT != 0
	RETURN 0

PRINTL
PRINTFORML %조사선택(CALLNAME:TARGET, "는")% %조사선택(CALLNAME:PLAYER, "가")% 만들어낸 술의 탄막에 휩쓸렸다.
PRINTFORMW %CALLNAME:TARGET%의 기분은 이상하게 고양되고 있다.
