;=============================================================================
;피처 두 잔
;=============================================================================
;………………………………………………
;실행 판정
;………………………………………………
@COM_ABLE450
;개별 설정 커맨드 필터
SIF DITEMTYPE:999:450 > 0
	RETURN 0
;조교자奉仕 · ウフフ系커맨드를 유효로 하지 않으면 불가
SIF (FLAG:15 & 2048) == 0
	RETURN 0
;캐릭터 전용 커맨드를 유효로 하지 않으면 불가
SIF (FLAG:15 & 16384) == 0
	RETURN 0
;조교자가 天治でないとダメ
SIF NO:PLAYER != 142
	RETURN 0
;ビール2つ持っているか, アイテムなし設定でないとダメ
SIF ITEM:601 < 2 && NOITEM == 0
	RETURN 0
;主人に対する好感度が100이상 필요
SIF CFLAG:2 < 100
	RETURN 0
;反発刻印が0でないとダメ
SIF MARK:9
	RETURN 0
;수면 중에는 불가
SIF EQUIP:14
	RETURN 0
;猿轡付けていると無理
SIF TEQUIP:42
	RETURN 0
;샤워 중에는 불가
SIF TEQUIP:54
	RETURN 0
;○○목욕탕 입욕 중에는 불가
SIF TEQUIP:55
	RETURN 0
;기저귀 플레이 중에는 불가
SIF TEQUIP:58
	RETURN 0
;촉수 조교 중에는 불가
SIF TEQUIP:90
	RETURN 0
;실신 중에는 불가
SIF TFLAG:899 > 0
	RETURN 0
RETURN 1

;--------------------------------------------------
;커맨드 본체
;--------------------------------------------------
;술을 먹이는 것 환파크리입니다
;맥주를 단번에 2개(살) 감추게 하는 커맨드. 효과로서는 맥주에 의한 이 정신차리게 함3배와 같다
;1본절 약속할 수 있는 것이 장점. 그것보다 문제인 것은 절정 회수에 관련되지 않고 실행할 수 있는 근처인지도
@COM450
PRINTL 의식 「일단 피쳐 두 잔부터」
TSTR:0 = 의식 「일단 피처 두 잔부터」
CALL TRAIN_MESSAGE_COM450
;-------------------------------------------------
;소스의 계산
;-------------------------------------------------
DOWNBASE:0 += 60
DOWNBASE:1 += 60
SOURCE:40 = 1600

;맥주를 2개 소비
SIF NOITEM == 0
	ITEM:601 -= 2

;이 커맨드를 실행한 것을 기록
CFLAG:160 |= 1
RETURN 1

;--------------------------------------------------
;조교시 메시지
;--------------------------------------------------
@TRAIN_MESSAGE_COM450
CALL KOJO_TRAIN_MESSAGE_COM
SIF FLAG:10 == 0 || CFLAG:99 == 1 || RESULT != 0
	RETURN 0

PRINTL
PRINTFORML %CALLNAME:TARGET%에게 피처 두 잔의 맥주를 마시게 했다.
PRINTFORMW 산뜻한 목에 넘어가는 느낌에 %CALLNAME:TARGET%의 기분은 조금 상쾌해졌다.
