;=============================================================================
;알몸 앞치마
;=============================================================================
;………………………………………………
;실행 판정
;………………………………………………
@COM_ABLE90
;개별 설정 커맨드 필터
SIF DITEMTYPE:999:90 > 0
	RETURN 0
;강력 아이템·커맨드 무효시는 안 됨
SIF (FLAG:13 & 2) == 0
	RETURN 0
;수치 플레이계 확장 커맨드를 유효하게 하지 않으면 안 됨
SIF (FLAG:15 & 512) == 0
	RETURN 0
;해제는 언제라도 가능
SIF TEQUIP:59 == 1
	RETURN 1
;아이템「에이프런」을 가지고 있는지, 아이템 없음 설정이 아니면 안 됨
SIF ITEM:44 == 0 && NOITEM == 0
	RETURN 0
;조수는 안 됨
SIF ASSIPLAY
	RETURN 0
;HARD 모드 이상은 이상건
IF FLAG:3 >= 3
	;순종이 4이상 없으면 안 됨
	SIF ABL:10 < 4
		RETURN 0
	;기력이 500 이하라면 안 됨
	SIF BASE:1 <= 500
		RETURN 0
	;그 주회로 1번에서도 이상 경험을 붙이고 있으면(자) 안 됨(최초부터 있는 분은 OK)
	SIF CFLAG:11 & 4
		RETURN 0
ELSE
	;순종이 3이상 없으면 안 됨
	SIF ABL:10 < 3
		RETURN 0
ENDIF
;호감도가 500이상 없으면 안 됨
SIF CFLAG:2 < 500
	RETURN 0
;고통 또는 공포가 1000이상 있으면(자) 안 됨
SIF PALAM:16 >= 1000 || PALAM:17 >= 1000
	RETURN 0
;반감 또는 불쾌 또는 억울이 500이상 있으면(자) 안 됨
SIF PALAM:20 >= 500 || PALAM:21 >= 500 || PALAM:22 >= 500
	RETURN 0
;여주인인으로 상대가 동성의 경우는 레즈끼이 3이상 없으면 안 됨
IF TALENT:MASTER:120 == 0 && TALENT:120 == 0
	SIF ABL:22 < 3
		RETURN 0
;남자 주인으로 상대가 동성의 경우는 BL끼이 3이상 없으면 안 됨
ELSEIF TALENT:MASTER:120 && TALENT:120
	SIF ABL:23 < 3
		RETURN 0
ENDIF
;촉수 조교 중에는 불가
SIF TEQUIP:90
	RETURN 0
;수면중은 불가
SIF EQUIP:14
	RETURN 0
;거미집이 결여되어 있을 때는 안 됨
SIF TEQUIP:40 == 7
	RETURN 0
;승마 중에는 불가
SIF TEQUIP:43
	RETURN 0
;야외 플레이 중에는 불가
SIF TEQUIP:52
	RETURN 0
;목욕탕 플레이 중에는 불가
SIF TEQUIP:53
	RETURN 0
;00목욕탕 입욕 중에는 불가
SIF TEQUIP:55
	RETURN 0
;수치 플레이 중에는 불가
SIF TEQUIP:56
	RETURN 0
;의사 플레이 중에는 불가
SIF TEQUIP:57
	RETURN 0
;기저귀 플레이 중에는 불가
SIF TEQUIP:58
	RETURN 0
;학교수영복 플레이 중에는 불가
SIF TEQUIP:59 == 2
	RETURN 0
;코스프레 중에는 불가
SIF TEQUIP:59 > 2
	RETURN 0
;나체정식 중에는 안 됨
SIF TEQUIP:60
	RETURN 0
;시간정지 중에는 무리
SIF TEQUIP:71
	RETURN 0
RETURN 1

;--------------------------------------------------
;커맨드 본체
;--------------------------------------------------
;시츄보다 게임성을 추구하고 있다고 해석했으므로 새댁 플레이를 바탕으로 신규 작성
@COM90
PRINTL 알몸 앞치마
;-------------------------------------------------
;실행할 수 있을까의 판정
;-------------------------------------------------
IF TFLAG:699 == 0
	CALL COM_ORDER
	;실행할 수 없다
	SIF RESULT == 0
		RETURN 0
ENDIF

;-------------------------------------------------
;실행 결정
;-------------------------------------------------
TSTR:0 = 알몸 앞치마
CALL TRAIN_MESSAGE_COM090

DOWNBASE:0 += 0
DOWNBASE:1 += 10

IF TEQUIP:59 == 1
	TEQUIP:59 = 0
	RETURN 1
ENDIF

LOCAL:1 = 100
LOCAL:2 = 200

;PALAM:욕정을 본다
IF PALAM:13 < PALAMLV:1
	TIMES LOCAL:1 , 1.00
	SOURCE:19 = 0
ELSEIF PALAM:13 < PALAMLV:2
	TIMES LOCAL:1 , 1.10
	SOURCE:19 = 150
ELSEIF PALAM:13 < PALAMLV:3
	TIMES LOCAL:1 , 1.20
	SOURCE:19 = 300
ELSEIF PALAM:13 < PALAMLV:4
	TIMES LOCAL:1 , 1.30
	SOURCE:19 = 600
ELSE
	TIMES LOCAL:1 , 1.40
	SOURCE:19 = 1000
ENDIF

;ABL:순종을 본다
IF ABL:10 == 0
	TIMES LOCAL:1 , 1.00
	SOURCE:32 = 500
ELSEIF ABL:10 == 1
	TIMES LOCAL:1 , 1.20
	SOURCE:32 = 100
ELSEIF ABL:10 == 2
	TIMES LOCAL:1 , 1.40
	SOURCE:32 = 50
ELSEIF ABL:10 == 3
	TIMES LOCAL:1 , 1.50
	SOURCE:32 = 0
ELSEIF ABL:10 == 4
	TIMES LOCAL:1 , 2.10
	SOURCE:32 = 0
ELSE
	TIMES LOCAL:1 , 3.00
	SOURCE:32 = 0
ENDIF

;ABL:봉사 정신을 본다
IF ABL:13 == 0
	TIMES LOCAL:1 , 1.00
ELSEIF ABL:13 == 1
	TIMES LOCAL:1 , 1.20
ELSEIF ABL:13 == 2
	TIMES LOCAL:1 , 1.40
ELSEIF ABL:13 == 3
	TIMES LOCAL:1 , 1.60
ELSEIF ABL:13 == 4
	TIMES LOCAL:1 , 1.80
ELSE
	TIMES LOCAL:1 , 2.00
ENDIF

;ABL:노출벽을 본다
IF ABL:14 == 0
	SOURCE:12 = 100
	SOURCE:18 = 0
	SOURCE:31 = 100
	TIMES LOCAL:1, 0.60
ELSEIF ABL:14 == 1
	SOURCE:12 = 500
	SOURCE:18 = 50
	SOURCE:31 = 300
	TIMES LOCAL:1, 1.00
ELSEIF ABL:14 == 2
	SOURCE:12 = 100
	SOURCE:18 = 100
	SOURCE:31 = 50
	TIMES LOCAL:1, 1.60
ELSEIF ABL:14 == 3
	SOURCE:12 = 50
	SOURCE:18 = 150
	SOURCE:31 = 0
	TIMES LOCAL:1, 2.00
ELSEIF ABL:14 == 4
	SOURCE:12 = 0
	SOURCE:18 = 200
	SOURCE:31 = 0
	TIMES LOCAL:1, 2.60
ELSE
	SOURCE:12 = 0
	SOURCE:18 = 300
	SOURCE:31 = 0
	TIMES LOCAL:1, 3.80
ENDIF

;겁장이
SIF TALENT:10
	TIMES LOCAL:2 , 1.50
;프라이드 높다
SIF TALENT:16
	TIMES LOCAL:2 , 2.00
;무관심
SIF TALENT:21
	TIMES LOCAL:1 , 0.80
;감정결여
SIF TALENT:22
	TIMES LOCAL:1 , 0.80
;호기심
SIF TALENT:23
	TIMES LOCAL:1 , 1.50
;정조관념
SIF TALENT:30
	TIMES LOCAL:1 , 0.50
;부끄럼쟁이
SIF TALENT:35
	TIMES LOCAL:2 , 1.20
;헌신적
SIF TALENT:63
	TIMES LOCAL:1 , 1.50
;상애
IF TALENT:153
	TIMES LOCAL:1 , 3.00
;친애
ELSEIF TALENT:152
	TIMES LOCAL:1 , 2.50
;연모
ELSEIF TALENT:150
	TIMES LOCAL:1 , 2.00
ENDIF
;음란
SIF TALENT:170
	TIMES LOCAL:1 , 1.20

IF TALENT:MASTER:120
	;남자 싫다
	IF TALENT:37 && TALENT:150 == 0
		DOWNBASE:1 += 50
		TIMES LOCAL:1 , 0.30
		TIMES LOCAL:2 , 1.50
	ENDIF
	;BL기분 Lv2 미만
	IF ABL:23 < 2 && TALENT:120 && TALENT:150 == 0
		DOWNBASE:1 += 30
		TIMES LOCAL:1 , 0.40
		TIMES LOCAL:2 , 1.20
	ENDIF
ELSE
	;여자 증오
	IF TALENT:38 && TALENT:150 == 0
		DOWNBASE:1 += 50
		TIMES LOCAL:1 , 0.30
		TIMES LOCAL:2 , 1.50
	ENDIF
	;레즈끼 Lv2 미만
	IF ABL:22 < 2 && TALENT:120 == 0 && TALENT:150 == 0
		DOWNBASE:1 += 30
		TIMES LOCAL:1 , 0.40
		TIMES LOCAL:2 , 1.20
	ENDIF
ENDIF

SOURCE:11 += LOCAL:1
SOURCE:17 += LOCAL:1
SOURCE:31 += LOCAL:2

TEQUIP:59 = 1
;주인 경험 플래그
IF ASSIPLAY == 0
	SIF ABL:10 >= 3 || TALENT:150 || TALENT:152 || TALENT:160 || TALENT:170
		TFLAG:50 += 1
ENDIF
RETURN 1

;-------------------------------------------------
;알몸 앞치마 장착중
;-------------------------------------------------
@EQUIP_COM90
PRINTL <알몸 앞치마 중>

DOWNBASE:0 += 0
DOWNBASE:1 += 30

LOCAL = 100
LOCAL:1 = 200

;PALAM:욕정을 본다
IF PALAM:13 < PALAMLV:1
	TIMES LOCAL , 1.00
	SOURCE:19 += 0
ELSEIF PALAM:13 < PALAMLV:2
	TIMES LOCAL , 1.10
	SOURCE:19 += 150
ELSEIF PALAM:13 < PALAMLV:3
	TIMES LOCAL , 1.20
	SOURCE:19 += 300
ELSEIF PALAM:13 < PALAMLV:4
	TIMES LOCAL , 1.30
	SOURCE:19 += 600
ELSE
	TIMES LOCAL , 1.40
	SOURCE:19 += 1000
ENDIF

;ABL:순종을 본다
IF ABL:10 == 0
	TIMES LOCAL , 1.00
	SOURCE:32 += 500
ELSEIF ABL:10 == 1
	TIMES LOCAL , 1.20
	SOURCE:32 += 100
ELSEIF ABL:10 == 2
	TIMES LOCAL , 1.40
	SOURCE:32 += 50
ELSEIF ABL:10 == 3
	TIMES LOCAL , 1.50
ELSEIF ABL:10 == 4
	TIMES LOCAL , 2.10
ELSE
	TIMES LOCAL , 3.00
ENDIF

;ABL:봉사 정신을 본다
IF ABL:13 == 0
	TIMES LOCAL , 1.00
ELSEIF ABL:13 == 1
	TIMES LOCAL , 1.20
ELSEIF ABL:13 == 2
	TIMES LOCAL , 1.40
ELSEIF ABL:13 == 3
	TIMES LOCAL , 1.60
ELSEIF ABL:13 == 4
	TIMES LOCAL , 1.80
ELSE
	TIMES LOCAL , 2.00
ENDIF

;ABL:노출벽을 본다
IF ABL:14 == 0
	SOURCE:12 += 100
	SOURCE:18 += 0
	SOURCE:31 += 100
	TIMES LOCAL, 0.60
ELSEIF ABL:14 == 1
	SOURCE:12 += 500
	SOURCE:18 += 50
	SOURCE:31 += 300
	TIMES LOCAL, 1.00
ELSEIF ABL:14 == 2
	SOURCE:12 += 100
	SOURCE:18 += 100
	SOURCE:31 += 50
	TIMES LOCAL, 1.60
ELSEIF ABL:14 == 3
	SOURCE:12 += 50
	SOURCE:18 += 150
	SOURCE:31 += 0
	TIMES LOCAL, 2.00
ELSEIF ABL:14 == 4
	SOURCE:12 += 0
	SOURCE:18 += 200
	SOURCE:31 += 0
	TIMES LOCAL, 2.60
ELSE
	SOURCE:12 += 0
	SOURCE:18 += 300
	SOURCE:31 += 0
	TIMES LOCAL, 3.80
ENDIF

;겁장이
SIF TALENT:10
	TIMES LOCAL:1 , 1.50
;프라이드 높다
SIF TALENT:16
	TIMES LOCAL:1 , 2.00
;무관심
SIF TALENT:21
	TIMES LOCAL , 0.80
;감정결여
SIF TALENT:22
	TIMES LOCAL , 0.80
;호기심
SIF TALENT:23
	TIMES LOCAL , 1.50
;정조관념
SIF TALENT:30
	TIMES LOCAL , 0.50
;부끄럼쟁이
SIF TALENT:35
	TIMES LOCAL:1 , 1.20
;헌신적
SIF TALENT:63
	TIMES LOCAL , 1.50
;상애
IF TALENT:153
	TIMES LOCAL , 3.00
;친애
ELSEIF TALENT:152
	TIMES LOCAL , 2.50
;연모
ELSEIF TALENT:150
	TIMES LOCAL , 2.00
ENDIF
;음란
SIF TALENT:170
	TIMES LOCAL , 1.20

IF TALENT:MASTER:120
	;남자 싫다
	IF TALENT:37 && TALENT:150 == 0
		DOWNBASE:1 += 50
		TIMES LOCAL , 0.30
		TIMES LOCAL:1 , 1.50
	ENDIF
	;BL기분 Lv2 미만
	IF ABL:23 < 2 && TALENT:120 && TALENT:150 == 0
		DOWNBASE:1 += 30
		TIMES LOCAL , 0.40
		TIMES LOCAL:1 , 1.20
	ENDIF
ELSE
	;여자 증오
	IF TALENT:38 && TALENT:150 == 0
		DOWNBASE:1 += 50
		TIMES LOCAL , 0.30
		TIMES LOCAL:1 , 1.50
	ENDIF
	;레즈끼 Lv2 미만
	IF ABL:22 < 2 && TALENT:120 == 0 && TALENT:150 == 0
		DOWNBASE:1 += 30
		TIMES LOCAL , 0.40
		TIMES LOCAL:1 , 1.20
	ENDIF
ENDIF

SOURCE:11 += LOCAL
SOURCE:17 += LOCAL
SOURCE:31 += LOCAL:1

IF TALENT:120 == 0 && TALENT:PLAYER:120 == 0
	TCVAR:12 += 1
ELSEIF TALENT:120 && TALENT:PLAYER:120
	TCVAR:13 += 1
ENDIF

;노출 쾌락 경험 입수 판정
TFLAG:100 |= 2
;주인 경험 플래그
SIF ASSIPLAY == 0 && (ABL:10 >= 3 || TALENT:150 || TALENT:152 || TALENT:160 || TALENT:170)
	TFLAG:50 += 1

RETURN 1

;--------------------------------------------------
;명령 거부의 메시지
;--------------------------------------------------
@DENIAL_MESSAGE_COM_90
CALL KOJO_DENIAL_MESSAGE_COM
SIF FLAG:10 == 0 || CFLAG:99 == 1 || RESULT != 0
	RETURN 0

PRINTL
IF TEQUIP:59 == 1
	PRINTFORML %CALLNAME:TARGET%에게 입고 있는 앞치마를 벗도록 명령했지만 거부당했다.
	PRINTW 좀 더 조교가 필요한 것 같다.
ELSE
	PRINTFORML %CALLNAME:TARGET%에게 앞치마를 입도록 명령했지만 거부당했다.
	PRINTW 좀 더 조교가 필요한 것 같다.
ENDIF

;--------------------------------------------------
;조교시 메시지
;--------------------------------------------------
@TRAIN_MESSAGE_COM090
CALL KOJO_TRAIN_MESSAGE_COM
SIF FLAG:10 == 0 || CFLAG:99 == 1 || RESULT != 0
	RETURN 0

PRINTL
IF TEQUIP:59 == 1
	;상애 때
	IF TALENT:153
		PRINTFORML %조사선택(CALLNAME:PLAYER, "는")% %조사선택(CALLNAME:TARGET, "가")% 입고 있는 앞치마를 벗기려고 하자
		PRINTFORMW %조사선택(CALLNAME:TARGET, "는")% 조금 아쉬운 얼굴을 하면서도, 앞치마를 벗기 쉽게 몸을 움직였다.
	;반발 각인 Lv2 이상 때
	ELSEIF MARK:9 > 1
		PRINTFORML %조사선택(CALLNAME:PLAYER, "가")%, %조사선택(CALLNAME:TARGET, "가")% 입고 있는 앞치마를 벗기려고 하자
		PRINTFORMW %조사선택(CALLNAME:TARGET, "는")% 스스로 앞치마를 벗어 버리고, 그 앞치마를 멀리 던져 버렸다….
	;그 외 때
	ELSE
		PRINTFORMW %조사선택(CALLNAME:PLAYER, "는")% %조사선택(CALLNAME:TARGET, "가")% 입고 있는 앞치마를 벗겼다.
	ENDIF
ELSE
	;상애 때
	IF TALENT:153
		PRINTFORML %조사선택(CALLNAME:PLAYER, "는")% %CALLNAME:TARGET%의 알몸 위에 앞치마를 입히려고 하자
		PRINTFORMW %조사선택(CALLNAME:TARGET, "는")% 굉장히 기쁜 얼굴을 하면서, 앞치마를 입히기 쉽게 몸을 움직였다.
	;반발 각인 Lv2 이상 때
	ELSEIF MARK:9 > 1
		PRINTFORML %조사선택(CALLNAME:PLAYER, "는")% %CALLNAME:TARGET%의 알몸 위에 앞치마를 입히려고 하자
		PRINTFORMW %조사선택(CALLNAME:TARGET, "는")% 싫은 얼굴을 하면서 도망치려고 하므로, 그것을 붙잡아 억지로 앞치마를 입게 했다….
	;그 외 때
	ELSE
		PRINTFORMW %조사선택(CALLNAME:PLAYER, "는")% %CALLNAME:TARGET%의 알몸 위에 앞치마를 입혔다.
	ENDIF
ENDIF

;-------------------------------------------------
;고유의 실행 판정
;-------------------------------------------------
@COM_ORDER_90, ARG
LOCAL:99 = ARG
;ABL:노출벽
IF ABL:14
	SIF LOCAL:99
		RESULTS = %RESULTS% + 
	LOCAL:99 = ABL:14
	TFLAG:240 += LOCAL:99 * 4
	RESULTS = %RESULTS% %ABLNAME:14%LV{LOCAL:99}({LOCAL:99 * 4})
ENDIF

;PALAM:욕정
GETPALAMLV PALAM:13, 5
LOCAL:2 = RESULT
IF LOCAL:2
	SIF LOCAL:99
		RESULTS = %RESULTS% + 
	LOCAL:99 = LOCAL:2 * 2
	TFLAG:240 += LOCAL:99
	RESULTS = %RESULTS% %PALAMNAME:13%LV{LOCAL:2}({LOCAL:99})
ENDIF

;반항적
IF TALENT:11
	RESULTS = %RESULTS% - 
	LOCAL:99 = 5
	TFLAG:240 -= LOCAL:99
	RESULTS = %RESULTS% %TALENTNAME:11%({LOCAL:99})
ENDIF
;자제심
IF TALENT:20
	RESULTS = %RESULTS% - 
	LOCAL:99 = 5
	TFLAG:240 -= LOCAL:99
	RESULTS = %RESULTS% %TALENTNAME:20%({LOCAL:99})
ENDIF
;저항
IF TALENT:34
	RESULTS = %RESULTS% - 
	LOCAL:99 = 12
	TFLAG:240 -= LOCAL:99
	RESULTS = %RESULTS% %TALENTNAME:34%({LOCAL:99})
ENDIF
;부끄럼쟁이
IF TALENT:35
	RESULTS = %RESULTS% - 
	LOCAL:99 = 5
	TFLAG:240 -= LOCAL:99
	RESULTS = %RESULTS% %TALENTNAME:35%({LOCAL:99})
;부끄럼없음
ELSEIF TALENT:36
	SIF LOCAL:99
		RESULTS = %RESULTS% + 
	LOCAL:99 = 2
	TFLAG:240 += LOCAL:99
	RESULTS = %RESULTS% %TALENTNAME:36%({LOCAL:99})
ENDIF
;쾌감에 솔직
IF TALENT:70
	SIF LOCAL:99
		RESULTS = %RESULTS% + 
	LOCAL:99 = 5
	TFLAG:240 += LOCAL:99
	RESULTS = %RESULTS% %TALENTNAME:70%({LOCAL:99})
;쾌감의 부정
ELSEIF TALENT:71
	RESULTS = %RESULTS% - 
	LOCAL:99 = 5
	TFLAG:240 -= LOCAL:99
	RESULTS = %RESULTS% %TALENTNAME:71%({LOCAL:99})
ENDIF
;남자
IF TALENT:120
	RESULTS = %RESULTS% - 
	LOCAL:99 = 5
	TFLAG:240 -= LOCAL:99
	RESULTS = %RESULTS% %TALENTNAME:120%({LOCAL:99})
ENDIF

;연모
IF TALENT:150 && ASSIPLAY == 0
	SIF LOCAL:99
		RESULTS = %RESULTS% + 
	LOCAL:99 = 8
	TFLAG:240 += LOCAL:99
	RESULTS = %RESULTS% %TALENTNAME:150%({LOCAL:99})
ENDIF

;난이도 상승
;비디오로+10
TFLAG:241 = 27
SIF TEQUIP:50
	TFLAG:241 += 10
